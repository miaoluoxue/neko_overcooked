# 胡闹厨房 2（Overcooked! 2）玩法核心逻辑逆向

> **目的**：把这款游戏的整条玩法链路（从启动 → 加入玩家 → 大厅选主题 → 进对局 → 做菜 → 送餐 → 结算 → 回大厅）用源码级证据说清楚，
> 供自动化脚本（外部程序驱动键鼠 / 内嵌 BepInEx 插件）实现「全自动通关」使用。
>
> **分析对象**：Steam 版 Overcooked! 2（appid 728880），**Mono 运行时**（非 IL2CPP），x86，Unity 2017.4.8，CLR 2.0.50727。
> 反编译产物：`ilspycmd 9.1` → 2365 个 `.cs`（含 `InControl\`、`AStar\`、`XInputDotNetPure\`、`GameModes\`、`Team17.Online.Multiplayer.Messaging\`）。
>
> **读法约定**：
> - ✅ **源码确认** = 在反编译源码里读到明确实现（会给出类名/方法名）
> - ⚠️ **实测确认** = 在运行中的游戏里用探针验证过
> - ❓ **推断** = 由上下文推理，未直接验证
>
> **一个必须记住的坑**：`IngredientAssembledNode.GetEnumerator()` 实现是 `yield return this` —— **自引用**。
> 任何对配方树调用 `foreach` / `IEnumerable` 的代码都会**无限递归**（我们实际把游戏卡死过一次）。
> 遍历配方树**只能**沿着 `m_composition` / `m_optional` 数组字段走。

---

## 零、关键勘误（动手前先读这一节）

> 四路逆向交叉验证后**推翻此前判断**的部分。均 ✅ 源码确认（全库 grep 验证）。

### 0.1 源码里**不存在**的类（别再去找）

| 曾以为 | 真实情况 |
|---|---|
| `AttachingFoodSpawner` | ❌ 不存在。那是 **GameObject 名**；职责在 `TriggerAttachedSpawn` / `ServerTriggerAttachedSpawn` |
| `DryingPart` | ❌ 不存在。它是 `WashingStation.m_dryingStation`（一个 `PlateReturnStation` 引用） |
| `Utensil`（独立类） | ❌ 不存在。厨具身份靠组件组合（`CookableContainer` / `MixableContainer` / `PreparationContainer`） |
| `Meteor` | ❌ 不存在，只有 `MeteorManager` |
| `Raft` / `Boat` / `MovingPlatform` | ❌ 不存在。**河流关木筏 = `PilotMovement` + `Rigidbody`** |
| `KillPlane`（类） | ❌ 不存在。是**层名 + 场景物体名**，上面挂 `RespawnCollider(FallDeath)` |
| `GameUtils.GetRoundData()` | ❌ 不存在。`GetRoundData()` 在 `KitchenLevelConfigBase` 上 |

### 0.2 死字段（改了没效果，别为此重启）

`PickupItemSpawner.m_spawnCost`、`PlateStation.m_createPlateTime`、`PlateStation.m_platePrefab`、`RecipeList.Entry.m_weight`、`RecipeList.m_freestyle`、`KitchenLevelConfigBase.m_freestyle*`、三套非 PC 平台的 `StarBoundaries`、`ServerPlate.ReleaseReservation`、`CompositeAssembledNode.GetEnumeratorExhaustive`
—— **全部只有声明、零读取点**。

其中 **`m_createPlateTime` 是死字段**印证了早先判断：**送餐口不发盘子**（盘子真正的生成源是 `PlateStackBase.m_platePrefab`）。
而出单权重实际由 `RoundData.GetWeight()` 的**频率均衡算法**决定（`max(target − 已出现次数, 0)`），`m_weight` 在出单路径上被直接丢弃。

### 0.3 直接决定自动化成败的机制真相

| 机制 | 真相 |
|---|---|
| **切到一半能拿走吗** | ❌ **不能**。`ServerWorkstation.CanPickupItem:126-130` 只允许 `HasFinished() \|\| GetProgress()==0` ⇒ **一旦开始切必须切到底**（且粘性交互会自动连续切） |
| **灶台能同时烧两个锅吗** | ❌ 不能。`ServerAttachStation.m_item` 是**单值** |
| **盘子怎么回架子** | **销毁 + 重生**（非搬运）：服务端摘盘+关碰撞+运动学化 → 客户端淡出后 `Object.Destroy` → `AddToStack` 里 `ServerSpawnPrefab` **新盘** |
| **订单超时会消失吗** | ❌ **永不消失**：只扣 10 分 + 连击清零 + `ResetOrderLifetime` 把计时器**回满** ⇒ **可以慢慢做菜** |
| **连击怎么算** | 必须**按订单栏从左到右**交付（`m_comboIndex == 0`），插队即断 |
| **送错菜扣分吗** | ❌ 不扣分，只清连击/倍率，订单继续挂着 |
| **小费倍率何时生效** | **滞后一单**：第1单×1、第2单×1、第3单×2、第4单×3、第5单起×4（封顶） |
| **摆盘 `_dontRemove`** | 是**干跑开关**：`Plate.CanPlaceOnPlate` 先用**影子副本**试算 + `GetOrderPlatingPrefab` 查表确认后才真放 |
| **配方匹配** | **顺序无关**（`Union` + `used[]` 贪心配对）但**数量敏感** |
| **街机大厅主题** | ❌ 不是多数票，是**从所有有效选择里均匀随机抽一个** |
| **减分源** | 只有 `TotalTimeExpireDeductions`，分数**可为负** ⇒ **OC2 没有"输"** |

### 0.4 两条与"手感"直接相关、此前完全没意识到的机制

1. **切菜节拍由动画事件驱动**：`ServerWorkstation.cs:174` 硬编码 `GetTriggerData("Chop", "Impact", ...)`
   ⇒ 一次挥刀时刻 = 玩家砍击动画里 `Impact` 事件的时刻，循环周期 = 整段动画时长。**不是常量**。
2. **朝向由移动输入决定；松开方向键则朝向保持不变**（`Update_Rotation`：输入为 0 直接 return）。
   `TurnSpeed = 20 rad/s` + 固定 `1/60` 步进 ⇒ 180° 掉头约 **0.157 秒**。
   ⇒ **"转身没转完就按交互"会判定到旧方向的台子** —— 这正是实测"拿错隔壁箱子"的根因。
   正确姿势：**先面朝目标、松开方向键（朝向保留），再按交互**。

---

## 目录

0. [关键勘误（动手前先读）](#零关键勘误动手前先读这一节)
1. [输入与玩家控制](#一输入与玩家控制)
2. [食材来源与加工](#二食材来源与加工)
3. [容器、摆盘与送餐](#三容器摆盘与送餐)
4. [订单、关卡与模式](#四订单关卡与模式)
5. [对局生命周期与大厅](#五对局生命周期与大厅)
6. [寻路系统](#六寻路系统)
7. [自动化实测校准与踩坑](#七自动化实测校准与踩坑)
8. [结论：一条完整的自动化流程](#八结论一条完整的自动化流程)

---

## 一、输入与玩家控制

> 分析对象：`E:\pythonxx\tkry\overcooked_decomp\`（ILSpy 反编译 C#，Unity 2017.4.8 / Mono / OC2 2.x）。
> 标注约定：**【源码确认】**= 反编译源码中可直接读到的常量/逻辑；**【推断】**= 依赖 Unity 资源（prefab / ScriptableObject）内容、代码里看不到的字段值。
> 路径引用格式为 `文件名:行号`。

---

### 0. 结论速览

| 问题 | 结论 | 依据 |
|---|---|---|
| PC 键盘玩家怎么"加入" | 键盘是 InControl 里**一个** `StandardActionSet`（`Device == null`），通过按下 **Space**（`EngagementButton`）触发 `GetEngagementPad → StartPadEngagement → PadEngageRoutine → EngagePadToSlot`，被"别名"到某个 `EngagementSlot` 对应的 `PadNum` | `PCPadInputProvider.cs:179`、`PlayerManagerShared.cs:153-165,325-329`、`PCPlayerManager.cs:102-121` |
| "分屏双键盘"是什么 | **不是两个键盘设备**，而是同一个物理键盘被拆成左右两半：两个 User 的 `Pad` 都是 `PadNum.One`，但 `PadSide` 分别是 `Left`/`Right`；`StandardActionSet.IsKeyboardSplit()` 检测到后把键盘绑定换成 `m_SplitKeyboard`，`PadSidednessDefinition.FilterForSide` 再按左右把真实按键分给不同玩家 | `ServerUserSystem.cs:175-183`、`UserSystemUtils.cs:271-279`、`StandardActionSet.cs:119-153`、`PadSidednessDefinition.cs:43-63` |
| P1 / P2 默认键 | P1（左半）= **WASD** + 左Shift/左Ctrl/左Alt/E；P2（右半）= **方向键** + 右Shift/右Ctrl/右Alt/I | `PCPadInputProvider.cs:55-85` + `AmbiControlsMappingData.cs:35-78` |
| 移动是否有惯性 | 目标速度**瞬时**给出（`RigidbodyMotion.SetVelocity` 直接写 `rigidbody.velocity`），只有"打滑地面"通过 `PlayerPhysicsSurface.Slippiness` 混合出惯性 | `RigidbodyMotion.cs:23-26`、`ClientPlayerControlsImpl_Default.cs:852-870` |
| 朝向如何决定 | 由**左摇杆/移动输入方向**决定，`Vector3.RotateTowards(..., TurnSpeed(20 rad/s)*dt, ...)`，固定 1/60 步进 ⇒ 180° 掉头约 **0.16 s** | `ClientPlayerControlsImpl_Default.cs:373-391`、`PlayerControlsHelper.cs:73-84`、`PlayerControls.cs:30` |
| 交互对象如何选 | 以"身前 0.5 单位的探针点"为中心，在**半径 1、±90° 半圆**内取**离探针点最近**的候选（不是纯朝向优先） | `PlayerControls.cs:742-778`、`InteractWithItemHelper.cs:153-163,218-269` |
| 投掷怎么触发 | **按住交互键（WorkstationInteract）瞄准，松开的那一帧扔出** | `ClientPlayerControlsImpl_Default.cs:285-300` |
| 接球是否需要按键 | **不需要**，半径 2 / ±45° 内自动接；背对来球则被砸退 | `ServerPlayerControlsImpl_Default.cs:242-266`、`ClientPlayerControlsImpl_Default.cs:760-794` |

---

### 1. 输入框架的三层结构

```
物理设备层    InControl InputDevice / Keyboard(伪设备)   →  PCPadInputProvider.m_allDevices
     ↓ (PadNum 索引化 + EngagePad 别名)
槽位层        EngagementSlot (One..Four) + User           →  ServerUserSystem / ClientUserSystem
     ↓ (UserSystemUtils.BuildGameInputConfig)
配置层        GameInputConfig.ConfigEntry {Player, Pad, Side, AmbiControlsMapping}
     ↓ (PlayerInputLookup.GetButton/GetValue + Gate)
逻辑层        ILogicalButton / ILogicalValue              →  PlayerControls.ControlSchemeData
```

- **逻辑层入口**：`PlayerInputLookup`（`PlayerInputLookup.cs:9`）是单例，静态字段 `s_baseInputConfig`（`:93`）保存当前 `GameInputConfig`。
- `PlayerInputLookup.GetButton/GetValue`（`:222-226, 283-287`）返回的不是裸按钮，而是 `ReassignableButton/ReassignableValue` 包一层（`BuildReassignableElement`，`:326-342`），内部用 `WeakReference` 登记到 `m_reassignableElements`；`RegenerateControls()`（`:170-179`）在输入配置变化时重新调用每个 generator ⇒ **这就是"改键/换边后所有角色立刻按新键生效"的实现**。
- 所有逻辑按钮最终都被 `GateLogicalButton/GetGated`（`PlayerControls.cs:138-146`）包一层 `CanButtonBePressed`：

**【源码确认】** `PlayerControls.CanButtonBePressed()`（`PlayerControls.cs:453-468`）四个条件全部满足才放行：
1. `Application.isFocused`（窗口有焦点）；
2. `GetDirectlyUnderPlayerControl()`（该角色当前"被直接操控"，见 `PlayerSwitchingManager.AvatarSet.ActiveAvatar` 的 setter `PlayerSwitchingManager.cs:45-59`）；
3. 没有打开的 `T17DialogBox`；
4. 根菜单 `T17InGameFlow.Instance.m_Rootmenu.GetCurrentOpenMenu() == null`。

⇒ 双键盘分屏时若切换窗口导致失焦、或开着菜单，按键会被"闸门"整体归零（数值返回 0、按钮返回 false），不会残留按下状态。

---

### 2. PC 键盘玩家如何被识别与"加入"

#### 2.1 设备层：`m_allDevices` 快照机制

**【源码确认】** `PCPadInputProvider` 的**静态构造函数**（`PCPadInputProvider.cs:31-53`）在类型首次被使用时执行一次，构造"设备快照"：

```csharp
s_engagedPads = new Dictionary<ControlPadInput.PadNum, StandardActionSet>(...);
m_allDevices  = new List<StandardActionSet>();
m_BindingListener = new BindingListener();
InitialiseBindings();                                   // 建默认/用户键盘绑定
if (debugConfig.m_keyboardType == GameDebugConfig.KeyboardType.Actual)
    m_allDevices.Add(StandardActionSet.CreateForKeyboard(m_UserKeyboardBindings));   // ← 键盘占 1 个 slot
for (int num = 0; num < InputManager.Devices.Count; num++)
    m_allDevices.Add(StandardActionSet.CreateForJoystick(InputManager.Devices[num]));
InputManager.OnDeviceAttached   += OnDeviceAttached;
InputManager.OnDeviceDetached   += OnDeviceDetached;
```

要点：

| 事实 | 说明 | 位置 |
|---|---|---|
| 键盘是一个**伪设备** | `StandardActionSet.CreateForKeyboard` 里**不设置 `Device`**，所以 `actionSet.Device == null` | `StandardActionSet.cs:155-165` |
| 判定"这个 pad 是不是键盘" | `IsKeyboard(pad) => actionSet != null && actionSet.Device == null` | `PCPadInputProvider.cs:263-271` |
| 键盘的"接入状态" | `IsPadAttached(pad)`：`Device == null` 时**恒为 true**（键盘永远在线） | `PCPadInputProvider.cs:293-305` |
| 热插拔 | `OnDeviceAttached` 往 `m_allDevices` 追加一个 joystick set；`OnDeviceDetached` 按 `x.Device == _device` 移除 | `:224-232` |
| pad → 设备映射是**动态**的 | `GetActionSet(pad)`：先查 `s_engagedPads`，未命中时**数一数比它小的未占用 pad 有几个**，再取 `m_allDevices` 里第 N 个"尚未被任何 pad 占用"的设备 | `:234-261` |
| 加入 = 把已有设备**别名**过去 | `EngagePad(oldPadNum, newPadNum) => s_engagedPads.SafeAdd(_newPadNum, GetActionSet(_oldPadNum))`；`DisengagePad` 只移除别名，**不销毁设备** | `:278-291` |

⇒ 【源码确认】`m_allDevices` 是"设备清单快照"，`s_engagedPads` 是"pad 号 → 设备"的别名表；`EngagePad` 把键盘设备从 `PadNum.One` 复制到目标槽的 pad 号上，**同一时刻一个设备可以被多个 pad 号同时引用**（这正是"分屏双键盘"能做到的前提）。

`PCPadInputProvider` 还提供按键/轴读取（`:307-320`）：

```csharp
IsEngagementDown(pad) => GetActionSet(pad)?.EngagementButton.State ?? false;
IsDown(pad, button)   => GetActionSet(pad)?.ButtonActions[button].State ?? false;
GetValue(pad, value)  => GetActionSet(pad)?.ValueActions[value].Value ?? 0f;
```

#### 2.2 加入键是什么：`EngagementButton`

**【源码确认】** `StandardActionSet.SetBindings`（`StandardActionSet.cs:177-201`）第一行无条件执行：

```csharp
actionSet.EngagementButton.AddDefaultBinding(Key.Space);   // 键盘：Space = 加入
```

手柄在 `CreateForJoystick` 里绑 `InputControlType.Action1`（`:83`）。

**【源码确认】** 逻辑封装是 `LogicalPCEngagementButton`（`LogicalPCEngagementButton.cs:9-31`）：构造时取 `Singleton<PCPadInputProvider>.Get()`，`IsDown()` 里先查 `Application.isFocused`，再 `m_InputProvider.IsEngagementDown(m_Pad)`。它由 `PlayerInputLookup.GetEngagementButton(playerGameInput)` 创建（`PlayerInputLookup.cs:252-255`）。

#### 2.3 加入链：从 `BootstrapAwake` 到 `EngagePadToSlot`

**（a）启动自动加入** 【源码确认】

```
BootstrapManager.Awake()                       BootstrapManager.cs:13-16
  └─ EnsureSetup()                             :18-33
       ├─ 实例化 m_gameMetaEnvironmentPrefab
       └─ foreach child → SendMessage("BootstrapAwake", DontRequireReceiver)   ← 统一启动钩子
```

`PCPlayerManager.BootstrapAwake()`（`PCPlayerManager.cs:87-100`）即其中之一：

```csharp
EngagementSlot slot = EngagementSlot.One;
for (int i = 0; i < PlayerInputLookup.GetSystemControllerMaximum(); i++) {   // PC = 12
    PadNum padNum = (PadNum)i;
    bool attached = Singleton<PCPadInputProvider>.Get().IsPadAttached(padNum);
    if ((slot == EngagementSlot.One || attached) && slot != EngagementSlot.Count) {
        EngagePadToSlot(padNum, slot);
        slot++;
    }
}
```

⇒ **槽 One 无条件占用**：循环第一次 `slot == One` 短路，`PadNum.One` 被强制绑给槽 One。由于 `GetSystemControllerMaximum()` 对 `PCPadInputProvider` 返回 12（`PlayerInputLookup.cs:115-130`），且键盘设备（`Device == null`）在 `IsPadAttached` 中恒为 true —— **启动后 P1 永远落在"设备清单第 0 个"，也就是键盘**（前提是 `GameDebugConfig.m_keyboardType == KeyboardType.Actual`，见 `GameDebugConfig.cs:13-17,41`）。这是"PC 版默认 P1 是键盘"的根因。

**（b）中途加入** 【源码确认】

```
PlayerManagerShared.Start()                    PlayerManagerShared.cs:198-207
  └─ for pad in 0..GetSystemControllerMaximum():
       m_engagementButtons[pad] = PlayerInputLookup.GetEngagementButton(
                                      new PlayerGameInput(pad, PadSide.Both, UnsidedAmbiMapping))
                                     ↑ LogicalPCEngagementButton(pad)

PlayerManagerShared.GetEngagementPad(out circumstances)    :153-165
  └─ 遍历 m_engagementButtons：Value.JustPressed() && !m_engagedPads.ContainsKey(pad)
       → 返回该 pad（否则 PadNum.Count）

PlayerManagerShared.StartPadEngagement(pad, circ, cb)      :325-329
  └─ slot = GetUnengagedSlot()                             :288-299 (第一个未占用槽)
  └─ AddRoutine("PadEngageRoutine", slot, PadEngageRoutine(...))

PlayerManagerShared.Update()                               :229-235
  └─ while (routine.MoveNext()) 驱动协程队列

PCPlayerManager.PadEngageRoutine(...)                      PCPlayerManager.cs:102-107
  └─ profile = EngagePadToSlot(pad, slot); finishedCallback?.Invoke(profile)

PCPlayerManager.EngagePadToSlot(pad, slot)                 :109-121
  ├─ actionSet = PCPadInputProvider.EngagePad(pad, (PadNum)slot)   ← 设备别名到槽号
  ├─ profile   = new PCPlayerProfile(actionSet)                    ← UID = "Keyboard"/Device.Meta
  ├─ if (m_lostUsers.Count > 0 && m_lostUsers[0].UID != profile.UID) { DisengagePad(slot); return null; }
  ├─ profile.StickyEngagement = (slot == EngagementSlot.One)       ← 只有 P1 是"粘性"
  └─ AssignProfileToSlot(slot, profile)                            :237-250
       └─ m_engagedPads[slot] = profile; CallEngagementChangeCallback(slot, old, new)
```

**【源码确认】** `PCPlayerManager.CanEngage`（`:59-75`）额外拦截：若该 pad 是键盘、且槽 `_e` 的输入配置里已经有 1 条以上的 entry、或唯一那条的 `Side != PadSide.Both`，则在 `!CanChangeSplitPads` 时**拒绝加入**——防止键盘在"已经分屏"的情况下又被重复占用。

**【源码确认】** `PCPlayerManager.Update`（`:137-152`）每帧检查已占用槽的 `PlayerInputLookup.IsPadAttached(pad)`，false 就 `DisengageSlots(...)` ⇒ 手柄拔线会掉人；键盘因为 `Device == null` 恒 true，**永远不掉**。

**（c）掉线重连** 【源码确认】`PlayerManagerShared.LostEngagedPlayerRoutine`（`:331-408`）：把 `Dimmer/Rejoin` UI 打开、暂停时间、屏蔽其它 EventSystem，然后循环 `GetEngagementPad` 等玩家按同样的加入键，成功后走同一个 `PadEngageRoutine`。

**（d）Steam 版差异** 【源码确认】`SteamPlayerManager : PCPlayerManager`（`SteamPlayerManager.cs:8`）只重写 `EngagePadToSlot`（`:104-116`），把 profile 换成 `SteamPlayerProfile(SteamUser.GetSteamID(), SteamFriends.GetPersonaName(), actionSet)`，`UID = steamID + (Device==null ? "Keyboard" : Device.Meta)`。**设备/槽/加入链逻辑完全复用 PCPlayerManager**。

#### 2.4 从槽位到"这台机器上的第几个玩家"

**【源码确认】** 加入事件 → 建 User：

```
ServerUserSystem.OnEngagementChanged(slot, prev, next)     ServerUserSystem.cs:290-323
  └─ next != null && !m_ignoreEngagement
       └─ ConnectionModeSwitcher == Offline →
            AddUser(true(local), s_LocalMachineId, null, 0,0, slot, PadSide.Both, ...)   :316
```

**【源码确认】** 配置生成：

```
UserSystemUtils.BuildGameInputConfig()                     UserSystemUtils.cs:235-269
  ├─ 本地 User 先排、远端 User 后排
  ├─ foreach user → SetupUserConfig(pm, ref entries, user, index)      :271-279
  │     entry = new ConfigEntry(Player=(Player)index,
  │                             Pad=user.Engagement,          ← ★ Pad 就是 EngagementSlot
  │                             Side=user.PadSide,            ← ★ 左/右半由 User 携带
  │                             user.Machine,
  │                             user.PadSide != Both ? SidedAmbiMapping : UnsidedAmbiMapping)
  ├─ PadSplitManager.FixupConfigList(entries, UnsidedAmbiMapping)       PadSplitManager.cs:181-214
  │     （给缺人手的 Player 索引补 (Player, Pad, Both, Unsided) 条目；单一占用者的 UIHandedness = Side）
  └─ PlayerInputLookup.SetBaseInputConfig(new GameInputConfig(entries.ToArray()))
```

`SetBaseInputConfig`（`PlayerInputLookup.cs:153-160`）→ `s_this.RegenerateControls()` → `PCPadInputProvider.UpdateKeyboardButtons()` + `OnRegenerateControls()`。**整条链每次有人加入/换边都会重跑一遍。**

**【源码确认】** 开局把配置灌进角色：

```
ClientKitchenLoader 玩家分配完成                        ClientKitchenLoader.cs:231-237
  └─ foreach PlayerIDProvider → ClientInputTransmitter.Setup()

ClientInputTransmitter.Setup()                          ClientInputTransmitter.cs:111-138
  ├─ iD = GetComponent<PlayerIDProvider>().GetID();  m_bInControl = (iD != Player.Count)
  ├─ m_Pad.X/Y     = GetGated(PlayerInputLookup.GetValue(MovementX/Y, iD))
  ├─ m_Pad.Dash/Curse/Worksurface/Pickup = GetGated(GetButton(...))
  └─ controlSchemeData = new PlayerControls.ControlSchemeData(iD, controls)
       → PlayerControls.SetControlSchemeData(controlSchemeData)      PlayerControls.cs:806-833
```

`PlayerControls.ControlSchemeData` 的构造函数（`PlayerControls.cs:85-95`）就是"逻辑 ID → 物理按键"的最终落地点：

```csharp
m_pickupButton          = GetGated(PlayerInputLookup.GetButton(LogicalButtonID.PickupAndDrop,        Player));
m_worksurfaceUseButton  = GetGated(PlayerInputLookup.GetButton(LogicalButtonID.WorkstationInteract,  Player));
m_dashButton            = GetGated(PlayerInputLookup.GetButton(LogicalButtonID.Dash,                 Player));
m_curseButton           = GetGated(PlayerInputLookup.GetButton(LogicalButtonID.Curse,                Player));
m_moveX                 = GetGated(PlayerInputLookup.GetValue (LogicalValueID.MovementX,             Player));
m_moveY                 = GetGated(PlayerInputLookup.GetValue (LogicalValueID.MovementY,             Player));
```

远端玩家走 `ServerInputReceiver.Setup()`（`ServerInputReceiver.cs:155-173`），把同样的 `ControlSchemeData` 换成 `NetworkLogicalButton/NetworkLogicalValue` 包装，数据来自 `ClientInputTransmitter.UpdateSynchronising()`（`ClientInputTransmitter.cs:66-104`）发来的 `ControllerStateMessage`。**注意 `ServerInputReceiver.cs:163` 的 `buttonRequiresAppFocus = IsLocallyControlled()` 恒为 false（因为外层已判断 `!flag`）——这是反编译里可见的一处冗余写法。**

---

### 3. 分屏双键盘的源码级解释（核心）

#### 3.1 一个键盘，两个"玩家半区"

**【源码确认】** 关键在于：**第二个键盘玩家不是新设备，而是"同一 EngagementSlot 上的第二个 User"**。

```
FrontendPlayerLobby.OnSplitPad()                       FrontendPlayerLobby.cs:992-1018
  ├─ firstUnsplitUser = GetFirstUnsplitUser()                     :1374-1384  (Split == NotSplit)
  ├─ 若在联机中 → 先退回 Offline
  └─ ServerUserSystem.SplitUser(firstUnsplitUser.Engagement)       ServerUserSystem.cs:325-342

ServerUserSystem.SplitUser(slot)                       ServerUserSystem.cs:325-342
  └─ AddUser(true, s_LocalMachineId, null, 0,0, slot, PadSide.Both, ...)   ← 同一个 slot！
```

`AddUser` 里检测到"该 slot 已有一个本地 User"（`:175-183`）：

```csharp
User existing = UserSystemUtils.FindUser(users, null, machineId, engagement);
if (existing != null) {
    splitStatus = User.SplitStatus.SplitPadGuest;                       // 新用户 = 客
    side        = existing.PadSide != PadSide.Right ? PadSide.Right : PadSide.Left;
    existing.Split    = User.SplitStatus.SplitPadHost;                  // 老用户 = 主
    existing.PadSide  = side != PadSide.Right ? PadSide.Right : PadSide.Left;  // → 老用户 = Left
    num = (uint)m_Users.Count;
}
```

⇒ 【源码确认】**主用户（先加入的、即用户索引 0 = P1）拿到 `PadSide.Left`，客用户拿到 `PadSide.Right`。** 若原本已是 Right，则对调（`OnUseLeftHalfOnly` / `OnUseRightHalfOnly` / `OnUseFullPad` 也走同一字段，见 `FrontendPlayerLobby.cs:943-990` + `ClientMessenger.ControllerSettings` → `ServerUserSystem.cs:118-131`）。

#### 3.2 两个 User → 两条同 Pad 不同 Side 的 ConfigEntry

`UserSystemUtils.SetupUserConfig`（`:271-279`）对两个 User 各生成一条：

| User | Player 索引 | Pad | Side | AmbiControlsMapping |
|---|---|---|---|---|
| P1（SplitPadHost） | `Player.One` | `PadNum.One` | **Left** | `PlayerManagerShared.SidedAmbiMapping` |
| P2（SplitPadGuest） | `Player.Two` | `PadNum.One` | **Right** | `PlayerManagerShared.SidedAmbiMapping` |

两个资产由 `[AssignResource("SidedAmbiControlsMappingData"/"UnsidedAmbiControlsMappingData")]` 注入（`PlayerManagerShared.cs:59-65`），并通过 `SidedAmbiMapping`/`UnsidedAmbiMapping` 属性对外暴露（`:91-93`）。

#### 3.3 触发绑定切换：`IsKeyboardSplit()` → `m_SplitKeyboard`

**【源码确认】** `StandardActionSet.IsKeyboardSplit()`（`StandardActionSet.cs:119-135`）：

```csharp
GameInputConfig cfg = PlayerInputLookup.GetBaseInputConfig();
for (i in cfg.m_playerConfigs)
    if (PCPadInputProvider.IsKeyboard(cfg.m_playerConfigs[i].Pad) &&
        cfg.m_playerConfigs[i].Side != PadSide.Both)
        return true;                // ← 只要有一条"键盘 + 非 Both"的配置，就判定为分屏键盘
return false;
```

`RefreshKeyboardActions`（`:137-153`）在 `OnUpdateKeyboardButtons` 回调里被调用（`CreateForKeyboard` 时 hook，`:155-165`），当 `IsKeyboardSplit()` 状态**发生变化**（或 `bForce`）时 `ResetActions()` 并按分支重挂绑定：

```csharp
actionSet.ResetActions();                     // 清空全部 PlayerAction
if (m_isSplit) ModifyForSplitKeyboard(a, bindings);   // → SetBindings(a, bindings.m_SplitKeyboard)
else           ModifyForCombinedKeyboard(a, bindings);// → SetBindings(a, bindings.m_CombinedKeyboard)
```

⇒ **一个键盘 action set 在两种模式间切换**：单人/未分屏 → `m_CombinedKeyboard`（整套键盘都给 P1）；分屏 → `m_SplitKeyboard`（左半给 Left，右半给 Right）。

#### 3.4 左右半区怎么分给两个玩家：`FilterForSide`

**【源码确认】** 逻辑动作先映射到"环境无关按钮"，再由 `PadSide` 过滤：

```
PlayerInputLookup.GetSidedLogicalCombo(AmbiPadButton, PlayerGameInput)      PlayerInputLookup.cs:196-202
  └─ PlayerInputLookup.GetInputConfig().GetRealButtons(playerInput, ambiButton)
       GameInputConfig.GetRealButtons(...)                                  GameInputConfig.cs:68-74
         ├─ realButtons = playerInput.AmbiControlsMapping.GetRealButtons(ambiButton)
         │      AmbiControlsMappingData.GetRealButtons()                    AmbiControlsMappingData.cs:94-109
         └─ PadSidednessDefinition.FilterForSide(playerInput.Side, realButtons)   PadSidednessDefinition.cs:43-52
  └─ Array.ConvertAll(..., id => new LogicalPadButton<PCPadInputProvider>(id.pad, id.button))
  └─ new ComboLogicalButton(buttons)      // ComboType.Or
```

**【源码确认】** 侧别判定表（`PadSidednessDefinition.cs:3-41`）：

| 归属 | 真实按钮 | 真实轴 |
|---|---|---|
| **Left** | LB, LTrigger, Back, LeftAnalog, DPadLeft, DPadRight, DPadUp, DPadDown | LStickX, LStickY, DPadX, DPadY, LTrigger |
| **Right** | RB, RTrigger, A, B, X, Y, Start, RightAnalog | RStickX, RStickY, RTrigger |
| **Both** | 不过滤，全部 | 不过滤，全部 |

**【源码确认】** 逻辑动作 → 环境无关按钮（`PlayerInputLookup.LogicalToAmbiButton`，`:354-365`）与默认 `m_buttonMapping`（`AmbiControlsMappingData.cs:35-78`）：

| 逻辑动作 | AmbiPadButton | 默认 RealButtons | ∩Left | ∩Right |
|---|---|---|---|---|
| PickupAndDrop（捡/放） | `One` | A, RB, LB | **LB** | A, RB |
| WorkstationInteract（切/交互/扔） | `Two` | X, RTrigger, LTrigger | **LTrigger** | X, RTrigger |
| Dash（冲刺） | `Three` | B, DPadLeft, DPadRight | **DPadLeft, DPadRight** | **B** |
| Curse（表情） | `Four` | ⚠ 默认表**缺失** | — | — |
| PlayerSwitch（换角色） | `Five` | B, Y, DPadUp, DPadDown | **DPadUp, DPadDown** | **B, Y** |
| Horn（喇叭） | `Horn` | Y, DPadUp | **DPadUp** | **Y** |

移动轴（`PlayerInputLookup.LogicalToAmbiValue`，`:367-383`）：

| 逻辑轴 | AmbiPadValue 组合 | 值映射（`m_valueMapping`） |
|---|---|---|
| MovementX | `DPadX` + `StickX` | StickX → LStickX, RStickX（`:80-92`） |
| MovementY | `DPadY` + `StickY` | StickY → LStickY, RStickY |

⚠ 【推断】**`AmbiPadValue.DPadX/DPadY` 与 `AmbiPadButton.Four` 在反编译出的默认数组里都缺条目**（`AmbiControlsMappingData.cs:35-78` 只有 8 条 ButtonMapping、2 条 ValueMapping）。但代码一定会走到它们：`GetFixedValue(MovementX)` 必然构造 `DPadX` 组合（`PlayerInputLookup.cs:607-624`），`PadSplitManager.Start()` 必然 `GetFixedButton(Curse, ...)`（`PadSplitManager.cs:60-70`），而 `AmbiControlsMappingData.GetRealValues/GetRealButtons` 里 `Array.Find` 返回 null 后会直接 `NullReferenceException`（`:107-114`）。因此**随包发布的 `SidedAmbiControlsMappingData` / `UnsidedAmbiControlsMappingData` 资源里必然补上了 `Four` 与 `DPadX/DPadY`**——本仓库只有反编译代码，拿不到这两个 `.asset`，**"表情键"的确切按键无法从代码 100% 确定**。

#### 3.5 拼接结果：实测 P1=WASD、P2=方向键

**【源码确认】** 分屏绑定集（`PCPadInputProvider.GetDefaultSplitKeyboardBindings`，`:55-85`）+ 3.4 的侧别过滤 =

| 动作 | P1（Left，键来自 Split 集） | P2（Right，键来自 Split 集） |
|---|---|---|
| 移动 | `LStickX +D/-A`、`LStickY -W/+S` → **W A S D** | `RStickX +RightArrow/-LeftArrow`、`RStickY -UpArrow/+DownArrow` → **↑ ↓ ← →** |
| 捡 / 放（PickupAndDrop） | `LB = 左Shift` | `A = 右Shift`、`RB = 右Shift` |
| 切 / 交互 / 扔（WorkstationInteract） | `LTrigger = 左Ctrl` | `X = 右Ctrl`、`RTrigger = 右Ctrl` |
| 冲刺（Dash） | `DPadLeft = 左Ctrl`、`DPadRight = 左Alt` | `B = 右Alt` |
| 换角色（PlayerSwitch） | `DPadUp = E`、`DPadDown = 左Shift` | `B = 右Alt`、`Y = I` |
| 喇叭（Horn） | `DPadUp = E` | `Y = I` |
| 瞄准投掷 / 接球朝向 | 同 WorkstationInteract | 同 WorkstationInteract |

对应 Split 绑定集原文（`:55-85`）：`DPadDown→LeftShift`、`DPadLeft→LeftControl`、`DPadRight→LeftAlt`、`DPadUp→E`、`LeftAnalog→T`、`LB→LeftShift`、`LTrigger→LeftControl`、`A→RightShift`、`B→RightAlt`、`X→RightControl`、`Y→I`、`RightAnalog→P`、`RB→RightShift`、`RTrigger→RightControl`、`Back→Tab`、`Start→PadEnter`；轴 `LStickX +D/-A`、`LStickY +S/-W`、`RStickX +RightArrow/-LeftArrow`、`RStickY +DownArrow/-UpArrow`、`DPadY +DownArrow/-UpArrow`（**`DPadX` 在 Split 集里没有任何绑定**）。

⚠ 【源码确认的潜在重叠】左半区里 `左Ctrl` 同时被 `LTrigger`（交互/切菜/扔）和 `DPadLeft`（冲刺）绑定，`左Shift` 同时是 `LB`（拾取）与 `DPadDown`（换角色/冲刺 Side 过滤外的 DPadDown 属 Left 但 Dash 只取 DPadLeft/DPadRight）。⇒ 默认分屏下 P1 **左Ctrl 会同时触发"交互"与"冲刺"**。INI 里没有互斥检查，唯一能改的地方是重绑界面的自动解绑逻辑：`KeyboardRebindController.UpdateBinding`（`:90-106`）在给某动作设键时，对同一个 `KeyboardRebindElementSet` 里其它元素调用 `UnsetBinding(key)`。`m_AllowSecondaryKey` 允许一个动作存两个键（`KeyboardRebindButtonElement.cs:28-40`）。

⚠ 【推断】`DPadY`（Left 侧轴）在 Split 集里被绑到 `↑/↓`，而 `RStickY` 也被绑到 `↑/↓` ⇒ 若 `SidedAmbiControlsMappingData` 的 `DPadY` 非空，则 **P1 的纵向轴也会响应↑↓**（P1/P2 纵向重叠）。这依赖资源内容，无法从代码定论；从 `LogicalToAmbiValue` 与 `GetControlAxis` 的措辞看更像是遗留绑定而不是有意设计。

#### 3.6 双键盘模式被"打回"单人的条件

- `PadSplitManager.OnEngagementChanged`（`PadSplitManager.cs:163-169`）：非粘性用户掉线 → `ClearSplitForSlot(slot)`；`ClearSplitForSlot`（`:171-179`）把该 pad 的所有 entry 移除后用 `FixupConfigList` + `UnsidedAmbiMapping` 重建配置 ⇒ `IsKeyboardSplit()` 变 false ⇒ 绑定自动切回 `m_CombinedKeyboard`。
- `ServerUserSystem.RemoveUser`（`:237-278`）：移除 SplitPadHost 会连带移除其 SplitPadGuest；移除 Guest 会把 Host 的 `Split` 复位为 `NotSplit`、`PadSide` 复位为 `Both`（`:246-256`）。
- `ControlSchemeToggle`（`ControlSchemeToggle.cs:83-104`）在设置菜单里按 `users[0].ControlType` 与 `users[0].PadSide != PadSide.Both` 自动选中"键盘-分屏"或"键盘-合并"面板，说明官方把两者作为**两套并列的预设**呈现给玩家。
- 【源码确认】`T17FrontendFlow`（`:211, 385`）把 `LogicalButtonID.UIMultiChefMenu` 绑到键盘 **E**（`PlayerInputLookup.cs:518-523`），用于在街机/多人选厨界面 `FocusOnMultiplayerKitchen()`；而 `MultiChefUIController.OnSplitChange`（`MultiChefUIController.cs:110-124`）会按 `SidedAmbiControlsMappingData` 重新生成整张 `GameInputConfig` ⇒ **街机界面本身就是"就地切分屏"的入口**。

> 【推断（实测对得上）】所谓"PC 版支持分屏双键盘、街机模式下两人同时加入"，源码级真相是：**同一个键盘 = 一个 InControl 伪设备**；两个人 = 同一个 `EngagementSlot` 上的 `SplitPadHost` + `SplitPadGuest` 两个 `User`；靠 `PadSide`（Left/Right）+ `StandardActionSet.IsKeyboardSplit()` + `PadSidednessDefinition.FilterForSide` 把它切成"WASD 半"和"方向键半"。**不是两个键盘设备同时被识别。**

---

### 4. 键位映射（默认绑定总表）

#### 4.1 单人 / 未分屏（`m_CombinedKeyboard`，`PCPadInputProvider.cs:87-121`）

| 用途 | 键 |
|---|---|
| 交互（捡/放，`PickupAndDrop`） | **Space**（`A`） |
| 切/工作台（`WorkstationInteract`，按住瞄准/松开扔） | **左Ctrl**（`X`） |
| 冲刺（`Dash`） | 左Shift（`LB`）、右Shift（`RB`） |
| 喇叭（`Horn`） | **E**（`Y`） |
| 移动 | **WASD ∪ ↑↓←→**（`LStickX +{D,RightArrow} / -{A,LeftArrow}`；`LStickY -{W,UpArrow} / +{S,DownArrow}`） |
| 换角色 / 菜单（`PlayerSwitch`、`UIMultiChefMenu`） | E |
| 暂停（`Pause`） | Esc（`PlayerInputLookup.cs:504-513`） |
| 返回/取消（`UICancel`） | Esc（`:490-495`） |
| 确认（`UISelect`） | Space（`:469-481`） |
| 跳过（`UISkip`） | 左Alt（`:496-501`） |
| 结果页切档案（`UIResultsToggleProfile`） | 左Ctrl（`:526-531`） |
| 重开关卡（`UIRestartLevel`） | 同 Dash（`:538-543`） |
| 加入（`EngagementButton`） | **Space**（`StandardActionSet.cs:179`） |

> **注意**：单人模式下 **Space 同时是"加入键"和"捡/放键"**（`EngagementButton` 与 `Button.A` 都绑 Space）。已加入后 `GetEngagementPad` 会跳过已占用 pad（`PlayerManagerShared.cs:159`），所以不会重复加入；但"P1 已经在了、再按 Space"依然是捡/放。

#### 4.2 分屏（`m_SplitKeyboard`）⇒ 见 §3.5

#### 4.3 键盘之外的按键来源

**【源码确认】** 键位是**两级表**，都硬编码在代码里、可被存档覆盖：

| 层 | 内容 | 位置 |
|---|---|---|
| 默认表 | `m_DefaultKeyboardBindings`（`DefaultKeyBindings_Split` / `_Combined`） | `InitialiseBindings()` `PCPadInputProvider.cs:123-130` |
| 用户表 | `m_UserKeyboardBindings`，初始 `CopyFrom(Default)` | 同上 |
| 存档 | `KeyboardBindings.Save/Load(saveData)` → 键名 `"DefaultKeyBindings_Combined"`/`"_Split"`、`"UserKeyBindings_Combined"`/`"_Split"` | `KeyboardBindings.cs:20-31` |
| 读写时机 | `MetaGameProgress` 保存/加载时 `PCPadInputProvider.SaveBindings/LoadBindings`（`MetaGameProgress.cs:251,268`）；`LoadBindings` 失败 → `RestoreDefaultBindings()` | `PCPadInputProvider.cs:132-172` |
| 重绑 UI | `KeyboardRebindButtonElement`/`KeyboardRebindValueElement` 用 `PlayerInputLookup.GetRealButtons(LogicalButtonID, m_Side)` + `PCPadInputProvider.GetBindings(real, bSplit: m_Side != PadSide.Both)` 读显 | `KeyboardRebindButtonElement.cs:13,66-96`、`KeyboardRebindValueElement.cs:101-117` |
| 文案/提示里的按键 | `EmbeddedTextControlKeyLookup` 同样走"GetRealButtons + GetBindings"按当前 `Side` 取第一个键 | `EmbeddedTextControlKeyLookup.cs:70-88` |

⇒ **确认当前键位的三种手段**：① 读代码里的 `GetDefaultSplitKeyboardBindings/GetDefaultCombinedKeyboardBindings`；② 读存档里的 `*KeyBindings_Combined/_Split`；③ 用重绑界面（它显示的就是 `m_UserKeyboardBindings` 的真实内容，且会随 `PadSide` 变化）。

---

### 5. 玩家移动的实现

#### 5.1 组件与调用链

**【源码确认】** `PlayerControls`（`PlayerControls.cs:6-13`）声明必须依赖：

```csharp
[ExecutionDependency(typeof(PlayerInputLookup))]
[RequireComponent(typeof(PlayerIDProvider))]
[RequireComponent(typeof(PlayerAttachmentCarrier))]
[RequireComponent(typeof(AttachmentThrower))]
[RequireComponent(typeof(GroundCast))]
[RequireComponent(typeof(RigidbodyMotion))]
```

`Awake()`（`:569-581`）**动态 `AddComponent<PlayerControlsImpl_Default>()`**，`Start()`（`:592-597`）`SetActiveControlsImpl(m_impl_default)`。

```
PlayerControls.Update()                     PlayerControls.cs:646-661
  └─ m_activeControlsImpl.Update_Impl()
       PlayerControlsImpl_Default.Update_Impl()          PlayerControlsImpl_Default.cs:90-100
         ├─ m_clientImpl.Update_Impl()   (ClientPlayerControlsImpl_Default)   :201-236
         └─ m_serverImpl.Update_Impl()   (ServerPlayerControlsImpl_Default)   :117-124
```

**客户端每帧（仅本地玩家读输入，`m_playerIDProvider.IsLocallyControlled()`）**：

```csharp
if (flag) {                                     // IsLocallyControlled
    m_controls.UpdateNearbyObjects();           // ← 交互目标扫描（§6）
    Update_Carry();
    Update_Interact(deltaTime, isUsePressed, justPressed);
    Update_Throw(deltaTime, isUsePressed, justReleased, isSuppressed);
}
Update_Aim(deltaTime, isUsePressed);
Update_Movement(deltaTime, _netPaused: false);
Update_Falling(deltaTime);
```

**服务端每帧**（`ServerPlayerControlsImpl_Default.cs:117-124`）：`Update_Carry`（拿东西但脚下没地面 → 扔掉，`:143-150` / `PlayerControlsHelper.WouldFallIfHoldingNothing`）、`Update_Catch`（自动接球）、`Update_AutoSwitch`、`Update_Interacting`。**服务端不读输入**，一切以 `ChefEventMessage` / `InputEventMessage` / `ControllerStateMessage` 为准。

#### 5.2 移动数值（`PlayerControls.MovementData`，`:22-55`）

| 字段 | 默认值 | 含义 |
|---|---|---|
| `RunSpeed` | **4** | 常规最大速度（`vector * RunSpeed`） |
| `TurnSpeed` | **20** | 转向角速度，**单位 rad/s**（`Vector3.RotateTowards`） |
| `DashSpeed` | **8** | 冲刺速度 |
| `DashTime` | **1** | 冲刺持续（秒） |
| `DashCooldown` | **1** | 冲刺冷却（秒） |
| `MaxSpeed` | **12** | 最终速度上限（`Vector3.ClampMagnitude`） |
| `GravityStrength` / `...SlopedGround` | 10 / 5 | 重力（`ApplyGravityForce`，受 debug "New Gravity" 开关影响 `:908-924`） |
| `StepHeightMax` | **0.65** | 自动上台阶高度（`StepOntoObject` `:881-893`） |
| `FootstepLength` | 0.2 | 脚步声间隔 |
| `AutoSwitchTime` | 0.5 | 自动换人（`ServerPlayerControlsImpl_Default.cs:131-141`） |
| `ImpactData` | Multiplier 2 / Time 0.2 | 击退倍率与时长（`:57-63`） |
| `m_timeBeforeFalling` | 0.2 | 离地多久算"掉落中"（`PlayerControls.cs:270`） |
| `m_pickupDelay` | 0.5 | 拾取节流（`PlayerControls.cs:273`、`:252-257`） |
| `m_analogEnableDeadzoneThreshold` / `...AngleThreshold` | 0.25 / 15° | 见 §5.4 |

#### 5.3 移动计算：`ClientPlayerControlsImpl_Default.Update_Movement`

**【源码确认】** `:393-466`，关键点：

```
m_LeftOverTime += _deltaTime;  _deltaTime = 1f/60f;          ← ★ 逻辑固定 1/60 步进
PlayerControlsHelper.BuildControlAxisData(m_controls, ref m_controlAxisData)
x = MoveX.GetValue(); y = MoveY.GetValue();
dir = PlayerControlsHelper.GetControlAxis(x, y, XAxisAllignment, YAxisAllignment);   // 已 normalized
while (m_LeftOverTime >= 1f/60f) {
    Update_Rotation(...); Update_MovementSuppression(...);
    ApplyGravityForce(); ApplyWindForce(_deltaTime);
    target = movementScale * dir * RunSpeed;
    if (m_movementInputSuppressed) target = Vector3.zero;                 // 瞄准/会话交互期间禁止移动
    if (m_dashTimer > 0) {                                                // 冲刺：S 曲线插值到 forward * DashSpeed
        s = MathUtils.SinusoidalSCurve(m_dashTimer / DashTime);
        target = (1 - s) * target + s * (movementScale * forward * DashSpeed);
    }
    if (m_impactTimer > 0) { ... 用 m_impactVelocity 插值 ... }           // 被击退
    target = Vector3.ClampMagnitude(target, MaxSpeed);
    final = ProgressVelocityWrtFriction(m_lastVelocity, Motion.GetVelocity(), target, GetSurfaceData());
    m_controls.Motion.SetVelocity(final);  m_lastVelocity = final;        // ← 直接写 rigidbody.velocity
    ApplyGroundMovement(_deltaTime);                                      // + 地面/平台自身速度
    if (dashButton.JustPressed() && DashTime - m_dashTimer >= DashCooldown && m_impactTimer < 0) { 冲刺开始 }
}
```

**【源码确认】** `RigidbodyMotion.SetVelocity`（`RigidbodyMotion.cs:23-26`）= `m_rigidbody.velocity = _velocity;` ⇒ **目标速度是瞬时赋值的**。

**【源码确认】** 惯性来自 `ProgressVelocityWrtFriction`（`:852-870`）：把"游戏速度"拆到地面法线/切线空间，用 `PlayerPhysicsSurfaceProperties.Slippiness` 把目标速度与上一帧速度做插值（`MathUtils.Remap(slippiness, 0, 1, 1, dt)`），再用 `Slidiness` 加切向滑移分量 ⇒ **普通地面 Slippiness = 0 → 全速瞬时到位；冰面 = 保留速度 → 打滑**。`SpeedMultiplier` 缩放目标速度。

**【源码确认】** 键盘输入是数字量而非模拟量：`LogicalPadValue.GetValue`（`LogicalPadValue.cs:29-47`）在正/负按键按下时直接返回 `1f`/`-1f`，否则回落到 `PCPadInputProvider.GetValue(pad, value)`（键盘轴由 InControl 的按键绑定得到，也是 ±1）；`ComboLogicalValue.GetAbsMaxValue`（`ComboLogicalValue.cs:27-39`）在多路输入中取绝对值最大者；最后 `GetControlAxis` 做 `.normalized`（`PlayerControlsHelper.cs:55-71`，其中 **`z = -(MoveY)`**，`InversionType.Inverted` 时取反）。⇒ 键盘玩家**永远满速**，没有模拟摇杆的推杆量。

> **冲刺的条件**（`:437`）：`dashButton.JustPressed() && (DashTime - m_dashTimer) >= DashCooldown && m_impactTimer < 0`。冲刺期间与其他玩家碰撞 → `OnDashCollision`（`:529-563`）：双方 `m_dashTimer = float.MinValue`（互相取消冲刺），各自沿法线切向施加 `DashImpactData.Multiplier * dir * |v|` 的击退。

#### 5.4 朝向（facing）——交互判定的真正决定因素

**【源码确认】** `Update_Rotation`（`:373-391`）：

```csharp
if (m_controls.GetDirectlyUnderPlayerControl())
    PlayerControlsHelper.TurnTowardsControlAxis(xAxis, yAxis, xInvert, yInvert,
                                                m_controlAxisData.TurnSpeed, m_controlObject, _deltaTime);
else {   // 非直接操控：转向"被追踪的投掷物"
    var catcher = m_iCatcher as ClientAttachmentCatcher;
    var tracked = catcher?.GetTrackedThrowable();
    if (tracked != null) TurnTowardsDirection(obj, tracked.pos - transform.pos, TurnSpeed, dt);
}
```

`PlayerControlsHelper.TurnTowardsControlAxis`（`PlayerControlsHelper.cs:73-84`）：

```csharp
Vector3 dir = GetControlAxis(x, y, xInv, yInv);          // normalized，输入为 0 时返回 zero
if (dir != Vector3.zero) {
    Vector3 fwd = Vector3.RotateTowards(transform.forward, dir, turnSpeed * _deltaTime, 1000f).WithY(0f);
    transform.LookAt(transform.position + fwd, Vector3.up);
}
```

⇨ **关键结论**：

1. `TurnSpeed = 20 rad/s`，逻辑步长固定 `1/60 s` ⇒ 每步最多转 `20/60 ≈ 0.333 rad ≈ 19.1°`。
2. 反向掉头 180° 需要 `π / 0.333 ≈ 9.4` 步 ≈ **0.157 秒**。
3. **输入为 0（松开方向键）时朝向保持不变**（`dir == Vector3.zero` 直接 return，`ClientPlayerControlsImpl_Default.cs:76-84`）。
4. 因此：**"按住方向键冲向某个台子并立刻按交互键"时，`transform.forward` 可能还停留在 1~2 帧前的方向**；反向掉头后马上按交互，有极大概率判定到**背后的台子**。这就是"按交互键拿到哪个台子上的东西"的第一层机制，也是实际手感上"转身没转完就按，拿错台子"的根因。

**【源码确认】** 移动输入抑制（`Update_MovementSuppression`，`:340-371`）：进入瞄准投掷（`m_aimingThrow`）或会话交互（`m_sessionInteraction != null`）时 `m_movementInputSuppressed = true`（角色被"钉住"）；退出条件——输入幅值 ≥ `m_analogEnableDeadzoneThreshold² = 0.0625` 且与"进入抑制时记住的方向"（`m_lastMoveInputDirection`）夹角 ≥ `m_analogEnableAngleThreshold = 15°`。⇒ **瞄准时想取消只能"先大幅度改方向"**。

---

### 6. 交互判定：按交互键到底跟谁交互

#### 6.1 每帧预扫描：`PlayerControls.UpdateNearbyObjects()`

**【源码确认】** `PlayerControls.cs:692-778`。

```csharp
UpdateNearbyObjects() → FindNearbyObjects()
  m_tmpInteractionObjects.Reset();
  probe = InteractWithItemHelper.GetCollidersInArc(1f, Mathf.PI, m_Transform, m_colliders, m_interactMask, m_bGridSelection);
  ...
  SetInteractionObjects(m_tmpInteractionObjects);
```

`GetCollidersInArc`（`InteractWithItemHelper.cs:165-216`）：

```csharp
int n = Physics.OverlapSphereNonAlloc(position, 2f * detectionRadius, _colliders, _layerMask);
foreach collider:
    自身对象 → 置 null
    否则 !IsColliderInArc(collider, position, forward.WithY(0).normalized, detectionRadius, arc) → 置 null
    否则 _gridSelection && OwnedByGrid(collider.transform) → 置 null
if (_gridSelection) { 把 GetFacingGridOccupant(...) 塞进第一个空槽 }
return position + 0.5f * detectionRadius * normalized;        // ★ 探针点 = 身前 0.5 单位
```

`IsColliderInArc`（`:153-163`）：

```csharp
float cosHalf = Mathf.Cos(0.5f * _arc);
Vector3 v = (ClosestPointOnSurface(collider, position) - position).WithY(0f);
return v.sqrMagnitude > 0.001f && v.sqrMagnitude < _radius*_radius
       && Vector3.Dot(_forward, v.normalized) >= cosHalf;
```

⇒ 交互主扫描 = **半径 1.0、弧 `π`（即前向 ±90°）的水平半圆**，用"碰撞体表面最近点"而不是碰撞体原点做判定（`GetClosestPointOnSurface` 对 Sphere/Capsule 有专门处理，`:119-151`）。

**【源码确认】** 三个目标各自独立扫描（`PlayerControls.cs:742-778`）：

| 输出字段 | 扫描方式 | 过滤条件 |
|---|---|---|
| `m_interactable` | 手里**没拿东西**时 `ScanForComponent(colliders, probe, c => c.CanInteract(player))`；拿着东西时优先看手里的 `ClientUsableItem.CanInteract` | `ClientInteractable.enabled`（`ClientInteractable.cs:33-36`） |
| `m_TheOriginalHandlePickup` / `m_iHandlePickup` / `m_iHandlePlacement` | `ScanForObject(colliders, probe, m_PickupOrPlaceCondition)` 然后 `PlayerControlsHelper.GetControllingPickupHandler_Client / GetControllingPlacementHandler_Client` | 见下 |
| `m_gridLocation` | `ScanForComponent(colliders, probe, x => x.IsGridOccupant())` | `StaticGridLocation` |

`m_PickupOrPlaceCondition`（`PlayerControls.cs:433-451`）：

```csharp
if (carrier != null) {
    var held = carrier.InspectCarriedItem();
    if (held == null || !_object.IsInHierarchyOf(held)) {     // ★ 手里东西自身不算候选
        var pick = PlayerControlsHelper.GetControllingPickupHandler_Client(_object);
        var place = PlayerControlsHelper.GetControllingPlacementHandler_Client(_object);
        bool canPick  = pick?.CanHandlePickup(carrier) ?? false;
        bool canPlace = held != null && place != null;
        return canPick || canPlace;
    }
}
return false;
```

#### 6.2 选择算法：不是"朝向优先"，而是"身前探针点最近优先"

**【源码确认】** `InteractWithItemHelper.ScanForObject` / `ScanForComponent`（`:218-269`）：

```csharp
float best = float.PositiveInfinity;  GameObject result = null;
foreach (var collider in _colliders) {
    if (collider == null) continue;
    foreach (var c in ComponentCache<T>.GetComponents(collider.gameObject)) {
        if (!c.enabled || (_condition != null && !_condition(c))) continue;
        float d = (_targetPosition - c.transform.position).sqrMagnitude;   // _targetPosition = 探针点
        if (d < best) { best = d; result = c; }                            // 严格小于 → 平手时先入者胜
    }
}
```

**判定规则汇总：**

1. 候选池：以角色为中心、`m_interactMask` 层、**半径 1、前向 ±90°** 的碰撞体（可用 `LevelConfigBase.m_gridSelection` 打开"格子选择"模式）。
2. 从候选中筛出满足"功能性条件"的对象（能否拾取/放置/交互）。
3. 在合格候选中取**到"身前 0.5 单位探针点"距离最小者**。
4. **`m_colliders` 的遍历顺序来自 `Physics.OverlapSphereNonAlloc`，Unity 不保证顺序**；比较用严格 `<`，所以**距离完全相同时的胜者不可预测**（实践中很少见）。
5. 朝向只通过两点间接影响：**① 半圆的方向**（背后的台子不在候选池里）；**② 探针点的位置**（身前 0.5 单位偏向当前 forward 一侧）。

⇒ **源码级的操作含义**：想稳拿某个台子，正确姿势是"**先让角色面朝它、再按交互**"；按住方向键却还没转完身时按交互，探针点与半圆都还指向旧方向，就会拿到旧方向上的东西。若两个台子一前一后夹着玩家，只有前方 ±90° 内的那个会进入候选。

#### 6.3 "优先推荐"机制：Referral（转发）与 Priority

**【源码确认】** 对象上的"控制者"由两级决定（`PlayerControlsHelper.cs:175-230`）：

| 查询 | 第一优先（硬转发） | 第二优先（按优先级） |
|---|---|---|
| Client 拾取 | `ClientHandlePickupReferral` 若 `enabled && GetHandlePickupReferree() != null` → **直接返回 referree，完全忽略 priority**（`:194-211`） | `IClientHandlePickup[]` 中 `enabled` 且 `GetPickupPriority()` 最大者（`>` 严格大于） |
| Server 拾取 | `ServerHandlePickupReferral`（`:213-230`） | 同上 |
| Client 放置 | `ClientHandlePlacementReferral`（`:175-192`） | `IClientHandlePlacement[]` 按 `GetPlacementPriority()` |
| Server 放置 | `ServerHandlePlacementReferral`（`:164-173`） | `HandlePlacementUtils.GetHighestPriority`（`HandlePlacementUtils.cs:5-17`） |

`HandlePickupReferral` 本体只是个持有 `m_pickupReferralObject` 的 MonoBehaviour（`HandlePickupReferral.cs:3-7`），`GetHandlePickupReferree()` 返回的接口由同步组件（`ClientHandlePickupReferral.cs:11-31` / `ServerHandlePickupReferral.cs`）动态设置。

**【源码确认】referral 是"随状态切换"的**，典型场景：

| 场景 | 触发 | 设置 | 位置 |
|---|---|---|---|
| 物品被放到台面/工作台上 | `ServerAttachStation.OnItemPlaced` | 物品的 `ServerHandlePickupReferral/ServerHandlePlacementReferral/ServerAttachmentCatchingProxy` referree 全部设为**该台面** | `ServerAttachStation.cs:308-341`（`:319-333`） |
| 物品被从台面拿走 | `ServerAttachStation.OnItemTaken` | referree 置回 null | `:275-306` |
| 物品被玩家拿着 | `ServerPlayerAttachmentCarrier.CarryItem` | referree 设为 `BlockPickup(this)`（**阻止自己再捡自己身上的东西**）、`BlockCatching(this)` | `ServerPlayerAttachmentCarrier.cs:127-172`（`:135-144`） |
| 丢弃 | `TakeItem` | referree 清空 | `:174-182` |
| 投掷飞行中 | `ServerThrowableItem.HandleThrow` | `m_pickupReferral.SetHandlePickupReferree(this)` ⇒ 飞行中的物品"交互控制者"是它自己（但 `GetPickupPriority() = int.MinValue`、`CanHandlePickup = !m_flying`） | `ServerThrowableItem.cs:118-143, 255-267` |
| 飞行结束 | `EndFlight` | 清空 | `:216-233` |
| 垃圾桶 / 传送口 / 干净盘堆 / 水槽 / 背包 | 各自 `UpdateSynchronising` 内按状态切换 referree | `ServerRubbishBin.cs:74,124`、`ServerTeleportalAttachment*.cs`、`ServerCleanPlateStack.cs:34-66`、`ServerWashingStation.cs:110-126`、`ServerBackpack.cs:24-66`（含 `RegisterAllowReferralBlock(CanBlockReferral)`） |

`CanBeBlocked(carrier)`（`ClientHandlePickupReferral.cs:43-46`）会调用所有 `RegisterAllowReferralBlock` 回调，任一返回 true 就不覆盖 referree ⇒ 例如背包的"只允许从开口处取"这类规则。

⇒ **"优先推荐"实际有两套语义**：① **动态转发**（referral，硬覆盖，表达"这件东西现在归谁管"）；② **静态优先级**（`GetPickupPriority/GetPlacementPriority`，同一物件上多个 handler 时的仲裁）。

#### 6.4 按下交互键之后的两段式流程

**【源码确认】客户端（本地玩家，`ClientPlayerControlsImpl_Default`）：**

| 输入 | 方法 | 条件 → 结果 |
|---|---|---|
| `PickupAndDrop.JustPressed()` | `Update_Carry` `:238-263` | 手里有东西 → `PlayerControlsHelper.PlaceHeldItem_Client` → 有 `m_iHandlePlacement` 就发 `ChefEventMessage(Place)`，否则发 `(Take)`（原地丢）；手里没东西且 `m_iHandlePickup.CanHandlePickup(carrier)` → 发 `ChefEventMessage(PickUp, m_TheOriginalHandlePickup)`，受 `m_lastPickupTimestamp / m_pickupDelay(0.5s)` 节流；否则若 `m_interactable.UsePlacementButton` → 发 `(TriggerInteract)` |
| `WorkstationInteract` 按住 | `Update_Interact` `:265-283` | 预测状态机：`m_predictedInteracted` 变化 → 发 `ChefEventMessage(Interact, obj)`；`justPressed && interactable != null` → 发 `(TriggerInteract)` |
| `WorkstationInteract` 松开 | `Update_Throw` `:285-300` | 手里有 `IClientThrowable` 且 `CanHandleThrow(thrower, forward.XZ.normalized)` → 发 `ChefEventMessage(Throw)` |

**【源码确认】服务端裁决（`ServerPlayerControlsImpl_Default`）**：

| 消息 | 方法 | 二次校验 → 执行 |
|---|---|---|
| `PickUp` | `ReceivePickUpEvent` `:152-163` | 空手 + `GetControllingPickupHandler_Server(target)` 非空 + `CanHandlePickup` → `HandlePickup(carrier, forward.XZ.normalized)`（**朝向再次参与：传入的是角色世界前向**） |
| `Place` | `ReceivePlaceEvent` `:165-171` → `PlayerControlsHelper.PlaceHeldItem_Server`（`PlayerControlsHelper.cs:126-147`） | 有东西 + 目标有 placement handler + `CanHandlePlacement(carrier, forward, PlacementContext(Source.Player))` → `HandlePlacement(...)`；失败 → `OnFailedToPlace`；目标无效 → `carrier.TakeItem()`（原地丢） |
| `Take` | `ReceiveTakeEvent` `:173-179` | 有东西 → `m_iCarrier.TakeItem()` |
| `Interact` | `ReceiveInteractEvent` `:181-205` | 目标 `ServerInteractable.CanInteract` → `BeginInteraction/EndInteraction`（含"粘性交互"语义） |
| `TriggerInteract` | `ReceiveTriggerInteractEvent` `:207-218` → `TriggerInteractable` `:285-303` | `TriggerInteract(player, forward.XZ.normalized)` **并把 `InputEventMessage(TriggerInteraction)` 广播回客户端** |
| `Throw` | `ReceiveThrowEvent` `:220-240` | 见 §7 |
| `Curse`（表情） | `OnTrigger("Curse")` `:268-275`（Animator `ITriggerReceiver`） | 发 `InputEventMessage(Curse)` → 客户端 `DoCurse()` 播音效 + `CursePFXPrefab`（`ClientPlayerControlsImpl_Default.cs:655-659`） |

**【源码确认】** 交互高亮预告：`PlayerControls.SetInteractionObjects`（`:698-706`）比较新旧目标，对**发生变化的槽位**调用 `UpdateInteractAnticipation`（`:718-740`）→ 老目标的 `IAnticipateInteractionNotifications.OnInteractionAnticipationEnded(type, player)`、新目标的 `OnInteractionAnticipationStart(...)`，`InteractionType { Pickup, Placement, Interact, Catch, GridOccupant }`（`InteractionType.cs:1-8`）。实现例：`ClientAnticipateInteractionHighlight`（`:258-266`）做 `IncrementHighlight/DecrementHighlight`（计数式描边，支持多玩家同时命中）。

---

### 7. 投掷与接球（河流 / 木筏关的传递）

#### 7.1 触发条件：按住瞄准 + 松开投掷

**【源码确认】** 客户端（`ClientPlayerControlsImpl_Default`）：

```csharp
bool isUsePressed   = m_controlScheme.IsUseDown();        // WorkstationInteract 按住
bool justReleased   = m_controlScheme.IsUseJustReleased(); // 松开那一帧
bool isSuppressed   = m_controlScheme.IsUseSuppressed();
...
Update_Throw(deltaTime, isUsePressed, justReleased, isSuppressed);
Update_Aim  (deltaTime, isUsePressed);
```

- `Update_Throw`（`:285-300`）：`!isSuppressed && justReleased && clientThrowable != null && clientThrowable.CanHandleThrow(m_iThrower, forward.XZ.normalized)` → 发 `ChefEventMessage(ChefEventType.Throw, carriedItem)`。
- `Update_Aim`（`:302-338`）：`m_aimingThrow = isUsePressed && CanHandleThrow(...)`，本地且被直接操控时 `ThrowIndicator.Show(m_aimingThrow)`（切换瞄准/待机贴图，`PlayerControls.cs:221-231`），否则 `Hide()`。

⇒ **按住交互键 = 举起来瞄准（出现抛物线指示），松开 = 扔出去。** `m_controlScheme.IsUseSuppressed()` 由 `ClearEvents()` 设置（`PlayerControls.cs:122-136`）：凡是"在按住状态下发生了事件清空"（切菜单、暂停、换人、开始会话交互），就把这次按住标记为"已吞掉"，**防止松手时误扔**。

**【源码确认】** 服务端裁决（`ServerPlayerControlsImpl_Default.ReceiveThrowEvent`，`:220-240`）：

```csharp
if (_target != null && carried != null && !PlayerControlsHelper.IsHeldItemInsideStaticCollision(m_controls)) {
    var throwable = _target.RequireInterface<IThrowable>();
    Vector3 dir = m_controlObject.transform.forward.normalized.XZ();
    if (throwable.CanHandleThrow(m_iThrower, dir)) {
        m_iCarrier.TakeItem();
        throwable.HandleThrow(m_iThrower, dir);
        SendServerEvent(new InputEventMessage(EndThrow) { entityId = ... });
    } else m_iThrower.OnFailedToThrowItem(carried);
}
```

`IsHeldItemInsideStaticCollision`（`PlayerControlsHelper.cs:268-287`）用 `OverlapBoxNonAlloc`（半尺寸 `bounds.extents * 0.5f`）检查 `Default/Ground/Walls/Worktops/PlateStationBlock/CookingStationBlock` ⇒ **东西卡在墙里/台子里时不许扔**。`ServerAttachmentThrower.OnFailedToThrowItem` 是**空实现**（`ServerAttachmentThrower.cs:84-86`）。

#### 7.2 投掷物理

**【源码确认】** `ServerThrowableItem.HandleThrow`（`:118-143`）→ `IThrower.ThrowItem` = `ServerAttachmentThrower.ThrowItem`（`:44-58`）：

```csharp
Vector3 v = CalculateThrowVelocity(_directionXZ);
attachment.AccessMotion().SetVelocity(v);                       // 直接设速度
if (catchable != null) AlertPotentialCatchers(catchable, pos, _directionXZ);
m_throwCallback(_object);

Vector3 CalculateThrowVelocity(Vector2 d) {                     // :60-67
    v.x = d.x * m_thrower.m_throwForce;
    v.z = d.y * m_thrower.m_throwForce;
    v.y = Mathf.Tan(Mathf.PI/180f * m_thrower.m_throwInclination) * m_thrower.m_throwForce;
    return v;
}
```

参数来自 `AttachmentThrower` 组件字段（`AttachmentThrower.cs:5-10`）：`m_throwInclination`（`[Range(0,90)]`，仰角，决定射程）、`m_throwForce`（初速）——**【推断】** 具体数值在 prefab 上，反编译看不到。

`HandleThrow` 还做了三件事：① `m_pickupReferral.SetHandlePickupReferree(this)`（飞行中由自己管拾取）；② `SendStateMessage(m_flying=true, thrower)`；③ 用 `Physics.OverlapBoxNonAlloc` 找出起飞点上 `Attachments` 层的碰撞体并 `Physics.IgnoreCollision(true)`（`:138-142`），同时 `ResumeAndClearThrowStartCollisions()`（`:145-157`）恢复上一轮忽略的碰撞 ⇒ **避免刚出手就和自己/同层东西撞上**。

**【源码确认】** 落地/结束条件（`ServerThrowableItem`）：

| 条件 | 处理 | 位置 |
|---|---|---|
| 飞行 ≥ 0.1 s 且速度² < 0.0225（`|v| < 0.15`） | `EndFlight()` | `:159-177` |
| 飞行 ≥ 5 s（`c_maxFlightTime`） | `EndFlight()` | `:172-175` |
| `OnCollisionEnter` 且碰撞体不是自己的 thrower，接触法线与 `Vector3.up` 夹角 ≤ 45° | `EndFlight()` + `m_landedCallback(collision.gameObject)` | `:189-206` |
| 被挂载（`OnAttachChanged`） | `EndFlight()` | `:208-214` |

`EndFlight`（`:216-233`）清 referral、`m_previousThrower = m_thrower` 并保留 `m_throwerTimeout = 2f`（`ThrowableItem.cs:10-11`）供 `GetPreviousThrower()`（用于"谁扔的"逻辑），再广播状态。`ThrowableItem` 本体只有 `m_throwParticle`/`m_throwerTimeout` 两个字段（`ThrowableItem.cs`），逻辑全在 `ServerThrowableItem`/`ClientThrowableItem`。

**【源码确认】** `ServerThrowableItem` 同时实现 `IHandlePickup`，但 `CanHandlePickup => !m_flying`、`GetPickupPriority() => int.MinValue`、`HandlePickup` 是空实现（`:255-267`）⇒ **它只是"飞行中屏蔽拾取"的占位 handler**。

#### 7.3 接球（`AttachmentCatcher` / `ServerAttachmentCatcher`）

**【源码确认】** 两条完全不同的路径：

**路径 A：被动自动接（不需要按键）**

```
ServerPlayerControlsImpl_Default.Update_Catch(dt)              :242-266
  if (m_controls.m_bRespawning) return;                        ← 重生中不接
  ICatchable c = m_controls.ScanForCatch();
  if (c == null) return;
  if (m_iCatcher.CanHandleCatch(c, forward.XZ.normalized))
      m_iCatcher.HandleCatch(c, forward.XZ.normalized);
      SendServerEvent(InputEventMessage(Catch))
```

`PlayerControls.ScanForCatch()`（`:780-784`）：

```csharp
collidersInArc = InteractWithItemHelper.GetCollidersInArc(2f, Mathf.PI/2f, m_Transform, m_colliders, m_catchMask, _gridSelection: false);
// ← 半径 2、弧 π/2（前向 ±45°）、层 m_catchMask
return InteractWithItemHelper.ScanForComponent(m_colliders, collidersInArc, m_CatchCondition);
```

`m_CatchCondition`（`PlayerControls.cs:373-380`）：

```csharp
IThrowable th = _catchable.gameObject.GetComponent<IThrowable>();
IAttachment at = _catchable.gameObject.GetComponent<IAttachment>();
return _catchable.enabled && !(at?.IsAttached() ?? false) && (th?.IsFlying() ?? false);
```

⇒ **必须"在飞、没被挂载、组件启用"** 才进候选（所以躺在地上的东西不会被"接"）。

`ServerAttachmentCatcher.CanHandleCatch`（`ServerAttachmentCatcher.cs:36-70`）的额外门槛：

| 条件 | 说明 |
|---|---|
| `_object.AllowCatch(this, dir)` | 物品自身同意（可被策略覆盖） |
| `m_carrier.InspectCarriedItem() == null` | **必须空手** |
| `throwable.GetThrower() != 自己的 IThrower` | **不能接自己扔的** |
| `(self.pos - obj.pos).sqrMagnitude <= m_catchDistance²` | 距离上限（prefab 字段，`AttachmentCatcher.cs:10-11`） |
| `Vector2.Angle(velocity.XZ, -forward.XZ) <= m_catchAngleMax` | **来向必须与角色正前方相反** ⇒ 必须"面对"来球 |

成功后 `m_carrier.CarryItem(obj)`（`:72-78`）。

**路径 B：预警式转向（面向物品）**

```
ServerAttachmentThrower.AlertPotentialCatchers(obj, pos, dir)          :69-82
  Physics.SphereCastNonAlloc(pos, radius=1f, dir, hits, distance=10f, "Players")
  foreach hit (排除自己) → hit.gameObject.RequestInterface<IHandleCatch>()?.AlertToThrownItem(obj, this, dir)

ServerAttachmentCatcher.AlertToThrownItem(thrown, thrower, dir)        :80-99
  if (m_controls != null && m_controls.GetDirectlyUnderPlayerControl()) return;   ← ★ 本地被操控的玩家不会自动转向
  if (m_trackedThrowable != null && trackable.IsFlying()) return;
  if (m_carrier.InspectCarriedItem() == null && m_controls.GetCurrentlyInteracting() == null) {
      m_trackedThrowable = thrown.AccessGameObject();
      SendTrackingData(...);       → AttachmentCatcherMessage → ClientAttachmentCatcher.ApplyServerEvent(:13-17)
  }

ServerAttachmentCatcher.UpdateSynchronising()                          :106-117
  飞行结束 或 玩家变为"被直接操控" → 清空追踪
```

客户端的 `ClientAttachmentCatcher.GetTrackedThrowable()`（`:19-21`）正是 `ClientPlayerControlsImpl_Default.Update_Rotation` 在"非直接操控"时用来转向的目标（`:380-390`）。

⇒ **源码级玩法结论**：**接球本身不需要按任何键**，只要在半径 2 / ±45° 内、空手、面向来球方向即可。`AlertToThrownItem` 的自动转向**只对非本地直接操控的角色生效**，本地玩家必须自己把角色转过去（因为朝向由移动输入决定，见 §5.4）。这条也解释了为什么"背对飞来的物品会被砸倒"：

**【源码确认】** 被砸（`ClientPlayerControlsImpl_Default.OnThrowableCollision`，`:760-794`）：

```csharp
if (!throwable.IsFlying() || throwable.GetThrower() == m_iThrower) return;   // 自己扔的不算
ICatchable catchable = obj.RequestInterface<ICatchable>();
bool hit = true;
if (catchable != null && catchable.enabled)
    hit = !InteractWithItemHelper.IsColliderInArc(_collider, transform.position,
                                                  transform.forward.WithY(0).normalized, 2f, Mathf.PI/2f);
if (hit) {
    Vector2 kb = ThrownImpactData.Multiplier * contactNormal.XZ() * |relativeVelocity|;
    DoKnockback(KnockbackType.Throw, kb, contactPoint - transform.position);
    ... 网络：有 serverImpl → SendChefEffectMessage(Impact)；否则 → ChefKnockbackEventMessage
}
```

⇒ **同一条"半径 2 / ±45° 弧"决定"接住"还是"被砸"**：弧内 → 接；弧外 → 击退（`ThrownImpactData`：Multiplier 2 / Time 0.2，`PlayerControls.cs:42,57-63`）。`DoKnockback`（`:796-814`）在"冲刺中（`m_dashTimer > 0`）"时对 Throw 类型免疫。火焰另有 `HazardImpactData`（`OnFireHazardCollision`，`:835-850`）。

---

### 8. 玩家死亡与重生

#### 8.1 触发器：`RespawnCollider` + `KillPlane`

**【源码确认】** `RespawnCollider`（`RespawnCollider.cs:3-27`）：

```csharp
public enum RespawnType { Hit, Drowning, FallDeath, Car }
public RespawnType m_respawnType = RespawnType.FallDeath;
public LayerMask  m_respawnFilter = -1;       // 默认全部层
public bool       m_onlyRespawnables;
public string     m_onRespawnTrigger = string.Empty;
public GameObject m_onDeathEffect;
```

**【源码确认】** 没有 `KillPlane.cs` 这个类；**`KillPlane` 是一个层名 + 一个场景物体名**：

| 用法 | 位置 |
|---|---|
| `LayerMask.NameToLayer("KillPlane")`，用于"胶囊是否碰到致命面"的 `CheckCapsuleKillPlane` | `CapsuleCollisionHelper.cs:21,44,85-88` |
| `const string c_floorName = "KillPlane"`；`transform.FindChildRecursive("KillPlane").gameObject.RequireComponent<RespawnCollider>()` | `LevelBounds.cs:8,64-67` |

⇒ 【推断】关卡底部的致命面是一个位于 `"KillPlane"` 层、名字也叫 `KillPlane` 的物体，上面挂着 `m_respawnType = FallDeath` 的 `RespawnCollider`。

**【源码确认】** 触发链：

```
ServerRespawnCollider.OnTriggerEnter(Collider) / OnCollisionEnter(Collision)   ServerRespawnCollider.cs:41-49
  └─ ObjectAdded(gameObject)                                                   :28-39
       if ((m_respawnFilter.value & (1 << layer)) != 0 &&
           (!m_onlyRespawnables || obj.RequestInterfaceRecursive<IRespawnBehaviour>() != null))
       {
           SendRespawnMessage(obj);                       // 客户端特效
           ServerPlayerRespawnManager.KillOrRespawn(obj, this);
           if (m_onRespawnTrigger != "") gameObject.SendTrigger(m_onRespawnTrigger);
       }

ServerPlayerRespawnManager.KillOrRespawn(obj, collider)                        ServerPlayerRespawnManager.cs:22-36
  var rb = obj.RequestInterfaceRecursive<IRespawnBehaviour>();
  if (rb != null) ms_Instance.StartRespawning(rb, collider);   // 玩家/器皿/背包有 IRespawnBehaviour
  else            NetworkUtils.DestroyObject(obj);             // 普通物品直接销毁
```

`StartRespawning`（`:7-10`）就是 `StartCoroutine(rb.RespawnCoroutine(collider))`。

#### 8.2 玩家重生链

**【源码确认】服务端** `ServerPlayerRespawnBehaviour.RespawnCoroutine`（`:21-28`）：

```csharp
DropOrDestroyHeldItems(_collider);                    // :30-48
m_ServerData.m_RespawnType = _collider.Type;
m_ServerData.m_Phase       = RespawnMessage.Phase.Begin;
SendServerEvent(m_ServerData);
```

`DropOrDestroyHeldItems`：遍历两个 `PlayerAttachTarget`（手/背包），若死亡类型是 `FallDeath` 或 `Drowning` → `carrier.TakeItem(target)` + `_collider.ObjectAdded(item)`（**把道具也丢进击杀面 → 一并销毁/重生**）；其它类型 → `PlayerControlsHelper.DropHeldItem(controls, forward.XZ())`（原地掉落）。

**【源码确认】客户端** `ClientPlayerRespawnBehaviour`：

```
ApplyServerEvent(RespawnMessage{Phase=Begin})           :86-94
  └─ m_currentRespawn = BeginRespawn(m_RespawnType); MoveNext()

BeginRespawn(type)                                      :96-175
  ├─ m_isRespawning = true; Collider.enabled = false;
  ├─ (Host/离线) motion.SetVelocity(Vector3.zero)
  ├─ WindAccumulator.Reset(); m_playerControls.m_bRespawning = true;      ← ★ 见下
  ├─ 停粒子、Animator.SetBool("Dead", true)、TriggerNXRumble
  ├─ switch(type): FallDeath → 生成 m_fallingEffect + m_fallAudioTag
  │               Drowning  → m_drownAudioTag
  │               Car       → m_bApplyGravity=false; Motion.SetKinematic(true); m_carHitAudioTag
  ├─ animator.SetTrigger(type.ToDescription())
  ├─ StartCoroutine(RunSwitchToNextDelayed(m_switchDelay /*0.5*/))         :139, :177-190
  ├─ Parallel(RunHoverCounter(), RunAvatarDestroy(type))                   :140-154
  │     Drowning 时每帧夹住下落速度: velocity.y = max(m_maxDrowingVerticalVelocity /*-0.1*/, velocity.y)
  ├─ m_windAccumulator.enabled = false
  ├─ RunSpawnAtSpawnPoint()                                                :354-405
  ├─ m_isRespawning = false; m_bRespawning = false;
  ├─ 恢复粒子、m_bApplyGravity = true、（本地/Host）Motion.SetKinematic(false)
```

**【源码确认】`m_bRespawning` 的连锁效果**（这是"重生期间不能操作"的实现）：

| 读取点 | 效果 | 位置 |
|---|---|---|
| `ServerPlayerControlsImpl_Default.Update_Catch` 首行 | `if (m_controls.m_bRespawning) return;` ⇒ **重生中不能接球** | `ServerPlayerControlsImpl_Default.cs:244-247` |
| `ServerPlayerControlsImpl_Default.Update_Interacting` | `m_controls.m_bRespawning` ⇒ 立刻 `EndInteraction()` | `:277-283` |
| `ClientPlayerRespawnBehaviour.PauseMovement/ResumeMovement` | `PlayerControls.Suppress(this)`（本地玩家）⇒ `GetDirectlyUnderPlayerControl()` 变 false ⇒ 所有 Gate 过的输入归零、`CanButtonBePressed()` 返回 false | `:421-436` + `PlayerControls.cs:453-497` |

**【源码确认】出生点位置**由 `PlayerRespawnBehaviour` 维护：

| 步骤 | 逻辑 | 位置 |
|---|---|---|
| 初始化 | `m_startParent = transform.parent; m_startLocation = transform.localPosition;` | `PlayerRespawnBehaviour.cs:122-129` |
| 动态平台 | 若关卡未 `m_disableDynamicParenting`，注册 `GroundCast.RegisterGroundChangedCallback(OnGroundChange)`；首次踩到带 `IParentable` 的地面时 `m_startParent = parentable.GetAttachPoint(gameObject)`、`m_startLocation = InverseTransformPoint(pos)`、`m_initialGroundFound = true` | `:93-106, 131-153` |
| 运行时改点 | `ClientPlayerRespawnBehaviour.MoveRespawnPoint(loc, parent)`（木筏/移动平台关卡用） | `:192-201` |

⇒ **木筏/升降台关卡"死在平台上就重生在平台上"** 就是 `OnGroundChange` + `IParentable.GetAttachPoint` 实现的。

**【源码确认】重生视觉/时序参数**（`PlayerRespawnBehaviour.cs:41-79`）：

| 字段 | 默认 | 作用 |
|---|---|---|
| `m_respawnTime` | **5** | 重生总时长（`RunHoverCounter` 倒计时 UI，`RespawnCounterUIController`） |
| `m_switchDelay` | **0.5** | 触发强制换人的延迟（`RunSwitchToNextDelayed`） |
| `m_particleTime` | 1 | 出生特效时长 |
| `m_maxDrowingVerticalVelocity` | **-0.1** | 溺水时限制继续下沉 |
| `m_respawnParams` | `{Hit, Fade, 1, 2}`、`{Car, Fade, 1, 2}` | 按死亡类型配 `Type{Fade,Scale,None}` + `m_delay` + `m_duration` |

**【源码确认】`RunAvatarDestroy`**（`:228-344`）：

- `Type.Fade`：把全部 `SkinnedMeshRenderer` 换成 `m_fadeShader`，在 `m_duration` 内把 `_Color.a` 从 1 渐变到 0 → `Pause()`（`ClientChefSynchroniser`）→ `DeactivateAndMove()` → 恢复材质与 alpha。
- `Type.Scale`：delay 后把 `localScale` 从 1 缩到 0，再等剩余时间 → `DeactivateAndMove()`。
- `Type.None`：等满 `m_respawnTime` → `DeactivateAndMove()`。
- 找不到对应 `m_deathBy` 的条目 → 走"等满 `m_respawnTime`"分支（`:327-339`）。

`DeactivateAndMove()`（`:346-352`）：关渲染、`SetCharacterCollisionsEnabled(false)`（把层切到 **`"PlayersRespawn"`**，即重生期间不与其他玩家碰撞）、`SetParent(m_startParent, worldPositionStays: false)`、`localPosition = m_startLocation`。

`RunSpawnAtSpawnPoint()`（`:354-405`）：重设 parent/position/scale、`Collider.enabled = true`、`Motion.SetVelocity(zero)`、`ServerWorldObjectSynchroniser.ResumeAllClients()`、等 `ClientChefSynchroniser.IsReadyToResume()` 后 `Resume()`、播放 `m_spawnEffect` + `PlayerSpawn` 音效、`Animator.SetBool("Dead", false)`、等 `m_particleTime`、恢复风力/层/碰撞/`ResumeMovement()`。

**【源码确认】强制换人**（`:177-190`）：延迟 `m_switchDelay` 后，若 `PlayerSwitchingManager.SelectedAvatar(player)` 还是这个刚死的角色 → `ForceSwitchToNext(player)`（`PlayerSwitchingManager.cs:118-125`）⇒ 玩家自动控制同队的下一个存活角色。

**【源码确认】掉落表现**：`ClientPlayerControlsImpl_Default.Update_Falling`（`:468-488`）：`GroundCollider == null` 累计 `m_timeOffGround`，超过 `m_timeBeforeFalling = 0.2f` 触发 `m_fallingTriggerCallback(true)`（只有表现层订阅，如动画层）；重新落地触发 `false` 并清零。

---

### 9. 状态机与速查表

#### 9.1 加入/分屏状态机

| 当前状态 | 事件 | 转移 | 关键调用 |
|---|---|---|---|
| 无玩家 | 启动 `BootstrapAwake` | 槽One 被 `PadNum.One` 占用（键盘） | `PCPlayerManager.cs:87-100` |
| P1 已加入（键盘/`Side=Both`） | 按 **Space** | 键盘被拒绝（pad One 已被占用） | `PlayerManagerShared.cs:153-165` |
| P1 已加入 | 手柄按 A / 插入手柄后被检测到 | 占用下一个空槽（1→2→3） | `GetUnengagedSlot` `:288-299` |
| P1 键盘 `Both` | 前端 `OnSplitPad()` / 街机 MultiChef 菜单 | 新增 `SplitPadGuest`（同槽），Host=Left、Guest=Right；`IsKeyboardSplit()` 变 true → 绑定切到 Split 表 | `FrontendPlayerLobby.cs:992-1018`、`ServerUserSystem.cs:175-183`、`ServerUserSystem.cs:325-342`、`StandardActionSet.cs:119-153` |
| 分屏中 | 换边 `OnUseLeftHalfOnly/RightHalf` | 两个 User 的 `PadSide` 对调 | `FrontendPlayerLobby.cs:943-990` |
| 分屏中 | Guest 掉线/退出 | `ClearSplitForSlot` → 配置重建为 `Both` → 绑定切回 Combined 表 | `PadSplitManager.cs:163-179` |
| 分屏中 | 手柄再插入 | 用下一个空槽，键盘两人不受影响 | — |
| 任意 | 手柄拔线 | `PCPlayerManager.Update` 检测 `!IsPadAttached` → `DisengageSlots` | `PCPlayerManager.cs:137-152` |
| 粘性用户（仅槽One）掉线 | — | 打开 Rejoin UI、暂停、等待按加入键回来 | `PlayerManagerShared.cs:331-408` |

#### 9.2 交互按钮状态机（`ControlSchemeData`）

| 阶段 | 条件 | 客户端动作 | 服务端动作 |
|---|---|---|---|
| 空闲 | 无输入 | `FindNearbyObjects` 每帧刷新三类目标 + 高亮预告 | — |
| 按住 `WorkstationInteract` | `IsUseDown() && !IsUseSuppressed()` | `Update_Interact` 维护 `m_predictedInteracted`，发 `Interact`；`JustPressed` 发 `TriggerInteract` | `ReceiveInteractEvent` / `ReceiveTriggerInteractEvent` |
| 按住期间被"吞掉" | `ClearEvents()` 时该键仍按住（切菜单/暂停/换人/会话交互） | `m_supressUse = true` ⇒ 松手不触发投掷 | `PlayerControls.cs:122-136, 97-120` |
| 松开 `WorkstationInteract` | `IsUseJustReleased() && !IsUseSuppressed() && 手里有 IClientThrowable && CanHandleThrow` | 发 `Throw`；若在飞 → `DoThrow` 回调（音效/成就） | `ReceiveThrowEvent` → `TakeItem + HandleThrow` |
| 按 `PickupAndDrop` | `JustPressed()` | 有东西 → `Place`/`Take`；没东西 → `PickUp`（节流 0.5s）或 `TriggerInteract` | `ReceivePickUpEvent` / `ReceivePlaceEvent` / `ReceiveTakeEvent` |
| 按 `Dash` | `JustPressed && (DashTime - dashTimer) >= Cooldown && impactTimer < 0` | `m_dashTimer = DashTime`，本地特效 + `SendServerEvent(Dash)` | `InputEventMessage(Dash)` → 其它端 `DoDash()` |
| 冲刺撞人 | `m_dashTimer > 0` 且撞到别的 `PlayerControls` | 双方冲刺互相取消 + 互相击退 `DashImpactData` | `ServerMessenger.SendChefEffectMessage(Dash, point)` |
| 按 `Curse`（表情） | `ClientEmoteWheel` 用 `GetButton(Curse)` 开轮盘（`ClientEmoteWheel.cs:409`）；Animator trigger `"Curse"` | `DoCurse()` 音效 + `CursePFXPrefab` | `ServerPlayerControlsImpl_Default.OnTrigger("Curse")` → `InputEventMessage(Curse)` |
| 按 `PlayerSwitch` | `PlayerSwitchingManager.UpdateAvatarSet` 检测 `JustPressed` | 切换 `ActiveAvatar` → 旧角色 `SetDirectlyUnderPlayerControl(false)`、新角色 `true` + 传送粒子 | `PlayerSwitchingManager.cs:208-238` |
| 重生中 | `PlayerControls.Suppress` | 全部 Gate 输入归零 | `m_bRespawning` 阻止 `Update_Catch`、强制 `EndInteraction` |

#### 9.3 死亡/重生状态机

| 阶段 | 触发 | 结果 |
|---|---|---|
| Alive | 进入 `RespawnCollider` 且层命中级 filter | `RespawnCollider.ObjectAdded` |
| 分发 | — | 有 `IRespawnBehaviour` → 重生协程；否则 `NetworkUtils.DestroyObject` |
| Begin（服务端） | `RespawnCoroutine` | 按死亡类型丢/销毁持有物，广播 `RespawnMessage(Begin, type)` |
| Begin（客户端） | `ApplyServerEvent` | 关碰撞、清零速度、`m_bRespawning=true`、Animator `Dead=true`、死亡特效/音效 |
| 强制换人 | `m_switchDelay = 0.5 s` | `PlayerSwitchingManager.ForceSwitchToNext(player)` |
| 倒计时 UI | `RunHoverCounter` | `RespawnCounterUIController` 在出生点倒计时 `m_respawnTime = 5 s` |
| 消失 | `RunAvatarDestroy` | 按 `m_respawnParams` 播 Fade/Scale/None |
| 移位 | `DeactivateAndMove` | 移到 `m_startParent/m_startLocation`，层切到 `PlayersRespawn` |
| 复活 | `RunSpawnAtSpawnPoint` | 恢复位置/缩放/碰撞/速度/层/渲染/风力，播 `m_spawnEffect`，`Dead=false` |
| Alive | `ResumeMovement` 释放 Suppressor | 恢复输入 |

---

### 10. 结论与置信度清单

**【源码确认】（可直接引用）**

1. 键盘是 InControl 里的一个**伪设备**（`Device == null`）；`IsKeyboard(pad) => actionSet.Device == null`；`IsPadAttached` 对键盘恒 true。`PCPadInputProvider.cs:263-271, 293-305`
2. 设备清单 `m_allDevices` 在**静态构造**时快照，之后只随 `InputManager.OnDeviceAttached/Detached` 增删；`s_engagedPads` 是 pad→设备别名表，`EngagePad` 只做别名复制。`PCPadInputProvider.cs:31-53, 234-291`
3. 加入键：键盘 = **Space**（`StandardActionSet.cs:179`），手柄 = `Action1`（`:83`）；逻辑封装 `LogicalPCEngagementButton`。加入链 `GetEngagementPad → StartPadEngagement → PadEngageRoutine → PCPlayerManager.EngagePadToSlot → PCPadInputProvider.EngagePad`。
4. 启动自动加入走 Unity `SendMessage("BootstrapAwake")`；`PCPlayerManager.BootstrapAwake` 无条件占据槽 One ⇒ **PC 默认 P1 = 键盘**。`BootstrapManager.cs:18-33`、`PCPlayerManager.cs:87-100`
5. **分屏双键盘 = 1 个键盘设备 + 2 个 User（同 EngagementSlot，`SplitPadHost=Left` / `SplitPadGuest=Right`）+ 同 `Pad`（`PadNum.One`）不同 `Side` 的 2 条 `ConfigEntry`**；由 `StandardActionSet.IsKeyboardSplit()` 与 `PadSidednessDefinition.FilterForSide` 实现左右分键。`ServerUserSystem.cs:175-183,325-342`、`UserSystemUtils.cs:271-279`、`StandardActionSet.cs:119-153`、`PadSidednessDefinition.cs:43-63`
6. 默认分屏键位：**P1 = WASD + 左Shift(捡/放) + 左Ctrl(切/交互/扔) + 左Ctrl/左Alt(冲刺) + E(换人/喇叭)**；**P2 = ↑↓←→ + 右Shift + 右Ctrl + 右Alt + I**。单人模式 = 整套键盘归 P1（Space/左Ctrl/左Shift/E + WASD∪方向键）。`PCPadInputProvider.cs:55-121` + `AmbiControlsMappingData.cs:35-92`
7. 移动数值：RunSpeed 4、DashSpeed 8、MaxSpeed 12、TurnSpeed **20 rad/s**、Gravity 10/5、StepHeightMax 0.65；逻辑固定 1/60 步进；目标速度**直接写 rigidbody.velocity**（无加速度斜坡），惯性只来自 `PlayerPhysicsSurface.Slippiness/Slidiness/SpeedMultiplier`。`PlayerControls.cs:22-55`、`ClientPlayerControlsImpl_Default.cs:393-466,852-870`、`RigidbodyMotion.cs:23-26`
8. **朝向由移动输入决定**，`RotateTowards(forward, dir, TurnSpeed*dt)` + `LookAt`；输入为 0 时朝向不变；180° 掉头 ≈ 0.157 s ⇒ **"没转完身就按交互"会判定到旧方向的台子**。`ClientPlayerControlsImpl_Default.cs:373-391`、`PlayerControlsHelper.cs:73-84`
9. 交互判定 = **半径 1 / 前向 ±90° 半圆**（`m_interactMask`）→ 按功能条件过滤 → 取**离"身前 0.5 单位探针点"最近**者；`m_gridSelection` 打开时改用 `GetFacingGridOccupant` 的格子选法。`PlayerControls.cs:742-778`、`InteractWithItemHelper.cs:153-163,165-216,218-269`
10. 优先级机制：**referral 硬覆盖**（`GetHandlePickupReferree() != null` 时完全忽略 priority）> **`GetPickupPriority/GetPlacementPriority` 取最大**。referral 由台面/垃圾桶/传送口/背包/玩家携带等状态动态切换。`PlayerControlsHelper.cs:164-230`、`HandlePlacementUtils.cs:5-17`、`ServerAttachStation.cs:275-341`
11. 投掷 = **按住 WorkstationInteract 瞄准（显示指示器）、松开那一帧扔出**；服务端校验"卡在静态碰撞里则不许扔"；初速由 `AttachmentThrower.m_throwForce` + `m_throwInclination` 决定（`v.y = tan(倾角) * 力度`）。`ClientPlayerControlsImpl_Default.cs:285-338`、`ServerPlayerControlsImpl_Default.cs:220-240`、`ServerAttachmentThrower.cs:44-67`
12. 接球**不需要按键**：半径 2 / ±45°（`m_catchMask`）+ 空手 + 不是自己扔的 + 面向来球 ⇒ `CanHandleCatch` → `CarryItem`。同一弧内 → 接住，弧外 → `ThrownImpactData` 击退。`PlayerControls.cs:780-784`、`ServerAttachmentCatcher.cs:36-78`、`ClientPlayerControlsImpl_Default.cs:760-794`
13. **非本地被操控的角色**收到 `AlertToThrownItem` 会记录 `m_trackedThrowable` 并**自动转向飞来的物品**；**本地被操控的玩家不会自动转向**。`ServerAttachmentCatcher.cs:80-99`、`ClientAttachmentCatcher.cs:13-21`、`ClientPlayerControlsImpl_Default.cs:380-390`
14. 死亡/重生：`RespawnCollider.RespawnType{Hit,Drowning,FallDeath,Car}` → `ServerRespawnCollider.ObjectAdded` → `ServerPlayerRespawnManager.KillOrRespawn`（无 `IRespawnBehaviour` 则销毁）→ `ServerPlayerRespawnBehaviour.RespawnCoroutine` 广播 → `ClientPlayerRespawnBehaviour.BeginRespawn`（`m_respawnTime=5`、`m_switchDelay=0.5`、层切 `PlayersRespawn`、Fade/Scale/None、`m_startParent/m_startLocation` 重生）。`RespawnCollider.cs`、`ServerRespawnCollider.cs:28-49`、`ServerPlayerRespawnManager.cs:22-36`、`ClientPlayerRespawnBehaviour.cs:96-405`
15. **`KillPlane` 不是类，是层名 + 场景物体名**（`CapsuleCollisionHelper.cs:21,44,85-88`、`LevelBounds.cs:8,64-67`）。重生点会被 `GroundCast` + `IParentable.GetAttachPoint` 动态改到当前平台上（木筏/升降台）。`PlayerRespawnBehaviour.cs:93-153`

**【推断】（受资源/prefab 限制，需实测或读 `.asset` 才能定论）**

| # | 推断内容 | 依据与不确定性 |
|---|---|---|
| I1 | `SidedAmbiControlsMappingData` / `UnsidedAmbiControlsMappingData` 实际资源里补上了 `AmbiPadButton.Four`（表情/Curse）与 `AmbiPadValue.DPadX/DPadY` 的映射 | 反编译默认数组缺这些条目（`AmbiControlsMappingData.cs:35-92`），但 `PadSplitManager.cs:67`、`PlayerInputLookup.cs:523,537`、`GetFixedValue(MovementX)` 必然走到它们，缺了会 NRE |
| I2 | 因此**"表情键"（Curse）的确切默认按键无法从代码 100% 确定**；按 `AmbiPadButton.Four` 的惯用映射推测落在右侧钮（P2 ≈ I 或 右Alt），P1 侧可能为空 | 同上 |
| I3 | 分屏集里 `DPadY`（Left 侧轴）被绑到 ↑/↓，而 `RStickY` 也被绑到 ↑/↓ ⇒ 若 sided 资源的 `DPadY` 非空，**P1 的纵向轴可能也响应方向键**（与 P2 重叠） | 依赖资源内容 |
| I4 | 分屏默认下 P1 的 **左Ctrl 同时是"交互/切/扔"和"冲刺"**（`LTrigger` 与 `DPadLeft` 都绑 LeftControl） | 绑定表是源码确认的，但"是否真的冲突到影响手感"需实测确认 InControl 是否对同名 `PlayerAction` 有抑制 |
| I5 | `AttachmentThrower.m_throwForce` / `m_throwInclination` / `m_catchDistance` / `m_catchAngleMax` / `PlayerControls.m_interactMask` `m_catchMask` / `LevelConfigBase.m_gridSelection` 的具体数值都在 prefab/关卡资源里 | 反编译只有字段声明（`AttachmentThrower.cs:5-10`、`AttachmentCatcher.cs:6-11`、`PlayerControls.cs:275-282`） |
| I6 | 关卡底部致命面是名为 `KillPlane`、位于 `KillPlane` 层、挂 `RespawnCollider(FallDeath)` 的物体 | 由 `LevelBounds.cs:8,64-67` 与 `CapsuleCollisionHelper.cs:44` 推断，未见该类 |
| I7 | "街机(Party)模式两人可同时加入"对应的是 `FrontendPlayerLobby.OnSplitPad()` / `T17FrontendFlow.FocusOnMultiplayerKitchen()`（键盘 E 打开 MultiChef 菜单）两条入口 + `ServerUserSystem.SplitUser` | 源码确认了机制与入口；"街机模式"这个具体 UI 名称需与游戏内界面比对 |

---

### 11. 一页速查（把结论用起来）

| 你想做的事 | 源码依据 | 操作结论 |
|---|---|---|
| 单人 PC 玩 | `m_CombinedKeyboard` | WASD 或方向键移动；**Space** 捡/放；**左Ctrl** 切/加工/按住瞄准松开扔；**左Shift** 冲刺；**E** 喇叭/换人 |
| 同一键盘两人玩 | `SplitPadHost/Left` + `SplitPadGuest/Right` | P1 = **WASD + 左Shift(捡) + 左Ctrl(切) + 左Ctrl/左Alt(冲刺) + E(换人)**；P2 = **方向键 + 右Shift(捡) + 右Ctrl(切) + 右Alt(冲刺) + I(换人)**；通过前端/街机的 Split Pad 入口开启 |
| 想稳定拿到某个台子上的东西 | `IsColliderInArc(1f, π)` + `ScanForObject(探针点)` | **先面朝台子并松开方向键**（朝向会保留），再按交互；反向掉头后请等 ~0.16 s |
| 两个台子一前一后 | 半圆只覆盖前方 ±90° | 背后的台子不在候选池，站到两者之间侧身即可 |
| 把东西扔过河 | 按住交互 → 松开 | 按住出瞄准指示器，松开即扔；东西卡在墙里会被服务端拒绝 |
| 接住队友扔来的东西 | `ScanForCatch(2f, π/2)` + `CanHandleCatch` | **不用按键**：站好、空手、**面朝来球**、距离 2 以内；背对会被砸退 |
| 让 AI/客机角色自动接球 | `AlertToThrownItem` 只在 `!GetDirectlyUnderPlayerControl()` 时生效 | 本地被操控的角色没有自动转向补偿 |
| 掉进河里/被车撞 | `RespawnCollider.RespawnType` | Drowning：限制下沉并丢弃（销毁）手持物；Car：关重力+运动学；等待 5 s 复活并自动换人 |

---

## 二、食材来源与加工

> 本章除单独标注外，全部为 ✅ **源码确认**（给出类名/方法名/行号）。
> 标注约定：✅ 源码确认 ｜ ❓ 推断 ｜ ⚠️ 实测确认（本章无实测项）。
>
> **本章的一条总纲**：本作的"加工"全部由**服务器权威端**推进（`Server*` 前缀类），客户端只做表现与预测。任何自动化脚本要"干预加工"，必须作用于服务器端对象；只改客户端表现不会改变真实进度。

---

### 2.0 加工方式总览

| 加工方式 | 核心实现类 | 谁来推进 | 进度载体 | 完成判据 | 失败态 | 章节 |
|---|---|---|---|---|---|---|
| 切 / 剁 | `WorkableItem` + `ServerWorkableItem` + `ServerWorkstation` | 玩家按住交互，由**动画事件**节拍驱动 | `m_progress` / `m_subProgress`（int） | `m_progress == m_stages - 1` | 无（只有未完成） | 2.3 |
| 煮 / 炸 / 烤 | `CookingHandler` + `ServerCookingStation` + `CookableContainer` | 灶台自动，`UpdateSynchronising` 累加 | `m_cookingProgress`（float 秒） | `> m_cookingtime` | `> 2×` 变 Burnt，灶台随即起火 | 2.4 |
| 焦（Burnt） | `CookingHandler.GetCookedOrderState` | 同上 | 同上 | — | `> 2×m_cookingtime` | 2.4 / 2.5 |
| 火灾 | `ServerFlammable` + `FireConfigData` | 灶台 `Ignite()` + 邻近传播 | `m_fireStrength`（0..1 float） | 达到 1 即 `Ignite()` | 烧毁台子，需灭火 | 2.5 |
| 搅 / 混合 | `MixingHandler` + `ServerMixableContainer` + `ServerMixingStation` | 搅拌台自动 | `m_mixingProgress`（float 秒） | `>= m_mixingTime` | `> 2×` 变 OverMixed | 2.6 |
| 洗 | `WashingStation` + `ServerWashingStation` | 玩家按住交互（粘性交互） | `m_cleaningTimer` + `m_plateCount` | 每盘累计 2s | 无 | 2.7 |
| 装锅/装盘 | `IngredientContainer` + `ServerIngredientContainer` | 放料动作 | `m_contents` 列表 | 容量上限 | 满则拒绝 | 2.8 |

---

### 2.1 食材来源（Spawner 族）

#### 2.1.1 `PickupItemSpawner` —— 无限供应箱（**本章最重要的结论之一**）

**数据层**（`PickupItemSpawner.cs:4-14`，✅ 源码确认）：

```csharp
[RequireComponent(typeof(HandlePickupReferral))]
public class PickupItemSpawner : MonoBehaviour {
    public GameObject m_itemPrefab;
    public int m_pickupPriority;
    public int m_spawnCost = 1;
}
```

**逻辑层**（`ServerPickupItemSpawner.cs:5-46`，✅ 源码确认）：

- 触发条件：玩家对一个挂着 `ServerPickupItemSpawner` 的台子按交互键 → 走 `IHandlePickup` 仲裁。
- 调用链：
  1. `CanHandlePickup(ICarrier)`（`:31-34`）：`return (m_flammable == null || !m_flammable.OnFire()) && !m_canHandlePickupCallbacks.CallForResult(false, _carrier);`
  2. `HandlePickup(ICarrier, Vector2)`（`:36-46`）：
     - 取挂点：`parentable.GetAttachPoint(m_pickupItemSpawner.m_itemPrefab).position`（取不到就用台子自身位置）
     - `NetworkUtils.ServerSpawnPrefab(base.gameObject, m_itemPrefab, position, Quaternion.identity)`
     - `_carrier.CarryItem(gameObject)` —— 直接塞进玩家手里
- 结果：凭空产出一个 `m_itemPrefab` 并立刻被玩家拿起。

**关于「冷却 / 数量限制」的明确答案**（✅ 源码确认）：

| 问题 | 答案 | 证据 |
|---|---|---|
| 有冷却时间吗？ | **没有**。类里不存在任何计时字段 | `ServerPickupItemSpawner` 全类无 timer |
| 有数量上限吗？ | **没有**。不存在计数或 `m_maxNumber` | 同上 |
| `m_spawnCost = 1` 有用吗？ | **没有用**。全程序集搜索 `m_spawnCost` 只有 1 处命中，就是它自己的声明 | grep 全库仅 `PickupItemSpawner.cs:13` |
| 什么会阻止它出货？ | 只有：① 箱子自身 `ServerFlammable.OnFire()` 为真；② 外部注册的 `m_canHandlePickupCallbacks` 返回 false | `:33` |
| 出货会受"数量上限"系统影响吗？ | 不会。`m_itemPrefab` 仅在 `StartSynchronising` 里 `RegisterSpawnablePrefab` 注册网络预制体 | `:18` |

> ❓ **推断**：`m_spawnCost` 是遗留/未启用的设计字段（可能是早期"食材箱有库存"方案的残留）。自动化脚本可以放心把食材箱当成**无限、零冷却**的取货点。

#### 2.1.2 `AttachItemSpawner` / `PlacementItemSpawner` —— 台子本身就是"食材"

这两个不是"生成"食材，而是让**台子自己作为一个可被放入容器的食材/调料**：

- `AttachItemSpawner`（`AttachItemSpawner.cs:5-23`）：实现 `IContainerTransferBehaviour`；`CanTransferToContainer` → `AssembledNodeTransfer.CanCombineWithContents(m_orderDefinition.GetOrderComposition(), _container)`。它自己 `[RequireComponent(typeof(IOrderDefinition))]`，即台子有配方身份。
- `ServerAttachItemSpawner`（`ServerAttachItemSpawner.cs:72-85`）：当有物品被放到台子上（`OnItemAdded`）时，用内部 `HolderAdapter`（假装载者）以 `Vector2.left` 方向调用该物品的 `ServerPlacementContainer.HandlePlacement` —— 等于**台子主动把料喂给放在它上面的容器**（典型：酱料机/自动加料台）。
- `PlacementItemSpawner`（`PlacementItemSpawner.cs:5-28`）：与上者同构，但走 `IHandlePlacement` 方向；额外带 `m_condimentAchievementFilter`（`StatValidationList`），在 `ServerPlacementItemSpawner.HandlePlacement`（`:81-90`）里若料 ID 命中过滤器则 `ServerMessenger.Achievement(_, 803)`。
- `GetPlacementPriority() => int.MaxValue`（`:92-95`）—— **最高放置优先级**，即当多个台子都愿意接收时，调料台优先。

#### 2.1.3 `TriggerAttachedSpawn` + `SpawnableEntityCollection` —— 由触发器喂料

> **命名纠正**：源码中**不存在** `AttachingFoodSpawner` 类（全库 grep 无命中）。承担"把食材喂到传送带/台子上"职责的是 `TriggerAttachedSpawn`。

**数据层**（`TriggerAttachedSpawn.cs:5-44`，✅ 源码确认）：

```csharp
public WeightedPrefab[] m_attachmentPrefabs;   // 每项含 GameObject + float m_weight
public Transform m_gridPointSelector;         // 由这个点的网格位置找目标台子
public bool m_spawnInOrder;                   // true=按序循环, false=按权重随机
public string m_trigger;                      // 触发字
public int m_maxNumber = 50;                  // 存活上限
```

`Awake()` 里若 `m_attachmentPrefabs.Length > 0`，自动 `gameObject.AddComponent<SpawnableEntityCollection>()` 并为每个预制体 `RegisterSpawnable(prefab, null)`（`:35-43`）—— 这就是网络层能同步这些预制体的原因。

**逻辑层**（`ServerTriggerAttachedSpawn.cs:40-68`，✅ 源码确认）：

- 触发条件：`OnTrigger(trigger)` 且 `trigger == m_trigger`。
- 前置闸门（`:42`）：`m_attachStation == null || m_attachStation.HasItem() || (m_conveyenceReceiver != null && m_conveyenceReceiver.IsReceiving())` → 直接 return。**台子上有东西、或传送带正在接收，就不出货。**
- 数量闸门：`m_spawned.RemoveAll(o => o == null)`（清理已销毁）后，`m_spawned.Count < m_maxNumber` 才出货。**这是全库唯一真正的数量上限机制。**
- 选择：
  - `m_spawnInOrder == true` → `MathUtils.Wrap(m_lastSpawned + 1, 0, len)`，**按数组顺序循环**（`:52-54`）
  - `false` → `m_attachmentPrefabs.GetWeightedRandomElement().Value`，**按 `m_weight` 加权随机**（`:58`）
- 结果：`NetworkUtils.ServerSpawnPrefab(...)` 在 `m_gridPointSelector.position/rotation` 生成，`m_attachStation.AddItem(gameObject, m_gridPointSelector.forward.XZ())` 放到台子上，并记入 `m_spawned`。

`FindAttachStation()`（`:70-80`）：用 `m_gridPointSelector.position` 反查 `GridManager.GetGridOccupant` → `ServerAttachStation`。**所以"喂料点"指向哪个台子完全由该 Transform 的网格坐标决定，与层级无关。**

#### 2.1.4 `TriggerTimer` / `ServerTriggerTimer` —— 定时脉冲

- 数据（`TriggerTimer.cs:7-19`）：`m_startTrigger`、`m_completeTrigger`、`m_time`、`m_startTiming`、`m_triggerAtStart`。
- 逻辑（`ServerTriggerTimer.cs:58-93`）：
  - `OnTrigger(_trigger)`：若等于 `m_startTrigger` → `m_timer = m_time`
  - `UpdateSynchronising`：`m_triggerAtStart` 时开局立刻 `SendTrigger(m_completeTrigger)`；`m_timer > 0` 时按 `TimeManager.GetDeltaTime` 递减，归零时 `SendTrigger(m_completeTrigger)`
  - **关键**：计时结束后 `m_timer` 停在 0，不会自动重触发；要再次触发必须重新收到 `m_startTrigger`。
  - `m_startTiming`/`m_triggerAtStart` 二者都需要 `IFlowController.RoundActivatedCallback` 触发 `m_intitialised = true` 之后才生效（`:22-49`）—— **即"回合真正开始"后才开始计时**，避免开场倒计时期间就出货。

#### 2.1.5 传送带 `ConveyorStation`

**数据层**（`ConveyorStation.cs:3-21`，✅ 源码确认）：

```csharp
[RequireComponent(typeof(TabletopConveyenceReceiver))]
[RequireComponent(typeof(StaticGridLocation))]
[RequireComponent(typeof(AttachStation))]
public class ConveyorStation : MonoBehaviour {
    public enum XZDirection { Leftwards, Rightwards }
    public float m_conveySpeed = 1f;                          // 默认 1
    public XZDirection m_conveyanceDirectionXZ = XZDirection.Rightwards;
    public MeshRenderer m_targetRenderer;
}
```

**方向与多格拼接**（`ServerConveyorStation.cs:237-247`，✅ 源码确认）：

```csharp
int num  = (int)Mathf.Round(0f - base.transform.right.x);   // -right.x
int num2 = (int)Mathf.Round(0f - base.transform.right.z);   // -right.z
Leftwards  => new GridIndex(m_gridIndex.X - num,  m_gridIndex.Y, m_gridIndex.Z - num2)
Rightwards => new GridIndex(m_gridIndex.X + num,  m_gridIndex.Y, m_gridIndex.Z + num2)
```

- **方向来自 Transform**：取 `-transform.right` 四舍五入到整数网格轴，所以传送带朝哪边走完全由美术摆的旋转决定，`m_conveyanceDirectionXZ` 只是在"左/右"之间翻转符号。
- **多格拼接靠网格邻居**：`GetAdjacentReceiver()`（`:226-235`）= `m_gridManager.GetGridOccupant(GetNextGridIndex())` 再 `RequestInterface<IConveyenceReceiver>()`。**一格接一格，链条自动接起来**，不需要手动连线。
- `UpdateAdjacentReceiver()` 在 `OnEnable` 和 `m_pendingAdjacentUpdate` 时刷新（`:81-97`、`:142-148`），并在邻居变化时重新注册 `RefreshStartConveyToCallback`。

**搬运流程**（`ServerConveyorStation.cs:183-218` + `ServerTabletopConveyenceReceiver.cs:96-137`）：

| 步骤 | 代码位置 | 动作 |
|---|---|---|
| 1 | `OnItemAdded` `:99-110` | 物品上台 → 记录 `m_item`/`m_responsibleForItem`，注册 `LimitedQuantityItem.RegisterImpendingDestructionNotification`，启动 `DelayRefreshStartConveying()` |
| 2 | `DelayRefreshStartConveying` `:117-122` | `yield return null` → **延迟 1 帧**再 `RefreshStartConveying()`，避免放置与搬运同帧竞争 |
| 3 | `RefreshStartConveying` `:183-196` | 条件 `CanConveyToAdjacent() && m_item != null && !m_itemBeingDestroyed && !m_selfReceiver.IsReceiving() && !m_conveying`；满足则 `SetIsConveying(true)`、`m_adjacentReceiver.InformStartingConveyToMe()`、发 `ConveyorStationMessage`，其中 `m_arriveTime = ClientTime.Time() + 1f / GetConveySpeed()` |
| 4 | `ConveyTo` `:198-203` → `ServerTabletopConveyenceReceiver.ConveyToMe` | 逐帧 `prop = Clamp01(prop + dt * speed)`，`prop < 0.5` 阶段还在原台；到 0.5 时 `_priorConveyor.TakeResponsibilityForItem()` + `attachStation2.AddItem(...)` **移交所有权**；再跑到 `prop >= 1` |
| 5 | `EndConveyence` `:205-218` | 清状态、通知邻居、**再次调用 `RefreshStartConveying()`** → 形成链式传递（A→B 完成后 B 立刻开始往 C 送） |

- **传输耗时 = 1 / m_conveySpeed 秒**（`:192`）。默认速度 1 → **1 秒过一格**。
- 中断保护：`ConveyToMe` 在传送期间注册 `OnItemAdded`/`OnItemRemoved` 探测器，任一触发即 `aborted = true` 中止（`:105-122`）。
- 接收侧拒绝手动放置：`ServerTabletopConveyenceReceiver.AllowPlacement` → `return !m_receiving;`（`:36-39`）—— **正在收货的传送带不能被人放东西**。
- `CanConveyTo(item)`（`:139-146`）：`!m_itemJustRemoved && !m_receiving && m_attachStation.CanAttachToSelf(...)`；传入 `null` 时额外要求 `m_item == null`。

**表现层**：`ClientConveyorBeltCosmeticDecisions.cs:31` → `m_ScrollSpeed = ConveySpeedToUVSpeed * m_conveyorStation.m_conveySpeed`（贴图 UV 滚动速度与逻辑速度绑定）。`ConveyorBeltCosmeticDecisions` / `ServerConveyorBeltCosmeticDecisions` 本身是空壳组件，只用于挂同步器（`MultiplayerController.cs:438` 注册 `EntityType` 映射）。

**与 `TabletopConveyenceReceiver` 的关系**（✅ 源码确认）：

```
ConveyorStation  --[RequireComponent]-->  TabletopConveyenceReceiver   (数据/标记)
                                          ↓ 同物体
                                          ServerTabletopConveyenceReceiver : IConveyenceReceiver  (逻辑)
                                          ClientTabletopConveyenceReceiver                         (表现)
```

即 `ConveyorStation` **必须**同时挂 `TabletopConveyenceReceiver`；"接收"逻辑与"传送"逻辑是同一个 GameObject 上的两半，`ServerConveyorStation` 本身实现发送侧、`ServerTabletopConveyenceReceiver` 实现接收侧。另有 `TeleportalConveyenceReceiver` / `ServerTeleportalConveyenceReceiver`（传送门）与 `TabletopConveyenceWindReceiver` 作为 `IConveyenceReceiver` 的其它实现。

#### 2.1.6 食材来源对照表

| 来源类 | 出货触发 | 出货内容 | 冷却 | 数量上限 | 落到哪里 |
|---|---|---|---|---|---|
| `ServerPickupItemSpawner` | 玩家交互 | 固定 `m_itemPrefab` | **无** | **无** | 玩家手中 |
| `ServerAttachItemSpawner` | 有物品放到台子上 | 台子自身配方 | 无 | 无 | 被放置物品的容器 |
| `ServerPlacementItemSpawner` | 玩家把容器放到台子上 | 台子自身配方 | 无 | 无 | 容器 |
| `ServerTriggerAttachedSpawn` | 收到 `m_trigger` | `m_attachmentPrefabs` 按序/加权 | 无（由触发器节拍决定） | **`m_maxNumber = 50`** | 台子（`m_gridPointSelector` 所指） |
| `ServerTriggerTimer` | `m_startTrigger` / 开局 | 只发 `m_completeTrigger` 事件 | `m_time` 秒 | 单次 | —（本身不出货） |
| `ServerConveyorStation` | 上台即搬 | 搬运已有物品 | 速度 `m_conveySpeed` | — | 下一格 `IConveyenceReceiver` |
| `ServerFireHazardSpawner` | `m_spawnTrigger` | 火焰危险区 | — | 网格占位去重 | 网格/挂点 |

---

### 2.2 切（Chopping）

#### 2.2.1 `WorkableItem` —— 数据层

`WorkableItem.cs:4-44`（✅ 源码确认）：

```csharp
public ProgressUIController m_progressUIPrefab;
public GameObject m_nextPrefab;          // 切完变成什么
public int  m_stages = 8;                // 切片总数（默认 8）
public string m_animationVariable = "Progress";
public string m_gatherVariable    = "Gather";
public string m_dropVariable      = "Drop";
```

- `Awake()`：三个字符串各 `Animator.StringToHash` 缓存为 `m_iAnimationVariable` / `m_iGatherVariable` / `m_iDropVariable`。
- `GetNextPrefab()` → `m_nextPrefab`。
- **`m_stages` 是 `[SerializeField]`，默认值 8 只是 C# 侧默认**；每个食材的实际值由 prefab 覆盖。❓ **推断**：不同食材（番茄/洋葱/肉…）的 `m_stages` 不同，具体值需从 AssetBundle 提取，反编译源码里读不到。

#### 2.2.2 刀数计算 —— `GetChopTimeMultiplier`（**核心问题**）

`WorkableItem.cs:46-65`（✅ 源码确认）：

```csharp
public int GetChopTimeMultiplier(int _playerCount) {
    GameConfig gameConfig = GameUtils.GetGameConfig();
    if (gameConfig != null) {
        int result = 1;
        GameSession gameSession = GameUtils.GetGameSession();
        switch (gameSession.TypeSettings.Type) {
        case GameSession.GameType.Cooperative:
            result = ((_playerCount != 1) ? 1 : gameConfig.SingleplayerChopTimeMultiplier);
            break;
        case GameSession.GameType.Competitive:
            result = ((_playerCount >= 4) ? 1 : gameConfig.SingleplayerChopTimeMultiplier);
            break;
        }
        return result;
    }
    return 1;
}
```

`GameConfig.cs:19`（✅ 源码确认）：

```csharp
public int SingleplayerChopTimeMultiplier = 5;
```

**语义澄清（重要）**：这个方法的返回值**不是"时间倍率"**，它在 `ServerWorkableItem` / `ClientWorkableItem` 里被赋给 `m_chopsPerSlice`：

```csharp
m_chopsPerSlice = m_workable.GetChopTimeMultiplier(ServerUserSystem.m_Users.Count);  // ServerWorkableItem.cs:29
```
（客户端 `ClientWorkableItem.cs:40` 用 `ClientUserSystem.m_Users.Count`，并在 `OnGameStateChanged` 收到 `GameState.StartEntities` 时**重算一次**，`:43-50`；服务端 `OnGameStateChanged` 同理 `:112-118`。）

**"每刀必然推进"的推进规则**（`ServerWorkableItem.cs:62-78`，✅ 源码确认）：

```csharp
public void DoWork(ServerAttachStation _station, GameObject _worker) {
    if (HasFinished()) return;
    m_subProgress++;
    if (m_subProgress >= m_chopsPerSlice && !GameUtils.GetDebugConfig().m_infiniteChopping) {
        m_subProgress = 0;
        DoWork(_station, _worker, 1);          // 真正 +1 切片
        if (HasFinished()) ServerMessenger.Achievement(_worker, 5);
    }
}
```

- 每交互一次 → `m_subProgress++`
- **每累积 `m_chopsPerSlice` 次**才让 `m_progress++`（0→`m_stages-1`）
- `HasFinished()` = `m_progress == m_workable.m_stages - 1`（`:57-60`）
- 调试开关 `GameDebugConfig.m_infiniteChopping`（`GameDebugConfig.cs:35`）：为真时**永远不推进**（条件取反），用于调试

**刀数计算表**（✅ 源码确认，`m_stages` 默认 8）：

| 模式 | 用户数 `_playerCount` | `m_chopsPerSlice` | 切片数 `m_stages-1` | **总交互次数（刀数）** | 备注 |
|---|---|---|---|---|---|
| Cooperative | 1 | **5** | 7 | **35** | 单人合作 = 5 倍惩罚 |
| Cooperative | ≥2 | **1** | 7 | **7** | 标准 |
| Competitive | ≥4 | **1** | 7 | **7** | 4 人竞技 = 标准 |
| Competitive | 1–3 | **5** | 7 | **35** | 少于 4 人即 5 倍惩罚 |
| 任意 | 任意，`m_infiniteChopping=true` | — | — | ∞ | 永不完成 |

> ⚠️ **表内"7"来自 `m_stages` 默认值 8**。真实食材的 `m_stages` 由 prefab 覆盖，❓ **推断** 实际值多为 4~8 区间，需从 AssetBundle 反查。**公式才是可靠的：总刀数 = `(m_stages - 1) × m_chopsPerSlice`。**

**同一逻辑的第二份实现**（✅ 源码确认，注意重复）：`GameUtils.cs:532-547` 有一个**完全同构**的静态方法：

```csharp
public static int GetWorkableChopsPerSlice() {
    int count = ClientUserSystem.m_Users.Count;
    ... Cooperative: count != 1 ? 1 : SingleplayerChopTimeMultiplier
        Competitive: count >= 4 ? 1 : SingleplayerChopTimeMultiplier
}
```

> ❓ **推断**：`GameUtils.GetWorkableChopsPerSlice()` 供 UI/提示类代码使用，`WorkableItem.GetChopTimeMultiplier()` 供物品自身使用；两条路径使用**不同**的用户数来源（`ClientUserSystem` vs 服务端 `ServerUserSystem`），在联机分屏下理论上可能瞬时不一致。

#### 2.2.3 完成后 GameObject 如何被 `m_nextPrefab` 替换

`ServerWorkableItem.cs:80-105`（✅ 源码确认，逐步）：

```csharp
public void DoWork(ServerAttachStation _station, GameObject _worker, int _progress) {
    if (HasFinished()) return;
    m_subProgress = 0;
    m_progress = Mathf.Min(m_progress + _progress, m_workable.m_stages - 1);
    if (!HasFinished()) return;                                   // ← 未完成就此返回

    ServerLimitedQuantityItem li = m_workable.gameObject.RequestComponent<ServerLimitedQuantityItem>();
    if (li != null) li.AddInvincibilityCondition(() => true);     // ① 保护新物品不被限量系统回收

    Vector3 localPosition = base.transform.localPosition;         // ② 存位置
    Quaternion localRotation = base.transform.localRotation;      //    存旋转
    IAttachment component = NetworkUtils.ServerSpawnPrefab(base.gameObject, m_workable.GetNextPrefab())
                                        .GetComponent<IAttachment>();  // ③ 生成 m_nextPrefab
    GameObject gameObject = _station.TakeItem();                  // ④ 摘下旧物品
    _station.AddItem(component.AccessGameObject(), Vector2.up,
                     new PlacementContext(PlacementContext.Source.Player));  // ⑤ 放上新物品
    component.AccessGameObject().transform.localPosition = localPosition;    // ⑥ 复位
    component.AccessGameObject().transform.localRotation = localRotation;
    NetworkUtils.DestroyObject(base.gameObject);                  // ⑦ 销毁旧物品
}
```

关键点：
- **顺序**：先 spawn 新的，再 `TakeItem()` 摘旧的，再 `AddItem` 放新的，最后销毁旧的 —— 台子在极短时间内"空了一下"。
- 新物品的放置上下文标记为 `PlacementContext.Source.Player`（`:101`），会影响台子的 `CanAddItem` 分支判断（见 2.4.4）。
- 网络注册：`StartSynchronising` 里 `NetworkUtils.RegisterSpawnablePrefab(base.gameObject, m_workable.GetNextPrefab())`（`ServerWorkableItem.cs:28`、`ClientWorkableItem.cs:33`）—— **客户端能同步出 `m_nextPrefab`，靠的是这一步注册。**
- 成就：`ServerMessenger.Achievement(_worker, 5)`（`:75`），**仅在两参数 `DoWork` 路径**（即"切满 `m_chopsPerSlice` 刀"）触发；直接调用三参数版本不触发。

#### 2.2.4 切菜板如何接收"切"动作 —— `Workstation` / `ServerWorkstation`

**数据层**（`Workstation.cs:7-16`，✅ 源码确认）：

```csharp
[RequireComponent(typeof(Collider))]
[RequireComponent(typeof(AttachStation))]
[RequireComponent(typeof(Interactable))]
public class Workstation : MonoBehaviour {
    public string m_chopAnimState = "Chop";
    public string m_chopTrigger   = "Impact";
    public GameObject m_chopPFX;
}
```

**逻辑层**（`ServerWorkstation.cs`，✅ 源码确认）—— **这是全章最关键的一段机制**：

`StartSynchronising`（`:59-69`）：拿 `ServerAttachStation` 与 `ServerInteractable`，注册 `RegisterCallbacks(OnInteracterAdded, OnInteracterRemoved)`，并 **`m_interactable.SetInteractionSuppressed(true)`** —— 一开始禁止交互，直到台子上出现可加工物品。

`OnItemAdded`（`:151-159`）：若 `_iHoldable` 上能找到 `ServerWorkableItem` → 记下 `m_item` 并 **`SetInteractionSuppressed(false)`** 放开交互。

`OnInteracterAdded(GameObject _interacter, Vector2 _directionXZ)`（`:171-182`）—— **切菜节拍的全部来源**：

```csharp
AnimationEventData sed = _interacter.RequireComponentRecursive<AnimationEventData>();
sed.GetTriggerData("Chop", "Impact", out var o_clipDuration, out var o_triggerTime);
Interacter interacter = new Interacter(_interacter, o_triggerTime, o_clipDuration,
                                       delegate { OnChop(_interacter.transform); });
m_interacters.Add(interacter);
m_interactable.SetStickyInteractionCallback(InteractionIsSticky);
```

- **"Chop" 是动画剪辑名，"Impact" 是剪辑内的动画事件名**，两者硬编码在 `ServerWorkstation.cs:174`。
- `AnimationEventData.GetTriggerData`（`AnimationEventData.cs:57-64`）从序列化数据里查出该剪辑的 `m_duration` 与该事件的 `m_time`。
- 所以：**一次挥刀所需的时间 = 玩家砍击动画的 `Impact` 时间点；挥刀循环周期 = 整段剪辑时长。这是由动画资产驱动的，不是游戏常量。**

`Interacter.Update(float _dt)`（`:27-39`）：

```csharp
float num = m_actionTimer + _dt;
if (m_actionTimer < m_actionTime && num >= m_actionTime) m_callback();  // 跨过 Impact 点 → 触发一次
if (num >= m_actionLength) num -= m_actionLength;                       // 循环
m_actionTimer = num;
```

`UpdateSynchronising`（`:138-149`）逐帧对每个 `Interacter` 用 `TimeManager.GetDeltaTime(interacter.m_object)` 调用 `Update`。

`OnChop(Transform _interacter)`（`:184-192`）：

```csharp
if ((bool)m_item) {
    Vector2 normalized = _interacter.forward.XZ().normalized;
    m_attachStation.RotateForDirection(normalized);      // 台子朝向玩家转
    m_item.DoWork(m_attachStation, _interacter.gameObject);  // ← 推进一刀
}
```

**"粘性交互"（sticky）机制**（`:133-136`）：

```csharp
private bool InteractionIsSticky() {
    return m_interacters.Count == 0 || (m_item != null && !m_item.HasFinished());
}
```

- 只要物品**还没切完**，交互就"粘住"持续重复挥刀 → **玩家按住交互键就能连续切**，无需反复点按。
- 物品切完（`HasFinished()`）后粘性解除，需要重新按键。

**"半成品不能拿走"规则**（`:124-131`，✅ 源码确认）：

```csharp
private bool CanPickupItem() {
    if (base.enabled && (bool)m_item)
        return m_item.HasFinished() || m_item.GetProgress() == 0f;
    return true;
}
```

→ **只有「完全没切」或「已切完」的物品能被拿走；切到一半的物品锁在菜板上。** 这条直接决定了自动化脚本的时序：一旦开始切，就必须切到底。

`OnItemRemovedItem`（`:161-169`）：物品离开 → `SetInteractionSuppressed(true)` 重新禁止交互。

**客户端表现**（`ClientWorkableItem.cs`）：
- `StartSynchronising`（`:30-41`）用 `GameUtils.InstantiateUIController(m_workable.m_progressUIPrefab.gameObject, "HoverIconCanvas")` 造进度条，`m_guibar.AutoHide = false`、`SetFollowTransform(base.transform, Vector3.zero)`
- `OnSurfacePlacement`（`:99-105`）：只有放到带 `Workstation` 的台子上才 `m_animator.enabled = true`（切菜动画只在菜板上播）
- `UpdateSynchronising`（`:115-138`）：`m_onWorkstation` → `VisState.Working`；否则按 `m_progress > 0.5f * (m_stages - 1)` 在 `Working` / `Whole` 之间切（`IngredientMeshVisibility.VisState`）
- `ApplyServerEvent`（`:52-59`）：从 `WorkableMessage` 读 `m_onWorkstation` / `m_progress` / `m_subProgress`
- `UpdateProgressVisuals`（`:186-197`）：`m_animator.SetInteger(m_workable.m_iAnimationVariable, m_progress)` —— 把整数进度喂给动画状态机的 "Progress" 参数
- **进度条公式的一处瑕疵**（✅ 源码确认，`:190`）：

```csharp
m_guibar.SetProgress((float)(m_progress * m_chopsPerSlice + m_subProgress)
                     / (float)(m_chopsPerSlice * m_workable.m_stages - 1));
```

分母用的是 `m_stages` 而**不是** `m_stages - 1`。当 `m_chopsPerSlice = 1` 时正好约掉（`7/7 = 1.0`）；当 `m_chopsPerSlice = 5`、`m_stages = 8` 时最大值为 `35 / 39 ≈ 0.897` —— **单人/少人模式下切菜进度条永远走不到 100%**。❓ **推断**：纯表现层瑕疵，不影响 `HasFinished()` 判定。

---

### 2.3 煮 / 炸 / 烤（Cooking）

#### 2.3.1 `CookingHandler` —— 核心数值

`CookingHandler.cs:3-31`（✅ 源码确认，全文只有 32 行）：

```csharp
public class CookingHandler : MonoBehaviour {
    public float m_cookingtime = 10f;                 // 默认 10 秒
    public CookingStepData m_cookingType;             // 加工"种类"（油炸/煎/烤…的身份）
    public CookingStationType m_stationType;          // 必须匹配的灶台类型
    public CookingUIController m_cookingUIPrefab;
    public Vector3 m_cookingUIPrefabOffset = Vector3.zero;

    public CookedCompositeOrderNode.CookingProgress GetCookedOrderState(float _cookingProgress) {
        if (_cookingProgress > 2f * m_cookingtime) return CookedCompositeOrderNode.CookingProgress.Burnt;
        if (_cookingProgress > m_cookingtime)     return CookedCompositeOrderNode.CookingProgress.Cooked;
        return CookedCompositeOrderNode.CookingProgress.Raw;
    }
}
```

- **`m_cookingtime = 10f` 是默认值**，每个 prefab 可覆盖。
- `m_cookingType` 是 `CookingStepData`（ScriptableObject），含 `m_icon`、`m_iconSprite`、`m_sizzleSound`、`m_addToSound`、`m_uID`（`CookingStepData.cs:7-16`）。**这是"用什么方式做"的身份标识**，通过 `CookableProperties.AllowsCookingStep` 做白名单校验（见 2.3.5）。

#### 2.3.2 Raw / Cooked / Burnt 精确阈值表 ★

设 `T = m_cookingtime`（默认 10 秒）。✅ 全部源码确认：

| 进度 `p` | `GetCookedOrderState(p)` | `IsCooked()` | `IsBurning()` | `CookingUIController.State` | 能否继续加热 |
|---|---|---|---|---|---|
| `p ≤ T` | **Raw** | `false` | `false` | `Progressing` | 能 |
| `T < p ≤ 1.3T` | **Cooked** | `true` | `false` | `Completed`（音效 `ImCooked`） | 能 |
| `1.3T < p ≤ 2T` | **Cooked** | `true` | `false` | `OverDoing`（警告图标脉冲 + `CookingWarning` 音效） | 能 |
| `p > 2T` | **Burnt** | `true` | `true` | `Ruined` | **不能**（`Cook()` 返回 false） |

以 `T = 10s` 为例的具体秒数：

| 时间 | 状态 | 备注 |
|---|---|---|
| 0 – 10.0 s | Raw | |
| 10.0 – 13.0 s | Cooked | 安全区，UI 显示对勾 |
| 13.0 – 20.0 s | Cooked | UI 转警告（`OverDoing`） |
| > 20.0 s | **Burnt** | UI `Ruined`；若关卡开启了火灾配置，**灶台随即起火** |

**两处必须注意的边界细节**（✅ 源码确认）：

1. **`>` 与 `>=` 不对称**：
   - `CookingHandler.GetCookedOrderState` 用 **严格 `>`**（`:22`、`:26`）
   - `ServerCookingHandler.IsCooked()` 用 **`>=`**（`:64`：`m_cookingProgress >= AccessCookingTime`）
   - 结果：在 `p` **恰好等于** `T` 的那一瞬间，`IsCooked()` 为真而 `GetCookedOrderState()` 仍返回 `Raw`。订单判定（用 `GetCookedOrderState`）与内部逻辑（用 `IsCooked`）在这一刻不一致。

2. **UI 的 `OverDoing` / `Ruined` 阈值来自三处不同代码**（`ServerCookingHandler.SetCookingProgress` `:77-105`）：

```csharp
if (IsBurning())                                       m_cookingState = Ruined;      // 2.0T
else if (_cookingProgress >= cookingHandler.m_cookingtime) {
    if (m_cookingState == Progressing)                 m_cookingState = Completed;   // 1.0T
    if (_cookingProgress > 1.3f * cookingHandler.m_cookingtime) m_cookingState = OverDoing; // 1.3T
}
else                                                   m_cookingState = Progressing;
```

客户端 UI 的"过度烹饪量"换算（`ClientCookingHandler.cs:121-122`）：

```csharp
float b = Mathf.Clamp(2f * cookingHandler.m_cookingtime - 0.5f,
                      cookingHandler.m_cookingtime,
                      2f * cookingHandler.m_cookingtime);
m_gui.SetOverDoingAmount(MathUtils.ClampedRemap(m_cookingProgress, cookingHandler.m_cookingtime, b, 0f, 1f));
```

→ 警告图标脉冲强度在 `[T, 2T-0.5]` 区间内从 0 映射到 1。`CookingUIController.SetOverDoingAmount`（`:117-121`）用 `m_pulseMultipliers` 数组把 0..1 映射到脉冲速度档位。

**烹饪进度的推进**（`ServerCookingHandler.cs:107-115`，✅ 源码确认）：

```csharp
public bool Cook(float _cookingDeltatTime) {
    if (!IsBurning()) {
        SetCookingProgress(m_ServerData.m_cookingProgress + _cookingDeltatTime);
        return true;
    }
    return false;
}
```

- 参数名 `_cookingDeltatTime` 实际传入的是 **"秒 × 速度倍率"**（见 2.3.4），所以 `m_cookingProgress` 的单位是"等效秒"。
- 一旦 Burnt，`Cook` 返回 `false` 且**进度冻结**（不再增长）—— 烧焦的食物不会无限累积进度。

#### 2.3.3 `CookingStationType` 枚举对照表 ★

`CookingStationType.cs:1-11`（✅ 源码确认，枚举值顺序即整数值 0–7）：

| 值 | 名称 | 字面含义 | ❓ 推断的对应厨具 | 加热方式 |
|---|---|---|---|---|
| 0 | `Invalid` | 无效 | —（未设置/哨兵值） | — |
| 1 | `Hob` | 炉盘/灶眼 | 燃气灶、平底锅位 | 需放置容器（锅/平底锅） |
| 2 | `Oven` | 烤箱 | 烤箱、烤炉 | 需放置容器 |
| 3 | `DeepFatFryer` | 深油炸锅 | 油炸篮/炸锅 | 需放置容器 |
| 4 | `FirePit` | 火坑 | 篝火/火堆 | 可能容忍直接放置 |
| 5 | `Flamethrower` | 喷火器 | 喷火机关 | 常配 `CookingRegion` 区域式 |
| 6 | `Barbeque` | 烧烤架 | 炭火烤架 | 可能容忍直接放置 |
| 7 | `FloorBurner` | 地面燃烧器 | 地面火焰/熔岩 | 常配 `CookingRegion` 区域式 |

> ⚠️ **重要区分**：
> - **✅ 源码确认**：枚举的**存在与顺序**；以及"灶台与食材靠这个枚举做**严格相等**匹配"（`CookingStation.CanAddItem` 里 `baseCookable.GetRequiredStationType() != m_stationType → false`，`CookingStation.cs:39-41`）。
> - ❓ **推断**：每一档**具体对应厨房里的哪个厨具**。`m_stationType` 是 `[SerializeField]`，实际绑定在 prefab 上；反编译源码无法确定 `Hob` 到底绑在哪些模型上。要精确知道，需从 AssetBundle 反查 prefab 的序列化值。

#### 2.3.4 `CookingStation` / `ServerCookingStation` —— 谁在真正"加热"

**数据层**（`CookingStation.cs:6-53`，✅ 源码确认）：

```csharp
public GameObject m_flameEffect;              // 火焰特效（SetCookerOn 开关）
public CookingStationType m_stationType;      // 本灶台类型
public Collider m_itemBlock;                  // 挡住后续放置的碰撞体
public bool m_attachRestrictions = true;      // 是否限制只能放匹配类型

public virtual bool CanAddItem(GameObject _object, MixedCompositeOrderNode.MixingProgress? mixingProgress) {
    if (!m_attachRestrictions) return true;                       // ① 不限就放行
    IBaseCookable baseCookable = _object.RequestInterface<IBaseCookable>();
    if (baseCookable == null) return false;                       // ② 不是可烹饪物 → 拒绝
    if (baseCookable.GetRequiredStationType() != m_stationType) return false;  // ③ 类型不匹配 → 拒绝
    IIngredientContents ingredientContents = _object.RequestInterface<IIngredientContents>();
    if (ingredientContents != null && !ingredientContents.HasContents()) return true;  // ④ 空容器 → 放行
    if (mixingProgress.HasValue && mixingProgress.Value != MixedCompositeOrderNode.MixingProgress.Mixed)
        return false;                                             // ⑤ 没搅好 → 拒绝
    return true;
}
```

**逻辑层**（`ServerCookingStation.cs`，✅ 源码确认）：

| 环节 | 位置 | 行为 |
|---|---|---|
| 挂载识别 | `OnItemAdded` `:104-120` | `m_itemPot = _iHoldable.AccessGameObject().RequestInterface<ICookable>()`；若 `GetRequiredStationType() == StationType` 则注册配方变化回调；然后 **`m_cookingStation.m_itemBlock.enabled = false`**（有锅了，就不挡了） |
| 卸下 | `OnItemRemoved` `:122-139` | 取消回调、`OnOrderCompositionChanged(NullNode)`、**`m_itemBlock.enabled = true`** 恢复阻挡 |
| 能否点火 | `CanCook(contents)` `:141-183` | ① 灶台自己着火 → false ② 内容为 `NullNode` → false ③ 复合内容里若有 `MixedCompositeAssembledNode` 且 `m_progress != Mixed` → false ④ 若 `m_itemPot != null` 且内容是混合节点，则强制按 `Cooked` 校验 `GameUtils.IsValidRecipe(...)`，配方非法 → false |
| 点火 | `OnOrderCompositionChanged` `:185-188` | `SetCookerOn(CanCook(_contents))` |
| **实际加热** | `UpdateSynchronising` `:190-217` | `if (m_isTurnedOn && m_itemPot != null) { bool flag = m_itemPot.Cook(m_cookingSpeed * TimeManager.GetDeltaTime(base.gameObject)); ... }` |
| 起火 | 同上 `:195-202` | 若 `m_itemPot.IsBurning()` 且关卡 `m_hazardInfo.FireConfigData != null` → **`SetCookerOn(false)` + `m_flammable.Ignite()`** |

**核心公式**：

```
每帧进度增量 = m_cookingSpeed × TimeManager.GetDeltaTime(gameObject)
最终进度     = Σ (m_cookingSpeed × dt)      // 单位"等效秒"，与 m_cookingtime 同量纲
```

- `m_cookingSpeed` 基类默认 `1f`（`ServerCookingStation.cs:18`）
- **单个灶台同时只能挂一个物品**（`ServerAttachStation.m_item` 是单值字段，`ServerAttachStation.cs:64`）
- `m_cookingHandler.enabled = _contents.Length > 0`（`ServerCookableContainer.cs:144`）—— **锅空着时不烹饪**；`SetCookingProgress(0f)` 在内容清空时把进度归零

**`CanAddItem` 的玩家/游戏来源分叉**（`ServerCookingStation.cs:83-102`，✅ 源码确认）：

```csharp
switch (_context.m_source) {
case PlacementContext.Source.Game:   return true;                        // 系统放置：无条件放行
case PlacementContext.Source.Player:                                     // 玩家放置：走 CanAddItem 检查
    MixedCompositeOrderNode.MixingProgress? mp = null;
    IMixable mixable = _object.RequestInterface<IMixable>();
    if (mixable != null) mp = mixable.GetMixedOrderState();
    return m_cookingStation.CanAddItem(_object, mp);
default: return false;
}
```

> 这条解释了 2.2.3 里 `ServerWorkableItem` 用 `Source.Player` 放置切好的食材——**切完的食材放回灶台会走完整的类型校验**。

#### 2.3.5 `CookableContainer` —— 锅

**数据层**（`CookableContainer.cs:3-21`，✅ 源码确认）：

```csharp
[RequireComponent(typeof(IngredientContainer))]
[RequireComponent(typeof(PlacementContainer))]
[RequireComponent(typeof(CookingHandler))]
public class CookableContainer : MonoBehaviour {
    public OrderToPrefabLookup m_approvedContentsList;   // 允许的成品白名单
    public GameObject m_cosmeticsPrefab;
}
```

`Start()` 里 `m_approvedContentsList.CacheAssembledOrderNodes()` 预缓存简化节点。

**放置校验**（`CookableContainer.cs:44-60`，✅ 源码确认）：

```csharp
public bool AllowItemPlacement(GameObject _object, PlacementContext _context, IBaseCookable _iCookingHandler) {
    CookableProperties cp = _object.RequestComponent<CookableProperties>();
    if (cp == null || !cp.AllowsCookingStep(_iCookingHandler.AccessCookingType)) return false;  // ① 加工方式白名单
    if (m_approvedContentsList != null) {
        IOrderDefinition od = _object.RequestInterface<IOrderDefinition>();
        if (od != null && m_approvedContentsList.GetPrefabForNode(od.GetOrderComposition()) == null) return false;  // ② 成品白名单
    }
    return !_iCookingHandler.IsBurning();   // ③ 烧焦了就装不进东西
}
```

三条闸门：**① `CookableProperties.AllowsCookingStep`（按 `CookingStepData.m_uID` 匹配）；② `OrderToPrefabLookup` 配方白名单；③ 锅正在烧焦则拒收。**

**配方组装**（`ServerCookableContainer.GetOrderComposition` `:64-75` → `CookableContainer.GetOrderComposition` `:23-42`，✅ 源码确认）：

```csharp
CookedCompositeAssembledNode node = new CookedCompositeAssembledNode();
if (_cookableMixableContents != null) {
    if (!_isMixed) return _cookableMixableContents;                  // 没搅好的，原样返回
    node.m_composition = new AssembledDefinitionNode[1] { _cookableMixableContents };
} else {
    node.m_composition = _itemContainer.GetContents();               // 未混合：直接用容器内容
}
node.m_cookingStep     = _cookingHandler.AccessCookingType;
node.m_recordedProgress = _cookingHandler.GetCookingProgress() / _cookingHandler.AccessCookingTime;  // 归一化
node.m_progress        = _cookingHandler.GetCookedOrderState();
return node;
```

→ **订单判定用的结构**：内容 + 加工方式（`m_cookingStep`）+ 归一化进度 + Raw/Cooked/Burnt 三态。

**"预烹饪内容转移"（锅倒锅）**（`ServerCookableContainer.cs:106-134`，✅ 源码确认）：

```csharp
private float CalculateCombinedCookingProgress(float recipientProgress, int recipientContents,
                                              float receivedProgress, int receivedContents) {
    if (Mathf.Max(recipientProgress, receivedProgress) > 2f * m_cookingHandler.AccessCookingTime)
        return Mathf.Max(recipientProgress, receivedProgress);            // 任一已烧焦 → 取大的（保持烧焦）
    if (recipientContents == 0) return receivedProgress;
    if (receivedContents == 0)  return recipientProgress;
    return (Mathf.Min(recipientProgress,  T) + Mathf.Min(receivedProgress, T)) * 0.5f;  // 取平均，各自封顶 T
}
```

→ **两个都是"熟"的锅合并，结果取平均值**；已经烧焦的则以烧焦那个为准。转换后 `GameUtils.TriggerAudio(GameOneShotAudioTag.AddToPot, ...)`（`:116`）。

#### 2.3.6 食材是直接放灶台，还是必须先放锅里？★

**答案：两者都可能，取决于该物品自身是否实现 `ICookable`。主路径是"先放锅"。**（✅ 源码确认）

机制链条：

1. `ServerCookingStation.OnItemAdded` 取的是 **`m_itemPot = _iHoldable.AccessGameObject().RequestInterface<ICookable>()`**（`:106`）—— 它只认**挂在灶台上的那一个物品**。而 `ServerAttachStation` 一次只持有**一个** `IAttachment`（`ServerAttachStation.cs:64`），所以灶台上永远只有一个东西在加热。
2. 那个东西必须是 `ICookable`：
   - `CookableContainer`（锅/平底锅）**自身带 `[RequireComponent(typeof(CookingHandler))]`**，其 `ServerCookableContainer` 实现 `IOrderDefinition, IContainerTransferBehaviour` 并提供 `GetCookingHandler()`，被 `ICookable` 体系识别 → **锅就是那个被加热的对象，锅里的料是它的 `m_composition`。**
   - 一个裸食材 prefab 若自己也挂 `CookingHandler` + `CookableIngredient`，同样可以直接放到灶台上被加热。`CookableIngredient.cs:3-9` 正是为此存在：

```csharp
[RequireComponent(typeof(CookingHandler))]
public class CookableIngredient : MonoBehaviour {
    public IngredientOrderNode m_ingredientOrderNode;
}
```

3. `CookingStation.CanAddItem` 用 `GetRequiredStationType() != m_stationType` 做闸门（`:39-41`），**类型匹配与"是锅还是裸食材"无关**，只与 `CookingHandler.m_stationType` 有关。

**结论表**：

| 场景 | 灶台上放什么 | 谁在烹饪 | 是否可行 |
|---|---|---|---|
| 家常汤/炸物 | `CookableContainer`（锅） | 锅的 `ServerCookableContainer.m_cookingHandler`，热的是锅的内容数组 | ✅ 主路径 |
| 直接烤/直接煮的食材 | 裸食材（自带 `CookingHandler` + `CookableIngredient`） | 食材自身的 `ServerCookingHandler` | ✅ 可行（`CookableIngredient` 的存在即为证据） |
| 想把生食材**放到**已经架在灶台上的锅里 | 生食材 → 锅的 `PlacementContainer` | 锅在加热，食材进锅成为内容 | ✅ 可行 |
| 想让灶台同时加热两个独立物品 | — | — | ❌ 不可能，`m_item` 单值 |

> ❓ **推断**：具体哪些食材能"直接放灶台"，由每个 prefab 是否挂 `CookingHandler`/`CookableIngredient` 决定，需从 AssetBundle 反查。**从自动化脚本视角，最稳的通用策略是统一走"锅"路径**：先取锅 → 放灶台 → 往锅里加料 → 等熟 → 取锅。

#### 2.3.7 `CookableProperties` —— 加工方式白名单

`CookableProperties.cs:4-12`（✅ 源码确认）：

```csharp
public class CookableProperties : MonoBehaviour {
    [SerializeField] private CookingStepData[] AllowedCookingSteps = new CookingStepData[0];
    public bool AllowsCookingStep(CookingStepData _stepData) {
        return Array.FindIndex(AllowedCookingSteps, x => x != null && x.m_uID == _stepData.m_uID) != -1;
    }
}
```

→ **按 `CookingStepData.m_uID` 精确匹配**。这就是"番茄能煮不能炸、肉能煎不能煮"这类规则的实现方式。

#### 2.3.8 `HeatedCookingStation` —— 可变火力的灶台

**数据层**（`HeatedCookingStation.cs:3-20`，✅ 源码确认）：

```csharp
public class HeatedCookingStation : CookingStation {
    public HeatedStation m_heatSource;            // 可指向外部热源
    [Range(0,1)] public float m_cookingSpeedHigh     = 1f;
    [Range(0,1)] public float m_cookingSpeedModerate = 0.66f;
    [Range(0,1)] public float m_cookingSpeedLow      = 0.33f;
}
```

**逻辑层**（`ServerHeatedCookingStation.cs:9-48`，✅ 源码确认）：

- `StartSynchronising`：优先用 `m_heatSource` 上的 `ServerHeatedStation`，没有就用自己身上的；注册 `RegisterHeatRangeChangedCallback(OnHeatRangeChanged)`，然后**立刻 `OnHeatRangeChanged(HeatRange.Low)`** → **初始速度 = 0.33**
- 映射：`HeatRange.High → 1f`；`Moderate → 0.66f`；`Low → 0.33f`

→ **同一口灶，火力三档直接改变 `m_cookingSpeed`，从而线性改变实际烹饪耗时**：慢火需要 `T/0.33 ≈ 3×T`，中火 `T/0.66 ≈ 1.5×T`，大火 `T`。

#### 2.3.9 `CookingRegion` / `ServerCookingRegion` —— 区域式加热（喷火器/地面火）

**数据层**（`CookingRegion.cs:3-28`，✅ 源码确认）：

```csharp
[RequireComponent(typeof(TriggerRecorder))]
public class CookingRegion : MonoBehaviour {
    public ParticleSystem m_glowEffect;
    public float m_flameOffRadius = 2f;
    public AnimationCurve m_heightCurve;
    public float m_fadeDuration = 1f;
    public Renderer m_burnerRenderer;
    public ParticleSystem[] m_flameEffects;
    public Collider m_TriggerArea;
    public CookingStationType m_StationType;
}
```

**逻辑层**（`ServerCookingRegion.cs:31-60`，✅ 源码确认）：

```csharp
List<Collider> recent = m_recorder.GetRecentCollisions();
for (int i = 0; i < recent.Count; i++) {
    ICookable cookable = recent[i].gameObject.RequestInterface<ICookable>();
    if (cookable == null || cookable.GetRequiredStationType() != m_CookingRegion.m_StationType
        || m_cookedThisFrame.Contains(cookable)) continue;
    GridManager gm = GameUtils.GetGridManager(recent[i].transform);
    if (gm != m_gridManager) continue;
    if (gm.GetGridLocationFromPos(recent[i].transform.position) != m_gridIndex) continue;  // 必须同一格
    IOrderDefinition od = recent[i].gameObject.RequestInterface<IOrderDefinition>();
    if (od.GetOrderComposition().Simpilfy() != AssembledDefinitionNode.NullNode) {
        cookable.Cook(TimeManager.GetDeltaTime(base.gameObject.layer));   // ← 加热
        m_cookedThisFrame.Add(cookable);
    }
}
```

**与 `CookingStation` 的本质区别**：

| | `ServerCookingStation` | `ServerCookingRegion` |
|---|---|---|
| 加热对象 | 挂在 `ServerAttachStation` 上的**单个** `ICookable` | 触发区内的**任意多个** `ICookable`（只要同格、类型匹配） |
| 是否需要"放上去" | 需要（`AttachStation`） | 不需要，丢进区域即可 |
| 火力倍率 | `m_cookingSpeed`（受 `HeatedCookingStation` 影响） | 固定 1（直接 `dt`） |
| 空内容判定 | `OnContentsChanged` 控制 `enabled` | 每帧用 `Simpilfy() != NullNode` 判定 |
| 典型用途 | 炉灶、烤箱、炸锅 | 喷火器（`Flamethrower`）、地面火（`FloorBurner`） |

> ⚠️ `ServerCookingRegion` 里**没有回头检查 `IsBurning()` 后熄火**的逻辑；烧焦后的处理依赖食物自身的 `IsBurning()` 与火险系统。

**客户端侧**：`ClientCookingRegion` + `IClientCookingRegionNotified`；`ClientCookingHandler.EnterCookingRegion()/ExitCookingRegion()`（`:228-236`）切换 `m_isInCookingRegion`，影响 UI 显示策略（在区域内时 `m_hideAfterTime = 1f`，持续显示；`:124-137`）。

#### 2.3.10 烹饪 UI（`CookingUIController`）

`CookingUIController.cs:8-15`（✅ 源码确认）：

```csharp
public enum State { Idle, Progressing, Completed, OverDoing, Ruined }
```

| State | 触发条件 | 表现（`SetState` `:39-80`） |
|---|---|---|
| `Idle` | 无进度 / 已处理完 | 全部隐藏 |
| `Progressing` | `p < T` | 显示进度条，隐藏警告 |
| `Completed` | `p ≳ T`（从 `Progressing` 跃迁时） | 隐藏进度条，**播放 `GameOneShotAudioTag.ImCooked`**，跑 `RunTickAnimation()` 对勾淡入淡出（`m_tickAnimationLength`） |
| `OverDoing` | `p > 1.3T` | 显示 `m_warningIcon`，`SetOverDoingAmount` 按档位加速脉冲；**每次脉冲播放 `GameOneShotAudioTag.CookingWarning`**（`OnOverDoingPulse` `:131-134`） |
| `Ruined` | `p > 2T` | 进度条与警告都隐藏 |

- UI 实例创建：`ClientCookingHandler.StartSynchronising` `:66-73` → `GameUtils.InstantiateUIController(m_cookingUIPrefab.gameObject, "HoverIconCanvas")`，跟随 `NetworkUtils.FindVisualRoot(gameObject)`，偏移 `m_cookingUIPrefabOffset`
- 每帧数据来源：`ApplyServerUpdate` 读 `CookingStateMessage.m_cookingProgress` 与 `m_cookingState`（纯服务器推送，客户端不自己算进度）
- 客户端"烧焦物品"全局登记：`static StaticList<ClientCookingHandler> s_burntCookables`，进入/退出 `Ruined` 时 `Add`/`Remove`，并暴露 `OnBurntCookableAdded`/`OnBurntCookableRemoved` 事件（`:18`、`:30-52`、`:92-102`）—— 供表现层（烟雾、音效）订阅

---

### 2.4 烧焦与火灾

#### 2.4.1 从 Burnt 到起火的完整链路

```
p 超过 2×m_cookingtime
   ↓
CookingHandler.GetCookedOrderState() → Burnt              (CookingHandler.cs:22-25)
   ↓
ServerCookingHandler.IsBurning() → true                   (ServerCookingHandler.cs:67-70)
ServerCookingHandler.SetCookingProgress() → State.Ruined (ServerCookingHandler.cs:82-85)
   ↓
ServerCookingHandler.Cook() 返回 false，进度冻结           (ServerCookingHandler.cs:107-115)
   ↓
ServerCookingStation.UpdateSynchronising() 检测到 IsBurning()
   ├─ SetCookerOn(false)                                  (ServerCookingStation.cs:197)
   └─ m_flammable.Ignite()   ← 灶台起火 ★                  (ServerCookingStation.cs:198-201)
   ↓
ServerFlammable.OnFire() → true
   ├─ 台子上的 Interactable / PickupItemSpawner / Workstation 被 disable
   └─ 向邻近 Flammable 传播 EncourageFire
```

`ServerCookingStation.cs:190-207`（✅ 源码确认，关键几行）：

```csharp
if (m_isTurnedOn && m_itemPot != null) {
    bool flag = m_itemPot.Cook(m_cookingSpeed * TimeManager.GetDeltaTime(base.gameObject));
    if (m_isCooking && m_levelConfigBase != null && m_levelConfigBase.m_hazardInfo != null
        && m_levelConfigBase.m_hazardInfo.FireConfigData != null && m_itemPot.IsBurning()) {
        SetCookerOn(_isOn: false);
        if (m_flammable != null) m_flammable.Ignite();
    }
    if (flag != m_isCooking) SetCooking(flag);
}
```

**起火的两个前提条件**：
1. 灶台上的物品 `IsBurning()`（进度 > 2×）
2. 关卡配置 `m_levelConfigBase.m_hazardInfo.FireConfigData != null`（**未配置火灾的关卡永远不会烧起来**）

#### 2.4.2 `ServerFlammable` —— 火势数值模型

**`Flammable` 数据层**（`Flammable.cs:4-26`，✅ 源码确认）：

```csharp
public GameObject m_fireEffectPrefab;
public ProgressUIController m_progressUIPrefab;
public float m_fireSpreadRadius = 1.5f;             // 传播半径（默认 1.5）
public float m_overrideTargetFlammabilityTime;      // 覆盖被点燃目标的点燃耗时
public bool  m_overrideTargetFlammability;
public bool  m_startOnFire;
public GameLoopingAudioTag m_audioTag = GameLoopingAudioTag.Flames;
```

**`FireConfigData` 数值**（`FireConfigData.cs:3-14`，✅ 源码确认，全部为默认值）：

| 字段 | 默认值 | 含义 |
|---|---|---|
| `FireRecoveryTime` | **5f** | 火势自行恢复（复燃）所需时间 |
| `FlammabilityTime` | **5f** | 被邻近火源点燃所需时间 |
| `CooldownTime` | **1f** | 未着火时火势强度的衰减速度基准 |
| `EncouragementSupressedTime` | **2f** | 灭火后抑制"被点燃"的时长 |
| `CooldownSupressedTime` | **2f** | 抑制衰减的时长 |

**核心状态字段**（`ServerFlammable.cs`，✅ 源码确认）：
- `m_fireStrength`（float，`Mathf.Clamp01`，`:329-332`）—— 火势强度，**达到 1 就真正着火**
- `m_onFire`（bool）—— 是否正在燃烧
- `m_velocityStack`（`FastList<ActiveVelocities>`，容量 3）—— 三个速度令牌：`Encourage`（助燃）/ `Fight`（灭火）/ `Recover`（自恢复），每帧末尾 `ResetActiveVelocity` 清零（`:426-428`）

**关键方法**：

| 方法 | 位置 | 行为 |
|---|---|---|
| `Ignite()` | `:241-258` | `SetClampedFireStrength(1f)` → `SetImmolatedComponentsEnabled(false)` → 加入 `s_objectsOnFire` → 回调 `m_ignitionCallbacks(true)` → `m_onFire = true` |
| `SetImmolatedComponentsEnabled(bool)` | `:214-224` | 遍历 `m_immolatedComponents` 逐个 `monoBehaviour.enabled = _enabled` |
| `FightFire(t, dt, player)` | `:280-293` | `AddActiveVelocity(Fight, -(1/t))`；`SetClampedFireStrength(m_fireStrength - (1/t)*dt)`；`m_encourageSupressedTimer = EncouragementSupressedTime`；**强度归零 → `_Extinguish(false, player)`** |
| `Extinguish()` / `_Extinguish(shutdown, player)` | `:295-327` | 恢复被禁用的组件、移出 `s_objectsOnFire`、`m_onFire = false`、通知邻近对象 `StopEncourageFire(this)`、清三个速度令牌 |
| `EncourageFire(flammable)` | `:266-273` | 若未被抑制，`AddActiveVelocity(Encourage, 1f / CalculateFlammabilityTime(...))` |
| `OnFireIncrease(v, dt)` | `:334-349` | `SetClampedFireStrength(m_fireStrength + v*dt)`；**达到 1 → `Ignite()`** |
| `GetProximateFlammables(ref arr)` | `:444-476` | 3×3 网格邻域扫描，距离平方 < `m_fireSpreadRadius²` 的邻居 |
| `CalculateFlammabilityTime` | `:432-442` | 若助燃者设了 `m_overrideTargetFlammability` 就用它的 `m_overrideTargetFlammabilityTime`，否则用目标自己的 `FlammabilityTime` |

**"着火会禁用哪些组件"**（`ServerFlammable.Awake` `:187-200`，✅ 源码确认）：

```csharp
m_immolatedComponents = new MonoBehaviour[3] {
    base.gameObject.GetComponent<Interactable>(),
    base.gameObject.GetComponent<PickupItemSpawner>(),
    base.gameObject.GetComponent<Workstation>()
};
```

→ **着火的台子同时失去：交互能力（`Interactable`）、出货能力（`PickupItemSpawner`）、切菜能力（`Workstation`）。** 这就是"着火后炉子彻底废掉"的实现，也解释了 2.2.4 里为什么 `CanPickupItem` / `InteractionIsSuppressed` 之外还要额外检查 `OnFire()`。

`UpdateSynchronising`（`:351-430`）每帧的完整流程：

1. `m_flammable.m_startOnFire` → `Ignite()` 并清标志
2. `s_objectsOnFire.Count == 0 && !m_updatePending` → 提前返回（**性能优化：全场无火时直接跳过**）
3. 若 `m_fireEncouragedBy.Count > 0 && m_fireStrength < 1f` → 按 `Encourage` 速度 `OnFireIncrease`
4. 若 `OnFire()`：
   - 双缓冲交换 `m_prevProximateFlammables` / `m_currProximateFlammables`，重新扫描邻居
   - 对每个邻居：新增 → `StartEncourageFire(this)`；消失 → `StopEncourageFire(this)`；仍在 → `EncourageFire(this)`
   - `m_fireStrength < 1f` 时按 `Recover` 速度自恢复
5. 若未着火且 `m_fireStrength > 0 && m_cooldownSupressedTimer <= 0` → 按 `CooldownTime` 衰减 `m_fireStrength -= (1/CooldownTime)*dt`
6. 递减两个抑制计时器
7. **注意**：`:412-415` 有 `m_cooldownSupressedTimer = m_encourageSupressedTimer - dt;` —— 这里赋值用的是 `m_encourageSupressedTimer` 而不是自身，❓ **推断**这是一处笔误（应为 `m_cooldownSupressedTimer - dt`），表现为抑制时间被"另一个计时器"驱动，逻辑上不正确但不致命
8. 速度总和变化 → `SynchroniseClientState()` 推送 `m_fireStrength` / `m_fireStrengthVelocity` / `m_onFire` / `m_playerExtinguished`

#### 2.4.3 火会蔓延吗？★

**会，而且是双向助推式的蔓延。**（✅ 源码确认）

| 问题 | 答案 | 证据 |
|---|---|---|
| 会不会自己蔓延？ | **会**，但需要邻近对象**自身挂 `Flammable`**（否则扫到也拿不到 `ServerFlammable` 组件） | `GetProximateFlammables` `:444-476` |
| 传播范围多大？ | **3×3 网格邻域**内，且直线距离 < `m_fireSpreadRadius`（默认 **1.5** 单位） | `:446-464` |
| 传播多快？ | 邻居火势以 `1/FlammabilityTime` = **1/5 每秒**增长 → 从 0 到着火约 **5 秒** | `:270-272`、`:334-349` |
| 会隔空传吗？ | 不会，必须同网格邻域且距离达标 | `:458-464` |
| 两个都在烧会互相加强吗？ | **会**。双方互相 `EncourageFire`，各自都在累积助燃速度 | `:378-397` |
| 大火自己会回落吗？ | 会。着火期间若 `m_fireStrength < 1`，按 `1/FireRecoveryTime`（**1/5 每秒**）自恢复 | `:398-402` |
| 火会自然熄灭吗？ | **不会自己灭**。`OnFire()` 为真时只在 `FightFire` 的驱动下才归零；没有外部灭火手段的火会一直烧 | `:280-293` 是唯一的归零路径 |
| 灭火后有冷却吗？ | 有。`EncouragementSupressedTime = 2s` 内不会被重新点燃 | `:287`、`:408-411` |

**蔓延的调用链**：

```
A 处于 OnFire()
  → 每帧 GetProximateFlammables() 找到邻居 B（3×3 内且距离 < 1.5）
  → B.StartEncourageFire(A)  /  B.EncourageFire(A)          (ServerFlammable.cs:386, 395)
     → B 的 m_fireEncouragedBy 加入 A
     → B.AddActiveVelocity(Encourage, 1/FlammabilityTime)  (1/5 每秒)
  → B.UpdateSynchronising 里 m_fireEncouragedBy.Count > 0
     → B.OnFireIncrease(1/5, dt)                            (:366-371)
     → B.m_fireStrength 从 0 涨到 1 耗时 ≈ 5 秒
     → 达到 1 → B.Ignite()                                  (:344-347)
```

> ❓ **推断**：实战中"一个灶台烧焦 → 隔壁台子也烧起来"的连锁，正是这条 1.5 单位 / 3×3 邻域 / 5 秒的路径。**自动化脚本的规避策略很直接：不要把灶台密集摆放，或者保证烧焦后 5 秒内灭火。**

#### 2.4.4 危险区：`FireHazard` / `SplatHazard` / `HazardBase`

**类层次**（✅ 源码确认，全部极简）：

```
HazardBase (abstract MonoBehaviour)                    HazardBase.cs:3-5
 ├── FireHazard  : HazardBase                          FireHazard.cs:5-12
 └── SplatHazard : HazardBase                          SplatHazard.cs:1-3   (空实现)

ServerHazardBase (abstract ServerSynchroniserBase, IHandleGridTransfer)   ServerHazardBase.cs:4-26
 └── ServerFireHazard : ServerHazardBase               ServerFireHazard.cs:3-65
```

**`FireHazard` 数据层**（`FireHazard.cs:5-12`，✅ 源码确认）：

```csharp
[RequireComponent(typeof(Flammable))]
[RequireComponent(typeof(CapsuleCollider))]
public class FireHazard : HazardBase {
    public float m_lifetime;      // 自身"燃烧寿命"（要多久自己烧完）
    public float m_destroyTime;   // 熄灭后多久销毁
}
```

**`ServerFireHazard` 逻辑**（`ServerFireHazard.cs:19-64`，✅ 源码确认）：

```csharp
public override void UpdateSynchronising() {
    if (m_flammable == null) return;
    if (m_hazard.m_lifetime > 0f)
        m_flammable.FightFire(m_hazard.m_lifetime, TimeManager.GetDeltaTime(base.gameObject));  // ← 自己消耗自己
    if (m_destroyTimer > 0f) {
        m_destroyTimer -= TimeManager.GetDeltaTime(base.gameObject);
        if (m_destroyTimer <= 0f) NetworkUtils.DestroyObject(base.gameObject);
    }
}
public void Conflagrate() { m_flammable.Ignite(); m_destroyTimer = 0f; }
private void Shutdown()  { m_destroyTimer = m_hazard.m_destroyTime; }
private void OnIgnitionChange(bool _state) { if (!_state) Shutdown(); }
public override void HandleTransfer(GridIndex _index, GameObject _object) {
    if (_object.RequestComponent<FireHazard>() == null) m_flammable.Extinguish();  // 被非火物体挤掉就熄灭
}
```

要点：
- **`m_lifetime` 是"火自己烧多久"**：`FightFire(m_lifetime, dt)` 让火势以 `1/m_lifetime` 每秒衰减，正好 `m_lifetime` 秒后自动熄灭。
- 熄灭回调 `OnIgnitionChange(false)` → `Shutdown()` → `m_destroyTimer = m_destroyTime` → 到期 `DestroyObject`。
- 被网格上的其它物体挤掉（且那个物体不是 `FireHazard`）→ 立即 `Extinguish()`。

**`FireHazardSpawner` / `ServerFireHazardSpawner`**（✅ 源码确认）：

```csharp
public GameObject m_hazardPrefab;
public string m_spawnTrigger;
public bool m_alignToGrid = true;
```

`ServerFireHazardSpawner.Burn()`（`:30-71`）的分支逻辑：
- 目标格已有 `FireHazard` → `serverFireHazard.Conflagrate()`（**复燃**，而不是新建）
- 目标格为 `null` 或已有 `HazardBase` → 生成新的 `m_hazardPrefab`；若有 `m_targetTransform` 则挂在它的 `IParentable.GetAttachPoint(...)` 下并同步挂点关系，否则用 `NetworkUtils.ServerSpawnPrefab` 且 `m_alignToGrid` 时吸附网格
- 目标格有普通物体 → **直接 return，不生成**（火不会凭空出现在物体上）

#### 2.4.5 灭火：`FireExtinguishSpray` / `SprayingUtensil`

**数据层**：

`SprayingUtensil.cs:3-29`（✅ 源码确认）：

```csharp
public string m_startSprayTrigger;
public string m_stopSprayTrigger;
public GameObject m_sprayEffectPrefab;
public Transform m_effectAttachPoint;
public float m_sprayAngleInDegrees = 15f;      // 喷射锥角
public float m_sprayDistance = 4f;             // 射程
public GameLoopingAudioTag m_audioTag = GameLoopingAudioTag.ExtinguisherSpray;
public LayerMask m_CollisionLayerMask = -1;
public const float c_itemRadius = 0.6f;        // 物品半径补偿
```

`FireExtinguishSpray.cs:3-7`（✅ 源码确认）：

```csharp
public class FireExtinguishSpray : SprayingUtensil {
    public float m_exinguishTime = 0.5f;       // 扑灭需要 0.5 秒
}
```

**逻辑层**（`ServerFireExtinguishSpray.cs`，✅ 源码确认）：

| 环节 | 位置 | 行为 |
|---|---|---|
| 拿起灭火器 | `OnCarryBegun` `:14-22` | **`serverFlammable.SetCanCatchFire(false)`** —— 拿着灭火器的人/物不会着火 |
| 放下 | `OnCarryEnded` `:24-35` | `SetCanCatchFire(true)` 恢复 |
| 喷射中 | `UpdateSynchronising` `:37-57` | 遍历 `ServerFlammable.GetAllOnFire()`，凡 `IsInSpray(item.transform)` 为真 → `FightFire(m_exinguishTime, dt, player: true)` |

**喷射判定 `IsInSpray`**（`ServerSprayingUtensil.cs:117-139`，✅ 源码确认）：

```csharp
Vector3 lhs = _t.position - m_carrier.transform.position;
float num = (m_sprayDistance + 0.6f) * (m_sprayDistance + 0.6f);
if (lhs.sqrMagnitude < num) {                          // ① 在射程+0.6 内
    float num2 = Vector3.Dot(lhs, transform.forward);
    if (num2 > 0f && num2 < m_sprayDistance + 0.6f) {  // ② 在正前方
        float num3 = num2 * Mathf.Sin(Mathf.PI / 180f * m_sprayAngleInDegrees * 0.5f);  // ③ 半锥半径
        Vector3 vector = transform.position + transform.forward * num2;
        float num4 = (num3 + 0.6f) * (num3 + 0.6f);
        if (num4 > (position2 - vector).sqrMagnitude) return true;                     // ④ 在锥内
    }
}
return false;
```

→ 判定锚点是**持握者位置**（`m_carrier.transform`），方向是**持握者朝向**，锥角 15°、射程 4 米，另加 **0.6 米物品半径补偿**。

**射程自适应**（`UpdateSprayDistance` `:103-115`）：从 `m_effectAttachPoint` 向前 `Physics.RaycastNonAlloc`，取最近命中距离（排除自身碰撞体）作为实际 `m_sprayDistance` —— **贴墙时喷雾距离自动缩短**。

**启动/停止喷射**（`OnTrigger` `:91-101`）：由 `m_startSprayTrigger` / `m_stopSprayTrigger` 两个动画事件字符串驱动，同样是**动画事件驱动**（与切菜同一套路），并推送 `SprayingUtensilMessage.m_bSpraying` + 持握者实体 ID 到客户端。

**灭火耗时计算**：`FightFire(0.5, dt, true)` → 每秒削减 `1/0.5 = 2.0` 强度。因为 `m_fireStrength` 上限为 1，所以**从满火到熄灭恰好 0.5 秒**。这也意味着`忽略`强度：无论 `m_fireStrength` 是 0.3 还是 1.0，`FightFire` 每帧的削减量只由 `m_exinguishTime` 决定（`SetClampedFireStrength(m_fireStrength - num*dt)`），所以：
- 0.3 强度 → 约 0.15 秒
- 1.0 强度 → 0.5 秒

#### 2.4.6 烧毁的食材/锅如何销毁

| 类 | 位置 | 行为 |
|---|---|---|
| `IngredientDisposalBehaviour` | `IngredientDisposalBehaviour.cs:3-5` | **空标记类**（`public class IngredientDisposalBehaviour : MonoBehaviour { }`），仅用于 prefab 上打标记 |
| `ServerContentsDisposalBehaviour` | `ServerContentsDisposalBehaviour.cs:5-39` | 实现 `IDisposalBehaviour`；`AddToDisposer(ICarrier, IDisposer)` → 若容器有内容则播 `TrashCan` 音效 + `Achievement(_carrier, 14)`，然后 **`m_container.Empty()`** 清空；`WillBeDestroyed() => false`（自己不销毁，只清内容）；`Destroying(IDisposer)` → 有内容则播音效 |

`ServerContentsDisposalBehaviour.cs:13-21`（✅ 源码确认）：

```csharp
public void AddToDisposer(ICarrier _carrier, IDisposer _iDisposer) {
    if (m_container.GetContentsCount() > 0) {
        ServerMessenger.TriggerAudioMessage(GameOneShotAudioTag.TrashCan, base.gameObject.layer);
        ServerMessenger.Achievement(_carrier.AccessGameObject(), 14);
    }
    m_container.Empty();
}
```

> **关键**：这是"倒垃圾"的实现——把锅拿到垃圾桶上时，**锅本身留下，锅里的内容被清空**。烧焦的食材随之消失（它是锅的 `m_composition` 元素）。

**烧毁物体的销毁路径汇总**：

| 场景 | 销毁方式 | 位置 |
|---|---|---|
| 切完的旧食材 | `NetworkUtils.DestroyObject(base.gameObject)` | `ServerWorkableItem.cs:104` |
| 洗掉的脏盘子堆 | `NetworkUtils.DestroyObjectsRecursive(gameObject)` | `ServerWashingStation.cs:117` |
| 掉进地板的厨具 | `ServerUtensilRespawnBehaviour.RespawnCoroutine` → `SetActive(false)` → 重生 | `ServerUtensilRespawnBehaviour.cs:27-77` |
| 火焰危险区自然烧完 | `NetworkUtils.DestroyObject` | `ServerFireHazard.cs:34` |
| 锅中内容 | `m_container.Empty()` | `ServerContentsDisposalBehaviour.cs:20` |
| 干净的盘子堆被放到回盘站 | `NetworkUtils.DestroyObjectsRecursive` | `ServerCleanPlateUtensilRespawnBehaviour.cs:48` |

---

### 2.5 搅拌 / 混合（Mixing）

#### 2.5.1 `MixingHandler` —— 阈值

`MixingHandler.cs:3-25`（✅ 源码确认）：

```csharp
public float m_mixingTime = 10f;                 // 默认 10 秒
public CookingStepData m_mixingType;
public CookingUIController m_progressUIPrefab;

public MixedCompositeOrderNode.MixingProgress GetMixedOrderState(float _mixingProgress) {
    if (_mixingProgress > 2f * m_mixingTime) return MixedCompositeOrderNode.MixingProgress.OverMixed;
    if (_mixingProgress >= m_mixingTime)     return MixedCompositeOrderNode.MixingProgress.Mixed;
    return MixedCompositeOrderNode.MixingProgress.Unmixed;
}
```

**`MixingProgress` 枚举**（`MixedCompositeOrderNode.cs:6-11`，✅ 源码确认）：

```csharp
public enum MixingProgress { Unmixed, Mixed, OverMixed }
```

**阈值表**（设 `M = m_mixingTime`，默认 10s；✅ 源码确认）：

| 进度 `p` | `GetMixedOrderState` | `IsMixed()` | `IsOverMixed()` | UI State | 能否继续搅 |
|---|---|---|---|---|---|
| `0 ≤ p < M` | `Unmixed` | false | false | `Progressing` | 能 |
| `M ≤ p ≤ 1.3M` | `Mixed` | true | false | `Completed` | 能 |
| `1.3M < p ≤ 2M` | `Mixed` | true | false | `OverDoing` | 能 |
| `p > 2M` | `OverMixed` | true | true | `Ruined` | **不能**（`Mix()` 返回 false） |

与烹饪**完全同构**：`1.0 / 1.3 / 2.0` 三个阈值、`Completed/OverDoing/Ruined` 三个 UI 态、`Mix()` 在过度后返回 false。

> ⚠️ **边界差异**：`MixingHandler.GetMixedOrderState` 用 **`>=`**（`:20`），而 `CookingHandler.GetCookedOrderState` 用 **`>`**。两者在这一处不对称。

#### 2.5.2 `ServerMixingHandler` —— 进度推进

`ServerMixingHandler.cs:41-103`（✅ 源码确认）：

```csharp
public void SetMixingProgress(float _mixingProgress) {
    MixingHandler mh = GetMixingHandler();
    m_serverData.m_mixingProgress = _mixingProgress;
    CookingUIController.State old = m_serverData.m_mixingState;
    if (IsOverMixed())                                        m_serverData.m_mixingState = CookingUIController.State.Ruined;
    else if (IsMixed()) {
        if (m_serverData.m_mixingState == CookingUIController.State.Progressing)
            m_serverData.m_mixingState = CookingUIController.State.Completed;
        if (_mixingProgress > 1.3f * mh.m_mixingTime)          m_serverData.m_mixingState = CookingUIController.State.OverDoing;
    }
    else                                                      m_serverData.m_mixingState = CookingUIController.State.Progressing;
    if (m_serverData.m_mixingState != old) m_stateChangedCallback(m_serverData.m_mixingState);
}

public bool IsMixed()     => m_serverData.m_mixingProgress >= AccessMixingTime;      // >=
public bool IsOverMixed() => m_serverData.m_mixingProgress >  2f * AccessMixingTime; // >
public bool Mix(float _deltaTime) {
    if (!IsOverMixed()) { SetMixingProgress(m_serverData.m_mixingProgress + _deltaTime); return true; }
    return false;
}
```

→ **`ServerMixingHandler` 自己 `UpdateSynchronising` 不调用 `Mix()`**。真正的驱动在外面。

#### 2.5.3 谁在驱动 `Mix()` —— `ServerMixingStation` ★

> 这是一个容易漏掉的点：`ServerMixableContainer` 只是**容器侧**（管理内容与配方），真正逐帧推进混合进度的是**台子侧**的 `ServerMixingStation`。

`ServerMixingStation.cs:56-67`（✅ 源码确认）：

```csharp
public override void UpdateSynchronising() {
    if (m_bMixerOn && m_ItemMixer != null
        && m_ItemMixer.Mix(TimeManager.GetDeltaTime(base.gameObject))
        && m_ItemMixer.IsOverMixed()) {
        SetMixerOn(_bOn: false);          // 过度混合 → 自动停机
    }
    if (m_pendingSyncMessage) { m_pendingSyncMessage = false; SendServerEvent(m_data); }
}
```

**自动开关机逻辑**（`OnOrderCompositionChanged` `:139-149`，✅ 源码确认）：

```csharp
if (m_ItemMixer != null)
    SetMixerOn(_contents.Simpilfy() != AssembledDefinitionNode.NullNode
               && !m_ItemMixer.IsOverMixed() && !m_Flammable.OnFire());   // 有内容 + 未过度 + 未着火
else
    SetMixerOn(_contents.Simpilfy() != AssembledDefinitionNode.NullNode && !m_Flammable.OnFire());
```

**结论**：
- 把装有料的锅放到搅拌台上 → **自动开始搅拌，无需玩家按住任何键**。
- 搅到 `OverMixed` → **自动停机**（不会继续恶化，但也不会恢复）。
- 台子着火 → 停机。

**`ServerMixingStation` 其它要点**：

| 环节 | 位置 | 行为 |
|---|---|---|
| `Awake` | `:33-46` | 要求 `MixingStation` + `ServerAttachStation` + **`ServerFlammable`**；注册放置校验与增删回调；若台子上已有物品则立刻 `OnItemAdded` |
| 放置校验 | `CanAddItem` `:75-106` | `Source.Game → true`；`Source.Player →` 若 `!m_bAttachRestrictions` 放行，否则必须 `IMixable` 且（若是 `ICookable` 且有内容）**不能已经煮过**（`GetCookedOrderState() != Raw \|\| GetCookingProgress() > 0f` → 拒收） |
| 装上台子 | `OnItemAdded` `:108-121` | `m_ItemMixer = ...RequestInterface<IMixable>()`；注册 `RegisterOrderCompositionChangedCallback`；`m_itemBlock.enabled = false` |
| 摘下 | `OnItemRemoved` `:123-137` | 反注册，`m_itemBlock.enabled = true` |

**`MixingStation` 数据层**（`MixingStation.cs:3-13`，✅ 源码确认）：

```csharp
[ExecutionDependency(typeof(IFlowController))]
[RequireComponent(typeof(AttachStation))]
[RequireComponent(typeof(Flammable))]        // ← 搅拌台也会着火
public class MixingStation : MonoBehaviour {
    public Collider m_itemBlock;
    public bool m_bAttachRestrictions = true;
}
```

#### 2.5.4 `ServerMixableContainer` —— 容器侧

`ServerMixableContainer.cs:5-142`（✅ 源码确认）：

- 依赖：`MixableContainer` + `ServerMixingHandler` + `ServerPlacementContainer` + `ServerIngredientContainer`（+ 可选 `ServerIngredientCatcher`）
- `StartSynchronising`（`:21-40`）：**`m_MixingHandler.enabled = false`** —— 初始关闭；把 `m_stateChangedCallback` 桥接到 `m_orderCompositionChangedCallbacks` → 每次状态变化都重算配方
- `OnContentsChanged`（`:97-102`）：**`m_MixingHandler.enabled = _contents.Length > 0`** —— **空锅不搅**；内容清空时进度归零
- `GetOrderComposition`（`:104-107`）→ `m_mixableContainer.GetOrderComposition(m_IngredientContainer, progress/mixingTime, GetMixedOrderState())`

**`MixableContainer.GetOrderComposition`**（`MixableContainer.cs:13-20`，✅ 源码确认）：

```csharp
MixedCompositeAssembledNode node = new MixedCompositeAssembledNode();
node.m_composition      = _ingredientContents.GetContents();
node.m_recordedProgress = _recordedProgress;      // 归一化进度
node.m_progress         = _mixingProgress;        // Unmixed / Mixed / OverMixed
return node;
```

**放置规则 `MixableContainer.AllowItemPlacement`**（`MixableContainer.cs:22-115`，✅ 源码确认，逻辑相当绕）：

| 闸门 | 条件 | 结果 |
|---|---|---|
| ① | `_overMixed` | `false`（过度混合的锅拒收任何新料） |
| ② | 目标无 `IOrderDefinition` | `false` |
| ③ | 放入的是一个 `MixableContainer`（锅倒锅）且 `_cookingProgress == 0`： | |
| ③a | 源锅 `OverMixed` | `false` |
| ③b | 父级是 `CookingStation` 且源锅不是 `Mixed` | `false`（**没搅好的不能直接下锅**） |
| ③c | 源锅是 `Raw` 且双方 `CanTakeContents` | `true` |
| ④ | 父级是 `CookingStation`（非锅倒锅路径） | `false` |
| ⑤ | 有 `m_ApprovedIngredients` 白名单且 `AssembledDefinitionNode.Matching` 命中 | `true` |
| 默认 | — | `false` |

**"锅倒锅"的进度合并**（`ServerMixableContainer.cs:42-75`，✅ 源码确认）：

```csharp
public float CalculateCombinedMixingProgress(float recipientProgress, int recipientContents,
                                            float receivedProgress, int receivedContents) {
    if (Mathf.Max(recipientProgress, receivedProgress) > 2f * m_MixingHandler.AccessMixingTime)
        return Mathf.Max(recipientProgress, receivedProgress);     // 任一已过度 → 保持过度
    if (recipientContents == 0) return receivedProgress;
    if (receivedContents == 0)  return recipientProgress;
    return (Mathf.Min(recipientProgress,  M) + Mathf.Min(receivedProgress, M)) * 0.5f;  // 取平均，各自封顶 M
}
```

→ **与锅倒锅的烹饪进度合并完全同构**（见 2.3.5）。

---

### 2.6 清洗（Washing）

#### 2.6.1 `WashingStation` 数据层

`WashingStation.cs:3-24`（✅ 源码确认）：

```csharp
[RequireComponent(typeof(Interactable))]
public class WashingStation : MonoBehaviour {
    public PlateReturnStation m_dryingStation;       // 洗好的盘子送去哪
    public float m_cleanPlateTime = 2f;             // 每个盘子 2 秒
    public ProgressUIController m_progressUIPrefab;
    public GameObject[] m_dirtyPlates = new GameObject[0];

    public bool CanHandlePlacement(ICarrier _carrier, Vector2 _directionXZ, int _plateCount) {
        GameObject carried = _carrier.InspectCarriedItem();
        DirtyPlateStack dps = carried?.GetComponent<DirtyPlateStack>();
        return dps != null;                          // 只接受脏盘子堆
    }
}
```

#### 2.6.2 `ServerWashingStation` —— 清洗流程

**初始化**（`:33-50`，✅ 源码确认）：

```csharp
m_serverAttachStation = gameObject.RequireComponent<ServerAttachStation>();
m_serverAttachStation.RegisterOnItemAdded(OnItemAdded);
m_serverAttachStation.RegisterOnItemRemoved(OnItemRemoved);
m_handlePickupReferral = gameObject.RequireComponent<ServerHandlePickupReferral>();
m_originalPickupReferee = m_handlePickupReferral.GetHandlePickupReferree();
m_interactable = gameObject.GetComponent<ServerInteractable>();
m_interactable.RegisterCallbacks(OnInteracterAdded, OnInteracterRemoved);
m_interactable.enabled = false;                              // ← 初始禁用
m_interactable.SetStickyInteractionCallback(() => true);     // ← 永久粘性（按住即持续洗）
m_plateReturnStation = m_washingStation.m_dryingStation.gameObject.RequireComponent<ServerPlateReturnStation>();
m_dryingAttachStation = m_washingStation.m_dryingStation.gameObject.RequireComponent<ServerAttachStation>();
m_dryingAttachStation.RegisterOnItemAdded(OnItemAddedDryingStation);
m_dryingAttachStation.RegisterOnItemRemoved(OnItemRemovedDryingStation);
```

**投放脏盘子**（`:106-120`，✅ 源码确认）：

```csharp
private void OnItemAdded(IAttachment _attachment) {
    if (m_handlePickupReferral != null) m_handlePickupReferral.SetHandlePickupReferree(null);
    GameObject go = (_attachment as MonoBehaviour).gameObject;
    ServerStack stack = go.GetComponent<ServerStack>();
    if (stack != null) {
        m_serverAttachStation.TakeItem();
        NetworkUtils.DestroyObjectsRecursive(go);    // ← 整个脏盘堆被销毁
        AddPlates(stack.GetSize());                  // ← 数量转为待洗计数
    }
}
```

→ **脏盘子堆一旦放上水槽就消失，转化为 `m_plateCount` 整数。**

**逐盘清洗**（`:85-104`，✅ 源码确认，核心）：

```csharp
public override void UpdateSynchronising() {
    if (!(m_interactable != null) || !m_interactable.IsBeingInteractedWith() || m_plateCount <= 0) return;

    m_cleaningTimer += TimeManager.GetDeltaTime(base.gameObject)
                     * ((!m_interactable.AllowMultipleInteractors()) ? 1f : ((float)m_interactable.InteractorCount()));

    if (m_cleaningTimer > m_washingStation.m_cleanPlateTime) {   // 默认 2 秒
        m_cleaningTimer = 0f;
        m_plateCount--;
        ServerMessenger.Achievement(m_interactor.gameObject, 2);
        SendCleanedPlate();
        m_plateReturnStation.ReturnPlate();                       // ← 产出一个干净盘子
        if (m_plateCount == 0) m_interactable.enabled = false;
    }
}
```

**数值与规则**：

| 项 | 值/规则 | 来源 |
|---|---|---|
| 每盘耗时 | `m_cleanPlateTime`，默认 **2 秒** | `WashingStation.cs:11` |
| 计时速率 | `dt × (AllowMultipleInteractors ? InteractorCount : 1)` | `:91` |
| 多人同时洗 | **速率按人数线性叠加**（3 人同时洗 → 6 秒洗 3 盘） | `:91` |
| 交互方式 | **粘性交互**（`SetStickyInteractionCallback(() => true)`），按住即持续洗 | `:45` |
| 触发条件 | 必须正在被交互 + `m_plateCount > 0` | `:87` |
| 计数归零 | `m_interactable.enabled = false`（不能洗空气） | `:101-102` |
| 成就 | `ServerMessenger.Achievement(m_interactor.gameObject, 2)` 每盘一次 | `:96` |

**其他入口**：

- `HandlePlacement`（`:177-194`）也支持"直接把手上的脏盘堆交出去"（`ServerStack != null` → `DestroyCarriedItem()` + `AddPlates(size)`），即**既可以放也可以直接给**。
- `WashAllPlates()`（`:156-169`）：一次性把所有 `m_plateCount` 全部 `ReturnPlate()` 并清零 —— ❓ **推断**用于关卡开局/剧情事件瞬间清空积压。
- `GetPlacementPriority() => int.MaxValue`（`:200-203`）与 `ServerPlacementItemSpawner` 相同：**最高优先级**，即手上拿脏盘时水槽优先于其他台子接收。
- `CanHandlePlacement`（`:171-175`）：`(component == null && m_dryingAttachStation.CanHandlePlacement(...)) || m_washingStation.CanHandlePlacement(_carrier, _directionXZ, m_plateCount)` —— 非 `ServerStack` 物品被转发给**烘干站**处理。

**烘干站联动**（`:130-144`，✅ 源码确认）：

```csharp
private void OnItemAddedDryingStation(IAttachment _attachment) {
    if (m_interactable != null)
        m_interactable.SetInteractionSuppressed(
            _attachment.AccessGameObject() != null
            && _attachment.AccessGameObject().RequestComponent<ServerCleanPlateStack>() == null);
}
```

→ 烘干站上放了**非干净盘子堆**的东西时，水槽被禁止交互（避免逻辑冲突）。

#### 2.6.3 脏盘子 → 干净盘子：完整链路

```
玩家拿 DirtyPlateStack（脏盘堆）
   ↓ 放到 WashingStation
ServerWashingStation.OnItemAdded
   ├─ NetworkUtils.DestroyObjectsRecursive(脏盘堆)      ← 堆对象销毁
   └─ AddPlates(stack.GetSize())  → m_plateCount += N
   ↓ 玩家按住水槽交互（sticky）
UpdateSynchronising 每帧 m_cleaningTimer += dt × 洗盘人数
   ↓ 每累计 2 秒（m_cleanPlateTime）
   ├─ m_plateCount--
   ├─ Achievement(interactor, 2)
   └─ m_plateReturnStation.ReturnPlate()                 ← 产出一张干净盘子
        ↓
        ServerPlateReturnStation.ReturnPlate()            (ServerPlateReturnStation.cs:92-99)
        ├─ if (m_stack == null) CreateStack()
        │     └─ NetworkUtils.ServerSpawnPrefab(m_returnStation.m_stackPrefab)  ← 生成干净盘堆
        │        m_attachStation.AddItem(stack, rotation * Vector2.up)
        │        m_stack = ...RequireInterface<ServerPlateStackBase>()
        └─ m_stack.AddToStack()                            ← 往堆上加一张
```

**`ServerPlateReturnStation`**（`ServerPlateReturnStation.cs`，✅ 源码确认）：

| 成员 | 位置 | 行为 |
|---|---|---|
| `m_startingPlateNumber` | `:39-54`（`UpdateSynchronising` 首次 tick） | 若 > 0 → `CreateStack()` 再 `AddToStack()` N 次，**开局就有 N 张干净盘子** |
| `CanAddItem` | `:56-71` | `Source.Player → false`（**玩家不能往回盘站放东西**）；已有 `m_stack` → false；台子上有物品 → false |
| `CanRemoveItem` | `:73-76` | `m_stack == null \|\| m_stack.GetSize() > 0`（空堆不能拿） |
| `ReturnPlate` | `:92-99` | 见上 |
| `CanReturnPlate` | `:86-90` | 台子为空，或台子上的就是当前 `m_stack` |
| `GetPlatingStep` | `:108-119` | 从 `m_stackPrefab` 的 `PlateStackBase.GetPlatingStep()` 取（用于匹配盘子类型） |

**`DirtyPlateStack` / `CleanPlateStack`**（✅ 源码确认）：

```csharp
// DirtyPlateStack.cs:5-19
[RequireComponent(typeof(Stack))] [RequireComponent(typeof(HandlePlacementReferral))]
public class DirtyPlateStack : PlateStackBase {
    public PlatingStepData m_plateType;
    public GameObject m_washedPrefab;        // 洗后的样子
    public GameObject m_cleanPlatePrefab;    // 干净盘子预制体
    public override PlatingStepData GetPlatingStep() => m_plateType;
}

// CleanPlateStack.cs:6-12
[RequireComponent(typeof(Stack))] [RequireComponent(typeof(HandlePickupReferral))] [RequireComponent(typeof(HandlePlacementReferral))]
public class CleanPlateStack : PlateStackBase {
    public override PlatingStepData GetPlatingStep() {
        Plate plate = m_platePrefab.RequestComponent<Plate>();
        return plate.m_platingStep;          // 从盘子本体取
    }
}
```

**盘子堆容量**（`Stack.cs` + `ServerStack.cs`，✅ 源码确认）：

- `Stack.m_heightOffset` 决定每张盘子的垂直间距（`:10-11`）
- `ServerStack.StartSynchronising` 遍历子物体，凡 `child.gameObject.tag == m_stack.m_itemTag` 的都进 `m_stackItems`（`:19-27`）
- `AddToStack` / `RemoveFromStack` 增删并 `RefreshStackTransforms` + `RefreshStackCollider`（碰撞体高度 `= count × m_heightOffset`）
- **`GetSize()` 就是当前堆内的盘子数**
- ❓ **推断**：源码里**没有硬编码的堆上限**。上限由 prefab 的初始子物体数量与 `RefreshStackCollider` 的视觉合理性决定，需从 AssetBundle 反查。

#### 2.6.4 厨具重生：`UtensilRespawnBehaviour` 系列

**数据层**（`UtensilRespawnBehaviour.cs:3-12`，✅ 源码确认）：

```csharp
public class UtensilRespawnBehaviour : MonoBehaviour {
    public float m_respawnTime = 5f;      // 重生等待，默认 5 秒
    public float m_particleTime = 1f;
    public GameObject m_spawnEffect;
}
```

**基类逻辑**（`ServerUtensilRespawnBehaviour.cs`，✅ 源码确认）：

`RespawnCoroutine(ServerRespawnCollider _collider)`（`:27-77`）完整步骤：

| 步 | 代码 | 行为 |
|---|---|---|
| 0 | `:29-33` | `if (m_isRespawning) yield break;` 幂等保护；置 `m_isRespawning = true` |
| 1 | `:34-39` | `ReleaseFromAttached(attachment)` 从台子/玩家手上脱离；`attachment.ManualDisable(_clearPredicted: true)` |
| 2 | `:40-42` | 发 `RespawnMessage`：`m_RespawnType = RespawnCollider.RespawnType.FallDeath`，`m_Phase = Begin` |
| 3 | `:43-47` | **`ServerContentsDisposalBehaviour` → `AddToDisposer(null)`** —— **锅里的内容在这一步被清空（内容丢失）** |
| 4 | `:48` | `base.gameObject.SetActive(false)` |
| 5 | `:49-53` | 等 `m_utensilRespawnBehaviour.m_respawnTime`（默认 **5 秒**） |
| 6 | `:54-59` | `while (freeStation == null) { yield return null; freeStation = GetFreeAttachStation(); }` —— **每帧重试直到找到空位** |
| 7 | `:60-64` | `attachment.ManualEnable()`；`AddItemToStation(freeStation)` |
| 8 | `:65-76` | 恢复 Collider；发 `RespawnMessage`（`m_Phase = End`，`m_RespawnPosition = freeStation.GetAttachPoint(gameObject).position`）；`m_isRespawning = false` |

**`ReleaseFromAttached`**（`:160-189`）：若已附着，通过 `ServerHandlePickupReferral` 拿到裁决者；若是 `ServerAttachStation` 且有物品 → `TakeItem()`；若是 `ServerPlayerAttachmentCarrier.BlockPickup` → `ForceDetach()`。

**重生到哪：优先级算法**（`:101-158`，✅ 源码确认）：

```csharp
public virtual float GetStationRespawnPriority(ServerAttachStation _station)
    => GetRespawnDistance(_station.transform.position);     // 默认：到"理想位置"的距离

protected float GetRespawnDistance(Vector3 _position) {
    Vector3 origin = (m_IdealSpawnLocation != null) ? m_IdealSpawnLocation.transform.position : base.transform.position;
    return (_position - origin).magnitude;                  // ← 距离越小优先级越高
}

protected virtual ServerAttachStation GetFreeAttachStation() {
    float num = float.PositiveInfinity;
    if (m_allRespawnStations == null) m_allRespawnStations = GetRespawnStations();
    for (...) {
        if (s.enabled && s.gameObject.activeInHierarchy && IsInLevelBounds(s, levelConfig) && CanRespawnOnStation(s)) {
            float p = GetStationRespawnPriority(s);
            if (p < num) { result = s; num = p; }           // ← 取最小值
        }
    }
    return result;
}
```

**"理想位置" `m_IdealSpawnLocation`**（`:79-99`）：
- `OnSurfacePlacement(ServerAttachStation _station)`：**第一次被放到某台子上时记录该台子**
- `SetIdealRespawnLocation` / `GetIdealRespawnLocation` 可外部覆盖

→ **锅会重生回它第一次被放下的那个台子**（若被占则找最近的合适台子）。

**候选台子池 `GetRespawnStations()`**（`:155-158`）：基类 `Object.FindObjectsOfType<ServerAttachStation>()` —— **全场景所有 attach 台子**。

**基础排除规则 `CanRespawnOnStation`**（`:121-128`，✅ 源码确认）：

```csharp
if (_attachStation.CompareTag("CookingStation") || _attachStation.CompareTag("PlateReturn")
    || _attachStation.CompareTag("PlateStation")
    || _attachStation.gameObject.RequestComponent<RubbishBin>() != null
    || _attachStation.gameObject.RequestComponent<ConveyorStation>() != null
    || _attachStation.gameObject.RequestComponent<WashingStation>() != null)
    return false;
return _attachStation.CanAttachToSelf(base.gameObject);
```

→ **基础规则排除：灶台、回盘站、盘子站、垃圾桶、传送带、水槽。** 即"锅不会重生到灶台上"。

**`IsInLevelBounds`**（`:112-119`）：若关卡 `m_enableRespawnBounds` 为真，则要求 `LevelBounds.ActiveBoundsContain(_station.transform.position)`。

**三个派生类的差异**（✅ 源码确认）：

| 派生类 | 候选池 | 优先级调整 | 额外排除 | 特殊行为 |
|---|---|---|---|---|
| `ServerCookingUtensilRespawnBehaviour` | 全部 `ServerAttachStation` | **非 `CookingStation` 台子 +1000 惩罚**（`:15-18`） | 在基础排除上，**再要求灶台类型匹配**（见下） | 强偏向灶台，且必须类型对 |
| `ServerCleanPlateUtensilRespawnBehaviour` | 全部 `ServerAttachStation` | 无调整 | 额外允许 `PlateReturn`（从基础排除里移除了它） | 命中回盘站且有 `CleanPlateStack` → `ReturnPlate()` 后**自毁**（`:35-49`） |
| `ServerDirtyPlateStackUtensilRespawnBehaviour` | **仅所有 `ServerPlateReturnStation`**（`:40-43` 重写） | 优先 `m_IdealSpawnLocation`（`:31-38` 重写） | 只认可回盘站且其 `m_stackPrefab` 带 `DirtyPlateStack` | `AddItemToStation` 里**按堆大小循环 `ReturnPlate()` 再自毁**（`:45-59`） |

**`ServerCookingUtensilRespawnBehaviour.InvalidStationFilter`**（`:33-45`，✅ 源码确认）：

```csharp
private bool InvalidStationFilter(ServerAttachStation _station) {
    if (m_mixingHandler != null) return _station.gameObject.RequestComponent<MixingStation>() == null;
    if (m_cookingHandler != null) {
        CookingStation cs = _station.gameObject.RequestComponent<CookingStation>();
        return cs == null || cs.m_stationType != m_cookingHandler.m_stationType;   // ← 类型严格匹配
    }
    return false;
}
```

→ **一个锅只会重生到 `m_stationType` 与它自己 `CookingHandler.m_stationType` 完全一致的灶台上**；若它是可混合容器（有 `MixingHandler`），则只会重生到 `MixingStation` 上。

**`ServerCleanPlateUtensilRespawnBehaviour.CanRespawnOnStation`**（`:17-33`，✅ 源码确认）：

```csharp
ServerPlateReturnStation prs = _attachStation.gameObject.RequestComponent<ServerPlateReturnStation>();
if (prs != null) {
    PlateReturnStation pr = _attachStation.gameObject.RequireComponent<PlateReturnStation>();
    if (pr.m_stackPrefab != null && pr.m_stackPrefab.RequestComponent<CleanPlateStack>() != null) {
        return (m_platingStepData == null || prs.GetPlatingStep() == m_platingStepData)   // 盘子类型匹配
               && prs.CanReturnPlate();
    }
}
return _attachStation.CanAttachToSelf(base.gameObject);
```

---

### 2.7 厨具与器皿（容量）

#### 2.7.1 `IngredientContainer.m_capacity` —— 容量上限

`IngredientContainer.cs:3-15`（✅ 源码确认，全文只有 16 行）：

```csharp
[AddComponentMenu("Scripts/Game/Environment/IngredientContainer")]
public class IngredientContainer : MonoBehaviour {
    [SerializeField] public int m_capacity = 3;              // ← 默认容量 3
    [SerializeField] public Collider m_onSurfaceTriggerZone;
    public int GetCapacity() => m_capacity;
}
```

**`ServerIngredientContainer` 的容量校验**（`ServerIngredientContainer.cs:57-66`，✅ 源码确认）：

```csharp
public bool CanTakeContents(AssembledDefinitionNode[] _contents) {
    int num = m_ingredientContainer.m_capacity - m_contents.Count;
    return num >= _contents.Length;                 // 剩余槽位必须能装下全部
}
public bool CanAddIngredient(AssembledDefinitionNode _orderData) {
    return m_contents.Count < m_ingredientContainer.m_capacity;   // 未满
}
```

**容量语义**：`m_capacity` 是**内容条目数上限**（不是数量、不是重量）。默认 **3**。

| 容器 | 默认容量 | 说明 |
|---|---|---|
| 锅 / 平底锅（`CookableContainer`） | **3** | 一锅最多 3 样东西 |
| 搅拌碗（`MixableContainer`） | **3** | 同上 |
| `PreparationContainer`（包装类） | **3** | 同上 |
| 盘子（`Plate`） | ❓ | `Plate` 自身继承/借用 `IngredientContainer`，具体容量需从 prefab 反查 |

> ⚠️ `m_capacity` 是 `[SerializeField]`，**每个 prefab 可覆盖**。默认 3 是 C# 侧默认值。

`ServerIngredientContainer` 其它要点（✅ 源码确认）：
- `GetAllIngredientContainers()` 静态表 `ms_AllIngredientContainers`（`:22-39`），`OnEnable`/`OnDisable` 增删 → 全局可枚举所有容器
- `OnContentsChanged()`（`:138-144`）每次内容变化都 `SendServerEvent(m_ServerData)` 同步到客户端
- `UpdateSynchronising()`（`:146-152`）把 `m_onSurfaceTriggerZone.enabled` 绑定到 `m_attachment.IsAttached()` —— **只有被附着时触发区才生效**

#### 2.7.2 厨具组件依赖矩阵

| 器皿类 | `[RequireComponent]` 依赖 | 语义 |
|---|---|---|
| `CookableContainer`（锅） | `IngredientContainer`, `PlacementContainer`, `CookingHandler` | 可装料 + 可被放 + 可烹饪 |
| `MixableContainer`（搅拌碗） | `IngredientContainer`, `PlacementContainer`, `PhysicalAttachment`, `MixingHandler` | 可装料 + 可被放 + 可混合 |
| `PreparationContainer`（包装/半成品） | `PhysicalAttachment`, `IngredientContainer`, `HandlePlacementReferral` | 可装料，**自身会变成一个食材** |
| `CookablePreparationContainer` | 继承 `PreparationContainer` + `CookingHandler` | 包装后还能被烹饪 |
| `CookingStation`（灶台） | `AttachStation` | 单物品挂载 + 加热 |
| `MixingStation`（搅拌台） | `AttachStation`, `Flammable` | 单物品挂载 + 自动混合 + **可着火** |
| `Workstation`（菜板） | `Collider`, `AttachStation`, `Interactable` | 单物品挂载 + 交互切 |
| `WashingStation`（水槽） | `Interactable` | 粘性交互清洗 |
| `PlateReturnStation`（回盘站） | `AttachStation` | 产出干净盘堆 |
| `ConveyorStation`（传送带） | `TabletopConveyenceReceiver`, `StaticGridLocation`, `AttachStation` | 自动搬运 |
| `PickupItemSpawner`（食材箱） | `HandlePickupReferral` | 玩家交互无限取货 |

#### 2.7.3 `PreparationContainer` —— 自身成为食材的容器

`PreparationContainer.cs:3-28`（✅ 源码确认）：

```csharp
[RequireComponent(typeof(PhysicalAttachment))]
[RequireComponent(typeof(IngredientContainer))]
[RequireComponent(typeof(HandlePlacementReferral))]
public class PreparationContainer : MonoBehaviour, ISpawnableItem {
    public OrderToPrefabLookup m_containerRestrictions;   // 允许装入什么
    public IngredientOrderNode m_ingredientOrderNode;     // 它自己作为食材的身份
    public GameObject m_cosmeticsPrefab;
    public SubTexture2D GetSubTexture() => m_ingredientOrderNode.m_crateLid;
    public Sprite GetUIIcon() => m_ingredientOrderNode.m_iconSprite;
}
```

**核心机制**（`ServerPreparationContainer.GetAsOrderComposite` `:240-249`，✅ 源码确认）：

```csharp
CompositeAssembledNode node = new CompositeAssembledNode();
node.m_composition = m_itemContainer.GetContents();
Array.Resize(ref node.m_composition, node.m_composition.Length + 1);
node.m_composition[node.m_composition.Length - 1] = new IngredientAssembledNode(m_preparationContainer.m_ingredientOrderNode);
node.m_permittedEntries = m_preparationContainer.m_containerRestrictions.GetContentRestrictions();
node.m_freeObject = base.gameObject;
return node;
```

→ **容器把自己追加到内容数组末尾**，即"容器 = 最后一个食材"。这正是"面皮 + 馅料 → 卷饼"这类配方的实现方式。

**"自我转移"逻辑**（`AddToOtherContainer` `:106-126`，✅ 源码确认）：

```csharp
if (_container.HasContents()) {                    // 目标容器有料 → 全倒进自己
    for (...) m_itemContainer.AddIngredient(_container.GetContentsElement(i));
    _container.Empty();
}
CompositeAssembledNode asOrderComposite = GetAsOrderComposite();
_container.AddIngredient(asOrderComposite);        // 自己整体进入目标容器
asOrderComposite.m_freeObject = base.gameObject;
GetComponent<ServerPhysicalAttachment>().ManualDisable(_clearPredicted: true);
base.gameObject.SetActive(false);                  // 自己消失
```

→ **两个容器相遇时，互相"吃掉"对方，最后合成一个复合节点。**

**放置校验**（`CanHandlePlacement` `:166-189`）：要求 `CanAddOrderContents` 通过；`Tray` 恒放行；盘子需要 `CanTransferToContainer`；盘子还得 `m_ingredientOrderNode.m_platingStep == plate.m_platingStep`（**摆盘类型必须匹配**）。
**`GetPlacementPriority() => int.MinValue`**（`:261-264`）—— **最低优先级**，即它是"兜底接收者"。

#### 2.7.4 `OrderToPrefabLookup` —— 配方→预制体映射表

`OrderToPrefabLookup.cs:5-70`（✅ 源码确认）：

```csharp
public class ContentPrefabLookup {
    public OrderDefinitionNode m_content;                    // 配方节点
    public GameObject m_prefab;                              // 对应的成品预制体
    public int m_amountAllowed = 1;                          // 允许数量
    public OrderDefinitionNode[] m_restrictedContent;        // 互斥内容
    [NonSerialized] public AssembledDefinitionNode m_simplifiedAssembledContent;   // 缓存
}
```

- `CacheAssembledOrderNodes()`（`:29-39`）：对每项 `m_content.Simpilfy()` 预计算简化节点（`m_cachedAssembledOrderNodes` 标志位保证只算一次）
- `GetPrefabForNode(AssembledDefinitionNode)`（`:41-56`）：简化后逐个 `AssembledDefinitionNode.MatchingAlreadySimple(...)` 比对，命中返回 `m_prefab`，否则 `null`
- `GetContentRestrictions()`（`:58-70`）：转成 `OrderContentRestriction` 列表

**两个使用点**：
1. `CookableContainer.m_approvedContentsList`（`CookableContainer.cs:10, 19, 51-58`）—— 锅的成品白名单
2. `PreparationContainer.m_containerRestrictions`（`PreparationContainer.cs:10`，`ServerPreparationContainer.cs:246`）—— 包装容器的内容限制

---

### 2.8 本章速查表汇总

#### 表 A：全部加工时间常量（源码默认值）

| 常量 | 默认值 | 类·行号 | 含义 |
|---|---|---|---|
| `CookingHandler.m_cookingtime` | **10f** | `CookingHandler.cs:6` | 烹饪所需秒数 |
| `MixingHandler.m_mixingTime` | **10f** | `MixingHandler.cs:6` | 混合所需秒数 |
| `GameConfig.SingleplayerChopTimeMultiplier` | **5** | `GameConfig.cs:19` | 单人/少人时的每切片刀数 |
| `WorkableItem.m_stages` | **8** | `WorkableItem.cs:14` | 切片总数 |
| `IngredientContainer.m_capacity` | **3** | `IngredientContainer.cs:7` | 容器内容上限 |
| `WashingStation.m_cleanPlateTime` | **2f** | `WashingStation.cs:11` | 每盘清洗秒数 |
| `UtensilRespawnBehaviour.m_respawnTime` | **5f** | `UtensilRespawnBehaviour.cs:6` | 厨具重生等待 |
| `FireConfigData.FlammabilityTime` | **5f** | `FireConfigData.cs:7` | 被点燃耗时 |
| `FireConfigData.FireRecoveryTime` | **5f** | `FireConfigData.cs:5` | 火势自恢复时间 |
| `FireConfigData.CooldownTime` | **1f** | `FireConfigData.cs:9` | 未着火时火势衰减基准 |
| `FireConfigData.EncouragementSupressedTime` | **2f** | `FireConfigData.cs:11` | 灭火后免燃时长 |
| `FireConfigData.CooldownSupressedTime` | **2f** | `FireConfigData.cs:13` | 抑制衰减时长 |
| `Flammable.m_fireSpreadRadius` | **1.5f** | `Flammable.cs:13` | 火焰传播半径 |
| `FireExtinguishSpray.m_exinguishTime` | **0.5f** | `FireExtinguishSpray.cs:6` | 灭火所需秒数 |
| `SprayingUtensil.m_sprayAngleInDegrees` | **15f** | `SprayingUtensil.cs:18` | 喷雾锥角 |
| `SprayingUtensil.m_sprayDistance` | **4f** | `SprayingUtensil.cs:21` | 喷雾射程 |
| `SprayingUtensil.c_itemRadius` | **0.6f** | `SprayingUtensil.cs:29` | 物品半径补偿 |
| `ConveyorStation.m_conveySpeed` | **1f** | `ConveyorStation.cs:15` | 传送速度（格/秒） |
| `TriggerAttachedSpawn.m_maxNumber` | **50** | `TriggerAttachedSpawn.cs:31` | 同时存活上限 |
| `HeatedCookingStation.m_cookingSpeedHigh` | **1f** | `HeatedCookingStation.cs:12` | 大火倍率 |
| `HeatedCookingStation.m_cookingSpeedModerate` | **0.66f** | `HeatedCookingStation.cs:16` | 中火倍率 |
| `HeatedCookingStation.m_cookingSpeedLow` | **0.33f** | `HeatedCookingStation.cs:20` | 小火倍率 |
| `ServerUtensilRespawnBehaviour` 类型惩罚 | **1000f** | `ServerCookingUtensilRespawnBehaviour.cs:3` | 非灶台的优先级惩罚 |

#### 表 B：三套"三段式"状态机对照（阈值全是 1.0 / 1.3 / 2.0 倍）

| 维度 | 烹饪 | 混合 |
|---|---|---|
| 数据类 | `CookingHandler`（`m_cookingtime`） | `MixingHandler`（`m_mixingTime`） |
| 服务端 | `ServerCookingHandler` | `ServerMixingHandler` |
| 客户端 | `ClientCookingHandler` | `ClientMixingHandler` |
| 枚举 | `CookedCompositeOrderNode.CookingProgress`（Raw/Cooked/Burnt） | `MixedCompositeOrderNode.MixingProgress`（Unmixed/Mixed/OverMixed） |
| 正常态阈值 | `p > 1.0T`（`>`） | `p >= 1.0M`（`>=`） |
| 过度态阈值 | `p > 2.0T`（`>`） | `p > 2.0M`（`>`） |
| UI 警告阈值 | `p > 1.3T` | `p > 1.3M` |
| 推进方法 | `Cook(dt)` | `Mix(dt)` |
| 推进者 | `ServerCookingStation` / `ServerCookingRegion` | **`ServerMixingStation`** |
| 过度后 | `Cook()` 返回 false（进度冻结） | `Mix()` 返回 false |
| 容器装配 | `CookedCompositeAssembledNode` | `MixedCompositeAssembledNode` |
| 已过度能否再被组合 | 锅倒锅取 max（保持过度） | 锅倒锅取 max（保持过度） |
| 容器合并进度 | `(min(a,T)+min(b,T))×0.5` | `(min(a,M)+min(b,M))×0.5` |

#### 表 C：本章新增的成就 ID

| ID | 触发点 | 位置 |
|---|---|---|
| `5` | 切菜完成 | `ServerWorkableItem.cs:75` |
| `2` | 每洗好一张盘子 | `ServerWashingStation.cs:96` |
| `14` | 往垃圾桶倒掉有内容的锅 | `ServerContentsDisposalBehaviour.cs:18` |
| `803` | 放置的调料命中 `m_condimentAchievementFilter` | `ServerPlacementItemSpawner.cs:87` |

#### 表 D：源码中确认**不存在**的东西（避免自动化脚本走弯路）

| 被提到的名字 | 实际情况 |
|---|---|
| `AttachingFoodSpawner` | **不存在**。全库 grep 无命中；职责由 `TriggerAttachedSpawn` / `ServerTriggerAttachedSpawn` 承担 |
| `DryingPart` | **不存在**。"烘干"由 `WashingStation.m_dryingStation`（一个 `PlateReturnStation`）+ `ServerWashingStation` 的烘干站回调实现 |
| `Utensil`（独立类） | **不存在**。厨具身份靠组件组合（`CookableContainer` / `MixableContainer` / `PreparationContainer`）表达 |
| `PickupItemSpawner.m_spawnCost` | 声明了但**全库无读取点** → 死字段 |
| `ConveyorBeltCosmeticDecisions` / `ServerConveyorBeltCosmeticDecisions` / `ClientConveyorBeltCosmeticDecisions` | 数据类是**空壳**；纯表现（UV 滚动）在 `ClientConveyorBeltCosmeticDecisions.cs:31` |
| `IngredientDisposalBehaviour` | **空标记类**，无任何方法 |

---

### 2.9 自动化脚本可用的关键结论（本章行动要点）

1. **食材箱是无限零冷却的**——`ServerPickupItemSpawner` 无计时、无计数、`m_spawnCost` 是死字段。唯一障碍是箱子自身着火。
2. **切菜必须切到底**——`ServerWorkstation.CanPickupItem` 只允许拿走"未开始"或"已完成"的物品，切到一半的被锁；且粘性交互使得一旦开始就会持续切。
3. **刀数由人数决定**：合作模式 ≥2 人 = `m_stages-1` 刀；单人 = `5 × (m_stages-1)` 刀。**这是单人速通的巨大时间成本。**
4. **切菜节拍由动画驱动**（`"Chop"` / `"Impact"`），不是固定常量——所以"挥刀速度"与角色动画绑定，无法用固定帧数预测。
5. **烹饪有三个时间刻度**：`1.0×` 熟、`1.3×` 开始警告、`2.0×` 烧焦并**点燃灶台**（前提是关卡配了 `FireConfigData`）。
6. **烧焦后进度冻结**，不会无限累积；但灶台会持续燃烧，且火会以 **1.5 单位半径 / 5 秒**的速度点燃邻近 `Flammable` 台子。
7. **灭火 = 0.5 秒**（`m_exinguishTime`），射程 4 米、锥角 15°；拿着灭火器时自身不会着火。
8. **搅拌台是自动的**——把装料的锅放上去即自动开始，`OverMixed` 时自动停机，无需玩家操作。
9. **洗盘子 = 每张 2 秒 × 人手倍数**，且水槽是粘性交互（按住即可）；脏盘堆放上去即整体销毁并转为计数。
10. **锅掉地上会重生回"第一次被放下的台子"附近**（5 秒后），且**重生前内容会被清空**（`ServerContentsDisposalBehaviour`），并只会挑 `m_stationType` 匹配的灶台。
11. **灶台一次只能加热一个物品**（`ServerAttachStation.m_item` 单值）——所以"同时烧两个锅"必须用两个灶台。
12. **容器容量默认 3**（`IngredientContainer.m_capacity`），按内容条目数计。
13. **传送带 1 秒过一格**（默认 `m_conveySpeed = 1`），交接在行程 50% 处，且正在收货的传送带禁止人工放置。
14. **`TriggerAttachedSpawn.m_maxNumber = 50`** 是全库唯一真正的"数量上限"，且被"台子上有东西 / 传送带正在接收"两个闸门挡住时才不出货。

---

## 三、容器、摆盘与送餐

> 源码位置：`E:\pythonxx\tkry\overcooked_decomp\`（ILSpy 产物，Unity 2017.4.8 / Mono）。
> 标注约定：**【源码确认】** = 直接读到的代码事实（附 `文件:行号`）；**【推断】** = 由代码结构推论、未见显式实现。

### 3.0 一句话总览

胡闹厨房 2 的"摆盘"不是"把盘子放到食材上"，而是**食材把自己的订单节点灌进盘子容器里然后自我销毁**；"送餐"不是"把盘子运到窗口再提交"，而是**盘子被放到`PlateStation`上的那一刻，`AttachStation.OnItemAdded` 立刻触发交付**。整条链的核心抽象是 `IContainerTransferBehaviour` + `IIngredientContents` + `AssembledDefinitionNode` 三件套。**【源码确认】**

```
玩家交互键
  → IHandlePlacement.HandlePlacement(carrier, dir, PlacementContext(Source.Player))
    → 目标物上的 ServerPlacementContainer.HandlePlacement
      → 手持物.IContainerTransferBehaviour.TransferToContainer(carrier, 盘子容器, dontRemove:false)
        → AssembledNodeTransfer.CombineWithContents(...)   // 订单节点并入容器
        → carrier.DestroyCarriedItem()                     // 手持实体销毁
```

---

### 3.1 接口契约（先立规矩）

四个接口定义了整章的边界。**【源码确认】**

| 接口 | 文件 | 成员 |
|---|---|---|
| `IContainerTransferBehaviour` | `IContainerTransferBehaviour.cs:1-6` | `bool CanTransferToContainer(IIngredientContents)`；`void TransferToContainer(ICarrierPlacement, IIngredientContents, bool _dontRemove)` |
| `IIngredientContents` | `IIngredientContents.cs:1-20` | `CanAddIngredient` / `AddIngredient` / `RemoveIngredient(int)` / `GetContentsElement(int)` / `GetContents()` / `GetContentsCount()` / `CanTakeContents(AssembledDefinitionNode[])` / `Empty()` / `HasContents()` |
| `ICarrierPlacement` | `ICarrierPlacement.cs:1-10` | `InspectCarriedItem()` / `DestroyCarriedItem()` / `TakeItem()` |
| `IPlaceUnder` | `IPlaceUnder.cs:1-3` | **空标记接口**（无任何成员） |

两个关键语义点：

1. **`_dontRemove` 是"干跑"开关，不是"是否移位"**。所有实现里 `_dontRemove == true` 表示试算并保留原物，`false` 表示真正搬运并在最后销毁/清空来源。见 `IngredientToContainerBehaviour.cs:22-26`、`ServerPlate.cs:140-149`、`AssembledNodeTransfer.cs:224-233`。**【源码确认】**
2. **`IPlaceUnder` 标记的是"台面上已有的占用物"，不是手持物**。见 3.4。**【源码确认】**

`IPlaceUnder` 的实现者全库只有三个：`IngredientToContainerBehaviour`（`:4`）、`ItemToContainerBehaviour`（`:4`）、`ServerPreparationContainer`（`:5`）。**【源码确认，grep 全库仅 5 处命中】**

---

### 3.2 盘子与容器组件族

#### 3.2.1 `Plate` —— 盘子的数据壳

`Plate.cs` 只有 65 行，是一个极薄的 MonoBehaviour，靠 `RequireComponent` 把四种能力拼在身上：**【源码确认】**

```csharp
[RequireComponent(typeof(IngredientContainer))]      // :4  容量/触发区
[RequireComponent(typeof(HandlePlacementReferral))]  // :5  放置代理
[RequireComponent(typeof(PhysicalAttachment))]       // :6  可被吸附
[RequireComponent(typeof(PlacementContainer))]       // :7  可被放置内容
public class Plate : MonoBehaviour {
    [SerializeField] public PlatingStepData m_platingStep;  // :10-11
}
```

`m_platingStep` 是盘子的**唯一身份标识**（同款盘子共用同一个 `PlatingStepData` 资源），它在两处起决定性作用：`Plate.CanPlaceOnPlate:50` 的摆盘预校验，以及 `ServerOrderControllerBase.Matches:147` 的订单匹配。

`Plate.GetOrderComposition`（`Plate.cs:55-64`）是盘子内容的**规范化函数**：

```csharp
if (_contents.Length == 1 && _contents[0].GetType() != typeof(IngredientAssembledNode))
    return _contents[0];                       // 单个非"裸食材"→ 直接返回该节点本身
CompositeAssembledNode n = new CompositeAssembledNode();
n.m_composition = _contents;                   // 否则包一层 Composite
return n;
```

**含义**：盘上一块熟肉（`CookedCompositeAssembledNode`）→ 订单快照就是那块肉本身；盘上"面包+肉+生菜"→ 订单快照是一个三层 `CompositeAssembledNode`。**【源码确认】**

`Plate.CanPlaceOnPlate`（`Plate.cs:17-53`）有一个几乎没人注意的两级结构：

- **快路径**（`:25-45`）：如果盘子的 `ServerIngredientContainer` **恰好只有 1 个内容**且该内容带 `m_freeObject`（即它是一个独立实体），则转而问那个实体能不能吃下新东西：`handleOrderModification.CanAddOrderContents(new[]{ placedOrderComposition })`。这是"容器套容器"（如汤锅、平底锅、准备容器）**不吃掉**手持物的通道。
- **慢路径**（`:46-51`）：
  ```csharp
  if (containerTransferBehaviour.CanTransferToContainer(_ingredientContentsAdapter)) {
      containerTransferBehaviour.TransferToContainer(carrier, _ingredientContentsAdapter, _dontRemove: true);
      AssembledDefinitionNode orderComposition = GetOrderComposition(_ingredientContentsAdapter.GetContents());
      return GameUtils.GetOrderPlatingPrefab(orderComposition, m_platingStep) != null;
  }
  ```
  关键在于 `_dontRemove: true` 让 `TransferToContainer` **只对适配器（影子副本）生效**，不碰真实容器；然后把"并进去之后会长成什么样"拿去 `GetOrderPlatingPrefab` 查表。**这是一个纯函数式的"试摆盘"**：只有当试摆的结果能匹配到一张真实存在的菜品预制体时，才允许放。这是防止玩家把生土豆塞进汉堡盘的关键闸门。**【源码确认】**

#### 3.2.2 `PlatingStepData` —— 用 ID 做相等

`PlatingStepData.cs` 是 `ScriptableObject`，核心是 `m_uID`：**【源码确认】**

```csharp
[SelfAssignID(true)] public int m_uID;                  // :13-14
public override int GetHashCode() => m_uID.GetHashCode();  // :16-19
public bool Equals(PlatingStepData other) {              // :26-37
    if (object.ReferenceEquals(other, null)) return false;
    if (object.ReferenceEquals(other, this)) return true;
    return m_uID == other.m_uID;
}
public static bool operator ==(PlatingStepData lhs, PlatingStepData rhs) { ... }  // :39-46
public static bool operator !=(PlatingStepData lhs, PlatingStepData rhs) { ... }  // :48-51
```

**结论**：`_required.m_platingStep != _plateType` 这类比较走的是运算符重载 → `Equals` → **`m_uID` 逐值比较**，不是引用比较。所以"同款盘子"即使在网络两端是不同的实例，比较也成立；而"圆盘 vs 方盘"（不同 `PlatingStepData`）一定不相等。**【源码确认】** 这一点在 3.6 的订单匹配里是硬门槛。

#### 3.2.3 三套 `IngredientContainer`

**普通层（序列化数据）**：`IngredientContainer.cs` 全文 16 行：

```csharp
[SerializeField] public int m_capacity = 3;                  // :7
[SerializeField] public Collider m_onSurfaceTriggerZone;     // :10
public int GetCapacity() => m_capacity;                      // :12-15
```

`m_capacity` **默认 3**，盘子实际容量由预制体覆盖。**【源码确认】**

**服务端权威层**：`ServerIngredientContainer.cs`，`List<AssembledDefinitionNode> m_contents` + `ServerSynchroniserBase`：**【源码确认】**

| 方法 | 行号 | 行为 |
|---|---|---|
| `HasContents()` | `:52-55` | `m_contents.Count > 0` |
| `CanTakeContents(arr)` | `:57-61` | `m_capacity - m_contents.Count >= arr.Length` |
| `CanAddIngredient(node)` | `:63-66` | `m_contents.Count < m_capacity` |
| `AddIngredient(node)` | `:68-72` | `Add` 后 `OnContentsChanged()` |
| `RemoveIngredient(int i)` | `:74-84` | ⚠️ 用 `m_contents.Remove(assembledDefinitionNode)`（按值删）而非 `RemoveAt(i)` |
| `Empty()` | `:96-100` | `Clear()` + 通知 |
| `OnContentsChanged()` | `:138-144` | 回调 + `IngredientContainerMessage.Initialise(contents)` + `SendServerEvent` |

⚠️ **`RemoveIngredient(int i)` 的坑**：它不是按下标删，而是取 `m_contents[i]` 再用 `List<T>.Remove(T)` **按值删第一个匹配项**（`:78-79`）。由于 `AssembledDefinitionNode` 重写了 `Equals`（见 3.5.1），"匹配"是按配方语义匹配的。若盘上有两块相同食材，`RemoveIngredient(1)` 会删掉第 0 块。**这是真实的语义偏差，不是反编译噪音。**【源码确认】

**客户端镜像层**：`ClientIngredientContainer.cs` 只吃服务端广播，且写操作全部抛异常：**【源码确认】**

```csharp
public void Empty()          => throw new NotImplementedException();  // :73-76
public void AddIngredient(a) => throw new NotImplementedException();  // :83-86
public AssembledDefinitionNode RemoveIngredient(int i) => throw new NotImplementedException();  // :88-91
```

`ApplyServerEvent`（`:26-40`）处理两种消息：`ActiveState`（直接 `SetActive`）与 `ContentsChanged`（清空后 `AddRange(message.Contents)` 并触发本地回调）。**客户端永远不能改内容，只能重放服务端内容。**【源码确认】

#### 3.2.4 三套 `PlacementContainer`

**`PlacementContainer.cs`（普通层，全文 24 行）** —— 这是全章最关键的门卫：**【源码确认】**

```csharp
public virtual bool CanCombine(GameObject _placingObject,
        List<Generic<bool, GameObject, PlacementContext>> m_allowPlacementCallbacks,
        IIngredientContents _container, PlacementContext _context)
{
    if (!m_allowPlacementCallbacks.CallForResult(_result: true, _placingObject, _context))  // 闸门 A
        return false;
    IOrderDefinition orderDefinition = _placingObject.RequestInterface<IOrderDefinition>();  // 闸门 B
    if (orderDefinition != null && orderDefinition.GetOrderComposition().Simpilfy() != AssembledDefinitionNode.NullNode) {
        IContainerTransferBehaviour t = _placingObject.RequestInterface<IContainerTransferBehaviour>();  // 闸门 C
        if (t != null && t.CanTransferToContainer(_container))
            return _container.CanAddIngredient(orderDefinition.GetOrderComposition());        // 闸门 D
    }
    return false;
}
```

四个闸门：**A** 所有注册的 `allowPlacement` 回调里**至少一个返回 true**；**B** 手持物必须能描述自己的订单且非空；**C** 手持物必须实现 `IContainerTransferBehaviour` 且愿意转入**这个**容器；**D** 目标容器**有容量**接收它。

> **闸门 A 的语义陷阱（重要）**：`DelegateUtils.CallForResult(_result)`（`DelegateUtils.cs:5-15`）的实现是"遍历，**任意一个**返回值 `.Equals(_result)` 就返回 true"。所以 `CallForResult(true, ...)` = **OR 语义**。推论：**一个没有注册任何 allow 回调的 `PlacementContainer` 会拒绝一切放置**（`CallForResult` 返回 false → `!false` → `return false`）。**【源码确认】**
>
> 对照 `AttachStation.CouldAttachToSelfIfEmpty`（`AttachStation.cs:89-96`）用的是 `!_placementQuery.CallForResult(_result: false, ...)` = **AND 语义**（"没有任何回调投反对票"），且**零回调时返回 true（放行）**。**同一个 `Generic<bool,GameObject,PlacementContext>` 委托列表，在两个调用点被赋予了相反的聚合语义（OR vs AND）。这是本作放置系统里最容易被误读的一处设计。**【源码确认】

**`ServerPlacementContainer.cs`（服务端）** —— 实现 `IHandlePlacement, IPlacementSupression, IBaseHandlePlacement`：**【源码确认】**

- `GetPlacementPriority()` 基类返回 `0`（`:19-22`），托盘子类覆盖为 `1`（`ServerTrayPlacementContainer.cs:30-33`），`ServerPreparationContainer` 返回 `int.MinValue`（`ServerPreparationContainer.cs:261-264`）。
- `CanCombine`（`:24-27`）把 `m_allowPlacementCallbacks`、`m_ingredientContainer` 一并转发给 `PlacementContainer.CanCombine`。
- `CanHandlePlacement`（`:29-54`）三级策略：
  1. 手持物能否并进**我**；
  2. **反向**：手持物自己也是容器，且"我"能否并进**它**（`serverPlacementContainer.CanCombine(base.gameObject, _context)`，`:36-40`）——这就是两个容器互放的双向判定；
  3. 仅当载体是 `ServerPlayerAttachmentCarrier` 时，走 `ServerPreparationContainer.CanAddOrderContents` 特例（`:41-52`）。
- `HandlePlacement`（`:56-73`）是**真正执行**的地方：
  ```csharp
  if (CanCombine(gameObject, _context)) {
      IContainerTransferBehaviour t = gameObject.RequireInterface<IContainerTransferBehaviour>();
      t.TransferToContainer(_carrier, m_ingredientContainer, _dontRemove: false);  // :62
      m_ingredientContainer.InformOfInternalChange();                              // :63
      return;
  }
  // 否则尝试反向：把自己整体倒进手持容器
  ```

**`ClientPlacementContainer.cs`** 是纯预测镜像：`CanHandlePlacement`（`:19-32`）同样先 `CanCombine` 再试反向 `ClientPlacementContainer`；**它故意不实现 `HandlePlacement`**——客户端只做高亮预测，真实搬运等服务器广播。**【源码确认】**

---

### 3.3 摆盘/组装到底怎么发生

#### 3.3.1 `AssembledNodeTransfer` —— 组装的唯一实现

这是静态工具类，四个公开方法构成全部转移语义。**【源码确认】**

**（1）`CanCombineWithContents(node, container)`（`:5-82`）** 按容器类型分派：

- 有 `ServerMixableContainer`（搅拌碗）→ 走 `CanTransferPremixedContents`；`ItemAssembledNode` 直接 `false`（锅具不能进搅拌碗），`CompositeAssembledNode` 则把 `m_composition` 整体递进去（`:9-26`）。
- 有 `ServerCookableContainer`（锅/灶）→ 走 `CanTransferPrecookedContents`；`CookedCompositeAssembledNode` 会先被 `GetRecookFromNode` 拆解出 `(composition, progress)`（`:27-48`）。
- 有 `ServerItemContainer`（物品架）→ 只接受 `ItemAssembledNode`（`:49-57`）。
- **都没有（= 盘子）** → 落到通用子集判定（`:59-81`）：
  ```csharp
  for (int i = 0; i < _container.GetContentsCount(); i++)                   // 容器里的复合节点
      if (contents[i] 是 CompositeAssembledNode && !它.CanAddOrderNode(_ingredientToAdd)) return false;
  if (_ingredientToAdd 是 CompositeAssembledNode)                            // 来料也是复合节点
      for (int num = _container.GetContentsCount() - 1; num >= 0; num--)
          if (!来料.CanAddOrderNode(_container.GetContentsElement(num))) return false;
  return true;
  ```

**（2）`CombineWithContents(node, container, dontUpdate)`（`:84-159`）** —— 真正的合并动作，与 `CanCombine` 结构对称：搅拌碗/锅具分支先处理，之后：

```csharp
for (...) if (contents[i] is CompositeAssembledNode c && c.CanAddOrderNode(_ingredientToAdd)) {
    c.AddOrderNode(_ingredientToAdd, _dontUpdate);   // :141  并进已有复合节点
    return;                                          // 并成功就返回，不再 AddIngredient
}
if (_ingredientToAdd is CompositeAssembledNode add) {
    for (int num = count - 1; num >= 0; num--)        // :149-156 反向吸收
        if (add.CanAddOrderNode(contents[num])) { add.AddOrderNode(contents[num], _dontUpdate); _container.RemoveIngredient(num); }
}
_container.AddIngredient(_ingredientToAdd);            // :158 兜底：作为新元素追加
```

注意 `:134-145` 与 `:146-157` 的**两段式**：先试"并入已有节点"，再试"吸收零散节点"，最后才 `AddIngredient` 追加。**这就是"盘子上先有一块肉，再放一片面包，结果合成一个汉堡"的物理实现**：要么面包被并进那个已有复合节点，要么两个零散节点被合并进同一个复合节点，最后盘子的 `m_contents` 长度甚至可能**减少**。**【源码确认】**

**（3）`CanTransferFromContainer<T>`（`:186-216`）/（4）`TransferFromContainer<T>`（`:218-235`）** —— 泛型约束 `where T : MonoBehaviour, IOrderDefinition, IContainerTransferBehaviour`，**只有从"带 `IngredientContainer` 的源头"往外倒时才用**（汤勺 `ServerLadleContainer` 是唯一已知调用方，见 3.8）。`CanTransferFromContainer` 里还有两条"半成品不许倒"的规则：**【源码确认】**

```csharp
if (容器有 ServerLadleContainer && 源头有 ServerCookingHandler) return true;      // :191-194  汤勺特例
if (目标不是锅 && 源头有 ServerCookingHandler) {                                  // :195-203
    float p = handler.GetCookingProgress();
    return p == 0f || p >= handler.AccessCookingTime;                             // 必须"全生"或"全熟"
}
if (目标不是搅拌器 && 源头有 ServerMixingHandler) { ... 同理 ... }                 // :204-212
```

**即：夹生的东西既不能倒进盘子，也不能倒进搅拌碗——只有 0%（生）或 100%（熟）才能转移。** 这是"做完才能上菜"的硬性实现。**【源码确认】**

#### 3.3.2 `IngredientToContainerBehaviour` / `ItemToContainerBehaviour`

两者都是"长在食材/物品身上"的转移能力，也都是 `IPlaceUnder`。**【源码确认】**

```csharp
// IngredientToContainerBehaviour.cs:8-27
public bool CanTransferToContainer(IIngredientContents _container) {
    EnsureOrderDefinition();
    return AssembledNodeTransfer.CanCombineWithContents(m_orderDefinition.GetOrderComposition(), _container);
}
public void TransferToContainer(ICarrierPlacement _carrier, IIngredientContents _container, bool _dontRemove) {
    EnsureOrderDefinition();
    AssembledDefinitionNode n = m_orderDefinition.GetOrderComposition();
    if (_dontRemove) n = n.Simpilfy();
    AssembledNodeTransfer.CombineWithContents(n, _container, _dontRemove);
    if (!_dontRemove) _carrier.DestroyCarriedItem();     // :25  ← 手持物在这里被销毁
}
```

`ItemToContainerBehaviour.cs:16-28` 的差别只有一处：`CanTransferToContainer` 要求目标 GameObject 上必须有 `ItemContainer` 组件（`:22`），否则一律 false。

#### 3.3.3 `AssembledNodeTransfer` 的调用者是谁

**【源码确认】** 全库中 `CombineWithContents` / `CanCombineWithContents` 的调用点集中在：

| 调用者 | 行号 | 场景 |
|---|---|---|
| `IngredientToContainerBehaviour` | `:11, :22` | 裸食材/半成品被放入容器 |
| `ItemToContainerBehaviour` | `:24, :33` | 物品（盘子、锅等）被放入 ItemContainer |
| `ServerPlacementContainer.HandlePlacement` | 经 `PlacementContainer.CanCombine` | 服务端权威判定 |
| `ServerPlate.TransferToContainer` | `:144` | 盘子作为**被搬运方**整体倒入别的容器 |
| `ServerPlate.CanTransferToContainer` | `:121` | 同上，判定版 |
| `AssembledNodeTransfer.TransferFromContainer` | `:228` | 汤勺往外倒 |

#### 3.3.4 ⭐ 核心结论：手持切好的食材对着「放着盘子的台面」按交互键

**先看场景定义**：台面 = 一个 `ServerAttachStation`，上面已经吸附了一个 `Plate`（盘子）。玩家手上拿着切好的食材（比如切好的生菜，带 `IngredientToContainerBehaviour`）。

**完整调用链（全部源码确认）**：

1. **按键入口**：`PlayerControlsHelper.PlaceHeldItem_Server`（`PlayerControlsHelper.cs:126-147`）
   ```csharp
   IHandlePlacement handlePlacement = GetControllingPlacementHandler_Server(_target);   // :130
   if (handlePlacement.CanHandlePlacement(carrier, normalized, new PlacementContext(PlacementContext.Source.Player)))  // :134
       handlePlacement.HandlePlacement(carrier, normalized, new PlacementContext(PlacementContext.Source.Player));     // :136
   else handlePlacement.OnFailedToPlace(carrier.InspectCarriedItem());                                            // :140
   ```
   注意 `_target` 是**台面 GameObject**，`_control.CurrentInteractionObjects` 决定目标。`PlacementContext.Source.Player` 会被下游用于区分"玩家行为"与"传送带/重生"等自动化行为（见 `ServerPlateReturnStation.CanAddItem:58-61`）。

2. **`IHandlePlacement` 的解析优先级**：`GetControllingPlacementHandler_Server`（`:164-173`）先查 `ServerHandlePlacementReferral`（**代理优先**），没有才用 `HandlePlacementUtils.GetHighestPriority` 在全部 `IHandlePlacement` 里选优先级最高者。

3. **台面决定"往哪儿放"**：`ServerAttachStation.CanHandlePlacement`（`ServerAttachStation.cs:147-156`）
   ```csharp
   GameObject item = (m_item == null) ? null : m_item.AccessGameObject();   // :149 台面当前占用物 = 盘子
   IHandlePlacement placementHandler = null;
   if (item != null) placementHandler = item.RequestInterface<IHandlePlacement>();  // :153 盘子上的 ServerPlacementContainer
   return m_station.CalculatePlacementType<IHandlePlacement>(item, ..., placementHandler, ...) != PlacementType.NotValid;  // :155
   ```

4. **`AttachStation.CalculatePlacementType`（`AttachStation.cs:40-71`）** —— 因为 `_item`（盘子）非空且 `placementHandler` 非空，走 `:54-64`：
   ```csharp
   if (placementHandler.CanHandlePlacement(_iCarrier, _directionXZ, _context)) {
       if (CanPlaceUnder<T>(...)) return PlacementType.OntoAndUnderOccupant;   // :60
       return PlacementType.OntoOccupant;                                     // :62
   }
   return PlacementType.NotValid;                                             // :64
   ```

5. **盘子的 `CanHandlePlacement` → 四道闸门**：`ServerPlacementContainer.CanHandlePlacement`（`:29-54`）→ `CanCombine`（`:24-27`）→ `PlacementContainer.CanCombine`（`PlacementContainer.cs:9-20`）。其中**闸门 A** 调用的正是 `ServerPlate` 注册进去的那个回调：
   ```csharp
   // ServerPlate.cs:88-90
   m_ingredientContainer.RegisterContentsChangedCallback(OnContentsChanged);
   m_placementContainer = GetComponent<ServerPlacementContainer>();
   m_placementContainer.RegisterAllowItemPlacement(CanPlaceOnPlate);

   // ServerPlate.cs:187-190
   protected virtual bool CanPlaceOnPlate(GameObject _gameObject, PlacementContext _context) {
       return !IsReserved() && m_plate.CanPlaceOnPlate(_gameObject, new IngredientContainerAdapter(m_ingredientContainer));
   }
   ```
   `IngredientContainerAdapter`（`ServerPlate.cs:8-69`）是**真实容器内容的深拷贝影子**（构造时逐个 `Simpilfy()`，`:14-20`）。这正是 `Plate.CanPlaceOnPlate` 敢用 `_dontRemove: true` 做干跑的前提：**试算只污染影子，不污染真盘子。**【源码确认】

6. **闸门 C/D 再验一遍**（`PlacementContainer.cs:17-19`）：手持食材的 `CanTransferToContainer(真实盘子容器)` 与 `真实盘子容器.CanAddIngredient(食材订单)`。

7. **执行**：`ServerAttachStation.HandlePlacement`（`:163-190`）按 `switch` 分派，此处为 `PlacementType.OntoOccupant`（`:179-181`）→ **注意它调的不是格子自己，而是盘子的处理器**：
   ```csharp
   private void PlaceOntoOccupant(ICarrier _iCarrier, Vector2 _directionXZ, PlacementContext _context) {
       IHandlePlacement handlePlacement = m_item.AccessGameObject().RequireInterface<IHandlePlacement>();  // :222 盘子的
       handlePlacement.HandlePlacement(_iCarrier, _directionXZ, _context);                                 // :223
   }
   ```

8. **落盘**：`ServerPlacementContainer.HandlePlacement`（`:56-73`）
   ```csharp
   containerTransferBehaviour.TransferToContainer(_carrier, m_ingredientContainer, _dontRemove: false);  // :62 真搬运
   m_ingredientContainer.InformOfInternalChange();                                                      // :63 强制广播
   ```

9. **销毁手持物**：`IngredientToContainerBehaviour.TransferToContainer:22-26` → `CombineWithContents(...)` 把食材的订单节点并入盘子容器 → `_carrier.DestroyCarriedItem()`。

### ✅ 回答（这一条是本节点的核心答案）

> **食材会自动进盘，且玩家的手持物会被销毁、手变空。盘子留在台面上，内容增加。**

三个细节需要强调：

- **"进盘"的产物不一定是 `m_contents` 变长。** 见 3.3.1(2)：如果并进已有复合节点成功，`CombineWithContents` 会在 `:142` 直接 `return`，**`AddIngredient` 根本不会被调用**，盘子的 `m_contents` 甚至可能因为反向吸收（`:154`）而缩短。真正变化的是那个 `AssembledDefinitionNode` 的内部组成。**【源码确认】**
- **手持物销毁的时机是"合并之后"**，不是之前；且如果合并过程中 `CanAddOrderNode` 失败（理论上不该发生，因为闸门已预检），销毁依然会执行——`IngredientToContainerBehaviour` 没有在 `TransferToContainer` 里重验一次完整性，它信任调用方的 `CanTransferToContainer`。**【源码确认，属设计上的信任契约】**
- **如果台面上没有盘子而是空的**，则走 `PlacementType.OntoEmpty`（`AttachStation.cs:46`）→ `OnItemPlaced(_iCarrier.TakeItem(), ...)`（`ServerAttachStation.cs:174`），食材被**放到台面上**（`OnItemPlaced`，`:308-341`），不销毁、不进任何容器。**【源码确认】**

---

### 3.4 `PlacementType` 与 `IPlaceUnder`

`PlacementType.cs` 全文 9 行：**【源码确认】**

```csharp
public enum PlacementType { NotValid, OntoEmpty, ContentsOntoEmpty, OntoOccupant, UnderOccupant, OntoAndUnderOccupant }
```

`AttachStation.CalculatePlacementType<T>`（`AttachStation.cs:40-71`）的判定树（**这是整个放置系统的决策核心**）：

```
_item == null ?（台面空）
  ├─ CanAttachToSelf(_item=null, carried, ...)      → OntoEmpty            :44-47
  ├─ CanAttachContents(null, carried, ...)          → ContentsOntoEmpty    :48-51
  └─ 否则                                            → NotValid            :52
_item != null ?（台面被占）
  ├─ placementHandler == null                       → 检查 CanPlaceUnder   :66-69
  │    └─ 通过 → UnderOccupant / 否则 NotValid
  └─ placementHandler.CanHandlePlacement(...)
       ├─ CanPlaceUnder(...) 通过 → OntoAndUnderOccupant                    :58-61
       ├─ 否则                     → OntoOccupant                           :62
       └─ CanHandlePlacement 失败  → NotValid                               :64
```

**五种有效类型的执行映射**（`ServerAttachStation.HandlePlacement:171-189`）：**【源码确认】**

| `PlacementType` | 执行动作 | 行号 |
|---|---|---|
| `OntoEmpty` | `OnItemPlaced(_iCarrier.TakeItem(), ...)` —— 手持物整体放上台面 | `:173-175` |
| `ContentsOntoEmpty` | `OnContentsPlaced(手持容器, ...)` —— **只把容器的内容物倒到台面上** | `:176-178` |
| `OntoOccupant` | `PlaceOntoOccupant` —— 交给**占用物**的 `IHandlePlacement` | `:179-181` |
| `UnderOccupant` | `PlaceUnderOccupant` —— 塞到占用物**下面** | `:182-184` |
| `OntoAndUnderOccupant` | 先 `PlaceOntoOccupant` 再 `PlaceUnderOccupant`，**两个都做** | `:185-188` |

`ContentsOntoEmpty` 的实现很有意思（`ServerAttachStation.cs:192-203`）——它把容器内容物的 `m_freeObject` **实体**取出来，清空容器，把实体放上台面，最后 `freeObject.SetActive(true)`：**【源码确认】**

```csharp
AssembledDefinitionNode n = serverIngredientContainer.GetContents()[0];   // :197 只取第 0 个
GameObject freeObject = n.m_freeObject;                                   // :198
serverIngredientContainer.Empty();                                        // :199
OnItemPlaced(freeObject, _directionXZ, _context);                         // :200
freeObject.SetActive(value: true);                                        // :201
```

**这是一个真实的 "拆包" 通道**：把盘子/容器里的那个**实体对象**单独放到空格子上。注意它只处理 `GetContents()[0]` 且不检查 `freeObject` 是否为 null。**【源码确认】**

#### `IPlaceUnder` 的真实语义（易错点）

`AttachStation.CanPlaceUnder<T>`（`:73-82`）：**【源码确认】**

```csharp
GameObject gameObject = _iCarrier.InspectCarriedItem();      // :75 手持物
T val = gameObject.RequestInterface<T>();                    // :76 手持物的 IHandlePlacement
if (val != null && val.CanHandlePlacement(_holder, _directionXZ, _context)
    && CouldAttachToSelfIfEmpty(gameObject, _context, _placementQuery))
    return _item.RequestInterface<IPlaceUnder>() != null;    // :79  ← 检查的是【占用物】
return false;
```

**`_item` 是台面上的占用物，`gameObject` 才是手持物**。所以 `IPlaceUnder` 是挂在**被压住的那一方**身上的标记："我允许别人塞到我下面"。

`PlaceUnderOccupant`（`:226-236`）的实际动作：**【源码确认】**

```csharp
GameObject obj = _iCarrier.InspectCarriedItem();
ICarrier holder = GetHolder();                                // :229 = m_holderAdapter
if (holder.InspectCarriedItem() != null) {                    // :230 另一名玩家的手上有东西
    IHandlePlacement hp = obj.RequireInterface<IHandlePlacement>();
    hp.HandlePlacement(GetHolder(), _directionXZ, _context);  // :233 先塞进"另一个人手上的那个东西"
}
OnItemPlaced(_iCarrier.TakeItem(), _directionXZ, _context);   // :235 再把手里这个放上台面
```

- `GetHolder()` 返回 `m_holderAdapter`（`ServerAttachStation.cs:510-513`），即**第二载体**——【推断】在双人协作里是搭档的手/另一个 `ICarrier`，用于"我把东西递到你手里的容器，同时我腾出手"这类连锁搬运。
- 实现 `IPlaceUnder` 的三个类型（食材行为、物品行为、准备容器 `ServerPreparationContainer`）说明**能被"压"的是可容纳内容的物件**——即"盘子可以塞到台面上那口锅/那份食材下面"。**【推断，基于命中集合与 `:179` 的检查点，未见显式注释佐证】**

---

### 3.5 配方匹配

#### 3.5.1 节点类型与 `Equals` 的统一入口

`AssembledDefinitionNode`（`AssembledDefinitionNode.cs`）是抽象基类，实现 `IEnumerable<AssembledDefinitionNode>, Serialisable, IEnumerable`：**【源码确认】**

```csharp
public static AssembledDefinitionNode NullNode = new NullAssembledNode();  // :9
public GameObject m_freeObject;                                            // :11 指向承载它的实体
protected abstract bool IsMatch(AssembledDefinitionNode _node);            // :82
public abstract AssembledDefinitionNode Simpilfy();                        // :86
public abstract int GetNodeCount();                                        // :84
public override bool Equals(object _node) => Matching(this, _node as AssembledDefinitionNode);  // :77-80
```

**`Equals` 被彻底改写为配方匹配**（`:77-80`）——这就是 3.2.3 里 `List.Remove(T)` 会按语义删元素的根因。**【源码确认】**

四个公开的 `Matching` 重载（`:26-68`）逻辑完全一致，都做"两边 `Simpilfy()` 后 `node1.IsMatch(node2)`"：**【源码确认】**

```csharp
public static bool Matching(OrderDefinitionNode _node1, OrderDefinitionNode _node2)  // :26
public static bool Matching(AssembledDefinitionNode _node1, AssembledDefinitionNode _node2)  // :37
public static bool Matching(OrderDefinitionNode _node1, AssembledDefinitionNode _node2)  // :48
public static bool Matching(AssembledDefinitionNode _node1, OrderDefinitionNode _node2)  // :59
// 共同主体：
AssembledDefinitionNode a = _node1.Simpilfy();
AssembledDefinitionNode b = _node2.Simpilfy();
return a.IsMatch(b);
```

另有 `MatchingAlreadySimple`（`:17-24`）——**跳过 `Simpilfy()`，假设入参已简化**，用于热路径（`GameUtils.GetOrderPlatingPrefab`、`CompositeAssembledNode.Contains`）。**【源码确认】**

`Simpilfy()` 的语义（各子类实现）：**丢弃 `NullNode` 子树、压平单元素复合节点、剥离"未熟/未拌"的状态壳**。三个子类的模式完全一致：**【源码确认】**

| 类 | 关键简化规则 | 行号 |
|---|---|---|
| `CompositeAssembledNode` | `m_optional.Length == 0` 且 `m_composition.Length == 1` → 返回那唯一一个子节点（**塌缩**）；`Length == 0` → 返回 `NullNode` | `:107-118` |
| `CookedCompositeAssembledNode` | `m_progress == Raw` → 降级成普通 `CompositeAssembledNode`（**生 = 没有烹饪状态**）；否则保留 `m_cookingStep`/`m_progress` | `:56-107` |
| `MixedCompositeAssembledNode` | `m_progress == Unmixed` → 降级成普通 `CompositeAssembledNode` | `:51-102` |

`CookedCompositeAssembledNode.Simpilfy` 还有一条独有规则（`:94-100`）：单元素且该元素是 `CompositeAssembledNode` **且不是** `MixedCompositeAssembledNode` 时，把内层 composition 提升上来——**"混合物不拆，普通复合可拆"**。**【源码确认】**

#### 3.5.2 匹配是否与顺序有关？→ **无关（但 `Contains` 是贪心匹配）**

`CompositeAssembledNode.IsMatch`（`:64-74`）：**【源码确认】**

```csharp
protected override bool IsMatch(AssembledDefinitionNode _subject) {
    if (_subject.GetType() == typeof(CompositeAssembledNode)) {
        CompositeAssembledNode subject = _subject as CompositeAssembledNode;
        return AssumeTypeMatch(subject);                       // :69 复合 vs 复合
    }
    CompositeAssembledNode c = new CompositeAssembledNode();  // :71-73 单节点包装成复合再比
    c.m_composition = new AssembledDefinitionNode[1] { _subject };
    return IsMatch(c);
}
```

`AssumeTypeMatch`（`:76-79`）是**双向子集判定**：**【源码确认】**

```csharp
protected bool AssumeTypeMatch(CompositeAssembledNode _subject) {
    return Contains(m_composition.Union(m_optional), _subject.m_composition)
        && Contains(_subject.m_composition.Union(_subject.m_optional), m_composition);
}
```

两个方向都要求成立：**我的（必选 ∪ 可选）包含你的必选，且你的（必选 ∪ 可选）包含我的必选**。`m_optional` 是"允许有但不强求"的宽容项。

`Contains`（`:121-142`）是**顺序无关的贪心一对一匹配**：**【源码确认】**

```csharp
public static bool Contains(AssembledDefinitionNode[] _superSet, AssembledDefinitionNode[] _subSet) {
    bool[] used = new bool[_superSet.Length];
    for (int i = 0; i < _subSet.Length; i++) {          // 遍历子集
        bool found = false;
        for (int j = 0; j < _superSet.Length; j++) {    // 找第一个未被占用的匹配项
            if (!used[j] && (_subSet[i] == null || AssembledDefinitionNode.MatchingAlreadySimple(_subSet[i], _superSet[j]))) {
                used[j] = true; found = true; break;
            }
        }
        if (!found) return false;
    }
    return true;
}
```

### ✅ 结论：**匹配与顺序无关。**

理由链：**【源码确认】**
1. 两侧数组先做 `Union`（`AssumeTypeMatch:78`），`Union` 本身丢弃位置信息；
2. `Contains` 的双层循环只在 `used[]` 里做"一一占用"标记，**不比较下标**；
3. 因此 `[面包, 肉, 生菜]` 与 `[肉, 生菜, 面包]` 判定相同。

**但要留意一个真实缺陷**：由于 `m_optional` 是**数组而非集合**，`m_composition.Union(m_optional)`（`:78`）的 `Union` 用的是 `AssembledDefinitionNode.Equals`（即配方匹配），**重复的等价节点会被 `Union` 折叠掉**。若 `m_optional` 里有两个语义相同的条目（例如"可加两份番茄"），`Union` 后只剩一个，`Contains` 在需要两份时**可能误判为不匹配**。这是"顺序无关"设计引入的副作用。**【部分源码确认 + 部分推断】：`Union` 的去重行为源自 `Equals` 重写（源码确认），"重复可选条目会丢失"是推论，未在关卡数据中验证。**

#### 3.5.3 `CookedCompositeAssembledNode.IsMatch` 比了什么

**【源码确认】** `CookedCompositeAssembledNode.cs:38-45`：

```csharp
protected override bool IsMatch(AssembledDefinitionNode _subject) {
    if (_subject is CookedCompositeAssembledNode c && AssumeTypeMatch(c)) {
        return m_cookingStep.m_uID == c.m_cookingStep.m_uID      // ① 烹饪步骤 ID
            && m_progress == c.m_progress;                        // ② 熟度枚举
    }
    return false;
}
```

**只比这两项**（外加 `AssumeTypeMatch` 的 composition 子集判定）：

- **① `m_cookingStep.m_uID`**：`CookingStepData` 的 ID，对应"用什么方式做的"（煎/煮/炸/烤）。**不同烹饪方式的成品不匹配**，即使原料相同。
- **② `m_progress`**：`CookedCompositeOrderNode.CookingProgress` 枚举（`Raw` / `Cooked` / `Burnt`）。
- **注意：`m_recordedProgress`（精确进度浮点）不参与 `IsMatch`**。它只在两处被用：序列化（`:15-19`）和 `AssembledNodeTransfer.GetRecookFromNode`（`AssembledNodeTransfer.cs:164-166`）——即"把已熟的东西再放回锅里时，从记录的进度继续"。**【源码确认】** 所以糊到 51% 和糊到 99% 在订单匹配眼里是同一件事。
- **`_subject` 不是 `CookedCompositeAssembledNode` 时直接 `false`**：所以盘上放"生面包 + 熟肉"时，若配方要求的是"煎熟的汉堡"，用 `CookedCompositeAssembledNode` 作主语去比一个普通 `CompositeAssembledNode` 会失败——**这是 3.6 里 `Matching(_provided, _required)` 参数顺序为什么重要的原因。**【源码确认】

`MixedCompositeAssembledNode.IsMatch`（`:34-41`）同构，但只比 `m_progress`（`MixingProgress`）——混合物没有"步骤 ID"。**【源码确认】**

#### 3.5.4 ⚠️ `GetEnumerator()` 的自引用陷阱与正确遍历方式

**【源码确认】** 三个叶子类的实现完全一致：

```csharp
// IngredientAssembledNode.cs:30-33
public override IEnumerator<AssembledDefinitionNode> GetEnumerator() { yield return this; }
// ItemAssembledNode.cs:30-33
public override IEnumerator<AssembledDefinitionNode> GetEnumerator() { yield return this; }
// NullAssembledNode.cs:15-18
public override IEnumerator<AssembledDefinitionNode> GetEnumerator() { yield return this; }
```

而 `CompositeAssembledNode` 有两个不同的枚举器：**【源码确认】**

```csharp
// CompositeAssembledNode.cs:41-51  —— 注意：全库无任何调用点（死代码）
public IEnumerator<AssembledDefinitionNode> GetEnumeratorExhaustive() {
    for (int i = 0; i < m_composition.Length; i++) {
        yield return m_composition[i];                  // 先吐自己
        foreach (AssembledDefinitionNode item in m_composition[i]) yield return item;  // 再吐它的枚举结果
    }
}

// CompositeAssembledNode.cs:53-62  —— 被 IEnumerable<T> 契约调用
public override IEnumerator<AssembledDefinitionNode> GetEnumerator() {
    for (int i = 0; i < m_composition.Length; i++)
        foreach (AssembledDefinitionNode item in m_composition[i]) yield return item;   // 只吐"孩子的枚举结果"
}
```

**为什么"遍历树时绝不能调用枚举器"：**

假设你写一个最自然的递归遍历：

```csharp
void Walk(AssembledDefinitionNode n) {
    Console.WriteLine(n);
    foreach (var child in n) Walk(child);   // ❌ 致命
}
```

- 对 `CompositeAssembledNode`：`foreach` 吐出的是**孙子**（因为 `:57` 又进了一层子枚举器），**子节点本身被跳过**——层级错位。
- 对 `IngredientAssembledNode` / `ItemAssembledNode` / `NullAssembledNode`：`foreach` 吐出的是 **`this` 自己**（`:32` / `:32` / `:17`）——`Walk(child)` 等于 `Walk(自己)`，**无限递归 → 栈溢出**。这就是"自引用"的含义。

**正确遍历方式（三选一）：**

1. **推荐：直接用具体类型的数组字段递归**，叶子节点自然终止——
   ```csharp
   void Walk(AssembledDefinitionNode n) {
       // 先处理自身
       if (n is CompositeAssembledNode c) {          // 含 Cooked / Mixed 子类
           foreach (var child in c.m_composition) Walk(child);
           foreach (var child in c.m_optional)     Walk(child);   // m_optional 也要走
       }
       // 非 Composite 即为叶子，不递归
   }
   ```
   这是唯一同时正确处理 `m_optional` 的方式。**【源码确认：`m_composition` / `m_optional` 是唯一的树结构字段，声明于 `CompositeAssembledNode.cs:7-9`】**

2. **只做一层平铺展平**时可以用 `foreach`——**但不能递归**。库内正确用法的实例：
   ```csharp
   // IngredientContentsUIContainer.cs:125  —— UI 图标只需展平一层，不递归，所以安全
   foreach (AssembledDefinitionNode item in _order) {
       if (item is IngredientAssembledNode i && i.m_ingriedientOrderNode != null) SetNextIcon(...);
       if (item is ItemAssembledNode it && it.m_itemOrderNode != null) SetNextIcon(...);
   }
   ```
   另一个真实反例是 `BurritoCosmeticDecisions.HasContents`（`:31-38`）——它遍历的是 `_composite.m_composition` **数组本身**，不是枚举器，同样安全。

3. **需要"自己 + 全部后代"的展平序列**时用 `GetEnumeratorExhaustive()`（`:41-51`）——它显式先 `yield return m_composition[i]` 再展平其子项。**但注意它是死代码（全库零调用），且它自身也只下探两层**：`foreach (item in m_composition[i])` 对嵌套复合节点仍然会跳到孙辈。真正无限的深展平需要手写栈/队列。**【源码确认 + 推断】**

**补充坑**：`GetNodeCount()`（`Composite:212-220`）**是**正确的递归（`num += m_composition[i].GetNodeCount()`），可以放心用于规模估计。**【源码确认】**

---

### 3.6 送餐

#### 3.6.1 送餐口就是 `PlateStation`

`PlateStation.cs`（全文 46 行）几乎全是序列化字段：**【源码确认】**

```csharp
[SerializeField] public GameObject m_needsPlateFloatingUI;   // :23  "需要盘子" 提示
[SerializeField] public GameObject m_tipsFloatingNumberUI;   // :27  tip 飘字
[SerializeField] public PlateReturnStation[] m_returnStations; // :30  ★ 归还站列表
[SerializeField] public GameObject m_platePrefab;             // :33  ⚠️ 死字段，见 3.7.4
[SerializeField] public float m_createPlateTime;              // :36  ⚠️ 死字段，见 3.7.4
[SerializeField] public TeamID m_teamId;                      // :39  属于哪一队
[SerializeField] public string m_onFoodDeliveredTrigger;      // :42  交付时触发的动画 Trigger
[SerializeField] public DeliveryFX m_deliveryEffects;         // :45  交付特效
```

`[RequireComponent(typeof(Collider))]` + `[RequireComponent(typeof(AttachStation))]`（`:5-6`）——**送餐口在物理上就是一个"附加站台"，没有任何独立的触发器逻辑。** 交付完全寄生在 `AttachStation` 的"物品被放上了"事件上。**【源码确认】**

#### 3.6.2 `ServerPlateStation` —— 送餐的真正入口

`StartSynchronising`（`ServerPlateStation.cs:23-37`）注册了四件事：**【源码确认】**

```csharp
m_orderHandler = GameUtils.RequireManagerInterface<IKitchenOrderHandler>();  // :26  ★ 全局订单处理器
m_attachStation.RegisterOnItemAdded(OnItemAdded);            // :28
m_attachStation.RegisterOnItemRemoved(OnItemRemoved);        // :29
m_attachStation.RegisterAllowItemPlacement(CanAddItem);      // :30
m_attachStation.RegisterFailedToPlace(OnFailedToPlace);      // :31
m_plateStation = (PlateStation)synchronisedObject;           // :32
m_serverReturnStations = m_plateStation.m_returnStations.ConvertAll(x => x.gameObject.RequireComponent<ServerPlateReturnStation>());  // :33-36
```

`OnItemAdded`（`:84-105`）—— **这就是"送餐瞬间"**：

```csharp
private void OnItemAdded(IAttachment _iHoldable) {
    m_plate = _iHoldable.AccessGameObject().GetComponent<ServerPlate>();     // :86
    ServerIngredientContainer component = m_plate.GetComponent<ServerIngredientContainer>();  // :87
    if (!(component != null)) return;                                        // :88-91
    IIngredientContents component2 = component.GetComponent<IIngredientContents>();  // :92
    if (component2 != null && component2.GetContentsCount() >= 1) {          // :93  ★ 内容 ≥ 1
        AssembledDefinitionNode[] contents = component2.GetContents();
        for (int i = 0; i < contents.Length; i++)
            if (contents[i] != null && contents[i].m_freeObject != null)
                NetworkUtils.DestroyObject(contents[i].m_freeObject);        // :100 ★ 销毁食材实体
    }
    DeliverCurrentPlate();                                                   // :104
}
```

**触发条件（源码确认）**：一个 `ServerPlate` 被放置到送餐口的 `AttachStation` 上（`m_itemAdded` 回调），且它带 `ServerIngredientContainer`。**内容 ≥ 1 时先销毁所有 `m_freeObject` 实体（消除物理残留），再交付；内容为 0 时也会调用 `DeliverCurrentPlate()`（`:104` 在 `if` 之外）——即空盘子送上送餐口会走一次必然失败的交付。**【源码确认：`:104` 缩进层级与 `if` 块同级，位于 `:93` 的 `if` 之外】

`DeliverCurrentPlate`（`:66-73`）四步：**【源码确认】**

```csharp
private void DeliverCurrentPlate() {
    ServerPlate plate = m_plate;
    plate.Reserve(this);                                                              // :69 锁盘
    m_orderHandler.FoodDelivered(plate.GetOrderComposition(), plate.GetPlatingStep(), this);  // :70 交订单
    plate.StartDeliverySequence(this);                                                // :71 拆物理
    SendDeliverEvent(plate.gameObject, _success: true);                               // :72 通知客户端
}
```

⚠️ **参数名误导**：`StartDeliverySequence(this)` 传的是 **`ServerPlateStation` 自己**，而方法签名写的是 `ServerPlateStation _returnStation`（`ServerPlate.cs:192`）——**这个 `_returnStation` 是个纯粹的错误命名**（连同变量名 `serverAttachStation`），它拿的是**送餐口**的 `AttachStation`，不是归还站。归还站的选取发生在 `:70` 内部（见 3.7.2）。**【源码确认】**

`StartDeliverySequence`（`ServerPlate.cs:192-203`）：

```csharp
ServerAttachStation s = _returnStation.gameObject.RequireComponent<ServerAttachStation>();
GameObject gameObject = s.TakeItem();                    // :195 把盘子从送餐口摘下
foreach (Collider c in gameObject.RequestComponentsRecursive<Collider>()) c.enabled = false;  // :196-200 关碰撞
Rigidbody rb = GetComponent<IAttachment>().AccessRigidbody();
rb.isKinematic = true;                                   // :201-202 变运动学刚体
```

**服务端并不销毁盘子 GameObject**，只是把它从站台上摘下来、变成无碰撞的幽灵体；视觉上的销毁由客户端完成（见 3.6.6）。**【源码确认】**

`CanAddItem`（`:75-82`）与 `OnFailedToPlace`（`:112-115`）：

```csharp
private bool CanAddItem(GameObject _object, PlacementContext _context) {
    if (m_plate == null) return _object.GetComponent<ServerPlate>() != null;   // :77-80 空送餐口只收盘子
    return true;                                                              // :81
}
private void OnFailedToPlace(GameObject _object) { SendDeliverEvent(_object, _success: false); }  // :112-115
```

**【推断】** `:81` 的 `return true` 分支在实际交互中大概率不可达：该回调只在 `AttachStation.CouldAttachToSelfIfEmpty`（`AttachStation.cs:89-96`）里被调用，而那条路径仅在**台面为空**时进入（`CalculatePlacementType` 的 `_item == null` 分支，`AttachStation.cs:42-53`）；台面有盘子时走的是占用物自己的 `IHandlePlacement`。若该分支真被触达，`OnItemAdded:86-87` 会因非盘子对象而 `m_plate == null` → `NullReferenceException`。**这是一处潜在缺陷。**【源码确认（可达性分析）+ 推断（未在运行时验证）】**

#### 3.6.3 `FoodDelivered` → `OnFoodDelivered` 回调链

`IKitchenOrderHandler` 全文 4 行（`IKitchenOrderHandler.cs:1-4`）：**【源码确认】**

```csharp
public interface IKitchenOrderHandler {
    void FoodDelivered(AssembledDefinitionNode _definition, PlatingStepData _plateType, ServerPlateStation _station);
}
```

`ServerKitchenFlowControllerBase`（`ServerKitchenFlowControllerBase.cs`）实现它：**【源码确认】**

```csharp
public void FoodDelivered(AssembledDefinitionNode _definition, PlatingStepData _plateType, ServerPlateStation _station) {
    OnFoodDelivered(_definition, _plateType, _station);                        // :128-131
}

protected virtual void OnFoodDelivered(AssembledDefinitionNode _definition, PlatingStepData _plateType, ServerPlateStation _station) {
    m_plateReturnController.FoodDelivered(_definition, _plateType, _station);  // :135 ★ 先排归还
    ServerTeamMonitor monitorForTeam = GetMonitorForTeam(_station.GetTeamID()); // :136
    if (monitorForTeam.OnFoodDelivered(_definition, _plateType, out var o_orderID, out var o_entry,
                                       out var o_timePropRemainingPercentage, out var o_wasCombo)) {  // :137
        OnSuccessfulDelivery(o_orderID, o_entry, o_timePropRemainingPercentage, o_wasCombo, _station);  // :139
    } else {
        OnFailedDelivery(_station);                                            // :143
    }
}
```

**顺序要点**：归还站排程（`:135`）在订单匹配（`:137`）**之前**执行，**无论送餐成功还是失败，盘子都会被回收**。这是"失败的菜也要把盘子还回来"的实现方式。**【源码确认】**

`ServerTeamMonitor.OnFoodDelivered`（`ServerTeamMonitor.cs:26-41`）：**【源码确认】**

```csharp
public virtual bool OnFoodDelivered(AssembledDefinitionNode _definition, PlatingStepData _plateType,
        out OrderID o_orderID, out RecipeList.Entry o_entry, out float o_timePropRemainingPercentage, out bool o_wasCombo) {
    o_orderID = default; o_entry = null; o_timePropRemainingPercentage = 0f; o_wasCombo = false;
    if (m_ordersController.FindBestOrderForRecipe(_definition, _plateType, out o_orderID, out o_timePropRemainingPercentage)) {  // :32
        o_entry = m_ordersController.GetRecipe(o_orderID);        // :34
        bool restart = m_score.TotalCombo == 0;                   // :35
        o_wasCombo = m_ordersController.IsComboOrder(o_orderID, restart);  // :36
        m_ordersController.RemoveOrder(o_orderID);                 // :37 ★ 命中即摘单
        return true;
    }
    return false;
}
```

**注意 `restart = (m_score.TotalCombo == 0)`**：连击断了才重排序号（`:35`），这是 combo 机制的锚点。**【源码确认】**

#### 3.6.4 ⭐ `FindBestOrderForRecipe` 的匹配条件

`ServerOrderControllerBase.FindBestOrderForRecipe`（`ServerOrderControllerBase.cs:158-171`）：**【源码确认】**

```csharp
public bool FindBestOrderForRecipe(AssembledDefinitionNode _order, PlatingStepData _plateType, out OrderID o_orderID, out float _timePropRemainingPercentage) {
    o_orderID = new OrderID(0u);
    _timePropRemainingPercentage = 0f;
    List<ServerOrderData> list = m_activeOrders.FindAll(x => Matches(x.RecipeListEntry.m_order, _order, _plateType));  // :162
    ServerOrderData value = list.ToArray().FindLowestScoring(x => x.Remaining).Value;                                  // :163 ★ 剩余时间最少者优先
    if (value != null) {
        o_orderID = value.ID;
        _timePropRemainingPercentage = Mathf.Clamp01(value.Remaining / value.Lifetime);   // :167
        return true;
    }
    return false;
}

private bool Matches(OrderDefinitionNode _required, AssembledDefinitionNode _provided, PlatingStepData _plateType) {   // :145-156
    if (_required.m_platingStep != _plateType) return false;              // :147 ★★ 盘子类型必须严格相等
    if (_required.GetType() == typeof(WildcardOrderNode))
        return AssembledDefinitionNode.Matching(_required, _provided);    // :153 required 作主语
    return AssembledDefinitionNode.Matching(_provided, _required);        // :155 provided 作主语
}
```

### ✅ 回答：**`platingStep` 必须相等，这是第一个被检查的、不可绕过的条件。**

**【源码确认】** `:147` 的 `!=` 走的是 `PlatingStepData.operator !=`（`PlatingStepData.cs:48-51`）→ `Equals` → **`m_uID` 逐值比较**（3.2.2）。也就是说：**用圆盘端出的、配方要求方盘的菜，即使食材组合完全一致，也必定 `Matches` 返回 false → 一次"送餐失败"。** 这道检查在配方匹配**之前**执行（短路），代价为零。

**三个附加要点：**

1. **参数顺序是不对称的（`:151-155`）**：
   - 普通订单：`Matching(_provided, _required)` —— **玩家做出的东西作主语**。3.5.3 已知 `CookedCompositeAssembledNode.IsMatch` 在 `_subject` 类型不符时直接返回 false，所以这个方向决定了"玩家交出的必须是熟的那个节点类型"。
   - `WildcardOrderNode`（`WildcardOrderNode.cs:4-12`，只有 `m_cookingStep` 字段，`Convert()` 返回 `NullNode`）：**订单作主语**。因为通配订单的 `Convert()` 是 `NullNode`，反向比较无意义，所以翻转比较方向。**【源码确认】**
2. **选中策略是"最紧急优先"**：`:163` 的 `FindLowestScoring(x => x.Remaining)` —— 在**所有**匹配的活跃订单里挑 **`Remaining` 最小**（即最快要超时）的那个。这解释了"同时挂着两个相同订单时，先完成的那个会去救快过期的那一单"。**【源码确认】**
3. **失败没有兜底**：`FindAll` 结果为空 → `FindLowestScoring` 返回默认值 `null`（`:163-164`）→ 返回 `false` → `OnFoodDelivered` 走 `OnFailedDelivery`。**做错菜不会被任何订单吸收。**【源码确认】**

`FindLowestScoring` 的返回是 `KeyValuePair`，取 `.Value`；空集合时 `.Value` 为 `default(ServerOrderData)` = `null`。**【源码确认】**

#### 3.6.5 `OnSuccessfulDelivery` / `OnFailedDelivery` / `OnOrderExpired` 的后果

**成功**（`ServerKitchenFlowControllerBase.cs:147-171`）：**【源码确认】**

```csharp
int num  = m_kitchenFlowController.CalculateTip(_timePropRemainingPercentage);   // :149
int num2 = m_kitchenFlowController.CalculateBaseScore(_entry);                    // :150
int num3 = num * Mathf.Max(monitorForTeam.Score.TotalMultiplier, 1);              // :152 ★ tip × max(倍率,1)
monitorForTeam.Score.TotalBaseScore          += num2;                             // :153
monitorForTeam.Score.TotalTipsScore          += num3;                             // :154
monitorForTeam.Score.TotalSuccessfulDeliveries++;                                 // :155
if (_wasCombo) {
    monitorForTeam.Score.TotalCombo++;                                            // :158
    if (monitorForTeam.Score.TotalMultiplier < 4) monitorForTeam.Score.TotalMultiplier++;  // :159-162 ★ 上限 4
} else {
    monitorForTeam.Score.TotalMultiplier = 0;                                     // :166 ★ 断连清零
    monitorForTeam.Score.TotalCombo = 0;                                          // :167
    monitorForTeam.Score.ComboMaintained = false;                                 // :168
}
SendDeliverySuccess(_plateStation.GetTeamID(), _plateStation, _orderID, _timePropRemainingPercentage, num3, _wasCombo);  // :170
```

- **基础分**：`KitchenFlowControllerBase.CalculateBaseScore`（`KitchenFlowControllerBase.cs:12-15`）→ `_entry.m_scoreForMeal`，**由配方静态决定**。
- **小费（tip）**：`KitchenFlowControllerBase.CalculateTip`（`:17-26`）—— 在 `m_gameConfig.TipBoundaries` 里筛出 `PercentageTimeRemaining < _remainingTimePercent` 的档位，取其中阈值最高的那一档的 `ScoreValue`；无匹配返回 0。**即"剩的时间越多，tip 越高"。**
- **倍率**：`TotalMultiplier` 上限 **4**（`:159`）；`:152` 用 `Mathf.Max(multiplier, 1)` 保证**即使 multiplier 为 0，tip 也不会被清零**（0 倍率至少按 1 倍结算）。**【源码确认】**
- **combo 判定**：`ServerOrderControllerBase.IsComboOrder`（`:173-191`）—— `_restart` 为真时把当前单记为 `m_comboIndex = m_activeOrders.IndexOf(order)` 并返回 `m_comboIndex == 0`（**必须是队首**）；否则检查队首那张单是否仍是本单。**所以"连击"= 连续完成"当时排队的第一张订单"。**【源码确认】**

**失败**（`:173-180`）：**【源码确认】**

```csharp
protected virtual void OnFailedDelivery(ServerPlateStation _plateStation) {
    ServerTeamMonitor monitorForTeam = GetMonitorForTeam(_plateStation.GetTeamID());
    monitorForTeam.Score.TotalMultiplier = 0;    // :176 清倍率
    monitorForTeam.Score.TotalCombo = 0;         // :177 清连击
    monitorForTeam.Score.ComboMaintained = false; // :178
    SendDeliveryFailed(_plateStation.GetTeamID(), _plateStation);  // :179
}
```

**只清连击/倍率，不扣分、不删订单。** 那张订单继续挂着直到自然超时。**【源码确认】**

**超时**（`:116-126`）：**【源码确认】**

```csharp
protected virtual void OnOrderExpired(TeamID _teamID, OrderID _orderID) {
    int recipeTimeOutPointLoss = GameUtils.GetGameConfig().RecipeTimeOutPointLoss;
    monitorForTeam.Score.TotalTimeExpireDeductions += recipeTimeOutPointLoss;  // :120 ★ 唯一扣分点
    monitorForTeam.Score.TotalMultiplier = 0;    // :121
    monitorForTeam.Score.TotalCombo = 0;         // :122
    monitorForTeam.Score.ComboMaintained = false; // :123
    monitorForTeam.OrdersController.ResetOrderLifetime(_orderID);  // :124 ★ 重置生命周期（不删除）
    SendOrderExpired(_teamID, _orderID);          // :125
}
```

⚠️ **`:124` 只 `ResetOrderLifetime`（`ServerOrderControllerBase.cs:135-139` 把 `Remaining` 重置为 `Lifetime`），并不调 `RemoveOrder`。** 意味着超时的订单会**留在活跃列表里再次倒计时**——【推断】这是无限模式/持久关卡的"同一张单不断复现"机制，而非 bug；但在限时关卡里会导致订单列表被超时单长期占用。**【源码确认（代码行为）+ 推断（设计意图）】**

**超时的触发源**：`ServerOrderControllerBase.Update`（`:64-85`）每帧 `serverOrderData.Remaining -= num`，`<= 0f` 时 `m_orderTimeoutCallback(serverOrderData.ID)`（`:75`）。**【源码确认】**

#### 3.6.6 `SendDeliverySuccess` / `SendDeliveryFailed` 到客户端的路径

**服务端发包**（`ServerKitchenFlowControllerBase.cs:51-63`）：**【源码确认】**

```csharp
private void SendDeliverySuccess(TeamID _teamID, ServerPlateStation _plateStation, OrderID _orderID,
                                 float _timePropRemainingPercentage, int _tip, bool _wasCombo) {
    m_data.Initialise_DeliverySuccess(_teamID, _plateStation.gameObject, _orderID, _timePropRemainingPercentage, _tip, _wasCombo);  // :53
    m_data.SetScoreData(GetMonitorForTeam(_teamID).Score);   // :54 附带完整比分快照
    SendServerEvent(m_data);                                 // :55
}
private void SendDeliveryFailed(TeamID _teamID, ServerPlateStation _plateStation) {
    m_data.Initialise_DeliveryFailed(_teamID, _plateStation.gameObject);   // :60
    m_data.SetScoreData(GetMonitorForTeam(_teamID).Score);   // :61
    SendServerEvent(m_data);                                 // :62
}
```

用的是同一个 `KitchenFlowMessage m_data` 实例（`:9`）复用发包——**`SetScoreData` 保证每个包里都带最新比分，客户端无需额外请求。** 两个方法都携带 `_plateStation.gameObject` 引用，让客户端知道是**哪个**送餐口出了状况。

**另一条独立通道**：`ServerPlateStation.SendDeliverEvent`（`ServerPlateStation.cs:39-44`）发的是 **`PlateStationMessage`**（`EntityType.PlateStation`），用于驱动**纯视觉效果**：**【源码确认】**

```csharp
m_data.m_delivered = _object;   // :41
m_data.m_success = _success;    // :42
SendServerEvent(m_data);        // :43
```

**所以一次成功送餐服务端会发两个包**：`KitchenFlowMessage`（Delivery，含订单/分数/tip）和 `PlateStationMessage`（含盘子对象与成败）。**前者管逻辑与 UI 计分，后者管飞盘消失动画与"需要盘子"提示。** 这是双通道设计，容易在阅读时误以为只有一个。**【源码确认】**

**客户端接收（逻辑通道）**：`ClientKitchenFlowControllerBase.ApplyServerEvent`（`:46-83`）：**【源码确认】**

```csharp
case KitchenFlowMessage.MsgType.Delivery:
    ClientTeamMonitor monitor = GetMonitorForTeam(kitchenFlowMessage.m_teamID);
    monitor.Score.Copy(kitchenFlowMessage.m_teamScore);                       // :54 覆盖本地比分
    if (kitchenFlowMessage.m_success) {
        ClientPlateStation station = kitchenFlowMessage.m_plateStation.RequireComponent<ClientPlateStation>();
        OnSuccessfulDelivery(kitchenFlowMessage.m_teamID, kitchenFlowMessage.m_orderID,
            kitchenFlowMessage.m_timePropRemainingPercentage, kitchenFlowMessage.m_tip,
            kitchenFlowMessage.m_wasCombo, station);                          // :58
    } else {
        OnFailedDelivery(kitchenFlowMessage.m_teamID, kitchenFlowMessage.m_orderID);  // :62
    }
```

`OnSuccessfulDelivery`（`:135-155`）：播 `SuccessfulDelivery` 音效 → 从订单控制器取 `m_order.m_uID` → `OrdersController.OnFoodDelivered(success:true, orderID)` 移除 UI 卡片（`ClientOrderControllerBase.cs:75-85`）→ `UpdateScoreUI` 写 `DataStore` → 触发 `m_onMealDelivered(uID, _wasCombo)` 事件（`:142-145`）→ 打成就统计（`:146-154`）。**【源码确认】**

`OnFailedDelivery`（`:157-166`）：`OnFoodDelivered(success:false, orderID)` → `ClientOrderControllerBase.cs:86-92` 会给**所有**订单卡片播放 `RecipeFailureAnimation` → 触发 `m_onFailedDelivery()`。**【源码确认】** **注意失败时是"全部订单卡片抖一遍"，不是只抖一张**——因为服务端根本没说清是哪张单没配上。

**客户端接收（视觉通道）**：`ClientPlateStation.ApplyServerEvent`（`ClientPlateStation.cs:48-60`）：**【源码确认】**

```csharp
if (plateStationMessage.m_success) {
    ClientPlate plate = plateStationMessage.m_delivered.RequireComponent<ClientPlate>();
    DeliverPlate(plate);          // :54
} else {
    FailedToDeliver(plateStationMessage.m_delivered);   // :58
}
```

`DeliverPlate`（`:70-78`）：播 `ServiceBell` 音效 → 启动 `DeliverySequence` 协程 → 若有 `m_onFoodDeliveredTrigger` 则 `SendTrigger`。

`DeliverySequence`（`:80-139`）：关掉盘子所有 Collider（`:83-87`）→ 置 `isKinematic`（`:88-92`）→ 生成 `m_deliverPFXPrefab` 特效并等 `m_pfxToFadeDelayTime`（`:93-98`）→ 把材质换成 `m_fadeOutShader`（或切 `_Mode = 2` + `StandardShaderHelper.SetupMaterialWithBlendMode(..., Fade)`，`:99-115`）→ 逐帧把 `_Alpha` 从 1 降到 0，历时 `m_fadeTime`（`:116-137`）→ **`Object.Destroy(plate.gameObject)`（`:138`）**。

**关键结论**：**盘子 GameObject 的销毁发生在客户端**（`:138`），服务端只做"摘下来 + 关碰撞 + 运动学化"（`ServerPlate.cs:195-202`）。这与 3.7 的"重生新盘子"配合，构成完整的视觉循环。**【源码确认】**（此处不排除 `ClientSynchroniserBase` 基类有额外的销毁上报，但**在本文件内未见**。）

`FailedToDeliver`（`:141-149`）：播 `UIBack` 音效；若 `m_needsPlateCooldownTimer <= 0` 且被放的**不是盘子**（`_object.GetComponent<Plate>() == null`），就在该物体位置弹出 `m_needsPlateFloatingUI`（"需要盘子"图标），并设 1 秒冷却（`NEEDS_PLATE_COOLDOWN_TIME = 1f`，`:7`）。**【源码确认】** 这就是玩家把道具放上送餐口时看到的提示。

---

### 3.7 盘子的循环

#### 3.7.1 送餐成功后盘子怎样：`Reserve` + `StartDeliverySequence`

`ServerPlate` 的**预约锁**（`ServerPlate.cs:205-219`）：**【源码确认】**

```csharp
public void Reserve(UnityEngine.Object reserveOwner) { m_reservation = reserveOwner; }   // :205-208
public void ReleaseReservation(UnityEngine.Object reserveOwner) { m_reservation = null; } // :210-213
public bool IsReserved() => m_reservation != null;                                        // :215-218
```

`Reserve` 的**唯一调用点**是 `ServerPlateStation.DeliverCurrentPlate:69`。`IsReserved()` 的**读取点**有三处：`ServerPlate.cs:108`（不许整体倒出）、`ServerPlate.cs:189`（不许再往上放）、`ServerTray.cs:163`（托盘同上）。**【源码确认，全库 grep】**

⚠️ **`ReleaseReservation` 全库零调用点**——**这是一个死方法**。盘子在 `Reserve` 之后**永久保持 reserved 状态**（直到对象被销毁）。**【源码确认】** 效果上没问题（反正盘子马上就飞走了），但如果将来要复用同一个 `ServerPlate` 实例（例如"送餐失败后盘子退回原地"），**这里会挡住一切后续交互**。

#### 3.7.2 `PlateReturnController` 与归还站的选取

`ServerKitchenFlowControllerBase.StartSynchronising:32-35` 构造并 `Init()` 归还控制器：**【源码确认】**

```csharp
PlateReturnController.PlateReturnControllerConfig cfg = default;
cfg.m_plateReturnTime = (base.LevelConfig as KitchenLevelConfigBase).m_plateReturnTime;   // :33
m_plateReturnController = new PlateReturnController(ref cfg);                            // :34
m_plateReturnController.Init();                                                          // :35
```

`PlateReturnController.Init`（`PlateReturnController.cs:41-59`）：扫描场景所有根对象，收集 `ServerPlateReturnStation`，**并只保留 `CompareTag("PlateReturn")` 的那些**（`:53-56`）。**【源码确认】**

`FoodDelivered`（`:61-73`）—— 送餐时排定归还：**【源码确认】**

```csharp
public void FoodDelivered(AssembledDefinitionNode _definition, PlatingStepData _plateType, ServerPlateStation _station) {
    ServerPlateReturnStation returnStation = _station.GetReturnStation(_plateType);   // :63 ★ 优先用送餐口自己配的
    if (returnStation != null) {
        m_platesToReturn.Add(new PlatesPendingReturn(returnStation, cfg.m_plateReturnTime, _plateType));  // :66
    } else if (m_plateReturnStations.Count > 0) {
        returnStation = FindBestReturnStation(_plateType);                            // :70 ★ 全局兜底
        m_platesToReturn.Add(new PlatesPendingReturn(returnStation, cfg.m_plateReturnTime, _plateType));  // :71
    }
}
```

`Update`（`:75-94`）每帧倒计时，到点后：**【源码确认】**

```csharp
if (station == null || gameObject == null || !activeInHierarchy || !IsInLevelBounds(station)) {
    platesPendingReturn.m_station = FindBestReturnStation(m_platingStepData);   // :85 失效则换一个
} else if (platesPendingReturn.m_station.CanReturnPlate()) {
    platesPendingReturn.m_station.ReturnPlate();                               // :89 ★ 真正还盘
    m_platesToReturn.RemoveAt(num);                                            // :90
}
```

**注意 `:87` 的 `else if`**：如果目标站台此刻不能还盘（被占），**这一项不会被移除**，会一直重试到能还为止。**【源码确认】**

`IsInLevelBounds`（`:96-99`）：`m_levelConfig.m_enableRespawnBounds` 关闭时恒真，否则检查 `LevelBounds.ActiveBoundsContain`。

#### 3.7.3 ⭐ `m_returnStations` / `GetReturnStation(PlatingStepData)` 的作用

`ServerPlateStation.GetReturnStation`（`ServerPlateStation.cs:51-59`）：**【源码确认】**

```csharp
public ServerPlateReturnStation GetReturnStation(PlatingStepData _plateType) {
    int num = m_serverReturnStations.FindIndex_Predicate(x => x.GetPlatingStep() == _plateType);  // :53
    if (num >= 0) return m_serverReturnStations[num];   // :55-57
    return null;                                        // :58
}
```

而 `m_serverReturnStations` 来自 `PlateStation.m_returnStations` 的转换（`:33-36`）。`ServerPlateReturnStation.GetPlatingStep`（`ServerPlateReturnStation.cs:108-119`）**不是读自己的字段**，而是**去读它要生成的栈预制体**：**【源码确认】**

```csharp
public PlatingStepData GetPlatingStep() {
    if (m_returnStation.m_stackPrefab != null) {
        PlateStackBase s = m_returnStation.m_stackPrefab.RequestComponent<PlateStackBase>();
        if (s != null) return s.GetPlatingStep();
    }
    return null;
}
```

而 `CleanPlateStack.GetPlatingStep`（`CleanPlateStack.cs:8-12`）再去问盘子预制体：**【源码确认】**

```csharp
public override PlatingStepData GetPlatingStep() {
    Plate plate = m_platePrefab.RequestComponent<Plate>();
    return plate.m_platingStep;
}
```

### ✅ 作用的完整回答

**`m_returnStations` 是"送餐口 → 归还站"的显式映射表，用 `PlatingStepData` 作为键做类型路由。** 链路是：

```
PlateStation.m_returnStations[]              （场景里手工配置）
  → ServerPlateStation.m_serverReturnStations （RequireComponent 转换，ServerPlateStation.cs:33-36）
    → GetReturnStation(plateType) 按 m_uID 精确匹配    （:51-59）
      → PlateReturnController.FoodDelivered 优先采用它 （PlateReturnController.cs:63-67）
```

**为什么需要它**：`PlatingStepData` 用 `m_uID` 做相等（3.2.2），而归还站的身份又**间接**来自"它能生成的干净盘子预制体上的 `Plate.m_platingStep`"（三层间接：`ServerPlateReturnStation` → `PlateStackBase` → `Plate`）。**所以圆盘和方盘可以各有自己的归还堆栈，`m_returnStations` 保证"送出去的什么盘子，就从对应的那摞补回来"。** 如果 `m_returnStations` 为空，`GetReturnStation` 返回 `null`（`:58`），`PlateReturnController` 退化到全局兜底 `FindBestReturnStation`（`PlateReturnController.cs:101-108`）：**在带 `"PlateReturn"` 标签的所有站里找第一个 `GetPlatingStep() == _plateType` 且激活且在图内的**。

⚠️ **`FindBestReturnStation` 用的是 `FastList.Find`（`:105`）——返回第一个匹配项，且找不到时返回 `null`。** 若 `:71` 拿到的是 `null`，`PlatesPendingReturn.m_station` 会是 `null`，靠 `Update:83` 的判空在下一帧再找一次——**不会崩，但会静默不还盘**。**【源码确认】**

#### 3.7.4 ⭐ `m_createPlateTime` 是否真的没被用？→ **确认：是死字段**

**验证方法**：全库 grep `m_createPlateTime`（`.cs` 全量，2365 个文件）。**【源码确认】**

```
overcooked_decomp\PlateStation.cs
Line 36: 	public float m_createPlateTime;
```

**结论：全库仅 1 处命中，就是它的声明本身（`PlateStation.cs:36`）。零读取、零写入。这是一个纯粹的死序列化字段**——很可能是早期"送餐口按固定间隔自动补盘子"设计（名字暗示"创建盘子的时间"）的遗留物，后被 `PlateReturnStation` + `PlateReturnController` 的堆栈模型取代。

**顺带发现两个同类死字段/死方法（同样用全库 grep 验证）**：**【源码确认】**

| 符号 | 声明位置 | 命中数 | 结论 |
|---|---|---|---|
| `PlateStation.m_createPlateTime` | `PlateStation.cs:36` | **1（仅声明）** | 死字段 |
| `PlateStation.m_platePrefab` | `PlateStation.cs:33` | **1（仅声明）** | 死字段 |
| `ServerPlate.ReleaseReservation` | `ServerPlate.cs:210-213` | **1（仅声明）** | 死方法 |
| `CompositeAssembledNode.GetEnumeratorExhaustive` | `:41-51` | **1（仅声明）** | 死方法 |

⚠️ **`PlateStation.m_platePrefab` 的陷阱**：grep `m_platePrefab` 会命中 7 处，但其中 6 处属于**另外两个同名不同类的字段**：

- `PlateStackBase.m_platePrefab`（`PlateStackBase.cs:7`）—— **真正在用的那个**，被 `ServerPlateStackBase.AddToStack:34`（`NetworkUtils.ServerSpawnPrefab(base.gameObject, m_plateStack.m_platePrefab)`）和 `ClientPlateStackBase:49` 使用；
- `CleanPlateStack.GetPlatingStep:10`、`ServerPlateReturnStation.GetPlatingStep:112` 用的都是 `PlateStackBase` 的那个。

**所以盘子的实际生成完全不经过 `PlateStation`，而是经过 `PlateStackBase.m_platePrefab`。** 对比 `PlateStation.m_teamId`（`ServerPlateStation.cs:63` 在用）、`m_returnStations`（`:33-36` 在用）、`m_onFoodDeliveredTrigger`（`ClientPlateStation.cs:74` 在用）、`m_needsPlateFloatingUI`/`m_tipsFloatingNumberUI`/`m_deliveryEffects`（均在用）——**`PlateStation` 上只有 `m_platePrefab` 和 `m_createPlateTime` 这两个字段是死代码。**【源码确认】**

#### 3.7.5 归还站如何"变出"盘子

`ServerPlateReturnStation.ReturnPlate`（`ServerPlateReturnStation.cs:92-99`）：**【源码确认】**

```csharp
public void ReturnPlate() {
    if (m_stack == null) CreateStack();   // :94-96
    m_stack.AddToStack();                 // :98
}
private void CreateStack() {              // :101-106
    GameObject go = NetworkUtils.ServerSpawnPrefab(base.gameObject, m_returnStation.m_stackPrefab);
    m_attachStation.AddItem(go, base.transform.rotation * Vector2.up);   // :104 栈本体吸附到站台
    m_stack = go.RequireInterface<ServerPlateStackBase>();
}
public bool CanReturnPlate() {            // :86-90
    GameObject go = m_attachStation.InspectItem();
    return go == null || (m_stack != null && go == m_stack.gameObject);
}
```

`ServerPlateStackBase.AddToStack`（`ServerPlateStackBase.cs:32-36`）：**【源码确认】**

```csharp
public virtual void AddToStack() {
    GameObject item = NetworkUtils.ServerSpawnPrefab(base.gameObject, m_plateStack.m_platePrefab);  // :34 ★ 生成全新盘子
    m_stack.AddToStack(item);                                                                        // :35
}
```

### ✅ 盘子循环的本质：**销毁 + 重生，而不是搬运**

```
送餐成功
  → ServerPlate.StartDeliverySequence   盘子摘下、关碰撞、运动学化（服务端）
  → ClientPlateStation.DeliverySequence Object.Destroy(盘子)（客户端淡出后销毁）
  → PlateReturnController 倒计时 m_plateReturnTime
  → ServerPlateReturnStation.ReturnPlate
  → ServerPlateStackBase.AddToStack → ServerSpawnPrefab(PlateStackBase.m_platePrefab)  ← 全新的盘子对象
  → 出现在 CleanPlateStack（干净盘子堆）顶部
```

**玩家拿到的是新对象，不是原来那个。** 这是理解"盘子为什么会瞬间回到架子上"的关键。**【源码确认】**

`UpdateSynchronising`（`ServerPlateReturnStation.cs:39-54`）在第一次同步时根据 `m_startingPlateNumber` 预填充：**【源码确认】**

```csharp
if (m_startingPlateNumber > 0) {
    CreateStack();
    for (int i = 0; i < m_startingPlateNumber; i++) m_stack.AddToStack();   // :49-52
}
```

#### 3.7.6 脏盘子 → 清洗 → 干净盘子 的完整闭环

这条闭环**不经过`送餐口`**，而是走"盘子变脏堆在归还站"的路线。

**第一段：干净盘 → 脏盘（在归还站换预制体）**

`ServerCleanPlateUtensilRespawnBehaviour.CanRespawnOnStation`（`ServerCleanPlateUtensilRespawnBehaviour.cs:17-33`）：**【源码确认】**

```csharp
if (_attachStation.CompareTag("CookingStation") || _attachStation.CompareTag("PlateStation")
    || _attachStation.gameObject.RequestComponent<RubbishBin>() != null
    || _attachStation.gameObject.RequestComponent<ConveyorStation>() != null
    || _attachStation.gameObject.RequestComponent<WashingStation>() != null)
    return false;                                                              // :19-22 这些地方不许"重生"盘子
ServerPlateReturnStation r = _attachStation.gameObject.RequestComponent<ServerPlateReturnStation>();
if (r != null) {
    PlateReturnStation prs = _attachStation.gameObject.RequireComponent<PlateReturnStation>();
    if (prs.m_stackPrefab != null && prs.m_stackPrefab.RequestComponent<CleanPlateStack>() != null)
        return (m_platingStepData == null || r.GetPlatingStep() == m_platingStepData) && r.CanReturnPlate();  // :29
}
return _attachStation.CanAttachToSelf(base.gameObject);                        // :32
```

`AddItemToStation`（`:35-49`）—— **如果一个`ServerPlate`落到了归还站，直接销毁自己并让站台还一个盘**：**【源码确认】**

```csharp
ServerPlateReturnStation r = _station.gameObject.RequestComponent<ServerPlateReturnStation>();
if (r == null) { base.AddItemToStation(_station); return; }
ServerPlate sp = base.gameObject.RequestComponent<ServerPlate>();
if (sp != null) r.ReturnPlate();              // :46 ★ 还一个干净盘
NetworkUtils.DestroyObjectsRecursive(base.gameObject);   // :48 ★ 销毁这个盘子对象
```

**第二段：脏盘堆 → 清洗**

`DirtyPlateStack.cs` 只有 20 行，三个字段定义了它的全部行为：**【源码确认】**

```csharp
[SerializeField] public PlatingStepData m_plateType;      // :8  自己是哪种盘子
[SerializeField] public GameObject m_washedPrefab;        // :11 ★ 洗完后生成的"干净盘堆"预制体
[SerializeField] public GameObject m_cleanPlatePrefab;    // :14 ★ 玩家手持时洗完后直接给的干净盘
public override PlatingStepData GetPlatingStep() => m_plateType;   // :16-19
```

`ServerDirtyPlateStack.StartSynchronising`（`ServerDirtyPlateStack.cs:11-26`）接上 `ServerWashable`：**【源码确认】**

```csharp
if (dirtyPlateStack.m_washedPrefab != null) {
    NetworkUtils.RegisterSpawnablePrefab(base.gameObject, dirtyPlateStack.m_washedPrefab);     // :17
    m_washable = base.gameObject.RequireComponent<ServerWashable>();                           // :18
    m_washable.RegisterAllowWashCallback(AllowWash);                                            // :19
    m_washable.RegisterFinishedCallback(OnWashingFinished);                                     // :20
}
if (dirtyPlateStack.m_cleanPlatePrefab != null)
    NetworkUtils.RegisterSpawnablePrefab(base.gameObject, dirtyPlateStack.m_cleanPlatePrefab);  // :24
```

`AllowWash`（`:92-95`）→ `m_stack.GetSize() > 0`（**空堆不许洗**）。**【源码确认】**

`AddToStack` 有一个**很聪明的自适应**（`:82-90`）：**【源码确认】**

```csharp
public override void AddToStack() {
    base.AddToStack();
    if (m_washable != null) {
        Washable washable = base.gameObject.RequireComponent<Washable>();
        m_washable.SetDuration(washable.m_duration * m_stack.GetSize());   // :88 ★ 时长 = 基础时长 × 盘子数量
    }
}
```

**盘子越多洗得越久**（线性）。**【源码确认】**

`ServerWashable.UpdateSynchronising`（`ServerWashable.cs:108-133`）推进进度：**【源码确认】**

```csharp
if (!HasFinished()) {
    float num = m_washDuration * (float)m_durationMultiplier;    // :113
    float num2 = CalculateWashRate();                            // :114 所有水源的速率之和
    if (num2 > 0f || num2 < 0f) { m_progress += num2 * TimeManager.GetDeltaTime(...); m_progress = Mathf.Clamp(m_progress, 0f, num); }  // :115-119
    if (m_progress >= num) { m_finishedCallback(); m_hasFinished = true; ... WashingFinishedAchievement(); }  // :120-126
}
```

**注意 `CalculateWashRate`（`:135-147`）的语义是反的**：`if (!m_allowWashCallbacks.CallForResult(_result: false))` → **只有在没有任何回调返回 false（即全部允许）时才累加速率**；否则返回 0。配合 `ServerWashable.cs:137` 与 `ServerDirtyPlateStack.AllowWash:92-95`，**空堆的速率为 0，进度冻结**。**【源码确认】**

`OnWashingFinished`（`ServerDirtyPlateStack.cs:97-151`）—— **洗涤完成的分支逻辑**：**【源码确认】**

```csharp
if (m_carrier != null && m_stack.GetSize() == 1) {                                  // :102 玩家手上、且只有 1 个盘子
    GameObject obj = NetworkUtils.ServerSpawnPrefab(base.gameObject, dirtyPlateStack.m_cleanPlatePrefab);  // :104 直接生成干净盘
    ...
    attachment = obj.RequireInterface<IAttachment>();
} else {                                                                            // :113 否则
    GameObject obj2 = NetworkUtils.ServerSpawnPrefab(base.gameObject, dirtyPlateStack.m_washedPrefab);     // :114 生成"干净盘堆"
    ...
    ServerPlateStackBase s = attachment.AccessGameObject().RequireComponent<ServerPlateStackBase>();
    for (int i = 0; i < m_stack.GetSize(); i++) s.AddToStack();                      // :122-125 按原有数量重新填满
}
if (m_carrier != null || m_attachStation != null) {                                  // :127 原地替换
    Vector3 localPosition = base.transform.localPosition;
    Quaternion localRotation  = base.transform.localRotation;
    if (m_carrier != null) { m_carrier.TakeItem(); m_carrier.CarryItem(attachment.AccessGameObject()); }        // :133-135
    else { m_attachStation.TakeItem(); m_attachStation.AddItem(attachment.AccessGameObject(), Vector2.up); }    // :139-141
    attachment.AccessGameObject().transform.localPosition = localPosition;           // :143 保持位置
    attachment.AccessGameObject().transform.localRotation = localRotation;           // :144 保持朝向
} else {
    attachment.AccessRigidbody().transform.SetPositionAndRotation(base.transform.position, base.transform.rotation);  // :148
}
NetworkUtils.DestroyObjectsRecursive(base.gameObject);                               // :150 ★ 销毁脏盘堆
```

**"原地无感替换"是这个方法的精髓**：先记录 `localPosition`/`localRotation`，替换对象后**把变换写回去**（`:143-144`），最后才销毁旧对象。玩家看到的是"手上的脏盘子堆直接变成干净盘堆"，没有任何位移。`m_carrier` 与 `m_attachStation` 分别由 `ICarryNotified`（`:59-70`）和 `ISurfacePlacementNotified`（`:72-80`）回调维护。**【源码确认】**

**第三段：`WashingStation`（手压式洗碗池）**

`WashingStation.cs`（25 行）字段：**【源码确认】**

```csharp
[SerializeField] public PlateReturnStation m_dryingStation;         // :8  ★ "晾干站"，同时是干净的归还站
[SerializeField] public float m_cleanPlateTime = 2f;                // :11
[SerializeField] public ProgressUIController m_progressUIPrefab;    // :14
[SerializeField] public GameObject[] m_dirtyPlates = new GameObject[0];  // :17
public bool CanHandlePlacement(ICarrier c, Vector2 d, int _plateCount) {
    GameObject go = c.InspectCarriedItem();
    DirtyPlateStack dps = (go != null) ? go.GetComponent<DirtyPlateStack>() : null;
    return dps != null;                                             // :19-24 只接脏盘堆
}
```

`ServerWashingStation.StartSynchronising`（`ServerWashingStation.cs:33-50`）—— **`m_dryingStation` 是关键**：**【源码确认】**

```csharp
m_plateReturnStation = m_washingStation.m_dryingStation.gameObject.RequireComponent<ServerPlateReturnStation>();  // :46
m_dryingAttachStation = m_washingStation.m_dryingStation.gameObject.RequireComponent<ServerAttachStation>();      // :47
m_dryingAttachStation.RegisterOnItemAdded(OnItemAddedDryingStation);      // :48
m_dryingAttachStation.RegisterOnItemRemoved(OnItemRemovedDryingStation);  // :49
```

**即：洗碗池的"晾干区"被直接当成一个 `ServerPlateReturnStation` 用**——洗好的盘子由它 `ReturnPlate()` 生成。这就是 3.7.5 里"干净盘子从哪来"在洗碗池场景下的答案。**【源码确认】**

进度推进（`:85-104`）：**【源码确认】**

```csharp
if (!m_interactable.IsBeingInteractedWith() || m_plateCount <= 0) return;
m_cleaningTimer += TimeManager.GetDeltaTime(base.gameObject)
                 * (m_interactable.AllowMultipleInteractors() ? m_interactable.InteractorCount() : 1f);  // :91 ★ 多人一起按更快
if (m_cleaningTimer > m_washingStation.m_cleanPlateTime) {
    m_cleaningTimer = 0f;
    m_plateCount--;                                     // :95
    ServerMessenger.Achievement(m_interactor.gameObject, 2);  // :96
    SendCleanedPlate();                                 // :97
    m_plateReturnStation.ReturnPlate();                 // :98 ★ 生成一个干净盘到晾干站
    if (m_plateCount == 0) m_interactable.enabled = false;   // :99-102
}
```

**是"每 `m_cleanPlateTime` 秒洗好一个"，不是"一次性洗完一堆"。** `m_cleanPlateTime` 默认 2 秒（`WashingStation.cs:11`）。**【源码确认】**

`OnItemAdded`（`:106-120`）—— **脏盘堆放到洗碗池上就直接被吸收**：**【源码确认】**

```csharp
ServerStack component = gameObject.GetComponent<ServerStack>();
if (component != null) {
    m_serverAttachStation.TakeItem();                  // :116
    NetworkUtils.DestroyObjectsRecursive(gameObject);   // :117 ★ 堆对象销毁
    AddPlates(component.GetSize());                     // :118 转成计数
}
```

**盘子堆变成了一个整数 `m_plateCount`。** 之后每个计数在 `:98` 靠 `ReturnPlate()` 变成一个真实盘子。**【源码确认】**

`OnItemAddedDryingStation`（`:130-136`）—— **晾干区只允许放干净盘堆**：**【源码确认】**

```csharp
m_interactable.SetInteractionSuppressed(_attachment.AccessGameObject() != null
    && _attachment.AccessGameObject().RequestComponent<ServerCleanPlateStack>() == null);
```

`CanHandlePlacement`（`:171-175`）/ `HandlePlacement`（`:177-194`）/ `GetPlacementPriority = int.MaxValue`（`:200-203`）：**洗碗池在所有站台里优先级最高**，抢在别的交互之前处理脏盘堆（`:180-185`）。**【源码确认】**

`WashAllPlates`（`:156-169`）是**一次性洗完全部的入口**（循环 `ReturnPlate()` 并清零），本库内未被调用——**【推断】**留给作弊/调试或特定关卡管理器。

**第四段：`DryingPart` 的寻址结论**

⚠️ **全库 grep `DryingPart` 零命中。** **不存在名为 `DryingPart` 的类。** 所谓的"晾干部件"在代码里就是 `WashingStation.m_dryingStation`（一个 `PlateReturnStation` 引用，`WashingStation.cs:8`），运行时被 `ServerWashingStation` 提升为 `ServerPlateReturnStation` + `ServerAttachStation`（`:46-47`）。**【源码确认，全库 grep 验证】** 若任务书中的 `DryingPart` 来自资源名/预制体名而非脚本名，则它在 C# 侧没有对应类型。

**闭环总结（源码确认）**：

```
[干净盘堆 CleanPlateStack]  ← 玩家取盘
        ↓ 用完/送餐后
[服务端摘盘 + 客户端淡出销毁]  （占位：3.7.5）
        ↓
[归还站重新生成干净盘]  ← ServerCleanPlateUtensilRespawnBehaviour.AddItemToStation:46
        ↓ （另一条路线）玩家把盘子扔进垃圾桶/送到脏盘归还站
[DirtyPlateStack 脏盘堆]  → 玩家拾起（m_carrier != null）
        ↓ 放到 WashingStation
[计数 m_plateCount]  → 长按交互，每 2 秒 -1
        ↓ 每次 returnPlate()
[m_dryingStation（= PlateReturnStation 型晾干站）生成 CleanPlateStack]
        ↓
[回到干净盘堆]  ← 闭环
```

`ServerDirtyPlateStackUtensilRespawnBehaviour`（`:31-43`）负责"脏盘堆找不到家时"的兜底重生：**【源码确认】**

```csharp
protected override ServerAttachStation[] GetRespawnStations() {
    return Object.FindObjectsOfType<ServerPlateReturnStation>()
        .ConvertAll(x => x.gameObject.RequireComponent<ServerAttachStation>());   // :42 所有归还站的站台
}
protected override bool CanRespawnOnStation(ServerAttachStation _attachStation) {
    ServerPlateReturnStation r = _attachStation.gameObject.RequestComponent<ServerPlateReturnStation>();
    if (r != null) {
        PlateReturnStation prs = _attachStation.gameObject.RequireComponent<PlateReturnStation>();
        if (prs.m_stackPrefab != null && prs.m_stackPrefab.RequestComponent<DirtyPlateStack>() != null)   // :23 ★ 只认生成脏盘堆的站
            return (m_platingStepData == null || r.GetPlatingStep() == m_platingStepData) && r.CanReturnPlate();
    }
    return false;
}
protected override void AddItemToStation(ServerAttachStation _station) {
    ServerPlateReturnStation r = _station.gameObject.RequestComponent<ServerPlateReturnStation>();
    if (r == null) { base.AddItemToStation(_station); return; }
    ServerDirtyPlateStack s = base.gameObject.RequireComponent<ServerDirtyPlateStack>();
    for (int i = 0; i < s.GetSize(); i++) r.ReturnPlate();   // :54-57 按原数量补回脏盘
    NetworkUtils.DestroyObjectsRecursive(base.gameObject);   // :58
}
```

**干净盘与脏盘各有一套 `CanRespawnOnStation`，靠 `m_stackPrefab` 上挂的是 `CleanPlateStack` 还是 `DirtyPlateStack` 来区分**（`ServerCleanPlateUtensilRespawnBehaviour.cs:27` vs `ServerDirtyPlateStackUtensilRespawnBehaviour.cs:23`）。**这是同一套归还站既能收干净盘又能收脏盘的分流机制。**【源码确认】**

---

### 3.8 托盘与汤勺

#### 3.8.1 `Tray` / `ServerTray` / `ClientTray` —— 多槽位盘子

`Tray` **继承 `Plate`**（`Tray.cs:4`），所以它自动拥有 `m_platingStep`、`IngredientContainer`、`PlacementContainer`、`ServerPlate` 全套能力。新增的是**槽位**概念：**【源码确认】**

```csharp
[SerializeField] public OrderToPrefabLookup[] m_slots;   // Tray.cs:6-7

public void GetIngredientContents(AssembledDefinitionNode _node, PlacementContext _context, List<int> _availableSlots) {  // :9-23
    _availableSlots.Clear();
    for (int i = 0; i < m_slots.Length; i++)
        foreach (OrderContentRestriction r in m_slots[i].GetContentRestrictions())
            if (AssembledDefinitionNode.MatchingAlreadySimple(r.m_content.Simpilfy(), _node.Simpilfy()))
                _availableSlots.Add(i);     // :20 记下"这个食材能放进哪些槽"
}

public bool AreContentsDifferent(AssembledDefinitionNode[] a, AssembledDefinitionNode[] b) {  // :25-43
    if (a == null || b == null) return true;
    if (a.Length == b.Length) {
        for (int i = 0; i < a.Length; i++)
            if (!AssembledDefinitionNode.Matching(a[i], b[i])) return true;   // :35 ★ 逐位比较，顺序敏感！
        return false;
    }
    return true;
}
```

⚠️ **`AreContentsDifferent` 是顺序敏感的（`:35` 按下标逐位比）**，与 3.5.2 的 `Contains` 顺序无关形成对比。它只用于**缓存失效判断**（`ServerTray.CalculateSlots:200`），不影响配方匹配。**【源码确认】**

`ServerTray` **继承 `ServerPlate`**（`ServerTray.cs:4`），核心是 `TrayIngredientContainerAdapter`（`:6-136`）—— **一个"要不要写回真实容器"的代理**：**【源码确认】**

```csharp
public TrayIngredientContainerAdapter(ServerIngredientContainer _container, IIngredientContents _containerToFilter,
                                      OrderToPrefabLookup _lookup, bool _addToReal, Tray _tray) {
    ...
    foreach (OrderContentRestriction r in _lookup.GetContentRestrictions())
        for (int num = _containerToFilter.GetContentsCount() - 1; num >= 0; num--) {      // :37 倒序遍历
            AssembledDefinitionNode n = _containerToFilter.GetContentsElement(num).Simpilfy();
            if (AssembledDefinitionNode.MatchingAlreadySimple(n, r.m_content.Simpilfy())) {
                m_contents.Add(n);
                if (GameUtils.GetOrderPlatingPrefab(m_tray.GetOrderComposition(GetContents()), m_tray.m_platingStep) == null)
                    m_contents.Remove(n);                    // :46 ★ 不合法就撤回
                else
                    _containerToFilter.RemoveIngredient(num); // :50 ★ 合法才真正从源容器取走
                break;
            }
        }
}
```

**这是"逐槽试填、不成则撤"的算法**：每加一个食材就立刻用 `GetOrderPlatingPrefab` 验一次合法性，不合法就回滚。`_addToReal` 决定 `AddIngredient`（`:68-89`）和 `Empty`（`:113-130`）是否同步写到真实 `ServerIngredientContainer`——**干跑用 `false`，真操作用 `true`**（对比 `ServerTrayPlacementContainer.cs:22` 的 `modifyReal: true` 与 `:158` 的 `modifyReal: false`）。**【源码确认】**

`ServerTray.GetIngredientContents`（`:166-191`）：遍历所有可用槽，构造 `槽内容 + 新食材` 的候选数组，用 `GetOrderPlatingPrefab` 验一次，**第一个合法的槽就返回**（`:185-188`）→ 返回对应的适配器。**【源码确认】**

`ServerTrayPlacementContainer`（`ServerTrayPlacementContainer.cs`）覆盖了 `CanCombine`（`:16-28`）/`HandlePlacement`（`:35-65`）/`GetPlacementPriority = 1`（`:30-33`）：**【源码确认】**

```csharp
// :18-20 空托盘 + 手持另一个托盘 → 直接允许（托盘叠放转移）
if (!m_ingredientContainer.HasContents() && _placingObject.RequestComponent<ServerTray>() != null) return true;
IIngredientContents c = m_tray.GetIngredientContents(_placingObject, _context, modifyReal: true);
if (c == null) return false;
return m_trayPlacementContainer.CanCombine(_placingObject, m_allowPlacementCallbacks, c, _context);
// :38-47 空托盘接收另一个托盘的全部内容（逐元素 AddIngredient，然后 Empty 源）
```

**客户端**：`ClientTray.cs:44` 与 `:157`、`ClientTrayCosmeticDecisions.cs:48` 都靠 `GameUtils.GetOrderPlatingPrefab(...) != null` 来决定显示哪个模型——**完全复用服务端的合法性判定函数做纯视觉预测**。**【源码确认】**

`TrayPlacementContainer.cs` / `TrayIngredientContainer.cs` 都是空派生类（各 3 行），纯粹为类型标记。**【源码确认】**

#### 3.8.2 `Ladle`（汤勺）—— 用 `TransferFromContainer` 的唯一样本

`LadleContainer.cs`（44 行）：**【源码确认】**

```csharp
[RequireComponent(typeof(IngredientContainer))]
[RequireComponent(typeof(PlacementContainer))]
[RequireComponent(typeof(PhysicalAttachment))]
public class LadleContainer : MonoBehaviour {
    [SerializeField] private CookingStepData m_cookingStep;    // :8-9 ★ 只能舀"这种烹饪方式"的东西
    public AssembledDefinitionNode GetOrderComposition(AssembledDefinitionNode[] _contents) { ... }  // :11-20 与 Plate 同构
    public bool AllowItemPlacement(GameObject _object, PlacementContext _context, IIngredientContents _contents) {  // :22-43
        IOrderDefinition od = _object.RequestInterface<IOrderDefinition>();
        if (od == null || _contents.HasContents()) return false;   // :25 ★ 汤勺一次只装一样
        CookingHandler ch = _object.RequestComponentRecursive<CookingHandler>();
        if (ch != null) {
            if (ch.m_cookingType != m_cookingStep) return false;    // :32 ★ 烹饪方式必须一致
            if (od.GetOrderComposition() is CompositeAssembledNode c && !_contents.CanTakeContents(c.m_composition)) return false;
            return true;
        }
        return false;                                               // :42 没在烹饪的东西不许舀
    }
}
```

`ServerLadleContainer.cs`（69 行）**是唯一使用 `AssembledNodeTransfer.CanTransferFromContainer/TransferFromContainer` 的地方**：**【源码确认】**

```csharp
public void TransferToContainer(ICarrierPlacement _carrier, IIngredientContents _plateContainer, bool _dontRemove) {
    AssembledNodeTransfer.TransferFromContainer(this, _plateContainer, _dontRemove);   // :53
}
public bool CanTransferToContainer(IIngredientContents _container) {
    return AssembledNodeTransfer.CanTransferFromContainer(this, _container);           // :58
}
```

**与 `IngredientToContainerBehaviour` 的本质区别**：汤勺用 `TransferFromContainer`，**它会先取 `_originObject.GetOrderComposition()`（整个容器的复合订单），而不是单个食材的节点**（`AssembledNodeTransfer.cs:223`），并在 `_dontRemove == false` 时 `serverIngredientContainer.Empty()` 清空自己（`:232`）。**这就是"一勺把锅里/碗里的东西整体倒出去"的实现。**【源码确认】

参数名 `_plateContainer`（`ServerLadleContainer.cs:51`）暗示汤勺的目标几乎总是盘子。配合 3.3.1(4) 的 `ServerLadleContainer` 特例（`AssembledNodeTransfer.cs:191-194`：**目标有汤勺容器 + 源头有烹饪处理器时无条件允许**），**汤勺是唯一能绕过"必须全生或全熟"限制的容器**。**【源码确认】**

---

### 3.9 ⭐ 一次完整的摆盘 + 送餐：时序

**场景**：单人关，玩家手上拿着**切好的生菜**；要做一道"面包 + 熟肉 + 生菜"的汉堡，配方要求**方盘**。目标送餐口已配好 `m_returnStations` 指向一个干净方盘堆。

| # | 阶段 | 触发 | 调用链（源码确认） | 结果 |
|---|---|---|---|---|
| 0 | **准备** | 场景加载 | `PlateReturnController.Init`（`PlateReturnController.cs:41-59`）扫全场，只收 `CompareTag("PlateReturn")` 的站；`ServerPlateReturnStation.UpdateSynchronising:39-54` 按 `m_startingPlateNumber` 预填干净盘堆 | 归还站上出现 N 个干净方盘 |
| 1 | **取盘** | 玩家交互 | `ServerCleanPlateStack.CanHandlePickup:71-74`（`GetSize() > 0`）→ `HandlePickup:76-80` → `RemoveFromStack`（`:55-69`，解绑 referral）→ `_carrier.CarryItem(gameObject)` | 玩家手持方盘 |
| 2 | **加面包** | 玩家对空台面按交互 | `AttachStation.CalculatePlacementType`（`AttachStation.cs:40-71`）：`_item == null` → `CanAttachToSelf`（`:44`）→ **`OntoEmpty`**（`:46`）→ `ServerAttachStation.OnItemPlaced(_iCarrier.TakeItem(), ...)`（`:174`）→ `:308-341` 挂载、设旋转、注册三种 referral、广播 `OnSurfacePlacement` | 面包落在台面上（**不在盘子里**） |
| 3 | **把面包放进手上的盘** | 玩家手持盘对面包按交互 | `_item` = 面包 → `placementHandler` = 面包的 `IngredientToContainerBehaviour`？**否**，`IngredientToContainerBehaviour` 不是 `IHandlePlacement` → `placementHandler == null` → 走 `CanPlaceUnder`（`:66-69`）→ **`_item`（面包）实现了 `IPlaceUnder`**（`IngredientToContainerBehaviour:4`）→ `PlacementType.UnderOccupant`（`:68`）→ `PlaceUnderOccupant`（`:226-236`）→ 手上盘子的 `IHandlePlacement.HandlePlacement(holder, ...)` 先把面包并入盘 → `OnItemPlaced(_iCarrier.TakeItem(), ...)` | 面包进盘，台面腾空 → **这就是"拿着盘子去贴食材"的手感来源** |
| 4 | **加熟肉** | 玩家手持盘对灶台上的熟肉按交互 | 同 #3：`UnderOccupant` → `PlaceUnderOccupant` → `ServerPreparationContainer.HandlePlacement`（`ServerPreparationContainer.cs:191-224`）→ `TransferToContainer`/`AddOrderContents`（`:209-217` / `:206-207`） | 熟肉并入盘，`m_contents` 变成 `[面包, 熟肉]` 或合并成单个 `CompositeAssembledNode` |
| 5 | **加生菜（核心场景）** | 玩家手持生菜对**放着盘子的台面**按交互 | ① `ServerAttachStation.CanHandlePlacement:147-156`：`_item` = 盘子，`placementHandler` = 盘子的 `ServerPlacementContainer`<br>② `AttachStation.CalculatePlacementType:54-64`：`placementHandler.CanHandlePlacement` → ③<br>③ `ServerPlacementContainer.CanHandlePlacement:29-54` → `CanCombine:24-27` → `PlacementContainer.CanCombine:7-23`<br>④ **闸门 A**：`CallForResult(true, ...)` **OR 语义** → `ServerPlate.CanPlaceOnPlate:187-190`（`!IsReserved()`）→ `Plate.CanPlaceOnPlate:17-53`<br>⑤ `Plate.CanPlaceOnPlate:46-51`：**干跑** `TransferToContainer(carrier, 影子适配器, dontRemove:true)` → `GetOrderPlatingPrefab(orderComposition, m_platingStep) != null`<br>⑥ **闸门 C/D**：`IngredientToContainerBehaviour.CanTransferToContainer` + `ServerIngredientContainer.CanAddIngredient`<br>⑦ 返回 **`OntoOccupant`**（`AttachStation.cs:62`）→ `ServerAttachStation.PlaceOntoOccupant:220-224` → 盘子的 `HandlePlacement`<br>⑧ `ServerPlacementContainer.HandlePlacement:56-73`：`TransferToContainer(_carrier, m_ingredientContainer, dontRemove:false)`（`:62`）→ `InformOfInternalChange()`（`:63`） | **生菜自动进盘；`_carrier.DestroyCarriedItem()`（`IngredientToContainerBehaviour.cs:25`）销毁手持生菜；玩家手变空；盘子留在台面且内容 +1（或与已有节点合并）** |
| 6 | **服务端广播内容变化** | 步骤 5 的 `InformOfInternalChange` | `ServerIngredientContainer.OnContentsChanged:138-144` → 回调 + `IngredientContainerMessage.Initialise(contents)` + `SendServerEvent` | 所有客户端 `ClientIngredientContainer.ApplyServerEvent:26-40` 重放内容 → `ClientPlate`/`ClientAttachedOrderCosmeticDecisions` 更新模型 |
| 7 | **拿起装好的盘子** | 玩家交互 | `ServerCleanPlateStack` 不涉及；走盘子的 `IHandlePickup` 链 → `_carrier.CarryItem(盘子)` | 玩家手持方盘（内容 = 汉堡） |
| 8 | **送上送餐口** | 玩家手持方盘对送餐口按交互 | `ServerAttachStation.CalculatePlacementType:42-53`：送餐口**空** → `CanAttachToSelf` → `CouldAttachToSelfIfEmpty`（`AttachStation.cs:89-96`）→ **AND 语义** `CallForResult(false, ...)` → `ServerPlateStation.CanAddItem:75-82`：`m_plate == null` → `_object.GetComponent<ServerPlate>() != null` → **true** → **`OntoEmpty`**（`AttachStation.cs:46`）→ `ServerAttachStation.OnItemPlaced(_iCarrier.TakeItem(), ...)`（`:174`）→ `:308-341` → **`m_itemAdded(component)`（`:339`）** | **触发交付** |
| 9 | **交付** | 步骤 8 的 `m_itemAdded` | `ServerPlateStation.OnItemAdded:84-105`：<br>① `m_plate = _iHoldable...GetComponent<ServerPlate>()`（`:86`）<br>② `GetContentsCount() >= 1` → 遍历 `contents[i].m_freeObject` → **`NetworkUtils.DestroyObject(...)`（`:100`）**<br>③ **`DeliverCurrentPlate()`（`:104`）** | 盘上的食材实体被销毁（消物理残留） |
| 10 | **交付执行** | 步骤 9 | `DeliverCurrentPlate:66-73`：<br>① `plate.Reserve(this)`（`:69`）<br>② `m_orderHandler.FoodDelivered(plate.GetOrderComposition(), plate.GetPlatingStep(), this)`（`:70`）<br>③ `plate.StartDeliverySequence(this)`（`:71`）<br>④ `SendDeliverEvent(plate.gameObject, true)`（`:72`） | 盘子被锁 + 拆物理 + 发视觉包 |
| 11 | **排定归还** | 步骤 10 的 ② | `ServerKitchenFlowControllerBase.FoodDelivered:128-131` → `OnFoodDelivered:133-145`：<br>① **`m_plateReturnController.FoodDelivered(_definition, _plateType, _station)`（`:135`）**<br>② `GetMonitorForTeam(_station.GetTeamID())`（`:136`）<br>③ `monitorForTeam.OnFoodDelivered(...)`（`:137`） | **归还先于匹配执行**（成功失败都还盘） |
| 12 | **选归还站** | 步骤 11 的 ① | `PlateReturnController.FoodDelivered:61-73`：`_station.GetReturnStation(_plateType)`（`:63`）→ `ServerPlateStation.GetReturnStation:51-59`：按 `x.GetPlatingStep() == _plateType` 找（`PlatingStepData.m_uID` 精确匹配）→ 找到则 `m_platesToReturn.Add(new PlatesPendingReturn(站, m_plateReturnTime, 类型))`（`:66`） | 排入待还队列，倒计时 `m_plateReturnTime` |
| 13 | **订单匹配** | 步骤 11 的 ③ | `ServerTeamMonitor.OnFoodDelivered:26-41` → `m_ordersController.FindBestOrderForRecipe(_definition, _plateType, out orderID, out timeProp)`（`:32`）→ `ServerOrderControllerBase.FindBestOrderForRecipe:158-171`：<br>① `FindAll(x => Matches(x.RecipeListEntry.m_order, _order, _plateType))`（`:162`）<br>② `Matches:145-156`：**`if (_required.m_platingStep != _plateType) return false;`（`:147`）** → **方盘必须配方也要求方盘**<br>③ 非 `WildcardOrderNode` → `AssembledDefinitionNode.Matching(_provided, _required)`（`:155`）→ `Simpilfy` 双方 → `provided.IsMatch(required)` → `CompositeAssembledNode.AssumeTypeMatch:76-79` → 双向 `Contains`（顺序无关）<br>④ `FindLowestScoring(x => x.Remaining)`（`:163`）**选最紧急的**<br>⑤ `_timePropRemainingPercentage = Clamp01(Remaining / Lifetime)`（`:167`） | 命中订单 → `orderID` + 剩余时间比例（**失败则两者为默认值**） |
| 14 | **成功后处理** | 步骤 13 返回 true | `ServerTeamMonitor:34-38`：`GetRecipe(orderID)` → `restart = (TotalCombo == 0)`（`:35`）→ `IsComboOrder(orderID, restart)`（`:36`）→ **`RemoveOrder(orderID)`（`:37`）** → 返回 true → `ServerKitchenFlowControllerBase.OnSuccessfulDelivery:147-171` | 订单从活跃列表摘除 |
| 15 | **计分** | 步骤 14 | `OnSuccessfulDelivery:147-171`：`CalculateTip(timeProp)`（`:149`，时间越多 tip 越高）→ `CalculateBaseScore(_entry)`（`:150`，= `m_scoreForMeal`）→ `num3 = tip * Max(TotalMultiplier, 1)`（`:152`）→ 累加 base/tip/成功数（`:153-155`）→ combo 分支：`_wasCombo` 则 `TotalCombo++` 且 `TotalMultiplier` 上限 4（`:156-163`），否则清零（`:164-169`）→ `SendDeliverySuccess(...)`（`:170`） | 分数更新 |
| 16 | **发包（逻辑）** | 步骤 15 | `SendDeliverySuccess:51-56`：`m_data.Initialise_DeliverySuccess(teamID, station.gameObject, orderID, timeProp, tip, wasCombo)`（`:53`）→ `SetScoreData(Score)`（`:54`）→ `SendServerEvent`（`:55`） | 客户端收到 `KitchenFlowMessage(Delivery, success=true)` |
| 17 | **发包（视觉）** | 步骤 10 的 ④ | `SendDeliverEvent:39-44`：`m_data.m_delivered = 盘子gameObject`（`:41`）、`m_data.m_success = true`（`:42`）→ `SendServerEvent`（`:43`） | 客户端收到 `PlateStationMessage` |
| 18 | **客户端逻辑** | 步骤 16 | `ClientKitchenFlowControllerBase.ApplyServerEvent:46-83` → `case Delivery:`（`:51`）→ `monitor.Score.Copy(teamScore)`（`:54`）→ `OnSuccessfulDelivery(...)`（`:58`）→ `ClientOrderControllerBase.OnFoodDelivered(true, orderID):75-85` 移除 UI 卡片 + `RecipeSuccessAnimation`（`:82`）→ `UpdateScoreUI`（`:141`）→ `m_onMealDelivered(uID, wasCombo)`（`:144`）→ 成就统计（`:146-154`） | UI 卡片消失 + 分数跳动 + 音效 + 成就 |
| 19 | **客户端视觉** | 步骤 17 | `ClientPlateStation.ApplyServerEvent:48-60` → `DeliverPlate:70-78`（`ServiceBell` 音效 + `SendTrigger(m_onFoodDeliveredTrigger)`）→ `DeliverySequence:80-139`：关 Collider（`:83-87`）→ `isKinematic`（`:88-92`）→ 生成交付特效、等 `m_pfxToFadeDelayTime`（`:93-98`）→ 换 `m_fadeOutShader`（`:99-115`）→ `_Alpha` 1→0 历时 `m_fadeTime`（`:116-137`）→ **`Object.Destroy(plate.gameObject)`（`:138`）** | 盘子淡出并消失 |
| 20 | **服务端拆物理** | 步骤 10 的 ③ | `ServerPlate.StartDeliverySequence:192-203`：`TakeItem()` 从送餐口摘下（`:195`）→ 关所有 Collider（`:196-200`）→ `isKinematic = true`（`:201-202`） | 盘子不再参与物理与交互 |
| 21 | **还盘倒计时** | 每帧 | `ServerKitchenFlowControllerBase.OnUpdateInRound:91-102` → `m_plateReturnController.Update()`（`:100`）→ `PlateReturnController.Update:75-94`：`m_timer -= deltaTime`（`:80`）→ 到点后校验站台有效性（`:83`）→ `CanReturnPlate()`（`:87`，`ServerPlateReturnStation.cs:86-90`）→ **`ReturnPlate()`（`:89`）** → `RemoveAt(num)`（`:90`） | 归还站补回一个干净方盘 |
| 22 | **生成新盘** | 步骤 21 | `ServerPlateReturnStation.ReturnPlate:92-99` → `CreateStack()`（`:101-106`，首次）→ `ServerPlateStackBase.AddToStack:32-36` → **`NetworkUtils.ServerSpawnPrefab(base.gameObject, m_plateStack.m_platePrefab)`（`:34`）** | **全新的方盘对象出现在干净盘堆顶部** |
| 23 | **闭环** | — | 玩家再去取盘（回到步骤 1）；用脏的盘子走 3.7.6 的清洗闭环 | 循环继续 |

**失败分支（把不是订单要求的菜送到送餐口）**：步骤 13 的 `FindAll` 为空 → `:163` 返回 `null` → `FindBestOrderForRecipe` 返回 `false` → `ServerTeamMonitor.OnFoodDelivered:40` 返回 false → `ServerKitchenFlowControllerBase.OnFoodDelivered:143` → **`OnFailedDelivery:173-180`**：清 `TotalMultiplier`/`TotalCombo`/`ComboMaintained`（`:176-178`）→ `SendDeliveryFailed`（`:179`）→ 客户端 `OnFailedDelivery:157-166`：**给所有订单卡片播 `RecipeFailureAnimation`**（`ClientOrderControllerBase.cs:88-92`）。**不扣分、不删单；盘子照样走步骤 11 的归还流程。**

**空盘送上送餐口**：步骤 9 的 `GetContentsCount() >= 1` 为假，跳过销毁（`:93-103`），但 **`DeliverCurrentPlate()` 仍在 `:104` 无条件执行** → 走完整交付链 → 订单匹配必然失败（空节点的 `Simpilfy()` 返回 `NullNode`，无法匹配任何配方）→ `OnFailedDelivery`。**【源码确认】**

**不是盘子放上送餐口**：`ServerPlateStation.CanAddItem:77-80` 返回 false → `CouldAttachToSelfIfEmpty`（AND 语义）失败 → `CalculatePlacementType` 返回 `NotValid`（`AttachStation.cs:52`）→ 若 `placementHandler == null` 则最终 `ServerAttachStation.HandlePlacement` 的 `switch` **无匹配分支、什么都不做**（`:171-189`）；**但 `OnFailedToPlace` 会在别的路径触发**（`ServerAttachStation.cs:215-218`）→ `ServerPlateStation.OnFailedToPlace:112-115` → `SendDeliverEvent(_object, false)` → 客户端 `ClientPlateStation.FailedToDeliver:141-149` 弹出 `m_needsPlateFloatingUI`（1 秒冷却）。**【源码确认】**

---

### 3.10 结论速查与遗留问题

#### 3.10.1 核心结论（全部源码确认）

| 问题 | 结论 |
|---|---|
| **摆盘是怎么发生的** | 手持物把自己的 `IOrderDefinition.GetOrderComposition()` 通过 `IContainerTransferBehaviour.TransferToContainer` **灌进**盘子的 `ServerIngredientContainer.m_contents`，走 `AssembledNodeTransfer.CombineWithContents` 的两段式合并（并入已有复合节点 → 反向吸收 → 追加） |
| **判定在哪做** | `PlacementContainer.CanCombine` 的四道闸门（`PlacementContainer.cs:9-20`），其中 `Plate.CanPlaceOnPlate` 用**影子适配器 + `_dontRemove:true` 干跑**，再用 `GameUtils.GetOrderPlatingPrefab` 验合法性 |
| **手持切好的食材对放着盘子的台面按交互** | **食材自动进盘；手持物被 `_carrier.DestroyCarriedItem()` 销毁；玩家手变空；盘子留在台面上** |
| **配方匹配是否与顺序有关** | **无关**。`AssumeTypeMatch` 用 `Union` 消除位置，`Contains` 用 `used[]` 贪心一一占用，不比较下标 |
| **`CookedCompositeAssembledNode.IsMatch` 比什么** | 只比 `m_cookingStep.m_uID` + `m_progress`（`CookedCompositeAssembledNode.cs:42`）。**`m_recordedProgress` 不参与匹配**，只用于回锅续进度 |
| **`IngredientAssembledNode.GetEnumerator()` 为何危险** | 它 `yield return this`（`:32`），递归遍历会**无限自递归**；且 `CompositeAssembledNode.GetEnumerator` 只吐**孙辈**（`:57`），会跳过子节点。**正确做法：用 `CompositeAssembledNode.m_composition` / `m_optional` 数组字段递归，叶子类型不递归** |
| **送餐口是不是 `PlateStation`** | **是**。它就是 `AttachStation` + `ServerPlateStation`，交付寄生在 `RegisterOnItemAdded` → `OnItemAdded` → `DeliverCurrentPlate` |
| **`FindBestOrderForRecipe` 是否要求 platingStep 相等** | **是，硬性要求**（`ServerOrderControllerBase.cs:147`），用 `PlatingStepData.m_uID` 逐值比较，且是**第一个**短路检查 |
| **选中哪张订单** | `FindLowestScoring(x => x.Remaining)`（`:163`）→ **剩余时间最少的（最紧急的）那一张** |
| **送餐成功/失败/超时的后果** | 成功：加 base + tip×max(multiplier,1)，combo 时 multiplier+1（上限 4），`RemoveOrder`；失败：**只清 multiplier/combo，不扣分不删单**；超时：`TotalTimeExpireDeductions += RecipeTimeOutPointLoss` 且 `ResetOrderLifetime`（**不删单**） |
| **盘子循环的本质** | **销毁 + 重生**。服务端摘盘 + 关碰撞 + 运动学化（`ServerPlate.cs:192-203`），客户端淡出后 `Object.Destroy`（`ClientPlateStation.cs:138`），经由 `PlateReturnController` 倒计时后由 `ServerPlateStackBase.AddToStack` **`ServerSpawnPrefab` 一个全新盘子**（`ServerPlateStackBase.cs:34`） |
| **`m_returnStations` / `GetReturnStation` 的作用** | "送餐口 → 归还站"的显式类型路由表，键是 `PlatingStepData`（`m_uID`）。归还站的身份又间接来自它的 `m_stackPrefab` → `PlateStackBase` → `Plate.m_platingStep` 三层间接。缺失时退化到全局 `FindBestReturnStation` |
| **`m_createPlateTime` 是否被用** | **确认未被使用（死字段）**。全库 grep 仅 1 处命中 = 声明 `PlateStation.cs:36`。同类的还有 `PlateStation.m_platePrefab`（`:33`）、`ServerPlate.ReleaseReservation`（`:210-213`）、`CompositeAssembledNode.GetEnumeratorExhaustive`（`:41-51`） |
| **`DryingPart` 是否存在** | **不存在**。全库 grep 零命中。功能由 `WashingStation.m_dryingStation`（`WashingStation.cs:8`）承担 |

#### 3.10.2 明确标注为推断的部分

1. **`ServerPlateStation.CanAddItem:81` 的 `return true` 分支不可达** —— 基于 `AttachStation.CouldAttachToSelfIfEmpty` 仅在有/无占用物的分支中调用的可达性分析。未做运行时验证。若可达，`OnItemAdded:86-87` 会 NRE。
2. **`IPlaceUnder` 的语义解释（"盘子可以塞到锅/食材下面"）** —— 接口是空标记（`IPlaceUnder.cs:1-3`），语义完全从 `CanPlaceUnder` 的检查点（`AttachStation.cs:79` 检查占用物）和 `PlaceUnderOccupant` 的动作（`:226-236` 用第二载体 `_holderAdapter`）反推。
3. **`GetHolder()` / `m_holderAdapter` 是"搭档的手"** —— 从 `ServerAttachStation.cs:510-513` 与 `PlaceUnderOccupant:230` 的 `holder.InspectCarriedItem() != null` 判断反推，未见文档。
4. **`OnOrderExpired` 只 `ResetOrderLifetime` 不 `RemoveOrder` 是设计意图而非 bug** —— 代码行为确定（`ServerKitchenFlowControllerBase.cs:124` + `ServerOrderControllerBase.cs:135-139`），"无限模式复现同一张单"的解释是推测。
5. **`Union` 折叠重复可选条目会导致 `Contains` 误判** —— `Equals` 重写导致的 `Union` 去重是确定的，但"关卡数据里是否真有重复等价的可选条目"未验证。
6. **`GetEnumeratorExhaustive` 只下探两层的说法** —— 从 `:46` 的 `foreach (item in m_composition[i])` 静态推出；该方法是死代码，无运行时行为可观察。
7. **客户端 `Object.Destroy` 是否会同步通知服务端销毁** —— 本文件内未见上报逻辑；不排除 `ClientSynchroniserBase` 基类有额外机制（未读该基类的销毁路径）。
8. **`WashAllPlates` 的用途** —— 无调用点，推测为调试/作弊/关卡脚本入口。

#### 3.10.3 值得单独记住的三个"反直觉"设计

1. **两个聚合语义相反的委托列表**：`PlacementContainer.CanCombine` 用 `CallForResult(true)`（**OR**，零回调=拒绝一切），`AttachStation.CouldAttachToSelfIfEmpty` 用 `!CallForResult(false)`（**AND**，零回调=放行一切）。同一个 `Generic<bool,GameObject,PlacementContext>` 类型，取决于调用点。（`PlacementContainer.cs:9` vs `AttachStation.cs:93`；语义定义在 `DelegateUtils.cs:17-27`）
2. **pod 平台的 `Equals` 被改成配方匹配**：`AssembledDefinitionNode.Equals`（`:77-80`）走 `Matching`，导致 `ServerIngredientContainer.RemoveIngredient(int i)` 的 `List.Remove(T)`（`:79`）实际上删的是**语义上匹配的第一个元素**，不是下标 `i` 的元素。
3. **"送餐"是从"放置"事件里长出来的**：没有任何定时器、触发器或显式"提交"动作。盘子上送餐口那一刻（`ServerAttachStation.OnItemPlaced:339` 的 `m_itemAdded`）就是交付瞬间。同样地，"归还"是从"交付"里长出来的（`ServerKitchenFlowControllerBase.cs:135` 在匹配之前），**成功失败都还盘**。

---

## 四、订单、关卡与模式

> 分析对象：`E:\pythonxx\tkry\overcooked_decomp\`（ILSpy 反编译产物，2365 个 `.cs`，Unity 2017.4.8 / Mono）
> 标注约定：**【源码确认】** = 有明确类名/成员/行号可查；**【推断】** = 由命名、调用关系或数值默认值合理外推，无直接代码证据。

---

### 4.0 系统分层总览

订单系统在 OC2 里被切成 **6 层**，每层的数据类型不同，跨层靠 `Convert()` 和 BitStream 序列化衔接：

| 层 | 类型 | 所在文件 | 作用 |
|---|---|---|---|
| ① 原型层 | `OrderDefinitionNode`（`ScriptableObject` 树） | `OrderDefinitionNode.cs` | 编辑器里手摆的"配方蓝图"，美术资源 + 嵌套结构 |
| ② 运行层 | `AssembledDefinitionNode`（普通 C# 对象树） | `AssembledDefinitionNode.cs` | 可比较、可序列化的运行期配方 |
| ③ 池层 | `RecipeList` / `RecipeList.Entry` | `RecipeList.cs` | 一局里可用的菜谱池 + 权重 + 基础分 |
| ④ 出单层 | `RoundDataBase.GetNextRecipe()` | `RoundData.cs` / `DynamicRoundData.cs` | 按"频率均衡"权重抽下一个订单 |
| ⑤ 权威层 | `ServerOrderControllerBase` + `ServerOrderData` | `ServerOrderControllerBase.cs` | 服务端唯一真值：订单 ID、寿命、超时、匹配 |
| ⑥ 表现层 | `ClientOrderControllerBase` + `RecipeFlowGUI` + `RecipeWidgetUIController` | `RecipeFlowGUI.cs` 等 | 纯视觉镜像，不参与判定 |

**【源码确认】** 客户端订单**不参与任何判定**——`ClientOrderControllerBase.AddNewOrder()`（`ClientOrderControllerBase.cs:70`）给 `RecipeFlowGUI.AddElement()` 传的过期回调是 `m_expiredDoNothingCallback`（同文件 `:26-28`，空委托）。也就是说客户端 widget 自己的倒计时走完**什么都不做**；界面上看到的"订单超时闪烁"完全由服务端广播的 `OrderExpired` 消息驱动。

---

### 4.1 配方数据结构：`OrderDefinitionNode` 家族

#### 4.1.1 基类字段（`OrderDefinitionNode.cs:5-28`）【源码确认】

```csharp
public abstract class OrderDefinitionNode : ScriptableObject
{
    public RecipeWidgetUIController.RecipeTileData[] m_orderGuiDescription;  // :7  UI 图标树
    public PlatingStepData m_platingStep;                                    // :9  必须用什么盘子
    public GameObject m_platingPrefab;                                       // :11 上菜后的成品模型
    [SelfAssignID(true)] public int m_uID;                                   // :14 全局唯一 ID（网络用）
    public AssembledDefinitionNode Simpilfy() => Convert().Simpilfy();       // :16-20
    public abstract AssembledDefinitionNode Convert();                       // :22
    public override bool Equals(object _node)
        => AssembledDefinitionNode.Matching(this, _node as OrderDefinitionNode); // :24-27
}
```

`m_uID` 是**网络同步的主键**：`RecipeList.Entry.Serialise()` 只写 `m_uID`（32 bit）+ `m_scoreForMeal`（10 bit），`Deserialise()` 用 `GameUtils.GetOrderDefinitionNode(iD)` 反查（`RecipeList.cs:24-36`）。反查是 `GetAllOrderNodes()` 的**线性遍历**（`GameUtils.cs:478-489`）。

#### 4.1.2 五个子类及其独有字段【源码确认】

| 类 | 文件:行 | 独有字段 | `Convert()` 产出 |
|---|---|---|---|
| `CompositeOrderNode` | `CompositeOrderNode.cs:4-17` | `m_composition[]`、`m_optional[]` | `CompositeAssembledNode` |
| `CookedCompositeOrderNode` | `CookedCompositeOrderNode.cs:4-26` | `m_cookingStep`、`m_progress`（`Raw/Cooked/Burnt`） | `CookedCompositeAssembledNode` |
| `MixedCompositeOrderNode` | `MixedCompositeOrderNode.cs:4-23` | `m_progress`（`Unmixed/Mixed/OverMixed`） | `MixedCompositeAssembledNode` |
| `IngredientOrderNode` | `IngredientOrderNode.cs:5-17` | `m_iconSprite`、`m_crateLid`(`SubTexture2D`)、`m_colour` | `IngredientAssembledNode(this)` |
| `ItemOrderNode` | `ItemOrderNode.cs:5-20` | `m_iconSprite`、`m_crateLid`、`m_colour`、`m_heatValue[0,1]` | `ItemAssembledNode(this)` |
| `WildcardOrderNode` | `WildcardOrderNode.cs:4-12` | `m_cookingStep` | **`AssembledDefinitionNode.NullNode`** |

⚠️ `CookedCompositeOrderNode : CompositeOrderNode`、`MixedCompositeOrderNode : CompositeOrderNode`——**继承**，不是平行类。所以 `CompositeOrderNode` 的 `m_composition` / `m_optional` 对它们同样生效。

#### 4.1.3 嵌套结构示例（示意：蛋糕）

> 说明：具体某个配方长什么样存在 `.asset` 里，反编译的 C# 中**没有**任何具体配方实例。下面用蛋糕这个典型"烘焙 + 搅拌 + 可选装饰"配方演示节点组合方式，标为**【推断/示意】**。

```
【OrderDefinitionNode 树（编辑器原型）】

CookedCompositeOrderNode  "蛋糕"        m_uID=1042
│  m_platingStep  = PlatingStepData.Plate
│  m_platingPrefab= prefab_cake_plated
│  m_progress     = CookingProgress.Cooked
│  m_cookingStep  = CookingStepData.Baking
│  m_orderGuiDescription = [ 5 个 RecipeTileData，见下 4.1.4 ]
│
├─ m_composition[0] : MixedCompositeOrderNode "蛋糕糊"
│  │    m_progress = MixingProgress.Mixed
│  ├─ m_composition[0] : IngredientOrderNode "面粉"   m_uID=101
│  ├─ m_composition[1] : IngredientOrderNode "鸡蛋"   m_uID=102
│  └─ m_composition[2] : IngredientOrderNode "蜂蜜"   m_uID=103
│
├─ m_composition[1] : WildcardOrderNode "任意一种水果"   ← 占位符，Convert() 后消失
│       m_cookingStep = CookingStepData.None
│
└─ m_optional[0]    : ItemOrderNode "奶油装饰"   m_heatValue = 0.0
```

**【源码确认】** 关键点：`WildcardOrderNode` 在 `Convert()` 时直接返回 `NullNode`（`WildcardOrderNode.cs:8-11`），而 `CompositeAssembledNode.Simpilfy()` 会**剔除**所有 `NullNode`（`CompositeAssembledNode.cs:87-94, 98-105`）。所以野卡占位符在运行期树里**完全不存在**——它的意义纯粹是编辑器层面的"这里可以放任何东西"的注解，真正的"任意"语义由 `ServerOrderControllerBase.Matches()` 的分支处理（详见 4.2.3）。

#### 4.1.4 `m_orderGuiDescription`：与逻辑树**平行的第二棵树**【源码确认】

```
【RecipeTileData 扁平数组（下标引用，不是嵌套对象）】

m_orderGuiDescription[0] = { m_tileDefinition = { m_mainPictures=[蛋糕图], m_modifierPictures=[] },
                             m_children = [1, 3] }          ← 根（"蛋糕"+烘烤修饰）
m_orderGuiDescription[1] = { m_tileDefinition = { m_mainPictures=[面粉,鸡蛋,蜂蜜], m_modifierPictures=[搅拌图] },
                             m_children = [] }              ← 蛋糕糊叶子（3 张小图并排）
m_orderGuiDescription[2] = { ... 未被子节点引用？... }
m_orderGuiDescription[3] = { m_tileDefinition = { m_mainPictures=[奶油图], m_modifierPictures=[] },
                             m_children = [] }              ← 可选装饰叶子
```

结构定义在 `RecipeWidgetUIController.cs:10-16`：

```csharp
public class RecipeTileData {
    public RecipeWidgetTile.TileDefinition m_tileDefinition;
    public List<int> m_children = new List<int>();
}
```

- **根节点由算法找出来**：`FindHead()` 收集所有被引用过的 `m_children` 下标，返回**第一个没被任何节点当作孩子**的元素（`RecipeWidgetUIController.cs:347-365`）。根会实例化成 `TopRecipeWidgetTile`（带进度条），其余是 `RecipeWidgetTile`（`:182-194`）。
- 尺寸/位置由 `CalculateSize` / `CalculatePosition` 递归计算（`:279-345`），并按 `anchorMin.y` 排序决定渲染层级（`:197-210`）。

**重要架构事实【源码确认 + 推断】**：这棵树与 `OrderDefinitionNode.m_composition` 的嵌套树**没有任何代码级关联**——前者是美术手摆的图标排版，后者是逻辑配方。二者靠"人工保持一致"。若美术漏摆一个 tile，UI 上就看不出那个材料，但逻辑判定照样要。

`TileDefinition` 本体（`RecipeWidgetTile.cs:8-20`）：

```csharp
public class TileDefinition {
    public List<Sprite> m_mainPictures;       // 主图（可多张并排，如"面粉+鸡蛋+蜂蜜"）
    public List<Sprite> m_modifierPictures;   // 修饰图（如"已烘烤""已搅拌"徽章，会加高 tile）
    [HideInInspector] public bool m_largeIcon;      // 由 m_children.Count>0 运行时赋值
    [HideInInspector] public bool m_backgroundTop = true;
}
```

---

### 4.2 `Convert()` 到底产生什么，以及怎么匹配

#### 4.2.1 `Convert()` 的映射规则【源码确认】

```csharp
// CompositeOrderNode.cs:10-16 —— 唯一"搬运工"，其它子类都是它的变体
public override AssembledDefinitionNode Convert() {
    var n = new CompositeAssembledNode();
    n.m_composition = m_composition.ConvertAll(x => x.Convert());
    n.m_optional    = m_optional.ConvertAll(x => x.Convert());
    return n;
}
```

- `CookedCompositeOrderNode`（`:17-25`）额外搬运 `m_cookingStep` + `m_progress`
- `MixedCompositeOrderNode`（`:15-22`）额外搬运 `m_progress`
- `IngredientOrderNode` / `ItemOrderNode` 直接 `new XxxAssembledNode(this)`（**持有原型引用**，所以 `m_iconSprite` 等美术字段在运行期仍可访问）
- `WildcardOrderNode` → `NullNode`（丢失全部信息，只剩 `m_cookingStep` 留在原型上）

运行期基类 `AssembledDefinitionNode`（`AssembledDefinitionNode.cs:7-92`）：

```csharp
public static AssembledDefinitionNode NullNode = new NullAssembledNode();  // :9
public abstract AssembledDefinitionNode Simpilfy();                        // :86
protected abstract bool IsMatch(AssembledDefinitionNode _node);            // :82
```

#### 4.2.2 `Simpilfy()` —— 树的塌缩规则【源码确认】

`CompositeAssembledNode.Simpilfy()`（`:81-119`）三步：

1. 递归 `Simpilfy()` 每个 `m_composition[i]`，丢掉结果为 `NullNode` 的；
2. 同样处理 `m_optional[j]`；
3. **塌缩**：若 `m_optional.Length == 0` 且 `m_composition.Length == 1` → 直接返回那唯一的孩子（**层级被压平**）；若 `m_composition.Length == 0` → 返回 `NullNode`。

所以 `Simpilfy()` 后，`Composite_1(Composite_2(Leaf))` 会退化成 `Leaf`，而"只有一个主料且无可选项"的订单根本不会有 `CompositeAssembledNode` 节点。这影响 UI/日志里的树深，但不影响匹配。

#### 4.2.3 匹配算法【源码确认】

**盘型必须完全相等**（`ServerOrderControllerBase.cs:145-156`）：

```csharp
protected bool Matches(OrderDefinitionNode _required, AssembledDefinitionNode _provided, PlatingStepData _plateType) {
    if (_required.m_platingStep != _plateType) return false;              // 盘子不对 → 直接不匹配
    if (_required.GetType() == typeof(WildcardOrderNode))
        return AssembledDefinitionNode.Matching(_required, _provided);    // 野卡反向匹配
    return AssembledDefinitionNode.Matching(_provided, _required);        // 正常：以玩家实物为准
}
```

注意两个方向：
- **普通订单**：`Matching(_provided, _required)` —— `_provided` 是"主题"，`_required` 是"子集"
- **野卡订单**：`Matching(_required, _provided)` —— **参数顺序反过来**，让订单当主体

`CompositeAssembledNode.IsMatch()`（`:64-79`）核心是双向包含：

```csharp
protected bool AssumeTypeMatch(CompositeAssembledNode _subject)
    => Contains(m_composition.Union(m_optional), _subject.m_composition)
    && Contains(_subject.m_composition.Union(_subject.m_optional), m_composition);
```

解读（这是 `m_optional` 语义的关键）：

- **左半句**：订单的（主料 ∪ 可选项）必须能覆盖玩家给的全部主料 → 玩家不能多给订单里没提过的东西
- **右半句**：玩家的（主料 ∪ 可选项）必须能覆盖订单的全部主料 → 订单要求的每样都得在
- 所以 **`m_optional` 是"双向豁免"**：订单侧的可选项意味着"给了也接受"，玩家侧的可选项意味着"这个可以不算数"
- `Contains()` 是**带已用标记的贪心配对**（`:121-142`），用 `bool[] array` 保证一个玩家实物只被消耗一次 → **数量敏感**：订单要 2 份面粉，玩家只给 1 份就失败

`IsMatch` 在类型不匹配时会做**单元素包装**再比（`:64-74`）：玩家直接递了个叶子而不是组合 → 包成 `CompositeAssembledNode{m_composition=[leaf]}` 再来。这就是"单个食材直接放盘上"也能匹配的原因。

`NullAssembledNode.IsMatch()`（`NullAssembledNode.cs:20-23`）只认 `NullAssembledNode` —— 所以野卡订单如果把 `_provided` 当主体时永远只在传 `NullNode` 时成立；实际生效路径是 `Matches()` 里的反向分支。

#### 4.2.4 `m_optional` 参与匹配的完整例子

```
订单：蛋糕 { m_composition = [蛋糕糊], m_optional = [奶油装饰] }

玩家递上：{ m_composition = [蛋糕糊] }
  左半：{蛋糕糊,奶油} ⊇ {蛋糕糊}     ✓
  右半：{蛋糕糊} ⊇ {蛋糕糊}          ✓  → 成功，奶油是"可给可不给"

玩家递上：{ m_composition = [蛋糕糊, 奶油装饰] }
  左半：{蛋糕糊,奶油} ⊇ {蛋糕糊,奶油} ✓
  右半：{蛋糕糊,奶油} ⊇ {蛋糕糊}      ✓  → 成功

玩家递上：{ m_composition = [蛋糕糊, 生菜] }
  左半：{蛋糕糊,奶油} ⊇ {蛋糕糊,生菜} ✗  → 失败（生菜不在订单里）
```

---

### 4.3 "当前还挂在订单栏上的订单"怎么读

这是逆向里最容易搞错的部分——有 **3 份平行状态**，而且它们不保证一致：

| # | 状态 | 位置 | 权威性 |
|---|---|---|---|
| A | `ServerOrderControllerBase.m_activeOrders` : `List<ServerOrderData>` | `ServerOrderControllerBase.cs:10` | ✅ **唯一权威** |
| B | `ClientOrderControllerBase.m_activeOrders` : `List<ActiveOrder>` | `ClientOrderControllerBase.cs:24` | 服务端消息的镜像 |
| C | `RecipeFlowGUI.m_widgets` : `List<RecipeWidgetData>` | `RecipeFlowGUI.cs:61` | 纯 UI，含正在播死亡动画的 `m_dyingWidgets` |

**【源码确认】** 想读"还挂着的订单"，正确姿势是 **B（`ClientOrderControllerBase.m_activeOrders`）或 A（服务端）**，不要遍历 `RecipeFlowGUI.m_widgets`——后者在订单完成播放动画期间会把元素从 `m_widgets` 摘掉但塞进 `m_dyingWidgets`（`RecipeFlowGUI.cs:163-179`），并且它**不保存 `RecipeList.Entry`**（只有 `m_widget` 引用 + `m_timeLimit` + `m_order` 序号，见 `:6-23`）。

#### 4.3.1 `ClientOrderControllerBase.ActiveOrder`（`ClientOrderControllerBase.cs:8-22`）

```csharp
protected class ActiveOrder {
    public OrderID ID;
    public RecipeList.Entry RecipeListEntry;   // ← 要读订单内容就读这里
    public RecipeFlowGUI.ElementToken UIToken; // ← UI 句柄，可反查 widget
}
```

三层读取路径：

```
TeamID ──► ClientKinectFlowControllerBase.GetMonitorForTeam(teamID)
        ──► ClientTeamMonitor.OrdersController        (ClientTeamMonitor.cs:15)
        ──► ClientOrderControllerBase.m_activeOrders  (protected：外部不可直接访问)
        ──► .RecipeListEntry.m_order                  (OrderDefinitionNode)
        ──► .m_orderGuiDescription                    (图标树)
```

`GetRecipe(OrderID)` 是**唯一公开的按 ID 读数接口**（`ClientOrderControllerBase.cs:106-110`）。**注意它没有 null 检查**——传一个已完成的 `OrderID` 会直接 `NullReferenceException`（`activeOrder.RecipeListEntry`）。使用前必须先确认订单还在 `m_activeOrders` 里。

#### 4.3.2 `RecipeFlowGUI` 侧（UI 层）【源码确认】

```csharp
public ElementToken AddElement(OrderDefinitionNode _data, float _timeLimit,
                               VoidGeneric<ElementToken> _expirationCallback)   // :69-79
{
    int tableNumber = ClaimUnoccupiedTable();                                   // 随机挑空桌面位
    GameObject obj = GameUtils.InstantiateUIController(m_recipeWidgetPrefab.gameObject, ...);
    var w = obj.RequireComponent<RecipeWidgetUIController>();
    w.SetupFromOrderDefinition(_data, tableNumber);                             // → m_recipeTree = _data.m_orderGuiDescription
    var data = new RecipeWidgetData(w, 0f, m_nextIndex, _timeLimit, _expirationCallback);
    m_nextIndex++;  m_widgets.Add(data);  return new ElementToken(data);
}
```

- **`m_maxOrdersAllowed = 5`**（`RecipeFlowGUI.cs:52`，`[SerializeField]`）→ `m_occupiedTables = new bool[5]`（`:192`）。客户端 UI 最多 5 张订单卡。
- `ClaimUnoccupiedTable()` **随机**返回一个空闲桌面编号（`:81-105`）——所以订单卡生成时位置是随机的；`LayoutWidgets()` 每帧再按 `m_order` 序号重排到从左到右（`:228-244`）。
- `ElementToken` 是个**只包了一个引用**的 struct，靠引用相等判断同一性（`:25-43`）。

计时显示（`:199-212`）：

```csharp
public void UpdateTimers(float _dt) {
    for (...) {
        float remain = w.m_widget.GetTimePropRemaining();
        float num = Mathf.Max(remain - _dt / w.m_timeLimit, 0f);   // 归一化倒扣
        w.m_widget.SetTimePropRemaining(num);
        if (remain > 0f && num <= 0f) w.m_expirationCallback(new ElementToken(w));
    }
}
```

`m_timeLimit` = `ServerOrderData.Lifetime`（服务端传下来的订单寿命）。

`RecipeWidgetUIController.GetTimePropRemaining()`（`RecipeWidgetUIController.cs:102-109`）实际上只是转发 `m_topWidgetTile.GetProgress()`，而 `TopRecipeWidgetTile.m_value` 初值 **1f**（`TopRecipeWidgetTile.cs:36, 47-50`）。`SetTimePropRemaining` 还会在 `< 0.2f` 时把 Animator 的 `"Warning"` bool 置真（`RecipeWidgetUIController.cs:90-100`）。

#### 4.3.3 各回调的完整职责【源码确认】

```csharp
// ClientOrderControllerBase.cs:75-93
OnFoodDelivered(bool _success, OrderID _orderID):
    _success == true  → 找 ActiveOrder → m_gui.RemoveElement(token, new RecipeSuccessAnimation())
                        然后 RemoveAll(x => x.ID == _orderID)
    _success == false → 对【所有】还挂着的订单播 RecipeFailureAnimation()   ← 注意是全体闪烁

// :95-104
OnOrderExpired(OrderID _orderID):
    GameUtils.TriggerAudio(GameOneShotAudioTag.RecipeTimeOut, ...)
    m_gui.PlayAnimationOnElement(token, new RecipeFailureAnimation())
    m_gui.ResetElementTimer(token)      // → widget.SetTimePropRemaining(1f)
```

**关键【源码确认】**：`OnOrderExpired` **不从 `m_activeOrders` 移除订单**。订单超时后仍挂在栏上、计时器被重置回满。这与服务端行为一致（见 4.4.6）。

---

### 4.4 订单生命周期：生成 → 显示 → 匹配 → 完成/超时

```
┌─ 服务端（权威） ─────────────────────────────────────────────────────────┐
│ ① ServerOrderControllerBase.Update()          ServerOrderControllerBase.cs:64-85
│    每帧: num = TimeManager.GetDeltaTime(m_layer)  (计时器被抑制时为 0)
│    ├─ 对每个活跃订单: Remaining -= num
│    │    若 Remaining <= 0 → m_orderTimeoutCallback(serverOrderData.ID)
│    ├─ m_timerUntilOrder -= num
│    └─ if (m_autoProgress && !IsFull() && (m_timerUntilOrder < 0f || Count < 2))
│           AddNewOrder();  m_timerUntilOrder = GetNextTimeBetweenOrders();
│
│ ② AddNewOrder()                                :120-127
│    RecipeList.Entry[] next = m_roundData.GetNextRecipe(m_roundInstanceData)
│    for each entry → AddNewOrder(entry)
│
│ ③ AddNewOrder(entry)                           :112-118
│    new ServerOrderData(new OrderID(m_nextOrderID++), entry, GetNextOrderLifetime())
│    m_activeOrders.Add(...);  m_orderAddedCallback(ID)
│
│ ④ ServerKitchenFlowControllerBase.OnOrderAdded  ServerKitchenFlowControllerBase.cs:109-114
│    GetSerialisedOrderData(orderID) → SendOrderAdded(teamID, data)
│    → KitchenFlowMessage{ MsgType.OrderAdded, m_teamID, m_orderData }   → 广播
└──────────────────────────────────────────────────────────────────────────┘
                                    ↓ 网络
┌─ 客户端（表现） ─────────────────────────────────────────────────────────┐
│ ⑤ ClientKitchenFlowControllerBase.ApplyServerEvent   ClientKitchenFlowControllerBase.cs:66-68
│    MsgType.OrderAdded → OnOrderAdded(teamID, orderData)
│
│ ⑥ ClientOrderControllerBase.AddNewOrder(data)        ClientOrderControllerBase.cs:65-73
│    entry.Copy(data.RecipeListEntry)
│    token = m_gui.AddElement(entry.m_order, data.Lifetime, m_expiredDoNothingCallback)
│    m_activeOrders.Add(new ActiveOrder(data.ID, entry, token))
│
│ ⑦ RecipeFlowGUI.AddElement → RecipeWidgetUIController.SetupFromOrderDefinition
│    m_recipeTree = _data.m_orderGuiDescription;  StartCoroutine(RefreshSubObjectsAtEndOfFrame())
│      → 下一帧末 OnCreateSubObjects() 真正生成 tile 树
│    RecipeFlowGUI.Update() → LayoutWidgets() 每帧重排，按 m_order 从左到右
│
│ ⑧ ClientOrderControllerBase.Update() → m_gui.UpdateTimers(dt) 每帧推进进度条
└──────────────────────────────────────────────────────────────────────────┘
                                    ↓ 玩家上菜
┌─ 匹配（服务端） ─────────────────────────────────────────────────────────┐
│ ⑨ IKitchenOrderHandler.FoodDelivered(definition, plateType, station)
│    ServerKitchenFlowControllerBase.OnFoodDelivered             :133-145
│    ├─ m_plateReturnController.FoodDelivered(...)   （盘子回收计时 m_plateReturnTime）
│    └─ teamMonitor.OnFoodDelivered(...)   ← 成败在这里判定
│
│ ⑩ ServerTeamMonitor.OnFoodDelivered                            ServerTeamMonitor.cs:26-41
│    if (m_ordersController.FindBestOrderForRecipe(definition, plateType,
│                                     out orderID, out timePropRemaining)) {
│        entry = m_ordersController.GetRecipe(orderID);
│        bool restart = m_score.TotalCombo == 0;
│        wasCombo = m_ordersController.IsComboOrder(orderID, restart);
│        m_ordersController.RemoveOrder(orderID);       ← 唯一移除点
│        return true;
│    } return false;
│
│ 11 FindBestOrderForRecipe                                       ServerOrderControllerBase.cs:158-171
│    候选 = m_activeOrders.FindAll(x => Matches(x.RecipeListEntry.m_order, _order, _plateType))
│    value = 候选中 Remaining 最小者          FindLowestScoring(x => x.Remaining)
│    timePropRemaining = Clamp01(Remaining / Lifetime)           ← 决定小费档位
└──────────────────────────────────────────────────────────────────────────┘
```

#### 4.4.1 订单 ID 与容量【源码确认】

- `m_nextOrderID` 从 **1** 开始，`m_nextOrderID++`（`ServerOrderControllerBase.cs:8, 114`）
- `OrderID` 序列化只用 **8 bit**（`OrderID.cs:8, 29`）→ 到 255 后回绕。因为同时最多 5 单、且 ID 只在"当前活跃集合"内比较，**回绕不会造成歧义**（`RemoveOrder` 用 `FindIndex` 匹配，一旦集合清空即可重用）——**【推断】**但若有人缓存了旧 `OrderID` 并跨代查询，理论上会撞车。
- `m_maxOrdersAllowed`：`IsFull()` 用的上限（`:97-100`）。注意 `Update()` 里的条件 `|| m_activeOrders.Count < 2`（`:80`）——**当场上订单少于 2 张时，即使 `m_timerUntilOrder` 还没到点也立刻补单**。这是"开局/清空后快速铺满"的手感来源。

#### 4.4.2 出单随机的真相：`m_weight` 是死字段【源码确认，重要】

`RecipeList.Entry` 实现了 `IWeight`（`RecipeList.cs:10, 22`）：

```csharp
public class Entry : IWeight, Serialisable {
    public OrderDefinitionNode m_order;
    public float m_weight;               // :14
    public int m_scoreForMeal = 1;       // :16
    public float Weight => m_weight;     // :22
    public void Serialise(...)  { 写 m_uID(32); 写 m_scoreForMeal(10); }   // :24-28 ← 没写 m_weight
}
```

但 `RoundData.GetNextRecipe()` **完全忽略 `m_weight`**（`RoundData.cs:26-40`）：

```csharp
public override RecipeList.Entry[] GetNextRecipe(RoundInstanceDataBase _data) {
    var d = _data as RoundInstanceData;
    d.RecipeCount++;
    var pick = m_recipes.m_recipes.GetWeightedRandomElement((int i, RecipeList.Entry e) => GetWeight(d, i));
    //                                                                        ↑ lambda 的 e 参数被丢弃
    d.CumulativeFrequencies[pick.Key]++;
    return new RecipeList.Entry[1] { pick.Value };
}

private float GetWeight(RoundInstanceData _instance, int _recipeIndex) {
    int total = _instance.CumulativeFrequencies.Collapse((f, sum) => sum + f);
    float target = (float)(total + 2) / (float)m_recipes.m_recipes.Length;   // 平均应出现次数（+2 平滑）
    return Mathf.Max(target - _instance.CumulativeFrequencies[_recipeIndex], 0f);
}
```

**实际使用的权重是"频率均衡"（anti-frequency / 欠采样上浮）**：

- `target = (total + 2) / N` —— 到目前为止每个菜"理论上该出现几次"（`+2` 是 Laplace 平滑，保证开局每个菜都有非零机会）
- 权重 = `max(target − 已出现次数, 0)` —— **出现过越少的菜权重越高**
- 结果：菜谱池会被**均匀轮转**，长期看每种菜出现频率趋同，而不是按美术配置的 `m_weight` 加权

⚠️ `m_weight` 在整个反编译源码里**没有任何读取点**（仅 `RecipeList.Entry` 自身的 `Weight` 属性暴露，该属性由 `ArrayUtils.GetWeightedRandomElement<T>(this T[]) where T : IWeight` 这一无参重载消费（`ArrayUtils.cs:317-319`）——但那唯一的调用方是 `ServerTriggerAttachedSpawn.cs:58`，用于**随机装饰物预制体**，与菜谱无关）。

**【推断】** `m_weight` 是早期版本遗留 / 被策划弃用的字段。**改它不会有任何效果。**

`DynamicRoundData.GetWeight()`（`DynamicRoundData.cs:65-76`）算法相同，只是把 `N` 和 `CumulativeFrequencies` 换成**当前阶段的**。

#### 4.4.3 `DynamicRoundData`：分阶段关卡【源码确认】

```csharp
public class DynamicRoundData : RoundData {
    public class Phase { public RecipeList Recipes; public float Duration; }   // :9-14
    public Phase[] Phases = new Phase[0];
    public void MoveToNextPhase(RoundInstanceDataBase _data);                  // :31-41
    public float GetCurrentPhaseDuration(RoundInstanceDataBase _data);         // :43-47
    public int GetRemainingPhases(RoundInstanceDataBase _data);               // :49-53
}
```

`ServerDynamicOrderController`（`ServerDynamicOrderController.cs:3-27`）继承 `ServerFixedTimeOrderController`，新增 `MoveToNextPhase()` / `HasMorePhases()` / `GetCurrentPhaseDuration()`，并把 `AccessOrdersConfig` / `AccessRoundInstanceData` 暴露为 `protected`（`ServerFixedTimeOrderController.cs:18, ServerOrderControllerBase.cs:52`）——这些都是**专门为子类切换阶段而开的可见性**。切阶段时 `ResetOrderTimer()` 把 `m_timerUntilOrder` 归零，让新阶段立刻出单。

#### 4.4.4 连击（Combo）判定规则【源码确认】

```csharp
// ServerTeamMonitor.cs:35-36
bool restart = m_score.TotalCombo == 0;
o_wasCombo = m_ordersController.IsComboOrder(o_orderID, restart);

// ServerOrderControllerBase.cs:173-191
public bool IsComboOrder(OrderID _orderID, bool _restart) {
    if (_restart) {
        var d = m_activeOrders.Find(x => x.ID == _orderID);
        if (d != null) { m_comboIndex = m_activeOrders.IndexOf(d); return m_comboIndex == 0; }
        return false;
    }
    if (m_comboIndex >= 0 && m_comboIndex < m_activeOrders.Count)
        return m_activeOrders[m_comboIndex].ID == _orderID;
    return false;
}
```

推导出的实际规则：

1. 连击断了（`TotalCombo == 0`）时交付某一单 → `m_comboIndex` 被**锚定到该单在当前列表里的位置**，函数返回 `m_comboIndex == 0`
2. 因为成功的交付会从列表里 `RemoveAt`，而**只有 `m_comboIndex == 0` 才算续上连击**，所以：
   - **必须按订单栏从左到右的顺序依次交付**（index 0 就是最左边、最早生成的那张）→ 连击成立
   - 插队（交付 index ≠ 0 的单）→ `wasCombo = false` → 连击清零、倍率归零
   - 插队后想重新起连击，必须回去把**最左边**那张先清掉
3. `m_comboIndex` 一旦被设为非 0 值就**不会自己复位**，只能靠下一次 `restart = true` 时的重新锚定

**【推断】** "index 0 = 最左边"依据是 `ServerOrderControllerBase.AddNewOrder` 用 `List.Add` 追加（`:115`）而 `RecipeFlowGUI.LayoutWidgets` 按 `m_order` 递增从左往右排（`RecipeFlowGUI.cs:234-243`），两者顺序一致。

#### 4.4.5 交付成功：计分【源码确认】

```csharp
// ServerKitchenFlowControllerBase.cs:147-171
int tip  = m_kitchenFlowController.CalculateTip(_timePropRemainingPercentage);
int base_ = m_kitchenFlowController.CalculateBaseScore(_entry);        // = _entry.m_scoreForMeal
int tipAwarded = tip * Mathf.Max(monitorForTeam.Score.TotalMultiplier, 1);   // ← 先算，用旧倍率
Score.TotalBaseScore += base_;
Score.TotalTipsScore += tipAwarded;
Score.TotalSuccessfulDeliveries++;
if (_wasCombo) { TotalCombo++; if (TotalMultiplier < 4) TotalMultiplier++; }   // c_scoreMultiplierMax = 4
else           { TotalMultiplier = 0; TotalCombo = 0; ComboMaintained = false; }
SendDeliverySuccess(teamID, station, orderID, timePropRemaining, tipAwarded, wasCombo);
```

**倍率滞后一单**【源码确认】：`tipAwarded` 在倍率自增**之前**计算。所以第 1 单 ×1、第 2 单 ×1、第 3 单 ×2、第 4 单 ×3、第 5 单起 ×4（封顶）。

#### 4.4.6 超时：不删单，只扣分 + 重置计时【源码确认，最反直觉的一点】

```csharp
// ServerKitchenFlowControllerBase.cs:116-126
protected virtual void OnOrderExpired(TeamID _teamID, OrderID _orderID) {
    var monitor = GetMonitorForTeam(_teamID);
    monitor.Score.TotalTimeExpireDeductions += GameUtils.GetGameConfig().RecipeTimeOutPointLoss;  // 默认 10
    monitor.Score.TotalMultiplier = 0;
    monitor.Score.TotalCombo = 0;
    monitor.Score.ComboMaintained = false;
    monitor.OrdersController.ResetOrderLifetime(_orderID);   // Remaining = Lifetime，订单留在栏上
    SendOrderExpired(_teamID, _orderID);
}

// ServerOrderControllerBase.cs:135-139
public void ResetOrderLifetime(OrderID _orderID) {
    var d = m_activeOrders.Find(x => x.ID == _orderID);
    d.Remaining = d.Lifetime;
}
```

`RemoveOrder()`（`:129-133`）**只被 `ServerTeamMonitor.OnFoodDelivered` 的成事分支调用**（`ServerTeamMonitor.cs:37`）。

所以完整结论：

- **订单永远不会因为超时而从订单栏消失**
- 超时的代价是：扣 10 分（默认）、连击清零、倍率清零、该单计时器回满
- 玩家可以一直拖到菜做好；惩罚是"分数"，不是"订单消失"
- UI 上看到订单闪红后又"满血复活"，就是这个机制

**绕过口子**：`EnableOrderExpiration` 属性（`ServerOrderControllerBase.cs:40-50` / `ClientOrderControllerBase.cs:36-46`）为 `false` 时，`Update()` 里的 `num` 恒为 0（`:66`）→ **完全不扣时、永不超时**。对应 `GameModes.SettingKind.OrderExpiration` 这个可切换规则（见 4.7.4）。

#### 4.4.7 交付失败（送错菜）【源码确认】

```csharp
// ServerKitchenFlowControllerBase.cs:173-180
protected virtual void OnFailedDelivery(ServerPlateStation _plateStation) {
    monitorForTeam.Score.TotalMultiplier = 0;
    monitorForTeam.Score.TotalCombo = 0;
    monitorForTeam.Score.ComboMaintained = false;
    SendDeliveryFailed(teamID, station);
}
```

**送错菜没有直接扣分**——只清连击和倍率。而客户端的 `OnFoodDelivered(_success: false, ...)` 会**给订单栏上所有订单同时播失败动画**（`ClientOrderControllerBase.cs:88-92`），这是全局视觉反馈而非定向提示。

---

### 4.5 订单系统参数总表

#### 4.5.1 服务端 `OrdersConfig`（`ServerFixedTimeOrderController.cs:5-14`）【源码确认】

```csharp
public struct OrdersConfig {
    public int m_maxActiveOrders;
    public RoundDataBase m_roundData;
    public float m_orderLifetime;
    public float m_timeBetweenOrders;
}
```

`GetNextOrderLifetime()` → `m_orderLifetime`；`GetNextTimeBetweenOrders()` → `m_timeBetweenOrders`（`:26-34`）。**都是常量，不是随机范围**——所以 `ServerFixedTimeOrderController` 这个名字名副其实。

#### 4.5.2 三条构建路径及其差异【源码确认】

| 构建者 | `m_maxActiveOrders` 来源 | 文件:行 |
|---|---|---|
| `ServerKitchenFlowControllerBase.BuildOrderConfig()` | `m_kitchenFlowController.m_maxOrdersAllowed` | `ServerKitchenFlowControllerBase.cs:80-89` |
| `GameModes.ServerCampaignMode.BuildOrderConfig()` | **硬编码 `5`** | `GameModes\ServerCampaignMode.cs:31-41` |
| `GameModes.ServerSurvivalMode.BuildOrderConfig()` | **硬编码 `5`** | `GameModes\ServerSurvivalMode.cs:39-49` |

**【源码确认 + 推断】** `ServerCampaignFlowController` 和 `ServerSurvivalMode` 都通过 `m_gameModeSetupData.m_orderControllerBuilder` 覆盖掉了 base 的实现（`ServerCampaignFlowController.cs:59-62`、`GameSession.cs:298-307`），因此**战役/生存模式下 `KitchenFlowControllerBase.m_maxOrdersAllowed` 这个 Inspector 字段是无效的**，实际永远是 5。竞技（`ServerCompetitiveFlowController`）走 base 路径，该字段才生效。

其余三处常量：

```csharp
KitchenFlowControllerBase.cs:10    public int m_maxOrdersAllowed = 5;
RecipeFlowGUI.cs:52                private int m_maxOrdersAllowed = 5;    // 客户端 UI 上限
GameModes\ServerCampaignMode.cs:36 m_maxActiveOrders = 5;
```

#### 4.5.3 关卡级参数（`KitchenLevelConfigBase.cs:4-37`）【源码确认】

```csharp
public abstract class KitchenLevelConfigBase : LevelConfigBase {
    [Header("Order Parameters")]
    public float m_orderLifetime = 100f;          // :7   订单总寿命（秒）
    public float m_timeBetweenOrders = 15f;       // :9   出单间隔
    public float m_plateReturnTime = 10f;         // :11  空盘回收时间
    [Range(0f,20f)] public int m_recipesBeforeTimerStarts;  // :15 倒计时启动前的保底交付数

    [Header("Freestyle Recipes")]
    public int[] m_freestyleTimes = new int[0];              // :18
    public int[] m_freestyleScoreThresholds = new int[0];    // :20
    public int m_freestyleComboInterval = 4;                 // :22

    [Header("Game Mode Configs")]
    public CampaignModeConfig  m_campaignConfig  = new();    // :26
    public PracticeModeConfig  m_practiceConfig  = new();    // :29
    public SurvivalModeConfig  m_survivalConfig  = new();    // :32

    public abstract RoundData GetRoundData();                // :34
    public abstract float GetTimeLimit();                    // :36
}
```

⚠️ **死字段警告【源码确认】**：`RecipeList.m_freestyle`（`RecipeList.cs:50`）与 `KitchenLevelConfigBase` 的 `m_freestyleTimes` / `m_freestyleScoreThresholds` / `m_freestyleComboInterval`（`:18-22`）在整个 2365 文件的源码树里**只有声明、没有任何读取点**。

**【推断】** 这是"自由风格 / Freestyle 模式"被砍掉后的残留。OC2 里没有"随分数阈值开放额外菜谱"的机制在跑。改这些字段无效。

`m_recipesBeforeTimerStarts` 是**活的**，作用见 4.7.3。

---

### 4.6 关卡配置体系

#### 4.6.1 类继承树【源码确认】

```
LevelConfigBase (ScriptableObject)                  LevelConfigBase.cs:7-28
│   m_hazardInfo, m_disableDynamicParenting, m_gridSelection,
│   m_showIconPrompts, m_enableRespawnBounds,
│   m_objectives : LevelObjectiveBase[],
│   m_recipeMatchingList : RecipeMatchList,
│   virtual List<OrderDefinitionNode> GetAllRecipes()   // :24-27，基类返回空表
│
├── KitchenLevelConfigBase (abstract)                KitchenLevelConfigBase.cs:4-37
│   │   出单/盘子/模式配置；abstract GetRoundData() / GetTimeLimit()
│   │
│   ├── CampaignLevelConfigBase (abstract)           CampaignLevelConfigBase.cs:3-21
│   │   │   GetTimeLimit() → GetRoundData().m_roundTimer           // :5-9
│   │   │   GetAllRecipes() → m_recipes.m_recipes[].m_order        // :11-20
│   │   │
│   │   ├── CampaignLevelConfig                      CampaignLevelConfig.cs:5-14
│   │   │       public RoundData[] m_rounds;
│   │   │       GetRoundData() => m_rounds[0];        // :10-13  ★只取第 0 个
│   │   │
│   │   ├── ScriptedCampaignLevelConfig              ScriptedCampaignLevelConfig.cs:5-14
│   │   │       public ScriptedRoundData[] m_rounds;  GetRoundData() => m_rounds[0];
│   │   │
│   │   ├── DynamicCampaignLevelConfigBase / DynamicCampaignLevelConfig
│   │   ├── SinglePlayerLevelConfig
│   │   └── BossCampaignLevelConfig
│   │
│   └── GameModes.Horde.HordeLevelConfig            （m_waves / m_health / m_recipeMoney / m_plateReturnTime）
│
└── StoryLevelConfig                                  StoryLevelConfig.cs:3-11
        bool AutoStartDialogue; string[] OnionScript; string[] OnionReturnScript;
        （剧情关，无订单系统）
```

#### 4.6.2 `RecipeMatchList`：客户端缓存的配方的来源【源码确认】

```csharp
// RecipeMatchList.cs:5-12
[Serializable] public class RecipeMatchList : ScriptableObject {
    public RecipeMatchList[] m_includeLists;
    public OrderDefinitionNode[] m_recipes;
    public CookingStepData[] m_cookingSteps;
}
```

用途在 `ClientKitchenFlowControllerBase.CacheRecipeListData()`（`ClientKitchenFlowControllerBase.cs:100-120`）：

```csharp
m_cachedRecipeList = new OrderDefinitionNode[levelConfig.m_recipeMatchingList.m_recipes.Length];
levelConfig.m_recipeMatchingList.m_recipes.CopyTo(m_cachedRecipeList, 0);
for (int i = 0; i < levelConfig.m_recipeMatchingList.m_includeLists.Length; i++) {
    m_cachedRecipeList      = m_cachedRecipeList.Union(levelConfig.m_recipeMatchingList.m_includeLists[i].m_recipes);
    m_cachedCookingStepList = m_cachedCookingStepList.Union(...m_cookingSteps);
}
m_cachedAssembledRecipes = ...Convert().Simpilfy()  // 预转换，供客户端本地的锅/砧板判断用
```

**【源码确认】** 这是 **`m_recipeMatchingList`（订单系统之外的第二份菜谱清单）**，供客户端做**食材/工序合法性**判断（砧板能不能切、锅能不能煎），和 `RoundData.m_recipes`（出单池）是**两套独立数据**。`m_includeLists` 支持复用（DLC 关卡 include 本体的清单）。

⚠️ 若 `m_recipeMatchingList` 漏配某个菜，会出现"订单栏上有这道菜，但客户端不认这套工序"的诡异 bug。**【推断】**

#### 4.6.3 `RoundData`（`RoundData.cs:6-49`）【源码确认】

```csharp
public class RoundData : RoundDataBase {
    protected class RoundInstanceData : RoundInstanceDataBase {
        public int RecipeCount;
        public int[] CumulativeFrequencies;
    }
    public RecipeList m_recipes;          // :15  本局菜谱池
    public float m_roundTimer = 150f;     // :17  默认 150 秒
    public override RoundInstanceDataBase InitialiseRound();
    public override RecipeList.Entry[] GetNextRecipe(RoundInstanceDataBase _data);
}
```

- `RoundDataBase`（`RoundDataBase.cs:1-6`）只有 `InitialiseRound()` / `GetNextRecipe()` 两个抽象方法
- `GetTimeLimit()` 最终读的就是 `m_roundTimer`（`CampaignLevelConfigBase.cs:5-9`）
- **每个订单返回的是"长度可能 >1 的数组"**（`ServerOrderControllerBase.AddNewOrder` 会 `for` 遍历，`:120-127`），当前 `RoundData`/`DynamicRoundData` 都只返回 1 个（`:32`），但架构上支持一次出多单

#### 4.6.4 `m_scoreForMeal` 与星级

**`m_scoreForMeal`**【源码确认】：

```csharp
// RecipeList.cs:16
public int m_scoreForMeal = 1;                    // 默认 1
// KitchenFlowControllerBase.cs:12-15
public int CalculateBaseScore(RecipeList.Entry _entry) => _entry.m_scoreForMeal;
```

它是**交付成功时的固定基础分**，与剩余时间无关。会随订单一起网络同步（10 bit，上限 1023）。

**小费档位**【源码确认】（`GameConfig.cs:7-13` + `KitchenFlowControllerBase.cs:17-26`）：

```csharp
public RecipeTipBoundary[] TipBoundaries = new RecipeTipBoundary[4] {
    new RecipeTipBoundary(0f,    0),
    new RecipeTipBoundary(0.25f, 2),
    new RecipeTipBoundary(0.50f, 4),
    new RecipeTipBoundary(0.75f, 6)
};
public int DefaultDeliveryAward = 20;
public int RecipeTimeOutPointLoss = 10;
public int SingleplayerChopTimeMultiplier = 5;
```

```csharp
public int CalculateTip(float _remainingTimePercent) {
    var cands = m_gameConfig.TipBoundaries.FindAll(x => x.PercentageTimeRemaining < _remainingTimePercent);
    var best  = cands.FindHighestScoring(x => x.PercentageTimeRemaining);
    return best.Value?.ScoreValue ?? 0;
}
```

→ 剩余时间 >75% 得 6、>50% 得 4、>25% 得 2、否则 0。**严格小于**，所以正好 75.0% 只得 4 分。

**星级判定**【源码确认】（`SceneDirectoryData.cs:114-201`）：

```csharp
public class StarBoundaries {
    public int m_OneStarScore, m_TwoStarScore, m_ThreeStarScore, m_FourStarScore;
}

public int GetStarForPoints(int _points, bool _inNGPlus = false) {
    if (_points < OneStarScore)   return 0;
    if (_points < TwoStarScore)   return 1;
    if (_points < ThreeStarScore) return 2;
    if (_points < FourStarScore || !_inNGPlus) return 3;
    return 4;
}
```

调用点（`ClientCampaignFlowController.cs:148-155`）：

```csharp
protected virtual int GetStarRating(int _points) {
    var e = gameSession.LevelSettings.SceneDirectoryVarientEntry;
    bool inNGPlus = gameSession.Progress.SaveData.IsNGPEnabledForLevel(GameUtils.GetLevelID())
                 && gameSession.Progress.SaveData.NewGamePlusDialogShown;
    return sceneDirectoryVarientEntry.GetStarForPoints(_points, inNGPlus);
}
```

**【源码确认】第 4 颗星只在 New Game+ 生效**（`_inNGPlus` 为假时最多 3 星）。

⚠️ **平台变体星界未启用**【源码确认】：`PerPlayerCountDirectoryEntry` 声明了 `m_PCStarBoundaries` / `m_PS4StarBoundaries` / `m_XboxStarBoundaries` / `m_NXStarBoundaries` 四套（`:138-148`），但 `GetStarBoundaries()` **硬返回 `m_PCStarBoundaries`**（`:192-195`）：

```csharp
private StarBoundaries GetStarBoundaries() { return m_PCStarBoundaries; }
```

`GetStarBoundariesAllPlatforms()`（`:197-200`）只是把四套打包返回，**没有调用点**。**【推断】** 这是移植版遗留——分平台难度调参被关掉了，改 PS4/Xbox/Switch 那三套数值无效。

#### 4.6.5 关卡名 / scene 名与实际地图的对应关系

⚠️ **源码中不存在 `s_sushi_1_3` 这样的字符串**【源码确认的否定结论】：

- 对全库 grep `s_sushi`、`sushi_1_3` → **0 匹配**
- 对全库 grep 形如 `"s_xxx_` 的字符串字面量 → **0 匹配**（唯一的 `s_` 命中是 `UnmanagedMemoryManager.cs:38` 的 `"s_NeedFreeList..."` 日志文本）

实际机制【源码确认】：

```
SceneDirectoryData (ScriptableObject)
└─ Scenes : SceneDirectoryEntry[]                        SceneDirectoryData.cs:203
   ├─ Label            : string        （前端显示名）
   ├─ Theme            : LevelTheme    （寿司/气球/魔法/太空/激流/矿场…）
   ├─ World            : World         （Tutorial, One..Seven, DLC2_One..DLC13_One）
   ├─ StarCost         : int
   ├─ AvailableInLobby : bool          ← 街机模式是否能抽到
   ├─ ActuallyAllowed / IsHidden / LevelChainEnd
   ├─ m_supportedGameModes : int [Mask(typeof(Kind))]
   ├─ PreviousEntriesToUnlock : int[]
   └─ SceneVarients : PerPlayerCountDirectoryEntry[]    ← 按人数分档
      ├─ PlayerCount : int             （1/2/3/4，-1 = 通用兜底）
      ├─ LevelConfig : LevelConfigBase ← 每档人数一套独立配置！
      ├─ SceneName   : string  [SceneName]   ← ★真正的 Unity 场景名
      ├─ Screenshot  : Sprite
      └─ m_XXStarBoundaries
```

- **`SceneName` 就是 Unity 场景资源名**（带 `[SceneName]` 特性，`SceneDirectoryData.cs:133-134`）。`s_sushi_1_3` 这种命名是**场景资源命名约定**，存在 `SceneDirectoryData.asset` 里，**不在 C# 代码中**【推断】。
- **同一关不同人数 = 不同场景 + 不同 `LevelConfig`**（`PerPlayerCountDirectoryEntry`），所以 s_sushi_1_3 的 2 人版和 4 人版可能是两个不同 scene，且菜谱池/订单参数都可以不同。
- `GetSceneVarient(playerCount)`（`:103-111`）：先找 `PlayerCount == playerCount`，找不到则找 `PlayerCount == -1` 的通用项；都找不到返回 `null`。
- `Theme` 枚举共 **23 项**（`SceneDirectoryData.cs:8-34`）：`Sushi, Balloon, Wizard, Space, Rapids, Mine, Random, Beach, Resort, Wonderland, ChinaTown, Campsite, Treehouse, Keep, Courtyard, Battlements, Outside, Inside, Wonderland2, ChinaTown2, Summer, ChinaTown3` + `Null=-1` / `Count`。`c_bitsPerTheme = GetRequiredBitCount(22)`（`:205`）。
- `World` 枚举共 **27 个世界**（含 12 个 DLC 世界，`DLC2_One` ~ `DLC13_One`）。

**关卡 ID**【源码确认】（`GameUtils.cs:58-65`）：

```csharp
public static int GetLevelID() {
    var sd = gameSession.Progress.GetSceneDirectory();
    var ls = gameSession.LevelSettings;
    return sd.Scenes.FindIndex_Predicate(x => x.SceneVarients.Contains(ls.SceneDirectoryVarientEntry));
}
```

→ **关卡 ID 是"该 variant 在外层 `Scenes` 数组里的下标"**，不是 `SceneName`，也不是 `m_uID`。存档/高分表用的就是这个下标（`GameProgress.HighScores.Score.iLevelID`）。

**`GameUtils.GetLevelConfig()`**【源码确认】（`GameUtils.cs:204-221`）：

```csharp
public static LevelConfigBase GetLevelConfig() {
    LevelConfigBase cfg = null;
    IFlowController fc = RequestManagerInterface<IFlowController>();
    if (fc != null) cfg = fc.GetLevelConfig();                       // ① 优先：当前 FlowController
    if (cfg == null) {
        var ls = GetGameSession().LevelSettings;
        if (ls != null && ls.SceneDirectoryVarientEntry != null)
            cfg = ls.SceneDirectoryVarientEntry.LevelConfig;          // ② 兜底：GameSession 记录
    }
    return cfg;
}
```

`ServerFlowControllerBase.GetLevelConfig()`（`ServerFlowControllerBase.cs:143-146`）返回 `Awake()` 时缓存下来的 `LevelConfig`（`Awake` 从 `LevelSettings.SceneDirectoryVarientEntry.LevelConfig` 取，`:36-43`）。

**【源码确认】没有 `GameUtils.GetRoundData()` 这个方法**——`GetRoundData()` 是 `KitchenLevelConfigBase` 上的抽象方法（`:34`），调用形式一定是 `(GameUtils.GetLevelConfig() as KitchenLevelConfigBase).GetRoundData()`。

---

### 4.7 模式体系

#### 4.7.1 三套互相独立的"模式"枚举【源码确认】

OC2 里 "模式" 这个词对应 **3 个不同的枚举**，混用会造成误判：

```csharp
// ① 会话阵营：决定世界地图场景 + 是否存档
public enum GameSession.GameType { Cooperative, Competitive }      GameSession.cs:9-13
public class GameTypeSettings { public GameType Type; [SceneName] public string WorldMapScene = "WorldMap"; }  // :15-22

// ② 运行时模式：决定加载哪套前端/回退路径
public enum GameMode { OnlineKitchen, Campaign, Party, Versus, COUNT }   GameMode.cs:1-8

// ③ 玩法规则：决定用哪个 GameModes.* 实现
namespace GameModes { public enum Kind { Campaign, Practice, Survival, COUNT } }   GameModes\Kind.cs:3-9
```

**三者关系**【源码确认】：

| `GameMode` | 典型 `GameType` | 典型 `Kind` | 入口 |
|---|---|---|---|
| `OnlineKitchen` | — | — | 在线房间/错误回退态。`ServerGameSetup.BecomeClient()` 会把它设为 `COUNT`（`ServerGameSetup.cs:23-26`） |
| `Campaign` | `Cooperative` | `Campaign` | StartScreen → WorldMap (`GameSession.TypeSettings.WorldMapScene`) |
| `Party` | `Cooperative` | `Campaign` | StartScreen → Lobbies (`GameState.PartyLobby`) |
| `Versus` | `Competitive` | `Campaign` | StartScreen → Lobbies (`GameState.VSLobby`)，两队 |
| — | — | `Practice` | 关卡内练习（无计时/无压力） |
| — | — | `Survival` | 生存模式，见 4.7.5 |

`GameMode` 通过静态类同步（`ServerGameSetup.cs` / `ClientGameSetup.cs`）：服务端 setter 里 `ServerMessenger.GameSetup(m_Mode)` 广播（`ServerGameSetup.cs:11-20`），客户端在 `OnGameSetupMessage` 里赋值并更新 Rich Presence（`ClientGameSetup.cs:20-24`）。

#### 4.7.2 街机(Party) vs 故事(Campaign)——核心区别【源码确认】

| 维度 | `Party`（街机） | `Campaign`（故事） |
|---|---|---|
| **选关方式** | **投票选主题** → 从该主题的关卡里**随机抽一张** | 世界地图上**手动选关**（`GameState.CampaignMap` → `RunMapUnfoldRoutine`） |
| **是否存档** | ❌ 不存档 | ✅ 存档（星级/解锁/高分） |
| **星级** | 无（结算只显示分数） | 有（`GetStarForPoints`） |
| **解锁** | 无 | `Progress.RecordLevelProgress(levelID, stars, ref unlocks)` |
| **大厅** | `Lobbies` 场景常驻（`GameState.PartyLobby`） | 无大厅（`WorldMap`） |
| **回合结束后** | 回 `PartyLobby`（`ServerCampaignFlowController.cs:179-184`） | 回 `WorldMap`（`:167-178`） |
| **人数要求** | `m_bIsCoop` → 1 人也能开 | — |
| **关卡池** | `SceneDirectoryEntry.AvailableInLobby == true` 的关卡 | 全部（受星数/解锁链限制） |

**存档门禁**【源码确认】（`GameSession.cs:184-192`）——这是最直接的判据：

```csharp
private bool IsSaveable() {
    return m_saveManager != null && m_saveManager.ProfileLoaded && m_saveSlot != -1
        && (m_progress.UseSlaveSlot || !ConnectionStatus.IsInSession() || ConnectionStatus.IsHost())
        && m_gameTypeSettings.Type == GameType.Cooperative
        && ClientGameSetup.Mode != GameMode.Party
        && ClientGameSetup.Mode != GameMode.Versus;
}
```

→ **Party 和 Versus 明确被排除在存档之外**。而 `IsLoadable()`（`:184-187`）条件相同。

另外 `ClientCampaignMode.OnOutro` 里有一条与 Party 相关的成就统计（`GameModes\ClientCampaignMode.cs:132-135`）：

```csharp
if (ClientGameSetup.Mode == GameMode.Party)
    overcookedAchievementManager.IncStat(10, 1f, ControlPadInput.PadNum.One);
```

→ 说明 **Party 模式跑的就是 `ClientCampaignMode`**，只是 `ClientGameSetup.Mode` 不同导致分支行为差异。

#### 4.7.3 FlowController 家族【源码确认】

```
FlowControllerBase                                        FlowControllerBase.cs
├── ServerFlowControllerBase (abstract)                   ServerFlowControllerBase.cs:6-163
│   │   状态机: RunLevelIntro → InLevel → RunLevelOutro → GetNextScene()
│   │   RunRound() 每帧 OnUpdateInRound() 直到 HasFinished()
│   │
│   ├── ServerKitchenFlowControllerBase (abstract)        ServerKitchenFlowControllerBase.cs:5-189
│   │   │   盘子回收、KitchenFlowMessage、计分、IKitchenOrderHandler
│   │   ├── ServerCampaignFlowController                  ServerCampaignFlowController.cs:6-197
│   │   ├── ServerCompetitiveFlowController
│   │   └── ServerBossFlowController
│   ├── GameModes.Horde.ServerHordeFlowController          GameModes.Horde\ServerHordeFlowController.cs:10-336
│   ├── ServerStoryLevelFlowController
│   └── ServerWorldMapFlowController / ServerDynamicFlowController
│
├── ClientFlowControllerBase
│   ├── ClientKitchenFlowControllerBase (abstract)        ClientKitchenFlowControllerBase.cs:5-199
│   │   ├── ClientCampaignFlowController                  ClientCampaignFlowController.cs:8-156
│   │   ├── ClientCompetitiveFlowController               ClientCompetitiveFlowController.cs:7-122
│   │   └── ClientBossFlowController
│   ├── GameModes.Horde.ClientHordeFlowController
│   ├── ClientStoryLevelFlowController
│   └── ClientWorldMapFlowController / ClientDynamicFlowController
│
└── (大厅不走 FlowControllerBase)
    LobbyFlowController : MonoBehaviour                   LobbyFlowController.cs:5-396
    ├── ClientLobbyFlowController                         ClientLobbyFlowController.cs:9-981
    └── ServerLobbyFlowController                         ServerLobbyFlowController.cs:8-695
```

**各模式 FlowController 的实例化路径**【源码确认】——`GameSession` 是工厂：

```csharp
// GameSession.cs:298-318
public IServerMode GetGameModeServer(KitchenLevelConfigBase levelConfig) => GameModeKind switch {
    Kind.Campaign => new ServerCampaignMode(levelConfig.m_campaignConfig),
    Kind.Practice => new ServerPracticeMode(levelConfig.m_practiceConfig),
    Kind.Survival => new ServerSurvivalMode(levelConfig.m_survivalConfig),
    _ => null,
};
public IClientMode GetGameModeClient(KitchenLevelConfigBase levelConfig) => GameModeKind switch {
    Kind.Campaign => new ClientCampaignMode(levelConfig.m_campaignConfig),
    Kind.Practice => new ClientPracticeMode(levelConfig.m_practiceConfig),
    Kind.Survival => new ClientSurvivalMode(levelConfig.m_survivalConfig),
    _ => null,
};
```

`ServerCampaignFlowController.StartSynchronising()`（`ServerCampaignFlowController.cs:29-57`）里的接线：

```csharp
m_gameMode = gameSession.GetGameModeServer(base.LevelConfig as KitchenLevelConfigBase);
m_gameModeContext = new ServerContext { m_gameObject, m_levelConfig, m_teamCount = 1, m_flowController = this };
m_gameMode.Setup(m_gameModeContext, gameSession.GameModeSessionConfig, ref m_gameModeSetupData);
m_roundTimer = m_gameModeSetupData.m_roundTimer;  m_roundTimer.Initialise();
if (m_gameModeSetupData.m_onSessionConfigChangedCallback != null)
    gameSession.OnGameModeSessionConfigChanged += m_gameModeSetupData.m_onSessionConfigChangedCallback;
m_teamMonitor.Initialise(m_campaignFlowController.m_teamMonitor, BuildOrderController,
    _order => OnOrderAdded(TeamID.One, _order),
    _order => OnOrderExpired(TeamID.One, _order));
m_gameMode.Begin();
```

→ **玩法模式（GameModes.*）负责注入三样东西**：订单控制器工厂、回合计时器、一组交付/超时/结算回调。FlowController 只是个被动的宿主。

**战役模式的"计时器延迟启动"**【源码确认】（`GameModes\ServerCampaignMode.cs:52-71` + `GameModes\ClientCampaignMode.cs:53-79`）：

```csharp
public override void Begin() {
    if (ClientGameSetup.Mode == GameMode.Campaign && m_context.m_levelConfig.m_recipesBeforeTimerStarts > 0)
        m_recipeDeliverySuppressor = m_roundTimer.Suppressor.AddSuppressor(m_context.m_gameObject);
}
// 每成功交付一次 m_recipesDelivered++，达到 m_recipesBeforeTimerStarts 时 Release()
```

→ 战役关卡的 `m_recipesBeforeTimerStarts` 若 > 0，**开局回合计时器是暂停的**（`Suppressor` 机制，`ServerOrderControllerBase.Update` 里 `m_roundTimer.IsSuppressed` 时 `dt = 0`，`:66`），交够 N 个菜才启动倒计时。**注意 `ClientGameSetup.Mode == Campaign` 这个守卫**：Party 模式下即使配了 `m_recipesBeforeTimerStarts` 也不生效。

#### 4.7.4 `SessionConfig` 与可切换规则【源码确认】

```csharp
namespace GameModes {
    public enum SettingKind { Timer, Score, OrderExpiration, COUNT }   SettingKind.cs:5-12
}

// GameSession.cs
private SessionConfig m_gameModeSessionConfig = new SessionConfig();   // :77
public Kind GameModeKind { get => m_gameModeSessionConfig.m_kind; set { PendingGameModeSessionConfigChanges = true; ... } }  // :140-151
public bool GetGameModeSetting(SettingKind kind) => m_gameModeSessionConfig.m_settings[(int)kind];        // :285-288
public bool SetGameModeSetting(SettingKind kind, bool value) { ... PendingGameModeSessionConfigChanges = true; ... }  // :290-296
public void CommitGameModeSessionConfig();   // :246-266  提交并广播 ServerMessenger.SendHostModeConfigChanged
public void RevertGameModeSessionConfig();   // :268-283  回滚到 m_internalGameModeSessionConfig
```

这是一个**主机权威 + 事务式提交**的规则开关系统：本地改只写 `m_gameModeSessionConfig` 并置 `PendingGameModeSessionConfigChanges`，必须 `Commit` 才生效（广播 + 落盘），`Revert` 可丢弃。`SettingKind.OrderExpiration` 直接对应 4.4.6 里那个"永不超时"的开关。前端由 `GameModeUIData` / `ModeUIData` / `ModeSettingUIData` 描述（`GameModes\GameModeUIData.cs:6-13`）。

#### 4.7.5 生存模式（Survival）【源码确认】

与战役最大的差别：**回合计时器是可增减的**，且订单本身就是"续命资源"。

```csharp
// GameModes\ServerSurvivalMode.cs:31-72
ServerSurvivalMode.ServerOrderControllerBuilder → ServerFixedTimeOrderController (maxActive=5)

private void OnSuccessfulDelivery(OrderID orderID, RecipeList.Entry entry,
                                  float timePropRemainingPercentage, bool wasCombo, ServerPlateStation station) {
    float baseTime = m_config.m_recipeTimes.Get(entry.m_order);                    // 每道菜单独配的加时
    int   bonus    = SurvivalModeUtil.CalculateDeliveryBonus(m_config, timePropRemainingPercentage);
    m_roundTimer.AddTime((int)(m_config.m_timeMultiplier * baseTime) + bonus);
}

private void OnOrderExpired(TeamID teamID, OrderID order) {
    m_roundTimer.AddTime(-Math.Abs(m_config.m_recipeTimes.RecipeFailedPenalty));   // 超时倒扣
}
```

`SurvivalModeConfig`（`GameModes\SurvivalModeConfig.cs:7-23`）：`m_uiPrefab` / `m_competitiveUIPrefab`（按 `m_context.m_teamCount != 1` 选，`ClientSurvivalMode.cs:47`）/ `m_timeMultiplier` / `m_recipeTimes : RecipeIntData` / `m_outroFlowroutineData`。

`ClientSurvivalMode`（`GameModes\ClientSurvivalMode.cs:54-61`）用 `DataStore` 记录 `m_timeSurvived`，key 是 `new DataStore.Id("time.survived")`（`:21`）；`OnOutro` 把它写进 `GameProgress.HighScores.Score.iSurvivalModeTime`（`:84-89`）。

#### 4.7.6 Horde（保卫厨房）模式【源码确认】

`GameModes.Horde.ServerHordeFlowController : ServerFlowControllerBase, IKitchenOrderHandler`（`GameModes.Horde\ServerHordeFlowController.cs:10`）。

- 配置：`HordeLevelConfig` 的 `m_health`（`:69`）、`m_waves : HordeWavesData`（`:88-96`）、`m_recipeMoney : RecipeMoneyData`（`:315`）、`m_plateReturnTime`
- 不是"订单栏"，而是**每个目标位（`ServerHordeTarget`）一个订单**：`m_entries[targetIndex]`（`:87, 251`）
- 波次流程 `RunWaves()`（`:196-267`）：按 `HordeWaveData.m_spawns` 生成敌人 → 每个敌人抽一个 `wave.m_recipes.m_recipes.GetRandomElement()`（**注意这里用均匀随机 `GetRandomElement`，不是 `GetWeightedRandomElement`**，`:248`）→ 等所有敌人死光 → `EndWave` → 等 `wave.m_intervalSeconds` → 下一波
- 交付判定（`:301-335`）：`AssembledDefinitionNode.Matching(definition, entry.m_order)` → 对 → `serverHordeEnemy.Feed(entry)`，成功则 `TotalEnemiesDefeated++`，钱 `+= m_levelConfig.m_recipeMoney.Get(entry.m_order)`；不对 → `HordeFlowMessage.IncorrectDelivery`
- 结束条件：`HasFinished() => m_runWaves == null || base.HasFinished()`（`:121-124`），或 `m_score.TotalHealth <= 0`（`:135`）
- 计分统计类型不同：用 `TeamScoreStats`（Horde 自己的，带 `TotalHealth` / `GetTotalMoney()` / `TotalMoneySpent` / `TotalEnemiesDefeated`），**不是** `TeamMonitor.TeamScoreStats`

---

### 4.8 街机大厅（Party Lobby）：完整可操作流程

> 这是"街机模式怎么运作、怎么开始一局"的完整逆向。**全部【源码确认】**，个别数值来自 Inspector 默认值。

#### 4.8.1 大厅状态机（`LobbyFlowController.cs:7-17`）

```csharp
public enum LobbyState {
    PreSetup,
    Matchmake,              // 联机匹配中
    LocalSetup,             // 本地局设置
    OnlineSetup,            // 联机局设置
    LocalThemeSelection,    // 本地：选主题中
    OnlineThemeSelection,   // 联机：选主题中（带 45 秒倒计时）
    LocalThemeSelected,     // 本地：主题已定 → 立刻 StartLevel()
    OnlineThemeSelected     // 联机：主题已定 → 2 秒后 StartLevel()
}
```

状态转移图（`ServerLobbyFlowController.SetState()`，`:115-173`）：

```
PreSetup ──► ConnectionStatus.IsInSession() ? OnlineSetup : LocalSetup
LocalSetup  ──► (分配 m_userChoices[MaxPlayers]) ──► LocalThemeSelection
OnlineSetup ──► (分配 m_userChoices[MaxPlayers]) ──► OnlineThemeSelection
LocalThemeSelection  ──► 【AllUsersSelected()】 ──► LocalThemeSelected  ──► StartLevel()
OnlineThemeSelection ──► 【全员已选 或 45 秒超时 或 私人房全员已选】 ──► OnlineThemeSelected ──► StartLevel()
```

#### 4.8.2 可操作步骤（本地双人/单人街机）

| 步 | 操作 | 客户端发生什么 | 服务端发生什么 |
|---|---|---|---|
| 1 | 主菜单 StartScreen 选 **Party** → 进入 `Lobbies` 场景 | `ClientLobbyFlowController.Awake()` 建立并注册 `Mailbox.Client` 的 `MessageType.LobbyServer`（`:92`），隐藏主题菜单（`:81-83`） | `LobbyFlowController.Awake()` 缓存 `m_coopSceneDirectories` / `m_vsSceneDirectories`（`:166, 308-319`） |
| 2 | （自动） | `ClientLobbyFlowController.Start()`：因 `m_connectionMode == eNone`，`AddComponent<ServerLobbyFlowController>()`（`:135-141`） | `ServerLobbyFlowController.Awake()` → `PreSetup()` |
| 3 | （自动）`PreSetup()` | — | `m_bIsCoop = (m_lobbyInfo.m_gameType == Cooperative)`（`:92`）；所有用户 `Team = None`；`ResetTimer(m_lobbyFlow.m_timeLimit)` ← **默认 45 秒**（`LobbyFlowController.cs:96`）；`SetState(LocalSetup)` |
| 4 | **出现主题选择转盘** | `SetState(LocalThemeSelection)` → `m_themeSelectMenu.Show(primaryGamepad, ...)`（`:504-510`）；`StateStrings` 切到 `ChooseTheme` | `ServerLobbyFlowController.SetState(LocalSetup)` → 建 `m_userChoices` → 递归进入 `LocalThemeSelection` |
| 5 | **用摇杆/方向键选一个主题 → 确认** | `OnThemeButtonClicked(button)`（`:185-196`）→ 若是 DLC 按钮先查 `m_dlcManager.IsDLCAvailable()`（未拥有则点了没反应）→ `SelectTheme(theme)` | — |
| 6 | — | `SelectTheme(theme)`（`:567-592`）：对**每个本地 chef** 发一条 `LobbyClientMessage{ Type=ThemeSelected, m_theme, m_chefIndex }`；`Analytics.LogEvent("Theme Vote", ...)`；UI 切到 `WaitingForOthers` + `ShowSelectedThemes()` | — |
| 7 | — | — | `OnLobbyClientMessage`（`:61-88`）→ `SelectTheme(theme, chefIndex)`（`:195-215`）→ 写 `m_userChoices[chefIndex]` → `SendAllUserThemeSelection()` 广播给所有人（让大家看到别人的选择） → 检查 `AllUsersSelected()` |
| 8 | （自动）本地局：立即进 `LocalThemeSelected` | `Update()`（`:904-927`）显示倒计时 | `AllUsersSelected()` 本地分支返回 `m_bIsCoop \|\| ServerUserSystem.m_Users.Count > 1`（`:641-667`）→ 双人及以上立刻成立 |
| 8' | （自动）联机局：等满或超时 | 显示 45 秒倒计时（`SetTimer`，`:935-948`） | `Update()`（`:402-455`）`m_timeLeft -= dt`，到 0 → `OnlineThemeSelected`；私人房（`ePrivate`）且全员已选则立即进入 |
| 9 | （自动）`StartLevel()` | — | 见 4.8.3 |
| 10 | **观看轮盘动画** | 收到 `LobbyMessageType.FinalSelection`（`:397-401`）→ 注册 `LevelLoadByIndex`/`ByName` → `AnimateThemeSelection(chefIndex)`（`:665-727`） | — |
| 11 | （自动）加载关卡 | — | `DelayedLevelLoad()` → `ServerMessenger.LoadLevel(sceneName, GameState.LoadKitchen, true, GameState.RunKitchen)`（`:638`） |

#### 4.8.3 `StartLevel()` 内部逐步（`ServerLobbyFlowController.cs:457-484`）

```csharp
protected void StartLevel() {
    FillUndecidedUserChoices();                     // ① 没选的人 → SelectTheme(LevelTheme.Random, i)
    SceneDirectoryData.LevelTheme theme = PickTheme();   // ② 决定最终主题
    List<int> voters = m_userChoices.Where(c => c != null && c.m_theme == theme).Select(i).ToList();
    int chefIndex = 0;
    if (!m_lobbyFlow.UnanimousSelection(m_userChoices) && UserSystemUtils.AnyRemoteUsers())
        chefIndex = voters[UnityEngine.Random.Range(0, voters.Count)];   // ③ 非全票 → 在"投了中选主题的人"里随机挑一个当赢家
    if (m_lobbyInfo.m_gameType == GameSession.GameType.Competitive)
        AssignUsersToTeams();                        // ④ Versus：洗牌分队
    m_message.m_type = LobbyServerMessage.LobbyMessageType.FinalSelection;
    m_message.m_selectionUpdate = new SelectionUpdate { m_theme = theme, m_chefIndex = chefIndex };
    ServerMessenger.LobbyMessage(m_message);         // ⑤ 广播中选主题 + 赢家下标
    PickLevel(theme);                                // ⑥ 抽关卡
}
```

**① `FillUndecidedUserChoices()`**（`:175-184`）：没选的（`m_userChoices[i] == null` 或 `m_theme == LevelTheme.Count`）→ `SelectTheme(LevelTheme.Random, i)`。**随机主题也是一种"票"**。

**② `PickTheme()`**（`:503-521`）——**这是"投票"的真实语义**：

```csharp
if (m_state == LobbyState.LocalThemeSelected) {
    var b = m_lobbyFlow.m_themeSelectMenu.GetButtonForTheme(m_userChoices[0].m_theme);
    return b.Theme;                                        // 本地局：只看 0 号玩家的选择
}
if (m_state == LobbyState.OnlineThemeSelected) {
    var items = m_userChoices.FindAll(x => x != null && x.m_theme != LevelTheme.Count);
    var pick = items.GetRandomElement();                   // 联机局：从所有有效选择里【均匀随机抽一个】
    if (pick != null) return m_lobbyFlow.m_themeSelectMenu.GetButtonForTheme(pick.m_theme).Theme;
}
return LevelTheme.Random;
```

⚠️ **【源码确认，反直觉】不是"多数票"**：联机局是从**所有玩家的有效选择里均匀随机抽一个**。3 个人投 Sushi、1 个人投 Space → 有 1/4 概率选中 Space。单个人的选择永远有 1/N 权重，与"多少队友投了它"无关。

`UnanimousSelection()`（`LobbyFlowController.cs:274-297`）只用于**两件事**：判断是否播放轮盘抽奖动画（全票时跳过，`ClientLobbyFlowController.cs:674`）和决定 `chefIndex` 是否要随机挑。**它不参与主题选择本身**。

**③ 赢家 `chefIndex`**：全票 → 0（无轮盘动画，直接确认）；非全票且有远程玩家 → 在"投了中选主题的人"里随机挑一个，用于播放 40+chefIndex+1 次循环的轮盘动画，最后放大到 `m_selectedEndScale = 1.3f`（`LobbyFlowController.cs:137`、`ClientLobbyFlowController.cs:688-717`）。

**④ `AssignUsersToTeams()`**（`:486-501`，仅 `GameType.Competitive`）：把 `[0..N-1]` 洗牌后逐个 `ChangeTeam(i)`。`ChangeTeam()`（`:226-281`）的规则是"谁人少去哪队"，且每队上限 `CeilToInt(N/2)`。

#### 4.8.4 `PickLevel(theme)`：主题 → 具体关卡的随机抽（`:523-603`）

```csharp
Predicate<SceneDirectoryData.SceneDirectoryEntry> match = entry =>
       entry.AvailableInLobby
    && entry.Theme != SceneDirectoryData.LevelTheme.Null
    && entry.Theme != SceneDirectoryData.LevelTheme.Count
    && (entry.Theme == _theme || _theme == SceneDirectoryData.LevelTheme.Random);
```

然后：

1. 遍历 `m_lobbyFlow.GetSceneDirectories()`（**Party → `m_coopSceneDirectories`，Versus → `m_vsSceneDirectories`**，`LobbyFlowController.cs:321-329`），把匹配的关卡**累积**进一个 `FastList<SceneDirectoryEntry>(60)`，并记 `array[i] = fastList.Count` 作为**每个 SceneDirectory（= 每个 DLC）的累积上界**
2. 若主题是 `Random`，额外检查该 SceneDirectory 对应的 DLC 是否可用（`dLCManager.IsDLCAvailable(dLCFrontendData)`）——不可用的整个目录跳过（`:547-562`）
3. `int idx = Random.Range(0, fastList.Count)` → 均匀抽一张
4. 反查 `idx < array[i]` 定位到属于哪个 SceneDirectory → `GetDLCIDFromSceneDirIndex(gameType, sceneDirIndex)` → **这就是要加载的 DLC ID**
5. `sceneDirectoryEntry.GetSceneVarient(ServerUserSystem.m_Users.Count)` → 按当前人数取对应的 `PerPlayerCountDirectoryEntry`
6. 若返回 `null`（该人数没有变体）：
   - **Coop** → 直接 `return`，**什么都不做**（静默失败，会卡在大厅）【源码确认，`:580-583`】
   - **Versus** → 弹 `"Text.Versus.NotEnoughPlayers.Title"` 对话框，确认后回 `StartScreen`/`MainMenu` 并把 `ServerGameSetup.Mode = GameMode.OnlineKitchen`（`:584-598`）
7. 成功 → `StartCoroutine(DelayedLevelLoad(sceneVarient.SceneName, dlcID))`

**`DelayedLevelLoad()`**（`:605-639`）：

```csharp
m_message.m_type = LobbyServerMessage.LobbyMessageType.CreateGameSession;
m_message.m_dlcID = _dlcID;
ServerMessenger.LobbyMessage(m_message);                                  // 让客户端先建 GameSession
yield return CoroutineUtils.TimerRoutine(m_lobbyFlow.m_themeSelectionDuration + 1f, layer);  // 等 3 秒
gameSession.TypeSettings.WorldMapScene = "Lobbies";                      // 结束后回大厅
persistentObject.AddPersistingLevel("Lobbies");
persistentObject.AddPersistingLevel("Loading");
persistentObject.AddPersistingLevel(_levelName);
ServerMessenger.LoadLevel(_levelName, GameState.LoadKitchen, bUseLoadingScreen: true, GameState.RunKitchen);
```

客户端收到 `CreateGameSession` → `m_lobbyFlow.CreateLobbySession(m_lobbyInfo.m_gameType, dlcID)`（`:402-404`）→ `LobbyFlowController.CreateLobbySession()`（`:331-359`）按 `GameType` 从 `m_CoopGameSessionPrefabs` / `m_CompetitiveGameSessionPrefabs` 里找 `DLC == dlcID` 的 `GameSession` 预制体实例化，并设 `TypeSettings.WorldMapScene = "Lobbies"`。

**【源码确认】`WorldMapScene = "Lobbies"`** 是街机模式的标志——玩完一局回来还是大厅，而不是世界地图。

#### 4.8.5 主题按钮的可用性裁剪【源码确认】

`ClientLobbyFlowController.Start()` → `DisableUnusableThemes()`（`LobbyFlowController.cs:376-395`）：

```csharp
int mask = 64;                                        // bit 6 = Random 永远可用
foreach (var sd in GetSceneDirectories())
    for (int j = 0; j < sd.Scenes.Length; j++)
        mask |= 1 << (int)sd.Scenes[j].Theme;
for (int k = 0; k < 22; k++) {
    var theme = (SceneDirectoryData.LevelTheme)k;
    if (!MaskUtils.HasFlag(mask, theme)) m_themeSelectMenu.DisallowTheme(theme);
}
```

→ **只有在当前模式（Coop/Versus）的关卡目录里真正出现过、且已拥有的 DLC 主题才会亮起**。没有对应关卡的按钮被禁用。

#### 4.8.6 联机路径【源码确认】

`ClientLobbyFlowController.Start()`（`:133-157`）：

```csharp
if (!ConnectionStatus.IsInSession() || ConnectionStatus.IsHost()) {
    if (m_lobbyInfo.m_connectionMode == OnlineMultiplayerConnectionMode.eNone)
        AddComponent<ServerLobbyFlowController>();          // 纯本地
    else if (ClientUserSystem.m_Users.Count > 1) HostGame();
    else                                        TryJoinGame();
} else {                                                     // 非主机的联机客户端
    SetState(LobbyState.OnlineSetup);
    m_message.m_type = LobbyClientMessage.LobbyMessageType.StateRequest;
    ClientMessenger.LobbyMessage(m_message);                 // 向主机要一份当前状态
    UpdateUIColours();
}
```

- `HostGame()`（`:315-339`）：`ConnectionModeSwitcher.RequestConnectionState(NetConnectionState.Server, ServerOptions{ gameMode = m_bIsCoop ? GameMode.Party : GameMode.Versus, visibility, hostUser, connectionMode })`
- `TryJoinGame()`（`:205-220`）：`NetConnectionState.Matchmake` + `MatchmakeData{ gameMode = Party/Versus, User, connectionMode }`
- 匹配失败 → 回 `NetConnectionState.Offline` → 再 `HostGame()`（`:312`）

**联机拓扑消息**【源码确认】：

| 消息 | 方向 | 类型 |
|---|---|---|
| `LobbyClientMessage` | Client → Server | `ThemeSelected` / `StateRequest` / `TeamChangeRequest`（`ServerLobbyFlowController.cs:68-87`） |
| `LobbyServerMessage` | Server → Client | `StateChange` / `ResetTimer` / `TimerUpdate` / `SelectionUpdate` / `FinalSelection` / `CreateGameSession`（`ClientLobbyFlowController.cs:370-405`） |

#### 4.8.7 离开大厅【源码确认】

`Leave` 按钮（`UIKitCancel`）→ `ShowLeaveDialog()`（`:797-812`）→ 确认 → `OnLeaveConfirmed()`（`:826-862`）：

- 若是主机且有远程玩家：先把房间改成 `ePrivate` + `GameMode.OnlineKitchen`，重连后再 Leave
- 否则直接 `NetConnectionState.Offline`
- `Leave()`（`:881-886`）：`ServerGameSetup.Mode = GameMode.OnlineKitchen; ServerMessenger.LoadLevel("StartScreen", GameState.MainMenu, false);`

**若所有远程玩家中途掉线**（`ServerLobbyFlowController.OnUserRemoved`，`:303-344`）：取消待加载的协程，回到 `Lobbies`/`PartyLobby`（或 `VSLobby`）并销毁大厅对象。

---

### 4.9 特殊玩法系统

#### 4.9.1 传送门（Teleportal）

**组件族**【源码确认】：

```
Teleportal (MonoBehaviour)                        Teleportal.cs:3-24
├─ Transform m_teleportPoint            [AssignChild("TeleportPoint")]
├─ float     m_teleportArc = 90f        [Range(0,180)]  ← 入口方向锥角
├─ GameObject m_exitPortal                             ← 出口传送门对象
├─ float     m_receiveDelay                            ← 出口端延迟
├─ float     m_cooldownTime                            ← 冷却
└─ bool      m_allowImmediateReteleport                ← 是否允许立刻回传

接口：ITeleportalSender / ITeleportalReceiver / ITeleportable
实现组合（按可传送对象类型）：
  TeleportalPlayerSender     / TeleportalPlayerReceiver      厨师
  TeleportalAttachmentSender / TeleportalAttachmentReceiver  食材、盘子
  TeleportalConveyenceReceiver                               ★传送带出货口
  TeleportalMapAvatarSender  / TeleportalMapAvatarReceiver   世界地图小头像
  WorldMapTeleportal / ServerWorldMapTeleportal              地图节点间传送
每个都有 Client*/Server* 双半（ServerTeleportal.cs 等）
```

**`ServerTeleportal` 逻辑**【源码确认】（`ServerTeleportal.cs:7-318`）：

- `StartSynchronising()`（`:48-64`）：从自身收集 `ITeleportalSender[]`（`m_senders`）和 `ITeleportalReceiver[]`（`m_selfReceivers`）；从 `m_teleportal.m_exitPortal` 收集 `m_exitReceivers`
- 触发（`:307-317`）：

```csharp
private void OnTriggerStay(Collider collider) {
    if (m_teleportRoutine == null && !IsTeleporting() && !IsReceiving()) {
        var t = FindTeleportable(collider.transform);       // 沿父级向上找 ITeleportable
        if (t != null && CanTeleport(t) && IsInTeleportArc(collider.gameObject))
            Teleport(t);
    }
}
```

- **方向锥判定**（`:259-278`）：`IsInTeleportArc` 用传送门的 `transform.right` 与 `m_teleportArc` 在 XZ 平面构成锥体；`IsFacingPortal` 用常量 `c_facingTeleportAngleMax = 45f`（`:13`）检查物体朝向。**所以站在传送门上但背对它是不会触发的。**【源码确认】
- 传送协程（`:199-229`）：`TeleportFrom` → `SendTeleportFrom` 广播 → `sender.TeleportFromMe()` → `TeleportTo`（先等 `m_receiveDelay`，再 `while (!receiver.CanTeleportTo(_object)) yield`，最后 `receiver.TeleportToMe()`）
- 防抖（`:231-257`）：`EndTeleport` 启动 `PostTeleportCooldown()`（`m_cooldownTime`），并在 `m_allowImmediateReteleport == false` 时把对象加入 `m_recentlyTeleported`；`UpdateSynchronising()`（`:164-185`）在对象**离开锥体并且不再朝向传送门**时才把它从抑制列表移除
- 状态同步：`TeleportalMessage`（`Initialize_State` / `Initialize_StartTeleport` / `Initialize_EndTeleport`，`:66-85`）

**`TeleportalConveyenceReceiver`**【源码确认】（`TeleportalConveyenceReceiver.cs:3-10`）：

```csharp
[RequireComponent(typeof(Teleportal))]
[RequireComponent(typeof(StaticGridLocation))]
public class TeleportalConveyenceReceiver : MonoBehaviour {
    [SerializeField][AssignChild("TeleportPoint")] public Transform m_attachPoint;
}
```

→ 它是 **"食材/盘子从台面直接传走"** 的实现：`StaticGridLocation` 让它挂在格子系统里（可被传送带/机械臂识别），`m_attachPoint` 是接收落点。典型用法：火星/太空关的"把原料用传送门送到厨房"。

#### 4.9.2 大炮（Cannon）

**数据结构**【源码确认】（`Cannon.cs:3-39`）：

```csharp
public class Cannon : MonoBehaviour {
    public ProjectileAnimation m_animation;
    public string m_launchTrigger, m_enableTrigger, m_disableTrigger;
    public GameObject m_button;                       // 发射按钮
    public GenericVoid<GameObject> EndCannonRoutine;  // 动画结束回调
    [HideInInspector] public Transform m_target, m_attachPoint, m_exitPoint;
    // Awake: base.transform.FindChildRecursive("Target"/"AttachPoint"/"ExitPoint")   ← 按名字硬找
}
```

**`ServerCannon` 逻辑**【源码确认】（`ServerCannon.cs:5-141`，`EntityType.Cannon`）：

- `m_handlers = m_cannon.GetComponents<IServerCannonHandler>()`（`:33`）→ `ServerCannonPlayerHandler` 处理厨师、`ServerCannonAttachmentHandler`（若有）处理物品
- 装填：`Load(obj, endInteractionCallback)`（`:64-73`）→ `GetHandler(obj)?.Load(obj)` + 广播 `CannonMessage{m_loadedObject, m_state = Load}`
- 就绪：`SetReadyToLaunchCallback(cb)`（`:137-140`），`UpdateSynchronising()` 轮询它在真时给 `m_button` 发 `m_enableTrigger`（`:39-49`）
- 发射：`OnTrigger(_trigger)`（`:103-117`）——`_trigger == m_cannon.m_launchTrigger && !m_flying` → 记 `m_pilotRotation.m_transformToRotate.eulerAngles.y` 作为发射角 → 广播 `CannonState.Launched`
- 卸货：`Unload(obj)`（`:75-83`）→ `obj.transform.SetPositionAndRotation(m_cannon.m_exitPoint.position/rotation)`
- 成就：按发射按钮且已装填 → `ServerMessenger.Achievement(_interacter, 802)`（`:56-62`）
- 会话内包装：`CannonSessionInteractable` / `Client*/Server*CannonSessionInteractable`（用于在线厨房的"投票/准备"交互）

**【推断】** 大炮的角度由玩家操作 `PilotRotation` 决定（`ServerCannon.cs:34, 111-114` 引用了 `PilotRotation`），即"厨师站上去转炮台"。

#### 4.9.3 移动平台 / 河流关木筏

⚠️ **源码中不存在 `Raft` / `Boat` / `MovingPlatform` 这样的 MonoBehaviour**【源码确认的否定结论】。

实际机制【源码确认】——**通用的"Pilot（驾驶）"系统**：

```
PilotMovement.cs:4-6     [RequireComponent(typeof(Rigidbody))]
                         public float MoveSpeed = 4f;
                         public float SnapHalfLife = 0.1f;
                         public RigidbodyMotion RigidbodyMotion;
    Start()  : RigidbodyMotion.SetKinematic(true)                    // :27  运动学，靠脚本推位
    Update() : 记录 m_previousPose，统计静止帧 m_belowThresholdCounter  // :31-44
    EstimateAverageVelocity() : 指数平滑出速度（拖着厨师跟着动）        // :46-56
    HasMoved() : return m_belowThresholdCounter < 5                  // :58-61

PilotRotation.cs / ServerPilot*.cs / ClientPilot*.cs / PilotRotationMessage.cs
FollowPilotMoved.cs / FollowPilotDirection.cs                        // 让乘客/装饰跟随

MovingPlatformCosmeticDecisions.cs:3-4   [RequireComponent(typeof(PilotMovement))]
    ActiveLightstripMaterial / InactiveLightstripMaterial / LightStripMeshes[] / LightPFXName = "glow (2)"
    OnPilotStatusChanged(bool _hasPilot)   // :24-35  有人驾驶时点亮灯带 + 开粒子
Server/ClientMovingPlatformCosmeticDecisions : 同步这盏灯
```

→ **河流关（Rapids）的"木筏/平台"= 一个挂了 `PilotMovement` + `Rigidbody` 的物体，厨师站上去驾驶它**；`MovingPlatformCosmeticDecisions` 只负责"有人/没人"的灯光反馈。没有专门的木筏类。**【推断】** 具体哪些关卡用、速度多少，全在预制体/场景上配。

#### 4.9.4 火星/陨石（Meteor）

⚠️ **只有 `MeteorManager` 一个类**【源码确认的否定结论】：全库 grep `Meteor` 只有 `MeteorManager.cs:3` 一处类声明，**没有独立的 `Meteor` 类**。

```csharp
// MeteorManager.cs:3-47
public class MeteorManager : MonoBehaviour {
    [SerializeField] private GameObject m_meteorPrefab;
    [SerializeField] private Point3 m_minGridIndex;      // 网格盒最小角
    [SerializeField] private Point3 m_maxGridIndex;      // 网格盒最大角
    [SerializeField] private float m_minTimePeriod;
    [SerializeField] private float m_maxTimePeriod;
    private GridManager m_gridManager;
    private float m_timer;

    void Awake() { ResetTimer(); m_gridManager = GameUtils.GetGridManager(base.transform); }
    void ResetTimer() { m_timer = Random.Range(m_minTimePeriod, m_maxTimePeriod); }

    void Update() {
        m_timer -= TimeManager.GetDeltaTime(base.gameObject.layer);
        if (m_timer < 0f) {
            int x = Random.Range(m_minGridIndex.X, m_maxGridIndex.X + 1);
            int y = Random.Range(m_minGridIndex.Y, m_maxGridIndex.Y + 1);
            int z = Random.Range(m_minGridIndex.Z, m_maxGridIndex.Z + 1);
            Vector3 p = m_gridManager.GetPosFromGridLocation(new GridIndex(x, y, z));
            m_meteorPrefab.Instantiate(p, Quaternion.identity);     // ★不加偏移，直接落在格点上
            ResetTimer();
        }
    }
}
```

**【源码确认】** 陨石是**纯服务器本地生成**（没有 `Server*` 半，没走 `NetworkUtils.ServerSpawnPrefab`）——因为在离线/单机下 `Server*` 与 `Client*` 同进程；在线时这个组件只在主机上跑。**【推断】** 陨石预制体本身的破坏逻辑（砸坏台面/玩家）由其它通用组件承载，不在这个文件里。

#### 4.9.5 无人机/机械臂（AutoWorkstation）

**配置**【源码确认】（`AutoWorkstation.cs:3-18`）：

```csharp
[AddComponentMenu("Scripts/Game/Environment/AutoWorkstation")]
public class AutoWorkstation : MonoBehaviour {
    [SerializeField][Range(1f,16f)] public int m_choppingPower = 8;
    [SerializeField] public string m_workTrigger = string.Empty;
    [SerializeField] public string m_workFinishedTrigger = string.Empty;
    [SerializeField] public GameObject m_workFinishedTarget;
}
```

**`ServerAutoWorkstation : ServerSynchroniserBase, ITriggerReceiver`**【源码确认】（`ServerAutoWorkstation.cs:6-339`，`EntityType.AutoWorkstation`）：

- `StartSynchronising()`（`:65-90`）：`m_attachStations = RequestComponentsInImmediateChildren<ServerAttachStation>()` → **只找直接子物体上的 AttachStation**；每个注册三个回调：`RegisterOnItemAdded` / `RegisterOnItemRemoved` / `RegisterAllowItemPickup` / `RegisterAllowItemPlacement`（按 station 下标捕获）
- 交互开始（`:313-321`）：玩家第一次交互 → `StartWorking()`；`SetStickyInteractionCallback(InteractionIsSticky)` 让"还有没做完的活"时交互持续
- **`StartWorking()`（`:179-204`）——机械臂的双模式**：

```csharp
bool allLoaded = true;
for (int i = 0; i < m_attachStations.Length; i++)
    if (m_workableItems[i] == null && m_attachStations[i] != null && m_attachStations[i].HasItem())
        { allLoaded = false; break; }

var aed = base.gameObject.RequireComponentRecursive<AnimationEventData>();
if (allLoaded) {
    aed.GetTriggerData("Chop", "Impact", out clipDuration, out triggerTime);
    m_workAction = new WorkAction(triggerTime, clipDuration, OnChop);       // ← 干活
} else {
    aed.GetTriggerData("Jam",  "Impact", out clipDuration, out triggerTime);
    m_workAction = new WorkAction(triggerTime, clipDuration, OnJam);        // ← 卡住
}
SynchroniseInteractionState(true);
```

- `OnChop()`（`:223-235`）：

```csharp
for (int i = 0; i < m_attachStations.Length; i++) {
    var station = m_attachStations[i];
    var item = m_workableItems[i];
    if (item != null) item.DoWork(station, base.gameObject, m_autoWorkstation.m_choppingPower);
}
```

- `OnJam()`（`:237-239`）：**空实现**。这就是"某个槽位没料 → 机械臂空转卡顿抖动"的表现来源
- 完工（`:206-221`）：`StopWorking()` → 若 `m_workFinishedTrigger != ""`，把它发给 `m_workFinishedTarget`（或自身）
- 物品可拿取条件（`:241-256`）：只在加工中且 `item.HasFinished() || item.GetProgress() == 0f` 时允许拿走（**中途抢料会被拒绝**）
- 放置条件（`:258-265`）：玩家来源的放置只在"没在加工中"（`m_workAction == null`）时允许
- 同步（`:92-119`）：`AutoWorkstationMessage.m_working` + `m_items[]`（`EntitySerialisationEntry`），停工时会清空全部 `m_items`

#### 4.9.6 `SwitchStation` 与 `ConveyorStation`

**`SwitchStation`**【源码确认】（`SwitchStation.cs:3-6`）——**空壳标记类**：

```csharp
[RequireComponent(typeof(Interactable), typeof(AttachStation))]
public class SwitchStation : MonoBehaviour { }
```

逻辑全在 `ServerSwitchStation` / `ClientSwitchStation` 里（基于 `Interactable` 交互 + `AttachStation` 放东西）。**【推断】** 典型用法：把一个重物（食材/盘子）压上去、或厨师按一下，触发场景机关（开合门、旋转台、升降平台）。`[RequireComponent]` 保证它必须同时有交互点和放置点——即"**能放东西的按钮**"。

**`ConveyorStation`**【源码确认】（`ConveyorStation.cs:3-21`）：

```csharp
[RequireComponent(typeof(TabletopConveyenceReceiver))]
[RequireComponent(typeof(StaticGridLocation))]
[RequireComponent(typeof(AttachStation))]
public class ConveyorStation : MonoBehaviour {
    public enum XZDirection { Leftwards, Rightwards }
    [SerializeField] public float m_conveySpeed = 1f;
    [SerializeField] public XZDirection m_conveyanceDirectionXZ = XZDirection.Rightwards;
    public MeshRenderer m_targetRenderer;
}
```

- 台面传送带：把盘/食材沿 XZ 的 ±X 方向匀速推走（`m_conveySpeed`）
- `ServerConveyorStation` / `ClientConveyorStation` + `ConveyorStationMessage` 负责同步
- **特殊关卡用法**【推断】：寿司店（Sushi）的"出餐带"、火星（Space）的"原料输送带"——把成品自动送到顾客侧的上菜位，或把原料从未加工区送到厨师手边

---

### 4.10 得分 / 评价 / 失败条件

#### 4.10.1 计分统计结构【源码确认】

```csharp
// TeamMonitor.cs:9-72
public class TeamMonitor.TeamScoreStats : Serialisable {
    public int  TotalBaseScore;
    public int  TotalTipsScore;
    public int  TotalMultiplier;             // 3 bit 同步 → 上限 7，实际封顶 4
    public int  TotalCombo;                  // 8 bit
    public int  TotalTimeExpireDeductions;
    public bool ComboMaintained = true;
    public int  TotalSuccessfulDeliveries;

    public int GetTotalScore() => TotalBaseScore + TotalTipsScore - TotalTimeExpireDeductions;   // :33-36
}
```

**总分公式**【源码确认】：`总分 = 基础分 + 小费分 − 超时扣分`

#### 4.10.2 三项构成【源码确认，均见 4.4.5 / 4.5 / 4.6.4】

| 项 | 来源 | 默认值 |
|---|---|---|
| `TotalBaseScore` | `+= RecipeList.Entry.m_scoreForMeal`（每单） | `1` |
| `TotalTipsScore` | `+= CalculateTip(剩余时间比) × max(TotalMultiplier, 1)` | 档位 0/2/4/6 |
| `TotalTimeExpireDeductions` | `+= GameConfig.RecipeTimeOutPointLoss`（每次超时） | `10` |

#### 4.10.3 结算面板【源码确认】

```csharp
// GameModes\ClientCampaignMode.cs:141-162
CoopStarRatingUIController.ScoreData scoreData = new();
scoreData.SuccessPoints               = score.TotalBaseScore;
scoreData.FailDeductions              = score.TotalTimeExpireDeductions;
scoreData.Tips                        = score.TotalTipsScore;
scoreData.Score                       = totalScore;
scoreData.StarRating                  = starRating;
scoreData.StarRatingIncreased         = starRating > num;               // num = 存档已有星数
scoreData.TotalSuccessfulDeliveries   = score.TotalSuccessfulDeliveries;
scoreData.JustUnlockedNGP             = justUnlockedNGP;
```

存档写入（`:107-123`）：

```csharp
int levelID = GameUtils.GetLevelID();
int stars = levelProgress?.ScoreStars ?? 0;
gameSession.Progress.RecordLevelProgress(levelID, starRating, ref _unlocks);
gameSession.Progress.RecordLevelScore(new GameProgress.HighScores.Score {
    iLevelID = levelID, iHighScore = totalScore, iSurvivalModeTime = 0
});
```

生存模式改填 `iSurvivalModeTime`（`GameModes\ClientSurvivalMode.cs:84-89`），且 `iHighScore = int.MinValue`。

#### 4.10.4 失败 / 结束条件【源码确认】

| 模式 | 结束条件 | 代码位置 |
|---|---|---|
| 战役 / 派对 | `m_roundTimer.TimeExpired() \|\| base.HasFinished()` | `ServerKitchenFlowControllerBase.cs:104-107` |
| 任意 | `base.HasFinished()` = `m_skipToEnd`（由 `SkipToEnd()` / `SkipLevel(stars)` 置位） | `ServerFlowControllerBase.cs:138-141, 153-156`；`ServerCampaignFlowController.cs:137-153` |
| 生存 | `ServerModifiableRoundTimer` 归零 | `GameModes\ServerSurvivalMode.cs:62-72` |
| Horde | `m_runWaves == null \|\| base.HasFinished()`，或 `TotalHealth <= 0` | `GameModes.Horde\ServerHordeFlowController.cs:121-124, 135` |
| 竞技 | `HasFinished()`（同战役）+ 两队比分对比 | `ClientCompetitiveFlowController.cs:47-87` |

**⚠️ OC2 没有"输"的概念**【源码确认 + 推断】：

- 送错菜**不扣分**，只清连击/倍率（`ServerKitchenFlowControllerBase.cs:173-180`）
- 订单超时扣 10 分，但订单**留在栏上**（4.4.6）
- 唯一的"减分源"是 `TotalTimeExpireDeductions`，且分数**可以为负**（`GetTotalScore()` 无下限夹取）
- 因此"失败"只表现为**星星少 / 分数低**，回合永远走完

**`SkipLevel(stars)` 的后门**【源码确认】（`ServerCampaignFlowController.cs:137-147`）——用于跳关时的分数伪造：

```csharp
public virtual void SkipLevel(int _stars) {
    var e = gameSession.LevelSettings.SceneDirectoryVarientEntry;
    m_teamMonitor.Score.TotalBaseScore = e.GetPointsForStar(_stars);   // 直接填到"刚好 N 星"的分数
    m_teamMonitor.Score.TotalTipsScore = 0;
    m_teamMonitor.Score.TotalTimeExpireDeductions = 0;
    SkipToEnd();
}
```

**回合结束后的跳转**【源码确认】：

| 模式 | 下一场景 | 代码 |
|---|---|---|
| 重开 | 当前 `SceneDirectoryVarientEntry.SceneName`（`GameState.LoadKitchen` → `RunLevelIntro`） | `ServerCampaignFlowController.cs:160-166` |
| Party | `TypeSettings.WorldMapScene`（= `"Lobbies"`，`GameState.PartyLobby`） | `:179-184` |
| Campaign | `TypeSettings.WorldMapScene`（= `"WorldMap"`，`GameState.CampaignMap` → `RunMapUnfoldRoutine`） | `:167-178` |
| Survival | `WorldMapScene`（`GameState.CampaignMap`） | `GameModes\ServerSurvivalMode.cs:8-17` |
| Horde | 重开 → 当前关；Campaign → WorldMap；Party → PartyLobby | `GameModes.Horde\ServerHordeFlowController.cs:274-299` |

---

### 4.11 逆向陷阱清单（改这些东西不会有效果）

| # | 字段 / 类 | 状态 | 证据 |
|---|---|---|---|
| 1 | `RecipeList.Entry.m_weight` | **死字段**，出单权重实际由 `RoundData.GetWeight()` 的频率均衡算法决定 | `RecipeList.cs:14`；`RoundData.cs:30, 35-40`（lambda 丢弃 `e`） |
| 2 | `RecipeList.m_freestyle` | **死字段**，无读取点 | `RecipeList.cs:50` |
| 3 | `KitchenLevelConfigBase.m_freestyleTimes` / `m_freestyleScoreThresholds` / `m_freestyleComboInterval` | **死字段**，无读取点 | `KitchenLevelConfigBase.cs:18-22` |
| 4 | `SceneDirectoryData.PerPlayerCountDirectoryEntry.m_PS4StarBoundaries` / `m_XboxStarBoundaries` / `m_NXStarBoundaries` | **未启用**，`GetStarBoundaries()` 硬返回 PC 那套 | `SceneDirectoryData.cs:141-148, 192-195` |
| 5 | `KitchenLevelConfigBase.m_recipesBeforeTimerStarts`（在 Party 模式） | **不生效**，有 `ClientGameSetup.Mode == Campaign` 守卫 | `GameModes\ServerCampaignMode.cs:54`；`ClientCampaignMode.cs:55` |
| 6 | `KitchenFlowControllerBase.m_maxOrdersAllowed`（在 Campaign/Survival） | **被覆盖成 5**，只有 Competitive 走 base 路径 | `ServerKitchenFlowControllerBase.cs:84` vs `ServerCampaignMode.cs:36` / `ServerSurvivalMode.cs:44` |
| 7 | `ServerOrderControllerBase.EnableOrderExpiration` | **有效但默认 true**；置 false 可让订单永不超时 | `:40-50, 66` |
| 8 | `ClientOrderControllerBase.m_expiredDoNothingCallback` | 客户端 widget 倒计时走完**什么都不做**（设计如此） | `ClientOrderControllerBase.cs:26-28, 70` |
| 9 | `OrderDefinitionNode.m_orderGuiDescription` | 与 `m_composition` **无代码关联**，需人工保持一致 | `OrderDefinitionNode.cs:7`；`RecipeWidgetUIController.cs:141-150` |
| 10 | `LevelConfigBase.m_recipeMatchingList` | 是出单池之外的**第二份**菜谱清单，漏配会导致"订单有但工序不认" | `ClientKitchenFlowControllerBase.cs:100-120` |
| 11 | `Meteor` 类 | **不存在**，只有 `MeteorManager` | 全库 grep |
| 12 | `Raft` / `Boat` / `MovingPlatform` MonoBehaviour | **不存在**，木筏是 `PilotMovement` + `Rigidbody` | 全库 grep |
| 13 | `GameUtils.GetRoundData()` | **不存在**，`GetRoundData()` 在 `KitchenLevelConfigBase` 上 | `GameUtils.cs` 全文件；`KitchenLevelConfigBase.cs:34` |
| 14 | `s_sushi_1_3` 之类的场景名字符串 | **不在 C# 里**，存在 `SceneDirectoryData.PerPlayerCountDirectoryEntry.SceneName` | 全库 grep 0 匹配 |
| 15 | `ClientOrderControllerBase.GetRecipe(orderID)` | **无 null 检查**，查已完成的订单会 NRE | `ClientOrderControllerBase.cs:106-110` |

---

### 4.12 源码确认 / 推断 索引

**✅ 源码确认**（有类名 + 文件:行号直接支持）：

- 全部 6 个 `OrderDefinitionNode` 子类的字段与 `Convert()` 映射（4.1.2）
- `Simpilfy()` 的三步塌缩规则、`Contains()` 的贪心配对与数量敏感性（4.2.2、4.2.3）
- `Matches()` 里野卡的反向参数顺序、盘型前置校验（4.2.3）
- 三份平行"活跃订单"状态及其权威次序（4.3）
- `Update()` 的出单条件含 `|| Count < 2`（4.4）
- **`m_weight` 在出单路径上被丢弃**、实际用频率均衡权重（4.4.2）
- **超时不算删除，只扣分 + `ResetOrderLifetime`**（4.4.6）
- 连击 = 必须按 `m_comboIndex == 0`（最左）顺序交付（4.4.4）
- 小费倍率**滞后一单**、封顶 ×4（4.4.5）
- 三处硬编码 `maxActiveOrders = 5` 与 `KitchenFlowControllerBase.m_maxOrdersAllowed` 的失效范围（4.5.2）
- `CampaignLevelConfig.GetRoundData() => m_rounds[0]` 只取第 0 轮（4.6.1）
- `GetStarForPoints` 第 4 星仅在 NG+；跨平台星界未启用（4.6.4）
- `GameUtils.GetLevelID()` 返回 `Scenes` 数组下标（4.6.5）
- **源码中不存在 `s_sushi_1_3` 字符串**、不存在 `Meteor` / `Raft` / `MovingPlatform` 类、不存在 `GameUtils.GetRoundData()`（4.6.5、4.9、4.11）
- 街机大厅完整状态机、`PickTheme()` 是**均匀随机而非多数票**、`AllUsersSelected()` 本地分支、`PickLevel()` 的累积上界抽法、`WorldMapScene = "Lobbies"`（4.8.3、4.8.4）
- `IsSaveable()` 明确排除 `Party` 和 `Versus`（4.7.2）
- `SessionConfig` 的 Commit/Revert 事务语义（4.7.4）
- 生存模式的 `AddTime` / 倒扣、Horde 的波次与均匀随机抽菜（4.7.5、4.7.6）
- 传送门的 45° 朝向常量与 `m_teleportArc` 锥体（4.9.1）
- `ServerAutoWorkstation` 的 `Chop`/`Jam` 双动画分支与空实现 `OnJam()`（4.9.5）
- `SwitchStation` 是空壳标记类（4.9.6）
- 总分公式与三项构成、`SkipLevel` 后门（4.10）

**🔶 推断**（无直接代码证据，由命名/调用关系/默认值外推）：

- 蛋糕配方的具体嵌套形态与 `m_orderGuiDescription` 的 tile 布局（4.1.3、4.1.4 示例）
- `m_comboIndex == 0` 对应"订单栏最左边"（由 `List.Add` 追加顺序 + `m_order` 递增排版推得，4.4.4）
- `m_weight` / `m_freestyle*` 是"被砍掉的模式留下的残骸"（4.4.2、4.5.3）
- `s_sushi_1_3` 是场景资源命名约定，存在 `SceneDirectoryData.asset` 里（4.6.5）
- 木筏/河流关由 `PilotMovement` 承载，具体关卡配置在预制体/场景上（4.9.3）
- 陨石预制体的破坏逻辑由其它通用组件承载（4.9.4）
- `SwitchStation` 用于"压重物触发机关"、`ConveyorStation` 用于寿司/火星的自动输送（4.9.6）
- `m_PS4StarBoundaries` 等是移植版分平台难度调参的遗留（4.6.4）
- `MeteorManager` 只在主机运行（无 `Server*` 半、不走 `NetworkUtils.ServerSpawnPrefab`，4.9.4）
- OC2 没有"输"的概念，只有分数与星级（4.10.4）

---

## 五、对局生命周期与大厅

> 本章全部为 ✅ 源码确认。

### 5.1 `GameState` 状态机

`GameState.cs` 定义了全流程状态。与厨房对局相关的部分：

| 状态 | 含义 |
|---|---|
| `MainMenu` | 主菜单 |
| `PartyLobby` | **街机大厅**（合作） |
| `VSLobby` | 对战大厅 |
| `LoadKitchen` / `LoadedKitchen` | 加载厨房场景 |
| `ScanNetworkEntities` → `ScannedNetworkEntities` | 扫描网络实体 |
| `StartSynchronising` → `StartedSyncronising` | 启动同步 |
| `AssignChefsToUsers` → `AssignedChefsToUsers` | 分配厨师给玩家 |
| `StartEntities` → `StartedEntities` | 生成实体 |
| `RunKitchen` | 厨房已就绪 |
| `RunLevelIntro` → `RanLevelIntro` | 关卡开场演出 |
| **`InLevel`** | **对局进行中** |
| `RunLevelOutro` → `RanLevelOutro` | 结算演出 |

### 5.2 `InRound` 是怎么来的（对局边界）

`InRound` 是 `IFlowController` 接口上的属性，也是外部程序判断"现在能不能操作厨师"的**唯一可靠信号**。

```csharp
// IFlowController.cs
public interface IFlowController { bool InRound { get; } }

// GameUtils.cs:116
public static IFlowController GetFlowController()   // → GameUtils.GetFlowController().InRound
```

**赋值的唯一位置**是 `SetRoundBehaviourActivation`：

```csharp
// ClientFlowControllerBase.cs:176 / ServerFlowControllerBase.cs:122
protected void SetRoundBehaviourActivation(bool _enabled)
{
    if (m_inRound != _enabled)
    {
        if (_enabled) this.RoundActivatedCallback();
        else          this.RoundDeactivatedCallback();
        m_inRound = _enabled;
    }
}
```

调用点：

```csharp
// ClientFlowControllerBase.RunLevel()  :124-142
SetRoundBehaviourActivation(true);          // ← 进入对局
m_campaignAudioManager.SetAudioState(AudioState.InLevel);
while (true) {
    if (base.enabled) { OnUpdateInRound(); ...; if (HasFinished()) break; }
    yield return null;
}
SetRoundBehaviourActivation(false);         // ← 对局结束
```

```csharp
// ServerFlowControllerBase.RunRound()  :104-120  同上
```

**结论**：
- `InRound == true` ⟺ 关卡开场演出播完、进入 `InLevel`、到 `HasFinished()` 为止。
- 开场演出与结算期间 `InRound == false` —— **此期间发移动/交互按键无效**（脚本必须等它变 true 再动）。
- ⚠️ 实测：`scene` 已经切到关卡场景（如 `s_sushi_1_3`）但 `InRound` 仍可能是 `false`（正在播开场），反之结算时 `scene` 还是关卡场景但 `InRound` 已为 `false`。**不能只看场景名**。

### 5.3 服务器端对局推进

```csharp
// ServerFlowControllerBase.UpdateSynchronising()  :54-91
case GameState.RunLevelIntro:
    if (AreAllUsersInGameState(GameState.RanLevelIntro)) {
        m_levelRoutine = RunRound();
        ChangeGameState(GameState.InLevel);
    }
    break;
case GameState.InLevel:
    if (m_levelRoutine == null || !m_levelRoutine.MoveNext())
        ChangeGameState(GameState.RunLevelOutro);
    break;
case GameState.RunLevelOutro:
    if (AreAllUsersInGameState(GameState.RanLevelOutro)) {
        m_State = GameState.RanLevelOutro;
        GameUtils.RequireManager<MultiplayerController>().StopSynchronisation();
        string nextScene = GetNextScene(out o_loadState, out o_loadEndState, out o_useLoadingScreen);
        if (!string.IsNullOrEmpty(nextScene)) {
            GameSession gameSession = GameUtils.GetGameSession();
            gameSession.FillShownMetaDialogStatus();
            ServerMessenger.GameProgressData(gameSession.Progress.SaveData, gameSession.m_shownMetaDialogs);
            ServerMessenger.LoadLevel(nextScene, o_loadState, o_useLoadingScreen, o_loadEndState);
        }
    }
    break;
```

**"下一局去哪"完全由 `GetNextScene()` 决定**，它对街机模式的处理：

```csharp
// ServerCampaignFlowController.cs:179-181
if (ServerGameSetup.Mode == GameMode.Party) { o_loadState = GameState.PartyLobby; ... }

// GameUtils.cs:812-813
case GameMode.Party:
    ServerMessenger.LoadLevel(typeSettings.WorldMapScene, GameState.PartyLobby, bUseLoadingScreen: true);
    // typeSettings.WorldMapScene == "Lobbies"（LobbyFlowController.CreateLobbySession 里设置）
```

**结论（关键）**：
> **街机(Party)模式一局打完后，游戏会自己回到 `Lobbies` 场景（街机大厅），而不是停在结算画面等人点"重来"。**
> 也就是说"自动开下一把"= **在大厅里把主题投票重新投一遍**，不需要处理"再来一局"按钮。

客户端侧对应 `ClientFlowControllerBase.OnGameStateChanged`：

```csharp
case GameState.RunLevelIntro: StartCoroutine(RunLevelIntro(finishedCallback));  // 回调发 RanLevelIntro
case GameState.InLevel:      m_levelRoutine = RunLevel(); StartCoroutine(m_levelRoutine);
case GameState.RunLevelOutro:m_isFinished = true; m_outroRoutine = RunLevelEnd();
```

### 5.4 街机大厅：主题投票 → 开局

#### 5.4.1 状态机

`LobbyFlowController.LobbyState`：

```
PreSetup → (Matchmake / LocalSetup / OnlineSetup)
         → (LocalThemeSelection / OnlineThemeSelection)
         → (LocalThemeSelected  / OnlineThemeSelected)
```

`LocalSetup` 与 `OnlineSetup` 的区别就是"是否联机会话"；本机双人合作走 **Local** 分支。

```csharp
// ServerLobbyFlowController.PreSetup()  :90-113
ResetTimer(m_lobbyFlow.m_timeLimit);              // m_timeLimit = 45f
if (ConnectionStatus.IsInSession()) SetState(LobbyState.OnlineSetup);
else                                SetState(LobbyState.LocalSetup);

// SetState: LocalSetup → 立刻 SetState(LocalThemeSelection)（:134-138）
```

#### 5.4.2 投票 = 点 UI 主题卡片

主题卡片**不是按键驱动的，是 Unity UI 按钮**：

```csharp
// ClientLobbyFlowController.Awake()  :84
m_lobbyFlow.m_themeSelectMenu.CarouselButtonClicked += OnThemeButtonClicked;

// :185-196
private void OnThemeButtonClicked(CarouselButton _button)
{
    ThemeSelectButton themeSelectButton = (ThemeSelectButton)_button;
    ... SelectTheme(themeSelectButton.Theme);
}

// :567-592
public void SelectTheme(SceneDirectoryData.LevelTheme _theme)
{
    if (m_state == LobbyState.Matchmake) { ... }        // 匹配中：只记本地
    else {
        List<int> localChefIndices = GetLocalChefIndices();
        for (...) {                                      // 每个本地玩家各发一条
            m_message.m_type = LobbyClientMessage.LobbyMessageType.ThemeSelected;
            m_message.m_theme = _theme;
            m_message.m_chefIndex = localChefIndices[i];
            ClientMessenger.LobbyMessage(m_message);     // → 服务器
        }
    }
    m_lobbyFlow.m_themeSelectMenu.Hide();
    ShowSelectedThemes();
    m_lobbyFlow.m_stateStrings.Update(StateStrings.State.WaitingForOthers);
}
```

⚠️ **实测确认**：在大厅里尝试过的键盘输入**全部无效**——`SPACE` / `ENTER` / `WASD` / 方向键 / 左右 `Shift` / 左右 `Ctrl` / 左右 `Alt` / `Tab` / `Q` / `E` / `Backspace`，以及各种组合与长按（共 20+ 种），画面逐帧比对确认**光标没有任何移动**。
原因就在上面：**主题选择是鼠标/EventSystem 的 UI 事件**（`CarouselButtonClicked`），不是逻辑按键。
→ **结论：自动化"在大厅开局"必须能发鼠标事件（或用 UI EventSystem 的导航/提交）。纯键盘模拟做不到。**

#### 5.4.3 服务器收到投票后：**本地合作下一个人的一票就够**

```csharp
// ServerLobbyFlowController.SelectTheme()  :195-215
public void SelectTheme(SceneDirectoryData.LevelTheme _theme, int _chefIndex)
{
    if (_chefIndex > -1 && _chefIndex < m_userChoices.Length) {
        m_userChoices[_chefIndex] = new LobbyFlowController.ThemeChoice { m_theme = _theme, m_chefIndex = _chefIndex };
        SendAllUserThemeSelection(_theme, _chefIndex);
    }
    if (AllUsersSelected() && (...private 或 4 人...)) {
        if (m_state == LobbyState.LocalThemeSelection)  SetState(LobbyState.LocalThemeSelected);
        else if (m_state == LobbyState.OnlineThemeSelection) SetState(LobbyState.OnlineThemeSelected);
    }
}

// :641-667
protected bool AllUsersSelected()
{
    if (m_lobbyFlow.IsLocalState(m_state))     // LocalSetup / LocalThemeSelection / LocalThemeSelected
        return m_bIsCoop || ServerUserSystem.m_Users.Count > 1;
        // ↑ 本地合作直接返回 true —— 一个玩家投一票就满足
    if (UserSystemUtils.AnyRemoteUsers()) { /* 每个玩家都必须投过票 */ }
    return false;
}
```

#### 5.4.4 定局：选主题 → 随机抽一张该主题的图 → 延迟加载

```csharp
// SetState: LocalThemeSelected → ResetTimer(m_themeSelectionDuration) + StartLevel()   :148-151
// m_themeSelectionDuration = 2f

protected void StartLevel()
{
    FillUndecidedUserChoices();                       // 没投票的按"随机"补上
    SceneDirectoryData.LevelTheme levelTheme = PickTheme();
    ... m_message.m_type = FinalSelection; ServerMessenger.LobbyMessage(m_message);
    PickLevel(levelTheme);
}

protected void PickLevel(SceneDirectoryData.LevelTheme _theme)
{
    // 从 SceneDirectory 里筛：AvailableInLobby && Theme 匹配（Random 则全都要）
    // 随机取一个 entry，再按"当前玩家数"取 SceneVarient
    SceneDirectoryData.PerPlayerCountDirectoryEntry sceneVarient = entry.GetSceneVarient(ServerUserSystem.m_Users.Count);
    m_delayedLevelLoad = StartCoroutine(DelayedLevelLoad(sceneVarient.SceneName, dlcID));
}

private IEnumerator DelayedLevelLoad(string _levelName, int _dlcID)
{
    ... 等 m_themeSelectionDuration + 1f 秒（= 3 秒）...
    gameSession.TypeSettings.WorldMapScene = "Lobbies";
    ServerMessenger.LoadLevel(_levelName, GameState.LoadKitchen, bUseLoadingScreen: true, GameState.RunKitchen);
}
```

**所以完整链路是**：

```
大厅(LocalThemeSelection)
   └─ [鼠标点击主题卡片] → SelectTheme → LobbyClientMessage.ThemeSelected → 服务器
        └─ AllUsersSelected() == true（本地合作恒真）
             └─ SetState(LocalThemeSelected) → StartLevel()
                  ├─ PickTheme()：取玩家投的主题
                  ├─ PickLevel()：随机抽该主题下一张 AvailableInLobby 的图，按人数取变体
                  └─ DelayedLevelLoad()：等 3 秒 → LoadLevel(图, LoadKitchen → RunKitchen)
                       └─ 对局开始（InRound → true）
```

#### 5.4.5 计时

- `LobbyFlowController.m_timeLimit = 45f`（在线投票时限；本地合作不等）
- `ServerLobbyFlowController.Update()` 里倒计时；到 0 → `OnlineThemeSelected`
- `FillUndecidedUserChoices()`：超时后没投的按 `LevelTheme.Random` 补

#### 5.4.6 离开大厅

```csharp
// ClientLobbyFlowController.Leave()  :881-886
ServerGameSetup.Mode = GameMode.OnlineKitchen;
ServerMessenger.LoadLevel("StartScreen", GameState.MainMenu, bUseLoadingScreen: false);
```
`m_leaveButton = PlayerInputLookup.GetUIButton(LogicalButtonID.UICancel)`（:792）—— **取消键（Esc）会弹"离开大厅"确认框**，自动化里要避免误触。

### 5.5 模式判定字段

```csharp
ServerGameSetup.Mode / ClientGameSetup.Mode   // GameMode 枚举
LobbySetupInfo.Instance.m_gameType            // GameSession.GameType.Cooperative / Competitive
GameSession.TypeSettings.WorldMapScene        // "Lobbies"
```

`GameSession.IsCampaign`（:186）等属性都显式判断 `ClientGameSetup.Mode != GameMode.Party && != GameMode.Versus` —— 说明 **Party 是独立于战役的模式**，它有自己的大厅场景与关卡池。

---

## 六、寻路系统

> 本章为 ✅ 源码确认 + ⚠️ 实测验证。

### 6.1 游戏自带网格寻路（应当直接复用）

**不要自己拿台子列表当障碍**。游戏内部就有权威的可行走网格：

```csharp
// GridNavSpace.cs:6-37
public class GridNavSpace : Manager
{
    private GridManager m_gridManager;
    private bool[,] m_nodeMap;
    private Point2 m_mapOffset;

    public Point2 GetNavPoint(Vector3 _position)          // 世界坐标 → 网格点
    {
        GridIndex idx = m_gridManager.GetGridLocationFromPos(_position);
        return GetPointFromIndex(idx);
    }

    public List<Vector3> FindPath(Point2 _start, Point2 _end)
    {
        SearchParameters sp = new SearchParameters(_start, _end, m_nodeMap);
        PathFinder pf = new PathFinder(sp);
        Converter<Point2, Vector3> conv = _input => m_gridManager.GetPosFromGridLocation(GetIndexFromPoint(_input));
        return pf.FindPath().ConvertAll(conv);            // 返回世界坐标序列
    }

    private void Start()
    {
        Point3 gridHalfSize = m_gridManager.GetGridHalfSize();
        m_nodeMap = new bool[2 * gridHalfSize.X + 1, 2 * gridHalfSize.Z + 1];
        m_mapOffset = new Point2(gridHalfSize.X, gridHalfSize.Z);
        for (int i = ...) for (int j = ...)
            m_nodeMap[i, j] = m_gridManager.GetGridOccupant(GetIndexFromPoint(new Point2(i, j))) == null;
            //                ↑ 可走 == 该格没有占用者
    }
}
```

**关键点**：`m_nodeMap[i,j] = GetGridOccupant(index) == null`
→ **墙壁、边界、橱柜、台子、任何占位物都自动是障碍**。这正是"自己建模"永远做不全的部分。

访问器（`GameUtils.cs:111`）：
```csharp
public static GridNavSpace GetGridNavSpace()
```

参考实现 —— 游戏自己的 AI/物件就是这么走的（`GridNavigator.cs:44-75`）：
```csharp
Point2 startPoint  = m_gridNavSpace.GetNavPoint(base.transform.position);
Point2 targetPoint = m_gridNavSpace.GetNavPoint(_target);
List<Vector3> path = m_gridNavSpace.FindPath(startPoint, targetPoint);
// 然后沿 path 逐点 MoveTowards
```

`Point2`（`Point2.cs`）只是 `{ public int X; public int Y; }` 的简单类。

### 6.2 本项目的实现方式

在 BepInEx 插件里加了一条 `path` 命令，**直接问游戏要路径**：

```
请求: {"cmd":"path","chef":0,"tx":10.8,"tz":10.8}
响应: {"path":[{"x":9.6,"z":9.6}, ...], "count":N, "from":{...}, "to":{...}}
```

实现（`NavPath.cs`）：`GameUtils.GetGridNavSpace()` → `GetNavPoint(厨师位置)` / `GetNavPoint(目标)` → `FindPath` → 序列化成 JSON。
**Unity API 必须在主线程调用**，所以它走"桥线程请求 → 主线程执行 → 桥轮询取结果"的模式（见 §7.6）。

Python 侧只用返回的路径点逐段走；只有当 `GridNavSpace` 取不到（返回 `error`）时，才退回到自建的 A*（把台子格子当障碍）—— 那只是兜底，**不保证正确**。

### 6.3 网格尺度

⚠️ 实测：厨房是 **1.2 世界单位**一格。
证据：同一关里灶台在 `x = 10.8 / 12.0 / 13.2`，切菜板在 `z = 4.8 / 6.0`，箱子在 `z = 4.8 / 6.0 / 7.2` —— 相邻间隔都是 1.2。
（自建 A* 兜底时用 `round(x / 1.2), round(z / 1.2)` 做格子索引。）

---

## 七、自动化实测校准与踩坑

> 本章全部是 ⚠️ **实测确认**，是真正决定"能不能自动跑起来"的部分。

### 7.0 分屏双键盘的实测键位（源码里查不到，实机标定）

游戏 PC 版支持**分屏双键盘**（不需要任何手柄/驱动），两套键位互不冲突：

| 功能 | P1（左侧分屏） | P2（右侧分屏） |
|---|---|---|
| 移动 | `W` `A` `S` `D` | `↑` `↓` `←` `→` |
| 捡起 / 放下 | `左Shift` | `右Shift` |
| 切碎 / 投掷 | `左Ctrl` | `右Ctrl` |
| 加速（冲刺） | `左Alt` | `右Alt` |
| 表情 | `E` | `P` |
| 切换/其他 | `T` | — |

⚠️ 实测：**P1 与 P2 必须在大厅里各自"加入"**（各自按自己的加入键）之后才会在厨房里出现两个厨师。本机实测两个厨师都能被 `PlayerControls` 枚举到（`Player 1` / `Player 2`），并且可以分别用上表两套键驱动。

### 7.1 输入方案：键盘 SendInput（不用虚拟手柄）

- ✅ **可行**：Python 用 `ctypes` 调 Win32 `SendInput` 发键盘事件，驱动**分屏双键盘**（P1=WASD，P2=方向键）。
- ❌ **已放弃**：ViGEm 虚拟手柄 —— 需要每台机器装内核驱动 + 重启，违背"要能在其他电脑上直接跑"的要求；本机实测也因为 `ViGEmBus` 设备未真正安装而失败（`vigem_connect` 返回 `VIGEM_ERROR_BUS_NOT_FOUND`，`Enum\Root\Nefarius` 缺失，重启无效）。
- ⚠️ `SendInput` 的 `INPUT` 结构体必须写对：`dwExtraInfo` 用 `ctypes.c_size_t`（x64 下 `INPUT` 大小为 40 字节）。写错会返回 0 且 `GetLastError()` = 87（参数错误）。

### 7.2 **窗口必须在真前台，否则按键全部丢失**（最大的坑）

`SetForegroundWindow` 会**静默失败**：Windows 只允许"当前前台进程"改前台。
表现极具迷惑性 —— 代码不报错，但按键全部发给了别的窗口，游戏看起来"完全没反应"。

⚠️ 实测（修复前后对比）：
```
修复前: GetForegroundWindow() 是别的窗口 → 按空格无数次，标题画面纹丝不动
修复后: AttachThreadInput 挂到前台线程再改 → GetForegroundWindow() == 游戏窗口
        按空格 2 次 → 场景 StartScreen → Loading  ✓
```

**修复办法**（`_force_foreground`）：
```python
fg = user32.GetForegroundWindow()
tid_fg = user32.GetWindowThreadProcessId(fg, None)
tid_me = kernel32.GetCurrentThreadId()
if tid_fg and tid_fg != tid_me:
    user32.AttachThreadInput(tid_me, tid_fg, True)      # 挂到前台线程
try:
    user32.BringWindowToTop(hwnd)
    user32.SetForegroundWindow(hwnd)
    user32.SetFocus(hwnd)
finally:
    if attached: user32.AttachThreadInput(tid_me, tid_fg, False)
return user32.GetForegroundWindow() == hwnd               # 必须验证返回值
```

**两条纪律**：
1. **必须检查 `GetForegroundWindow() == 游戏窗口` 再发按键**，不能假设成功。
2. **找窗口句柄要缓存**：按进程名找 PID 会起子进程/枚举，很贵；而导航每 0.17 秒就要发一次按键。只在前台检查失败时才重找。

另外：**全屏独占（WS_POPUP）模式下抢不到前台** —— 游戏必须跑在**窗口模式**。
⚠️ 实测：游戏窗口 `(0,0)-(1711,1093)`，是前台窗口，`activate_game()` 返回 `True`。

### 7.3 位置读数必须高频

导航是"读位置 → 按键"的闭环。如果位置读得慢，等于**拿旧坐标开车**。

⚠️ 实测：把厨师位置和整块场景布局放在同一次扫描里（1 秒 1 次）时，厨师一秒能走约 4 格，闭环必然来回震荡，日志表现为反复 `[导航] 卡住, 侧移脱困`。

**修复**：把两者拆开——
- **台子/烹饪进度**：1 秒扫一次（贵：`FindObjectsOfType` + 反射）
- **厨师位置/手持物**：**0.1 秒**扫一次（只扫 `PlayerControls`，很便宜）

### 7.4 台子类型识别的坑

- 一个物体常**同时挂多个组件**（`CookingStation` 也 `RequireComponent(AttachStation)`）。
  直接按类型枚举会让同一张台子出现好几条。
  → **按 `GameObject.GetInstanceID()` 去重，并给类型排优先级**（越专有越靠前）：
  `PlateStation` > `CleanPlateStack` > `DirtyPlateStack` > `PlateReturnStation` > `CookingStation` > ... > `Workstation` > `AttachStation`
- **`Workstation` = 切菜板**（`[RequireComponent(typeof(Interactable))]`，负责 chop）；**`AttachStation` = 普通台面**（只能放/拿）。
- 台子上"放着什么"**不是字段**，而是挂在 `AttachStation.m_attachPoint`（`Stack` 则是 `GetAttachPoint()`）**下面的子 Transform**。
  → 读台面内容 = 数 `attachPoint` 的 `childCount` 并取子物体名。这是"物品传递/接力"唯一的观测手段。
- 食材还在**箱子里**时，场景里**没有它的实例** —— 而"要用什么灶、煮多久、切几片"这些参数只存在于 **prefab 资源**上。
  → 必须同时扫 `Resources.FindObjectsOfTypeAll()`（用 `gameObject.scene.IsValid()` 区分是 prefab 还是场景实例）。

### 7.5 坐标与交互标定

⚠️ 实测校准：
| 按键 | 效果 |
|---|---|
| `D` | x 增大 |
| `A` | x 减小 |
| `S` | z 减小 |
| `W` | z 增大 |

- 交互半径约 **1 格**；导航的"到达"判定取 1.8 格先粗到、再收紧到 0.8 格。
- **相邻台子只隔 1.2 格**，站远了会同时落在两三个台子的交互范围内 → **会拿错东西**。
  ⚠️ 实测：从 `crate_sea`（海苔）位置交互，拿到的是隔壁 `crate_prawn`（虾）。
- **游戏是靠"朝向"决定交互哪一个**。所以接近目标时最后一段必须**朝着目标走**，朝向才对。
  实现上用"黄金角"换方向重试：第 1 次直取，失败就绕到侧面再朝目标走。
- 取到东西后**必须校验拿到的是不是目标**，不对就放回去重试。

### 7.6 Unity API 的线程纪律

`FindObjectsOfType` / `GetComponent` / `FindGameObjectsWithTag` / `Resources.*` **只能在主线程调用**。
TCP 桥跑在后台线程，所以大扫描一律走：

```
桥线程: 设置 job kind(+参数) → 轮询等结果(带超时)
主线程: Plugin.Update → Refresh() 里发现有待办 → 执行扫描 → 写回结果
```

桥线程**只做数据搬运**（读缓存字符串、读写普通字段、入队），绝不直接碰 Unity 对象。

### 7.7 版本与部署

- 游戏是 **Mono**，所以插件编译必须对齐 **CLR 2.0.50727**：
  用 Roslyn `csc.dll` + `-nostdlib+` + 引用 `C:\Windows\Microsoft.NET\Framework\v2.0.50727\{mscorlib,System,System.Xml}.dll`。
  用 .NET 4.0 编译出的 dll 会报 `Method not found: 'System.Threading.Monitor.Enter'`。
- 因此插件里**不能用** `lock` / `Monitor.Enter(obj, ref bool)` / `HashSet<>`（在 System.Core）；用普通类 + `List<object>` 替代，字符串拼接用 `string.Format`。
- x86：`-platform:x86`。引用 `UnityEngine.PhysicsModule.dll` 才能用 `Collider`。
- 插件 dll 放在 `<游戏根>\BepInEx\plugins\`；**游戏运行时 dll 被锁**，替换必须先退出游戏。

---

## 八、结论：一条完整的自动化流程

把上面所有章节串起来，外部脚本要"全自动通关"应当这样做：

```
[0] 启动/接管
    · 确保游戏是**窗口模式**（全屏独占抢不到前台）
    · 找到游戏窗口句柄并**缓存**；每次发按键前用 AttachThreadInput 强制前台并**验证成功**
    · 连接游戏内 BepInEx 桥（TCP 行 JSON）

[1] 进入对局
    · 故事模式：菜单导航用 WASD + Enter 确认（实测：S 选中"退出"、Enter 确认 → 回主菜单）
    · 街机模式：**主题投票是鼠标 UI**，纯键盘做不到 → 需要点击主题卡片
      （本地合作下一个玩家投票即开局；服务器随后等 3 秒随机抽图并加载）

[2] 等对局真正开始
    · 轮询 GameUtils.GetFlowController().InRound，**为 true 才开始操作**
    · 同时读 scene 名确认关卡

[3] 读现场（全部由桥按月/按需提供）
    · layout：台子(语义分类 + 台面内容) / 厨师(位置 + 手持物) / 正在烹饪的东西(进度/生熟/着火)
    · live：当前挂在订单栏上的订单 + 剩余时间
    · know：食材知识表(谁要切、切几片、谁要煮、用哪种灶、多久熟/多久焦、哪个箱子出)
    · details：订单池里每道菜的**嵌套**配方树 + 要求的容器(platingStep)

[4] 选单：取剩余时间最少的那张订单

[5] 推导做法（把配方树 × 食材知识表交叉）
    · 树里被 CookedCompositeAssembledNode 包着 = 要煮；要求 m_progress == Cooked
    · 叶子名能与某个物体的 next/ing 对上 = 知道从哪个箱子取、要不要切
    · 每算出一个材料就**立刻去处理并摆到摆盘位**（厨师一次只能拿一个）

[6] 执行（每个动作都闭环验证）
    · 移动：向游戏要 GridNavSpace 路径，逐格走；每段前重新规划
    · 取料：走到货源 → 交互 → **校验拿到的是不是目标**
    · 切：放到 Workstation，切到"板上的东西名字变了"（= nextPrefab 替换）为止
    · 煮：放上灶台/锅 → **盯实时进度**，落在 (cookTime, 2×cookTime] 立刻取下（生/焦都不算）
    · 摆盘：材料放到"已经有盘子的台面"，自动进盘
    · 送餐：端起盘子 → PlateStation → 放下

[7] 对局结束
    · InRound 变 false → 停止一切按键
    · 街机模式游戏会**自动回 Lobbies 大厅** → 回到 [1] 重新投票开下一把
```

**一句话总结整个逆向的关键结论**：
> 这个游戏把"能不能走"交给 `GridNavSpace`（所有占位物自动是障碍），
> 把"菜要怎么做"交给配方树 × prefab 上的加工参数，
> 把"做好了没有"交给 `IngredientContainer` 的内容匹配（**顺序无关**），
> 把"送对了没有"交给 `platingStep` + 订单匹配。
> **这四件事全部可以从游戏内部读到，不需要猜、也不需要硬编码任何一张菜谱。**
