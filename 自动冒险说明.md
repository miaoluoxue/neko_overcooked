# 自动冒险（本机运行版）

目标：让本机常驻 Python 程序负责启动页、冒险存档、地图选关、做菜引擎和结算续玩。运行后不依赖聊天窗口，也不需要持续调用云端模型。

## 使用入口

- `Start-Adventure.cmd`：启动 Steam 游戏并运行常驻控制器。窗口保持打开。
- `Pause-Adventure.cmd`：暂停 AI 并释放厨师控制，游戏计时不会暂停。
- `Resume-Adventure.cmd`：继续 AI。
- `Stop-Adventure.cmd`：停止整个自动流程并释放控制。

不要同时运行旧的 `Start-AI.cmd` 或其他做菜脚本。

## 当前验证情况

- 原版做菜桥接 DLL 已安装并能读取游戏状态、控制厨师。
- 最新自动菜单 DLL 已安装，实机验证后台通过启动页、加入两名本地玩家、继续冒险存档。
- 教程专用双人传菜流程实际完成：**148 分、3 星、送达 6 份、漏单 0 份**。结算证据保存在 `runtime/tutorial-verified-result.json`。
- 实机验证自动处理教程结算、从地图选择并进入 **1-1（s_sushi_1_1）**。
- 王座房需要走向出口；这一动作已通过游戏输入接口验证，并补入常驻程序。
- 停止命令已验证能停止控制器并释放游戏控制。
- 发现单人教程的两个厨师共用玩家身份，已增加身份检查，避免同时抢同一玩家输入。
- 双人控制的桥请求已串行化，避免原桥单个主线程请求槽被两个线程覆盖。
- 自动流程专用引擎取消了推测性批量投掷备料，先携带当前食材完成加工。
- 9 项本地测试通过：存档保护、锁定关卡过滤、弹窗限制，以及此前的菜谱、停止和传送带修复。
- **后续关卡仍使用仓库的通用规则引擎，尚未逐关验证，不能承诺全程通关或每关高分。**
- `run_tutorial.py` 根据教程的实际工位分配取料、传递、切菜、装盘和出餐，专门处理上下隔开的厨房。

## 行为

优先继续已有冒险存档；没有可继续存档时，只选择空槽。地图上只选择游戏判定已解锁的关卡，优先未获星关卡。程序不修改星数、分数或解锁条件。

关卡或菜单连续四分钟没有状态进展会退出并记录错误。它不会自动修复任意关卡的做菜策略；当前代码是规则式做菜 AI。电脑需要保持开机且游戏运行。

## 日志与状态

- `runtime/campaign-status.json`：常驻程序心跳、关卡、分数、是否运行和错误。
- `runtime/campaign.log`：本次后台验证的菜单动作日志。
- `runtime/campaign-cooking.log`：做菜行为日志。
- 游戏目录 `BepInEx/LogOutput.log`：两个游戏内模组的加载和桥接日志。

源码入口是 `run_campaign.py`；菜单桥源码是 `AutoCampaignBridge.cs`，本地端口为 48779，原做菜桥端口为 48778。
