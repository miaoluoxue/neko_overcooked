# 取锅食材防串菜

第 40 局 1789710653.341，日志记录取 BurritoMeat 时手中盘已含 pasta+prawn。普通 cook 的 `_get_plate_for_pot` 只排除重复材料，没有核对当前菜谱，导致海鲜盘混入肉。

新增 `_plate_can_take`：有 flow 时检查既有材料与待取材料的并集不得超出当前配方必需/可选材料；仍拒绝重复取同种食材。普通 cook 传入 flow，手持盘、组装台面盘和拿起后的盘都检查。未知 flow 的救锅路径仍保持原有重复材料保护，后续可继续收紧，但不可假定它属于当前订单。

依据：读取本机 CompositeAssembledNode.AssumeTypeMatch IL_0000–0038，双方组合必须相互包含，额外食材导致不匹配。84 项测试通过；恢复只检查重复材料的旧逻辑时，新测试出现 1 个断言失败、0 个运行错误。

不重启当前厨师，下次自动开局加载。气球关尚未通过。
