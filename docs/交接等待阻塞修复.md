# 交接等待阻塞修复（2026-09-18）

## 现场

会话 20260918-144553 第 7 局，P1 在时间戳 1789715614–1789715630 连续把 Pasta、Prawn、Fish 的 fetch 全部判为“等待已交接的切菜产物，保持空手接回”。此前的判断只要存在任何有效 chop 交接，就阻止所有取料，最长持续到 45 秒交接超时。

## 修复

- 只阻止重复领取已交接的原料，允许准备其他原料。
- 使用知识表的原料→切好食材关系，因此 Fish 和交接记录 UncookedFish 仍能对应。
- 收到 ChoppedFish 时，同时完成对应的 fetch Fish 和 chop UncookedFish，避免因为两个名字不同又去取一条鱼。
- 没有改变加工完成的物理判据。复核 ServerWorkableItem.HasFinished，IL_0000–0015 仍是进度达到 stages−1；原料与产物对应关系来自 ItemKnowledge.cs 的 GetNextPrefab。

## 验证

88 项测试通过。分别在内存恢复“全局禁止取料”和“只按同名完成 fetch”的旧逻辑，两项回归测试均按预期失败。由下一局自动启动的厨师加载更新，保留当前局与游戏进程。

实机结果：加载修复的第 9 局 s_balloon_2_3 获得 322 分、交付 5 份，超过 260 分的一星门槛，passed=true。已自动进入下一场景 s_balloon_2_2；首局仍在运行。
