# 2026-09-18 空锅归位修复

故事 1-5 的 seq36 得分 56，未达到 80 分。日志 1789699243.4336667 的快照显示，两口空平底锅均在普通台面上，而两个兼容灶台为空；厨师反复尝试把切好的番茄放到空灶上。期间灭火和糊锅清空确实成功，不能把这次失败全部归因于不会灭火。

新增 `neko/cookware_recovery.py`，仅将普通台面或自己手里的空锅送到空闲且类型匹配的灶上；尊重队友占位，放置前重读目标，放置后验证锅已经挂回灶台。不会搬正在烹煮或带食材的锅，也不会为归位丢掉手持食材。

依据：`CookingStation.CanAddItem` IL_001c–002e 检查加热设备类型，IL_0030–0048 接受空容器。完整反编译记录见 `cookware-return-evidence.txt`。

验证：73 项测试通过；在内存中移除忙碌锅保护后，回归测试失败，证明能抓到该安全边界的退化。

实机：未中断 seq37；该轮使用修改前已加载的控制器，以 86 分、1 星通关，不能把此次通过归功于新修复。后续关卡新启动的控制器自动加载修复；具体空锅归位动作仍需由后续实机日志验证。
