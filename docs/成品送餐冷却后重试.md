# 成品送餐冷却后重试（2026-09-20）

第 28 局 P1 放下成品后静止超过 123 秒；P2 日志明确显示 serve_any 被“本轮已经做过 2 次”排除。移动台面临时接近失败消耗了整份计划的次数预算。

serve_any 现在保留杂活总开关、可达性和菜品复核，使用已有 step_benched 失败冷却，不受杂活永久次数上限限制。冷却期间不选，结束后重新评估，不会因两次临时失败丢弃整盘料理。只改规划重试规则。

新增回归覆盖超次数仍可重试、冷却中拒绝、总开关关闭拒绝。旧代码失败，修改后 96 项测试通过。

2026-09-20：送餐候选失败记录台面名称、位置和时间。至少退避2秒且同一台面位移超过1.2后，解除该菜谱的送餐冷却，重新进入既有可达性与交互核验；静止目标保持原冷却。修复调度状态失效问题，不改变游戏交互机制。test_delivery_cooldown_rechecks_changed_counter_position 在旧代码失败，修复后全套99项通过。

2026-09-20：实机第64局P2在另一平台糊锅上反复等待45秒（1789866019开始，1789866065超时）。clear_burnt_pot 在接近未拿起的锅失败时释放黑板认领和本地任务，退避10秒，避免 _burnt_cleanup 阻塞全部后续工作。已拿起的锅继续保留恢复流程。调度修复不改变交互机制。回归 test_unreachable_burnt_pot_releases_cleanup_job 在旧实现失败，全套100项通过后通过现有supervisor加载。
