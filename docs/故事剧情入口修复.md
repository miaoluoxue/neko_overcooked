# 故事剧情入口修复

2026-09-18：1-6 seq39 以 372 分、2 星、8 单成功、0 单失败完成。旧桥只枚举 LevelPortalMapNode，遗漏 MiniLevelPortalMapNode 和其子类，因此未完成的城堡剧情不在候选中；2-1 解锁前，选关器误入了凯文 1。

桥现同时枚举烹饪和剧情入口，使用虚属性 LevelIndex（兼容 MultiLevelMiniPortalMapNode），从真实存档读取 Completed。剧情入口标记 story=true；仅允许游戏 IsLevelUnlocked 已确认解锁的入口。

选择器优先未完成剧情，再选择未通关主线；已完成剧情不会因零星而反复进入。没有未通关主线时优先补主线星数，不因隐藏关零星无限重试。所有 Throne_Room 场景都复用现有离开城堡的控制流程。

依据：ServerMiniLevelPortalMapNode.OnAllowedSelection IL_0038–0041 调用 OnSelectMiniLevelPortal，variant 为 0。GameProgress.GameProgressData.IsLevelChainComplete 从 PreviousEntriesToUnlock 检查前置完成状态。反编译记录见 story-portal-evidence.txt。

验证：桥编译成功，74 项回归测试通过；内存恢复旧零星优先算法时，剧情优先测试失败。实机重启并继续现有存档后，已通过新桥进入 Throne_Room_02，未改写解锁或星数。
