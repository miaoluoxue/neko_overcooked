# 已接收原料的取料进度（2026-09-20）

会话 20260920-044214 第 22 局：P1 手持 Pasta，但 fetch Pasta 仍在 pending，cook Pasta 因前置未完成被拒绝；随后放下、地面库存重新拾取，形成循环。

在接收食材的进度核对中，若实际手持与 fetch 目标匹配，紧随其后的 pending 步骤是同原料 cook，则只移除 fetch。保留 cook/assemble，不推断熟度，也不跳过 chop。这是规划状态修复，不改游戏机制或存档。

回归覆盖拾取后仅完成 fetch、错误食材不改进度、已经进入 cook 不跳步；旧实现失败，修复后 94 项测试通过。
