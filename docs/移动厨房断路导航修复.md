# 移动厨房断路导航修复

2026-09-18，s_balloon_2_3 seq3 日志 1789701559–1789701562 显示两人已端着完整料理，却反复遍历不可达的静态路径点。实时地形报告厨房之间不连通，但 `navigate_smart._plan` 在实时 A* 返回空时继续尝试原生静态路径及仅含台面障碍的 Python 路径，两者都不能证明移动厨房间存在地板。

现在实时地形有效时，以其路径结果为准；仍允许尝试移除暂时动态阻挡以及已有的控制台搭桥逻辑，但不会因真实断路而退回忽略地板的路线。没有实时地形时保留原有后备路径。

75 项回归测试通过；在内存中恢复“实时路径为空则采用静态路径”的旧逻辑后，断路测试失败。seq4 中通过 supervisor 的 resume 命令重建厨师进程，保持当前对局及游戏进程。桥健康，无解锁或存档修改。

本修复解决已确认的错误回退，不代表气球关已过。前两次最终结果为 36 和 -150；需继续验证移动厨房接通时的实际送餐率，以及备料/交接是否还有其他阻塞。

## 后续：接近层不得绕过导航失败

2026-09-18 第 16 局期间复查调用链：`_approach` 丢弃了 `navigate_smart` 的 False，继续 face 和最后 8 次直接方向键微调。这会绕过前面的实时地形保护。现在导航失败换下一候选；最后仍未到达时返回失败，不进入直接微调，也不因未指定 want 虚报成功。

80 项测试通过。新增用例模拟两条路线均拒绝，要求无直接按键、无转向及额外兜底；内存恢复旧行为后 1 个断言失败、0 个运行错误。此改动只守住导航返回值，不改变游戏交互距离/碰撞规则。通过 supervisor resume 重载厨师，仍需以实际结算评估整关结果。
