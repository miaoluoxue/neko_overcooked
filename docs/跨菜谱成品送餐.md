# 跨菜谱成品送餐（2026-09-20）

会话 20260920-044214 可见 P1 放下蘑菇意面而 P2 仍执行肉意面。`_chore_candidates` 只为当前 flow 查找完成料理，另一种成品不会进入送餐评分。

现在按当前订单的各个菜谱检查成品并去重；`op_serve_any` 按候选目标解析仍在订单中的菜谱，使用该菜谱进行原有取盘复核和交付验证。目标菜谱已消失则拒绝。交付其他菜谱不会完成当前菜谱的步骤，原 `_flow_completed_by` 的目标匹配保留。

只调整规划候选和菜谱上下文，不改变游戏机制、分数或存档。新增回归覆盖其他菜谱候选、同菜谱去重、正确交付上下文及订单消失拒绝。旧代码失败，修改后 93 项测试通过。
