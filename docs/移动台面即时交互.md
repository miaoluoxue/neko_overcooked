# 移动台面即时交互（2026-09-20）

恢复会话 20260920-044214 前两局，成品放上移动台面后接菜侧多次接近失败。排查发现 `_approach` 在导航前没有检查当前实时目标，即使已能交互也先追格点，可能错失移动台面的交互窗口。

机制依据：Assembly-CSharp.dll 中 InteractWithItemHelper.IsColliderInArc，IL_0000–005e，距离取碰撞体表面并检查朝向。修改仅在工位身份已确认、实时 `_aim_ok` 通过时跳过导航；后续菜品核验、拿放结果核验保留。

回归用例在旧代码上失败，修复后 91 项测试通过。下一局自动加载；是否改善通关表现仍需实机验证。

实机验证：会话 20260920-044214，第 3 局送出 1 份（基础分 60、小费 3、结算 -57），第 4 局最后 2.3 秒已送出 1 份（基础分 60、小费 5、总分 -55）。修复前同会话第 1、2 局均为零送餐。接力恢复，但仍低于 220 分通关线；这不能证明整关问题已解决。

后续修复：_approach 的微调循环仍沿用导航前坐标。每次微调前现在按工位 ID 重取位置；移动超过既有 0.5 单位阈值则走原有有界重规划，达到上限或工位消失时停止，不继续朝旧位置发键。机制依据仍为上文 IsColliderInArc 的实时表面/朝向判断。新增测试模拟重规划用尽后台面移动：旧实现继续发 8 次按键，新实现不发；95 项测试通过。
# 2026-09-20：到站后的实时交互优先于位置刷新

`_approach` 到达站位或进入微调后，原先先检查工位是否移动超过 0.5；刷新次数用完就失败，即便游戏已经给出了正确的交互目标。这会丢弃可用的靠岸瞬间。

现在先用最新地图、工位坐标及游戏交互目标执行 `_aim_ok`，成功就交回原有拿放核验；只有仍不可交互才重新寻路。未放宽交互半径，也未跳过实际手持变化核验。

机制依据：重新读取 `Assembly-CSharp.dll` 的 `InteractWithItemHelper.IsColliderInArc`，IL_0000–005e 以实时碰撞体表面距离与朝向判定，不以导航期间位移判定。

回归测试 `test_moving_counter_live_interaction_wins_over_refresh_limit` 覆盖到站、微调两个时点；修改前两个子用例均失败，修改后全套 97 项通过。通过现有 supervisor 的 resume 命令加载，不另启控制服务。实机送餐结果另行记录。

实机验证：第 41 局通过原 supervisor 换成 teamPid 21164；日志出现新增的到站交互分支，双厨继续取料、传料、煮料和洗盘。加载后确认交付 Pasta_MeatOnly_New 一份，得分 65，尚未达到 220 分通关线；该结果只验证恢复推进，不证明整体效率已解决。

2026-09-20 08:09：传菜台选择优先采用状态中的 placement_target，允许重试此前失败、但当前已成为放置目标的空移动台面。仍须通过接近、实时空位、手持不变、对齐及落盘验证。回归 test_ferry_prefers_live_dock_even_if_previously_attempted 在旧实现选错台面时失败，修复后全套98项通过。未改变游戏交互机制，只调整候选选择。
