# 地面交接回捡修复

2026-09-18，气球关 `s_balloon_2_3` 第 8 局仍未通过（-150，0 出餐）。桥和 supervisor 正常。

证据：`runtime/sessions/20260918-110700/cooking.log` 的 1789702692.917 记录 P2 把 Pasta 放到了队友可达地面，并登记 cook 交接；1789702693.038 又开始回捡。1789702701.835 的候选表因“已经交给队友”禁止自行 cook；随后丢下、回捡反复发生。

原因：候选评分遵守交接台账，但优先执行的 `_recover_ground_chop` 绕过了台账，看到可达食材就抓取。现在该入口也调用 `handoff_live`；有效交接期间留给队友，超时后允许重新取用。

交互依据：已读取本机 `Assembly-CSharp.dll`，`ServerPlayerControlsImpl_Default.ReceivePickUpEvent` IL_0012–0066：空手、存在有效拾取处理器且 CanHandlePickup 成功后才执行 HandlePickup。这是实际拿走物品；不能当成无副作用的库存检查。此次只改调度入口，没有改游戏判定。

验证：76 项测试通过。新增测试覆盖有效交接禁止拾取、过期后重新拾取。内存中移除守卫恢复旧逻辑，测试出现 1 个断言失败、0 个运行错误，确认能够抓到回归。

第 8 局结束后通过 supervisor 命令恢复连续推进，后续新厨师进程加载修复。此改动消除一个已确认的空转循环，不代表整个气球关已通过；移动台面导航、完整菜送达仍需实机验证。

## 后续：尊重下游步骤等待期

第 14 局 1789704227–1789704228：P2 的 cook UncookedFish 已进入冷却等待，但地面库存入口仍经 chop/cook 候选反复捡 ChoppedFish。随后所有工作不可做，又丢下重捡，甚至阻止了救 Prawn 锅的杂活。现在取已加工库存前检查同材料后续 cook 的等待期与交接状态；过期后可恢复取用。79 项测试通过；移除该守卫，新扩展测试出现 1 个断言失败，0 个运行错误。通过 resume 重载厨师，仍需验证整关成绩。
