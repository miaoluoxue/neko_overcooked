# 《胡闹厨房2》双厨自动通关 Mod — 方案定稿

> 状态：讨论定稿文档。无 LLM、全脚本；**不做 Python 假状态仿真，直接在真游戏里开发 C# Mod**；
> 规则 1:1 对齐游戏真实逻辑；三模式（合作/失误/捣蛋）叠在通关逻辑上，模式个体级随时可切。
> 依据：OC2 反编译（Mono/x86/Harmony 可行）+ 真实关卡逻辑源码确认。

---

## 0. 目标

一个 C# BepInEx Mod（跑在 Overcooked2 游戏进程内），驱动**两个独立个体**自动完成关卡：
- 主脑（任务黑板）统一派单与调度工作站，个体半自主拆步骤执行
- 三种个体模式：合作 / 失误 / 捣蛋，随时开关热切换
- 全自动通关，规则完全对齐真游戏（计时/分数/星级/订单节奏）
- 不做 LLM、不做直播状态、不做时间线导演

---

## 1. 关键决策记录

| # | 决策 |
|---|---|
| D1 | **不做 Python 假状态仿真**，直接 C# Mod 在真游戏里开发调试 |
| D2 | 规则 **1:1 对齐真游戏**：150s 计时 / 分数评星 / 单超时消失 / 保护期 / 5 单上限 / 权重随机来单 |
| D3 | **直接双厨**开发（不做单厨过渡） |
| D4 | 分工 = **主脑（黑板）中心化**：派订单级任务、调度工作站占用 |
| D5 | 个体 **半自主**：主脑只派"订单级"任务，拿/切/煮/装/送步骤自己拆 |
| D6 | 三模式**个体级**，随时热切换（外部开关 / 内部良心） |
| D7 | 合作=正常带自然失误；失误=笨拙；捣蛋=抗命（有度、可变、可被主脑察觉与降级） |
| D8 | 捣蛋处理：容忍度 → T1 催办（个体掷骰）→ T2 改派 + 派简单任务 → 模式切走恢复 |
| D9 | 双体都捣无人接单 = 本局失败（预期行为，不强行保底） |
| D10 | 无 LLM；猫娘未来 = 调 `set_mode` 的开关 |

---

## 2. 总体架构（Mod 内一体化）

```
┌──────────────────────────────────────────────┐
│ Overcooked2 游戏进程                          │
│  ┌────────────────────────────────────────┐  │
│  │ C# Mod (BepInEx + Harmony)             │  │
│  │                                        │  │
│  │  主脑 (任务黑板)                        │  │
│  │  · 派订单 / 调度工作站 / 容忍度监视      │  │
│  │        ↓ 派活              ↑ 汇报/要活  │  │
│  │  ChefA(半自主个体)    ChefB(半自主个体) │  │
│  │  · 模式: coop/clumsy/sabotage 随时切    │  │
│  │  · 自己拆步骤: 取→切→煮→装→送          │  │
│  │        ↓ 决策动作        ↑ 状态         │  │
│  │  状态读取 + 动作执行 (读游戏对象/驱动输入)│  │
│  └────────────────────────────────────────┘  │
│  游戏对象: OrderController/Chef/Station/Timer │
└──────────────────────────────────────────────┘
```

- Mod 直接在游戏进程内读对象、发动作——**不需要 TCP/HTTP 通信层**（那套是 Python 客户端方案的残留，删除）。
- 逻辑全在 C#：主脑/个体/三模式/真实规则全在游戏内实现。

---

## 3. 主脑（任务黑板）

**职责**：
1. **派订单级任务**：哪个订单给谁做（分配规则见下）
2. **调度工作站**：个体要用砧板/灶台前问主脑"空不空"；主脑派活时就保证两人台子不重叠
3. **容忍度监视**：对每个体维护容忍度，驱动捣蛋处理流程（§5）

**分配规则（待细化，初稿）**：来新单时，找"空闲且容忍度正常"的个体派；两人都闲按轮换或就近。个体完成一单后向主脑汇报、领下一单。

**状态**：
```
黑板 = {
  订单: [{id, recipe, 状态: 未派/进行中/完成/超时, 派给谁}],
  工作站: [{id, kind, 占用: A/B/空闲}],
  个体: [{id, 模式, 当前订单, 容忍度, 状态: 空闲/忙}]
}
```

---

## 4. 个体（半自主执行体）

每个个体 = 一个状态机 + 一个"当前订单任务"：

- **决策层（低）**：主脑只给"做订单#N"，个体自己拆：
  `去洋葱箱拿 → 去砧板切 → 去灶台煮 → 去盘子堆拿盘 → 盛汤 → 送餐口交付`
  每步先问主脑要台子，完成判定 = 读游戏状态达成（如持有物==已切洋葱 且 人在砧板旁）。
- **执行层**：把子目标翻译成动作发往游戏（移动/交互）。
- **模式字段**：`mode = coop | clumsy | sabotage`，外部 `SetMode(id, mode)` 随时热切，下一决策生效。

**模式含义**：
| 模式 | 行为 |
|---|---|
| 合作 coop | 主脑派啥好好做，带低概率自然失误（发呆/绕路/多切/忘盘折返） |
| 失误 clumsy | 想做对但笨手笨脚：高失误率、反应慢、常弄砸（可恢复） |
| 捣蛋 sabotage | 抗命：领了单不干正事、净搞破坏；间歇性 + 可变（§5） |

---

## 5. 捣蛋：可变性 + 主脑处理流程

### 5.1 捣蛋的可变性（不机械）
个体不是"坏30秒好30秒"定时器，而有**心情/良心状态**波动：
- 频率可变：每个空闲决策点按当前良心值掷骰（良心低老使坏、良心高认真干）
- 强度可变：轻（挡路/慢）→ 中（半成品放错台）→ 重（倒队友菜/烧糊）
- 良心漂移：随时间随机游走；队友求助/被主脑点名时回升（"被发现了，老实一会"）
- 情境收敛：订单快超时/局面快崩时捣蛋概率自动降

### 5.2 主脑处理流程（容忍度驱动）
```
① 常态: 派订单级任务, 个体自己拆步骤执行
② 检测: 个体领单却没进展 / 净干破坏 → 容忍度↑
③ 催办(T1): 主脑提醒"去做你订单的下一步"
   └→ 个体内部掷骰: 听劝(容忍度降) 或 继续捣(容忍度继续↑)
       (sabotage 模式大概率不听, 切回 coop 自然听)
④ 改派(T2): 把它没干完的订单转给另一个体
   同时给捣蛋个体派"简单任务": 拿盘子/端菜等跑腿活, 不给关键工序
   └→ 简单任务期间仍可使坏, 但频率压低(约正常捣蛋一半)
⑤ 恢复: 模式切走(外部开关/良心发现) → 恢复正常派单
   一直不结束 → 一直干简单活(无害但没贡献)
⑥ 放弃: 两个体都在捣、没人能接单 → 本局失败(预期)
```

---

## 6. 真实游戏规则对齐（判据 = 不自定义）

Mod 直接复用/读取游戏自身机制，不另造判据。反编译确认的真实逻辑：

| 机制 | 真实值/出处 | 对齐方式 |
|---|---|---|
| 整局计时 | `RoundData.m_roundTimer`（≈150s，随关变） | 读游戏计时器，不自己计时 |
| 关卡菜数 | `RecipeCount` | 读 RoundData |
| 板上最大单数 | `m_maxActiveOrders = 5` | 读/遵守 |
| 每单存活 | `m_orderLifetime`（超时消失=失败） | 读；超时单移除 |
| 两单间隔 | `m_timeBetweenOrders` | 读 |
| 保护期 | `m_recipesBeforeTimerStarts`（先送几单才起总表） | 读；遵守 |
| 来单顺序 | `GetNextRecipe()` 权重随机 | 不干预，来啥做啥 |
| 关卡结束 | 时间到 → 结算分数 | 读游戏结算 |
| 过关 | 分数评星，1 星 = 过关 | 以游戏判定为准 |

> 结论：**脚本只负责"尽量多送、送对"**。所有节奏/判负/结算都交给游戏本身，Mod 只做决策与执行。

---

## 7. 技术地基（已侦查 ✅）

| 事实 | 出处 |
|---|---|
| 引擎 = **Mono**，无 IL2CPP | `Managed/Assembly-CSharp.dll` 存在，无 il2cpp_data |
| exe = **x86** | PE 0x014C → 需 BepInEx x86 |
| 反编译完成 | `E:\pythonxx\tkry\overcooked_decomp\`（2365 文件） |
| 当前订单可读 | `ClientOrderControllerBase.m_activeOrders[]` → RecipeListEntry |
| 送餐成功/失败回调 | `OnFoodDelivered(bool, OrderID)` / `OnOrderExpired` |
| 模式核心 | `ServerCampaignMode` / `ClientCampaignMode`（GameModes/） |
| 订单调度 | `ServerFixedTimeOrderController`（OrdersConfig: maxActive=5, lifetime, interval） |
| 关卡配置 | `CampaignLevelConfig.m_rounds` / `KitchenLevelConfigBase` |
| 厨师实体 | `ServerPlayerControlsImpl_Default` / `ClientLocalAvatarSynchroniser` |
| 操作契约 | `PlayerControls.ControlSchemeData{ pickup, worksurfaceUse, dash, moveX, moveY }` |
| 台子/物品 | `CookingStation/ChoppingStation/AttachStation`、`AssembledDefinitionNode` 食材链 |
| 输入层 | 自带 InControl + XInputDotNetPure |

---

## 8. 开发顺序（全在真游戏内）

```
M1  BepInEx x86 装入 OC2 + HelloWorld 插件验证加载
M2  状态读取: 读出订单/计时/厨师/台子/持有物并日志验证
M3  动作执行: 驱动一个厨师动起来(移动/拿/切/送), 实测直驱 vs ViGEm
M4  主脑+个体最小闭环: 黑板派1单, 个体拆步骤, 做一道菜并送掉
M5  双厨协作通关一关: 分工+台子调度, 合作模式, 以游戏星级判定过关
M6  三模式逐个叠: 合作自然失误 → 失误笨拙 → 捣蛋(容忍度/催办/改派)
M7  打磨: 分数/星级达标, 卡死恢复, 节奏压力应对, 模式外部开关(未来猫娘接口)
```

每步真游戏验证；M1–M3 是地基（读得到+动得了），M4–M5 通关，M6 模式。

---

## 9. 技术要点 / 风险

- **操控执行（M3 实测）**：全脚本、逻辑在 Mod 内，倾向 **Harmony 直驱游戏输入**（设 moveX/moveY/触发键，精确可控，易叠失误噪声）；ViGEm 双虚拟手柄作对照实测。以 M3 实测定。
- **卡死恢复**：子目标超时（锁被占/状态错乱）→ 放弃当前步骤回报主脑重规划，不无限等待。
- **风险**：x86 BepInEx 兼容、Harmony hook 对本地权威侧（ServerXxx）、游戏更新破坏 hook、分数制下"过关"阈值随关变化（以游戏结算为准，不硬编码）。

---

## 10. 遗留待确认（不阻塞 M1–M3）

1. 主脑订单分配规则细化：轮换 / 就近 / 谁闲谁接（M4 前定）
2. 操控执行：直驱 vs ViGEm 由 M3 实测决定
3. 未来"模式外部开关"接口形态（HTTP / 热键 / N.E.K.O 插件桥）——M7 再议

---
状态：讨论定稿 · 待 M1 开工指令
