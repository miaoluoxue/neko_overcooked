胡闹厨房2 脚本通关技术文档：手柄Hook · 地图解析 · 360°自由寻路
文档版本：2.0
技术栈：Unity 2017.3 + BepInEx 5 (x86) + HarmonyLib + Mono Injection
目标：通过脚本程序化控制角色，以最优路径完成关卡，不修改游戏原有物理规则与碰撞逻辑

一、开源项目地址汇总
1.1 核心框架与工具
项目	地址	用途
BepInEx 5	https://github.com/BepInEx/BepInEx/releases	Unity Mod注入框架，必须选择 x86 版本
HarmonyLib	https://github.com/pardeike/Harmony	.NET运行时方法补丁库
AssetStudio	https://github.com/Perfare/AssetStudio	Unity资源解包工具，用于静态地图数据提取
UnityPy	https://github.com/K0lb3/UnityPy	Python版Unity资产提取/编辑模块，基于AssetStudio开发
1.2 TAS与输入注入参考
项目	地址	核心功能
overcooked-supercharged	https://github.com/hpmv/overcooked-supercharged	高层TAS开发框架，基于原始按钮输入，支持逐帧控制与精确输入注入
OvercookedAI	https://github.com/Extremelyd1/OvercookedAI	概念验证AI玩家，使用Mono Injection在游戏内执行C#脚本
BepInEx.UnityInput	https://github.com/erodozer/BepInEx.UnityInput	基础Harmony Hook，允许插件拦截和覆盖Unity的全局输入响应
1.3 Hook与移动干预参考
项目	地址	核心功能
Overcooked2-Jetpack	https://github.com/gua248/Overcooked2-Jetpack	展示如何Hook重力与天花板碰撞逻辑，是学习Hook物理系统的范本
Overcooked2-ManyRecipes	https://github.com/gua248/Overcooked2-ManyRecipes	包含完整HarmonyLib补丁代码范例（如 RoastPatch.cs）
OvercookedTool	https://github.com/hadeutscher/OvercookedTool	存档转换工具，可作为文件格式分析的参考
二、模块一：手柄控制Hook方案
2.1 核心原理
手柄控制Hook的本质是在游戏读取输入之前拦截Unity的输入系统，注入脚本计算出的指令，而非修改游戏逻辑本身。BepInEx.UnityInput项目提供了基础的Harmony Hook，允许插件拦截并覆盖Unity的全局输入响应。

2.2 实现路径
路径A：Hook Unity Input类

使用Harmony的 [HarmonyPatch] 属性拦截 UnityEngine.Input 中的关键方法：

csharp
using HarmonyLib;
using UnityEngine;

[HarmonyPatch(typeof(UnityEngine.Input), "GetAxis")]
class InputAxisPatch
{
    static bool Prefix(string axisName, ref float __result)
    {
        // 拦截移动轴输入
        if (axisName == "Horizontal" || axisName == "Vertical")
        {
            // 返回脚本计算出的方向值
            __result = ScriptInput.GetAxis(axisName);
            return false; // 跳过原始方法
        }
        return true; // 其他轴保持原逻辑
    }
}
路径B：定位游戏内部InputManager

根据反编译信息，《胡闹厨房2》的 Assembly-CSharp.dll 中存在 Game.Input.InputManager 类，它管理所有输入，包括动作映射（action maps）、设备配对（device pairing）和控制方案（control schemes）等。直接Hook这个类的输入处理方法，可以更精准地注入手柄指令。

路径C：参考TAS框架的输入注入机制

overcooked-supercharged 框架实现了 TASInput 功能，可以发送精确的控制器、鼠标和键盘输入到游戏。该框架基于原始按钮输入，其文档详细讨论了《胡闹厨房2》的TAS实现方式。这是理解输入注入机制的最佳参考。

2.3 编译依赖
参考 Overcooked2-ManyRecipes 和 Overcooked2-Jetpack 的编译配置，需将以下DLL复制到项目的 lib/ 目录：

游戏目录 (Overcooked2_Data/Managed/)：

Assembly-CSharp.dll

UnityEngine.dll

UnityEngine.CoreModule.dll

UnityEngine.UI.dll

UnityEngine.IMGUIModule.dll

UnityEngine.AnimationModule.dll

UnityEngine.AudioModule.dll

UnityEngine.ParticleSystemModule.dll

BepInEx目录 (BepInEx/core/)：

0Harmony20.dll

BepInEx.dll

BepInEx.Harmony.dll

2.4 关键注意事项
Jetpack MOD的README明确指出：手柄玩家需要设置一个键盘按键，并在Steam中通过控制器布局（Controller Layout）将手柄按钮映射到该键盘按键。这意味着游戏本身可能对原生手柄输入的Hook存在限制，走键盘事件映射是更稳定的方案。

三、模块二：地图解析
3.1 静态资源解析（AssetStudio / UnityPy）
AssetStudio 是一款跨平台的Unity资源探索、提取和导出工具，支持AssetBundles和内置资源。通过 File → Load folder 加载游戏资源文件夹，或使用 File → Extract folder 批量导出，可以获取关卡的静态数据，包括物体层级、变换信息、碰撞体等。

UnityPy 是AssetStudio的Python移植版本，支持通过类型树结构（typetree）以原生形式编辑所有对象类型。使用 pip install UnityPy 即可安装，适合需要脚本化批量处理地图数据的场景。

3.2 运行时内存解析（推荐方案）
对于脚本化通关，更优的方案是通过Hook游戏加载关卡的方法，在内存中直接读取已经实例化的游戏对象。这需要：

使用 dnSpy 或 ILSpy 反编译 Assembly-CSharp.dll

搜索 Level、Scene、Load、AssetBundle 等关键词，定位关卡加载相关的方法

在Harmony的 Postfix 中读取已实例化对象的 transform.position、碰撞体信息等，构建实时的地图数据模型

3.3 关卡动态性说明
《胡闹厨房2》的关卡并非完全静态。部分关卡存在动态关卡架构，如Level 3-1（海鲜关卡）和Level 4-2（矿坑）中，厨房的物理布局会在一轮游戏中途发生变化。此外，部分关卡包含移动地板、传送门、移动走道和不可通行的火焰等障碍物。地图解析方案必须能够处理这些动态变化，建议采用运行时内存解析而非纯静态解析。

四、模块三：360°自由寻路机制
4.1 现状说明
目前社区中不存在专门实现《胡闹厨房2》360°自由寻路的开源项目。 这一模块需要基于前两个模块的成果自行开发。不过，学术研究中有相关工作可以参考：Overcooked-AI 是一个基于《胡闹厨房》的合作AI基准环境，其中的规划智能体使用AI寻路算法，通过启发式方法决定探索哪些节点。DPT-Agent 项目实现了复杂的位姿验证逻辑，使用 A 寻路算法* 验证可达性。

4.2 实现架构
第一步：构建导航图

利用“地图解析”模块获得的数据，在脚本内存中构建导航网格（NavMesh）或路点图。参考Unity的NavMesh系统概念，将可行走区域（地板）标记为可通行节点，将障碍物（墙壁、桌子、灶台）标记为不可通行。对于动态关卡，需要在关卡布局变化时重新构建导航图。

第二步：实现A*寻路

在导航图上运行A算法，计算从当前位置到目标位置的最优路径。A算法在游戏AI中的标准应用方式是：采用导航网格替代传统网格，将可通行区域划分为多边形区域（节点），以减少节点数量并提升效率。

第三步：路径跟随与输入转换

将A*计算出的路径点序列转换为每一帧的移动方向向量（Vector2），然后通过“手柄控制Hook”模块的输入注入机制，将这些方向向量传递给游戏。核心逻辑是：方向 = (下一路径点 - 当前位置).normalized，通过Hook的输入函数逐帧注入。

第四步：运行时NavMesh干预（可选）

对于需要更底层控制的场景，可以利用Unity的 NavMeshBuilder 在运行时重新烘焙导航网格。BepInEx框架具备精确控制模组加载顺序和Patch原游戏私有方法的能力，可以用于在运行时动态修改NavMesh数据。

4.3 与TAS框架的整合
overcooked-supercharged 框架的逐帧控制能力，可以用于精确调试你的寻路逻辑：通过逐帧推进游戏，观察每一帧角色的位置变化和路径跟随情况，验证寻路算法的正确性。该框架的高层动作抽象设计，可以让你专注于路径规划逻辑，而不必处理底层输入映射的细节。

五、开发环境搭建
5.1 环境要求
Visual Studio 2019 或更高版本

.NET Framework 4.7.2

已安装《胡闹厨房2》的Steam版本

Python 3.6.0+（如需使用UnityPy）

5.2 搭建步骤
第一步：安装BepInEx 5 (x86)

下载BepInEx 5 (x86)版本，解压后拷贝到游戏根目录。注意：Steam版本游戏为32位，必须使用x86版本。Jetpack MOD文档确认了这一要求。

第二步：配置Visual Studio项目

创建C#类库项目，按2.3节的DLL清单配置引用。参考 Overcooked2-ManyRecipes 的项目结构，将DLL放入 lib/ 目录并在.csproj中引用。

第三步：编写插件入口

csharp
using BepInEx;
using HarmonyLib;

[BepInPlugin("com.yourname.oc2script", "OC2 Script Controller", "1.0.0")]
public class Plugin : BaseUnityPlugin
{
    void Awake()
    {
        var harmony = new Harmony("com.yourname.oc2script");
        harmony.PatchAll();
        Logger.LogInfo("OC2 Script Controller loaded");
    }
}
第四步：安装Python工具链（可选）

bash
pip install UnityPy
用于批量处理地图资源数据。

5.3 注意事项
开启BepInEx控制台可能导致无法以手柄进入游戏，需确保 BepInEx.cfg 中 [Logging.Console] 的 Enabled = false

Jetpack和ManyRecipes在街机公开模式中均无效，所有测试建议在离线模式下进行

多人联机时，非主机玩家的移动状态需要与主机同步，Hook逻辑需考虑网络同步问题

六、分阶段实施路线图
阶段	目标	参考资源	难度
阶段1	搭建BepInEx开发环境，编译并运行现有MOD	Jetpack README + ManyRecipes项目结构	⭐⭐
阶段2	编写简单Harmony补丁，Hook一个空函数验证流程	RoastPatch.cs 标准补丁写法	⭐⭐
阶段3	实现手柄输入Hook，让角色按脚本指令移动	BepInEx.UnityInput + overcooked-supercharged	⭐⭐⭐
阶段4	实现运行时地图解析，获取关卡物体坐标	dnSpy反编译 + AssetStudio静态分析	⭐⭐⭐
阶段5	在内存中构建导航图，实现A*寻路算法	Overcooked-AI / DPT-Agent寻路参考	⭐⭐⭐⭐
阶段6	整合三模块为完整脚本控制器，实现自动通关	全部上述资源	⭐⭐⭐⭐⭐
建议起点：从阶段1和阶段2开始，先确保BepInEx环境正常工作并成功加载一个Harmony补丁。关键决策点在阶段3，需要确认走 UnityEngine.Input Hook路径还是游戏内部 InputManager Hook路径——建议先用 BepInEx.UnityInput 验证可行性，如果游戏对手柄输入有额外处理层，再转向Hook游戏内部的InputManager。

